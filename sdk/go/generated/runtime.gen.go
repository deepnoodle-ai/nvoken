// Package generated provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package generated

import (
	"bytes"
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	"github.com/oapi-codegen/runtime"
	openapi_types "github.com/oapi-codegen/runtime/types"
)

// Defines values for BudgetBlockScope.
const (
	BudgetBlockScopeAPICredential BudgetBlockScope = "api_credential"
	BudgetBlockScopeAgent         BudgetBlockScope = "agent"
	BudgetBlockScopeApp           BudgetBlockScope = "app"
	BudgetBlockScopeCustomer      BudgetBlockScope = "customer"
	BudgetBlockScopeProviderKey   BudgetBlockScope = "provider_key"
	BudgetBlockScopeSession       BudgetBlockScope = "session"
	BudgetBlockScopeUser          BudgetBlockScope = "user"
)

// Valid indicates whether the value is a known member of the BudgetBlockScope enum.
func (e BudgetBlockScope) Valid() bool {
	switch e {
	case BudgetBlockScopeAPICredential:
		return true
	case BudgetBlockScopeAgent:
		return true
	case BudgetBlockScopeApp:
		return true
	case BudgetBlockScopeCustomer:
		return true
	case BudgetBlockScopeProviderKey:
		return true
	case BudgetBlockScopeSession:
		return true
	case BudgetBlockScopeUser:
		return true
	default:
		return false
	}
}

// Defines values for BudgetScope.
const (
	BudgetScopeAPICredential BudgetScope = "api_credential"
	BudgetScopeAgent         BudgetScope = "agent"
	BudgetScopeApp           BudgetScope = "app"
	BudgetScopeCustomer      BudgetScope = "customer"
	BudgetScopeProviderKey   BudgetScope = "provider_key"
	BudgetScopeUser          BudgetScope = "user"
)

// Valid indicates whether the value is a known member of the BudgetScope enum.
func (e BudgetScope) Valid() bool {
	switch e {
	case BudgetScopeAPICredential:
		return true
	case BudgetScopeAgent:
		return true
	case BudgetScopeApp:
		return true
	case BudgetScopeCustomer:
		return true
	case BudgetScopeProviderKey:
		return true
	case BudgetScopeUser:
		return true
	default:
		return false
	}
}

// Defines values for BuiltinToolDeclarationMode.
const (
	ModeBuiltin BuiltinToolDeclarationMode = "builtin"
)

// Valid indicates whether the value is a known member of the BuiltinToolDeclarationMode enum.
func (e BuiltinToolDeclarationMode) Valid() bool {
	switch e {
	case ModeBuiltin:
		return true
	default:
		return false
	}
}

// Defines values for BuiltinToolDeclarationName.
const (
	NameNvokenFetch BuiltinToolDeclarationName = "nvoken_fetch"
)

// Valid indicates whether the value is a known member of the BuiltinToolDeclarationName enum.
func (e BuiltinToolDeclarationName) Valid() bool {
	switch e {
	case NameNvokenFetch:
		return true
	default:
		return false
	}
}

// Defines values for CallbackDeliveryOutcome.
const (
	CallbackDeliveryOutcomeAbandoned CallbackDeliveryOutcome = "abandoned"
	CallbackDeliveryOutcomeFailed    CallbackDeliveryOutcome = "failed"
	CallbackDeliveryOutcomePending   CallbackDeliveryOutcome = "pending"
	CallbackDeliveryOutcomeSucceeded CallbackDeliveryOutcome = "succeeded"
)

// Valid indicates whether the value is a known member of the CallbackDeliveryOutcome enum.
func (e CallbackDeliveryOutcome) Valid() bool {
	switch e {
	case CallbackDeliveryOutcomeAbandoned:
		return true
	case CallbackDeliveryOutcomeFailed:
		return true
	case CallbackDeliveryOutcomePending:
		return true
	case CallbackDeliveryOutcomeSucceeded:
		return true
	default:
		return false
	}
}

// Defines values for CallbackToolDeclarationMode.
const (
	ModeCallback CallbackToolDeclarationMode = "callback"
)

// Valid indicates whether the value is a known member of the CallbackToolDeclarationMode enum.
func (e CallbackToolDeclarationMode) Valid() bool {
	switch e {
	case ModeCallback:
		return true
	default:
		return false
	}
}

// Defines values for CharLocationCitationType.
const (
	TypeCharLocation CharLocationCitationType = "char_location"
)

// Valid indicates whether the value is a known member of the CharLocationCitationType enum.
func (e CharLocationCitationType) Valid() bool {
	switch e {
	case TypeCharLocation:
		return true
	default:
		return false
	}
}

// Defines values for CompactionPolicyTriggerTokens1.
const (
	Auto CompactionPolicyTriggerTokens1 = "auto"
)

// Valid indicates whether the value is a known member of the CompactionPolicyTriggerTokens1 enum.
func (e CompactionPolicyTriggerTokens1) Valid() bool {
	switch e {
	case Auto:
		return true
	default:
		return false
	}
}

// Defines values for CreateInvocationRequestIfActive.
const (
	Interrupt CreateInvocationRequestIfActive = "interrupt"
	Reject    CreateInvocationRequestIfActive = "reject"
	Supersede CreateInvocationRequestIfActive = "supersede"
)

// Valid indicates whether the value is a known member of the CreateInvocationRequestIfActive enum.
func (e CreateInvocationRequestIfActive) Valid() bool {
	switch e {
	case Interrupt:
		return true
	case Reject:
		return true
	case Supersede:
		return true
	default:
		return false
	}
}

// Defines values for CreateInvocationRequestOnBudgetExhausted.
const (
	Pause  CreateInvocationRequestOnBudgetExhausted = "pause"
	Settle CreateInvocationRequestOnBudgetExhausted = "settle"
)

// Valid indicates whether the value is a known member of the CreateInvocationRequestOnBudgetExhausted enum.
func (e CreateInvocationRequestOnBudgetExhausted) Valid() bool {
	switch e {
	case Pause:
		return true
	case Settle:
		return true
	default:
		return false
	}
}

// Defines values for DocumentInputBlockType.
const (
	InputTypeDocument DocumentInputBlockType = "document"
)

// Valid indicates whether the value is a known member of the DocumentInputBlockType enum.
func (e DocumentInputBlockType) Valid() bool {
	switch e {
	case InputTypeDocument:
		return true
	default:
		return false
	}
}

// Defines values for DocumentInputSourceMediaType.
const (
	DocumentPDF DocumentInputSourceMediaType = "application/pdf"
)

// Valid indicates whether the value is a known member of the DocumentInputSourceMediaType enum.
func (e DocumentInputSourceMediaType) Valid() bool {
	switch e {
	case DocumentPDF:
		return true
	default:
		return false
	}
}

// Defines values for DocumentReferenceBlockMediaType.
const (
	ApplicationPdf DocumentReferenceBlockMediaType = "application/pdf"
)

// Valid indicates whether the value is a known member of the DocumentReferenceBlockMediaType enum.
func (e DocumentReferenceBlockMediaType) Valid() bool {
	switch e {
	case ApplicationPdf:
		return true
	default:
		return false
	}
}

// Defines values for DocumentReferenceBlockType.
const (
	TypeDocument DocumentReferenceBlockType = "document"
)

// Valid indicates whether the value is a known member of the DocumentReferenceBlockType enum.
func (e DocumentReferenceBlockType) Valid() bool {
	switch e {
	case TypeDocument:
		return true
	default:
		return false
	}
}

// Defines values for ErrorCode.
const (
	ErrorCodeBudgetConflict          ErrorCode = "budget_conflict"
	ErrorCodeBudgetExceeded          ErrorCode = "budget_exceeded"
	ErrorCodeDefinitionNotFound      ErrorCode = "definition_not_found"
	ErrorCodeForbidden               ErrorCode = "forbidden"
	ErrorCodeIdempotencyConflict     ErrorCode = "idempotency_conflict"
	ErrorCodeInternal                ErrorCode = "internal"
	ErrorCodeInvalidRequest          ErrorCode = "invalid_request"
	ErrorCodeInvocationNotPaused     ErrorCode = "invocation_not_paused"
	ErrorCodeInvocationNotWaiting    ErrorCode = "invocation_not_waiting"
	ErrorCodeMcpDiscoveryFailed      ErrorCode = "mcp_discovery_failed"
	ErrorCodeMediaFetchFailed        ErrorCode = "media_fetch_failed"
	ErrorCodeModelRetired            ErrorCode = "model_retired"
	ErrorCodeNotFound                ErrorCode = "not_found"
	ErrorCodeProviderKeyConflict     ErrorCode = "provider_key_conflict"
	ErrorCodeRateLimited             ErrorCode = "rate_limited"
	ErrorCodeSessionInvocationActive ErrorCode = "session_invocation_active"
	ErrorCodeSessionOptionsConflict  ErrorCode = "session_options_conflict"
	ErrorCodeToolResultConflict      ErrorCode = "tool_result_conflict"
	ErrorCodeToolResultExpired       ErrorCode = "tool_result_expired"
	ErrorCodeUnauthenticated         ErrorCode = "unauthenticated"
	ErrorCodeUnavailable             ErrorCode = "unavailable"
)

// Valid indicates whether the value is a known member of the ErrorCode enum.
func (e ErrorCode) Valid() bool {
	switch e {
	case ErrorCodeBudgetConflict:
		return true
	case ErrorCodeBudgetExceeded:
		return true
	case ErrorCodeDefinitionNotFound:
		return true
	case ErrorCodeForbidden:
		return true
	case ErrorCodeIdempotencyConflict:
		return true
	case ErrorCodeInternal:
		return true
	case ErrorCodeInvalidRequest:
		return true
	case ErrorCodeInvocationNotPaused:
		return true
	case ErrorCodeInvocationNotWaiting:
		return true
	case ErrorCodeMcpDiscoveryFailed:
		return true
	case ErrorCodeMediaFetchFailed:
		return true
	case ErrorCodeModelRetired:
		return true
	case ErrorCodeNotFound:
		return true
	case ErrorCodeProviderKeyConflict:
		return true
	case ErrorCodeRateLimited:
		return true
	case ErrorCodeSessionInvocationActive:
		return true
	case ErrorCodeSessionOptionsConflict:
		return true
	case ErrorCodeToolResultConflict:
		return true
	case ErrorCodeToolResultExpired:
		return true
	case ErrorCodeUnauthenticated:
		return true
	case ErrorCodeUnavailable:
		return true
	default:
		return false
	}
}

// Defines values for HostToolDeclarationMode.
const (
	ModeHost HostToolDeclarationMode = "host"
)

// Valid indicates whether the value is a known member of the HostToolDeclarationMode enum.
func (e HostToolDeclarationMode) Valid() bool {
	switch e {
	case ModeHost:
		return true
	default:
		return false
	}
}

// Defines values for HostToolResultAcceptanceStatus.
const (
	HostToolResultAcceptanceStatusCompleted HostToolResultAcceptanceStatus = "completed"
	HostToolResultAcceptanceStatusFailed    HostToolResultAcceptanceStatus = "failed"
)

// Valid indicates whether the value is a known member of the HostToolResultAcceptanceStatus enum.
func (e HostToolResultAcceptanceStatus) Valid() bool {
	switch e {
	case HostToolResultAcceptanceStatusCompleted:
		return true
	case HostToolResultAcceptanceStatusFailed:
		return true
	default:
		return false
	}
}

// Defines values for ImageInputBlockType.
const (
	InputTypeImage ImageInputBlockType = "image"
)

// Valid indicates whether the value is a known member of the ImageInputBlockType enum.
func (e ImageInputBlockType) Valid() bool {
	switch e {
	case InputTypeImage:
		return true
	default:
		return false
	}
}

// Defines values for ImageInputSourceMediaType.
const (
	ImageGIF  ImageInputSourceMediaType = "image/gif"
	ImageJPEG ImageInputSourceMediaType = "image/jpeg"
	ImagePNG  ImageInputSourceMediaType = "image/png"
	ImageWEBP ImageInputSourceMediaType = "image/webp"
)

// Valid indicates whether the value is a known member of the ImageInputSourceMediaType enum.
func (e ImageInputSourceMediaType) Valid() bool {
	switch e {
	case ImageGIF:
		return true
	case ImageJPEG:
		return true
	case ImagePNG:
		return true
	case ImageWEBP:
		return true
	default:
		return false
	}
}

// Defines values for ImageReferenceBlockMediaType.
const (
	ImageGif  ImageReferenceBlockMediaType = "image/gif"
	ImageJpeg ImageReferenceBlockMediaType = "image/jpeg"
	ImagePng  ImageReferenceBlockMediaType = "image/png"
	ImageWebp ImageReferenceBlockMediaType = "image/webp"
)

// Valid indicates whether the value is a known member of the ImageReferenceBlockMediaType enum.
func (e ImageReferenceBlockMediaType) Valid() bool {
	switch e {
	case ImageGif:
		return true
	case ImageJpeg:
		return true
	case ImagePng:
		return true
	case ImageWebp:
		return true
	default:
		return false
	}
}

// Defines values for ImageReferenceBlockType.
const (
	TypeImage ImageReferenceBlockType = "image"
)

// Valid indicates whether the value is a known member of the ImageReferenceBlockType enum.
func (e ImageReferenceBlockType) Valid() bool {
	switch e {
	case TypeImage:
		return true
	default:
		return false
	}
}

// Defines values for InvocationAcceptedEventType.
const (
	EventInvocationAccepted InvocationAcceptedEventType = "invocation.accepted"
)

// Valid indicates whether the value is a known member of the InvocationAcceptedEventType enum.
func (e InvocationAcceptedEventType) Valid() bool {
	switch e {
	case EventInvocationAccepted:
		return true
	default:
		return false
	}
}

// Defines values for InvocationFailureCode.
const (
	InvocationFailureCodeBudgetExceeded              InvocationFailureCode = "budget_exceeded"
	InvocationFailureCodeContextWindowExceeded       InvocationFailureCode = "context_window_exceeded"
	InvocationFailureCodeDeadlineExceeded            InvocationFailureCode = "deadline_exceeded"
	InvocationFailureCodeInputMediaRejected          InvocationFailureCode = "input_media_rejected"
	InvocationFailureCodeInternal                    InvocationFailureCode = "internal"
	InvocationFailureCodeMcpDiscoveryFailed          InvocationFailureCode = "mcp_discovery_failed"
	InvocationFailureCodeProviderError               InvocationFailureCode = "provider_error"
	InvocationFailureCodeProviderKeyUnavailable      InvocationFailureCode = "provider_key_unavailable"
	InvocationFailureCodeStructuredOutputUnsatisfied InvocationFailureCode = "structured_output_unsatisfied"
)

// Valid indicates whether the value is a known member of the InvocationFailureCode enum.
func (e InvocationFailureCode) Valid() bool {
	switch e {
	case InvocationFailureCodeBudgetExceeded:
		return true
	case InvocationFailureCodeContextWindowExceeded:
		return true
	case InvocationFailureCodeDeadlineExceeded:
		return true
	case InvocationFailureCodeInputMediaRejected:
		return true
	case InvocationFailureCodeInternal:
		return true
	case InvocationFailureCodeMcpDiscoveryFailed:
		return true
	case InvocationFailureCodeProviderError:
		return true
	case InvocationFailureCodeProviderKeyUnavailable:
		return true
	case InvocationFailureCodeStructuredOutputUnsatisfied:
		return true
	default:
		return false
	}
}

// Defines values for InvocationResultEventType.
const (
	EventInvocationResult InvocationResultEventType = "invocation.result"
)

// Valid indicates whether the value is a known member of the InvocationResultEventType enum.
func (e InvocationResultEventType) Valid() bool {
	switch e {
	case EventInvocationResult:
		return true
	default:
		return false
	}
}

// Defines values for InvocationStatus.
const (
	InvocationStatusCancelled  InvocationStatus = "cancelled"
	InvocationStatusCompleted  InvocationStatus = "completed"
	InvocationStatusFailed     InvocationStatus = "failed"
	InvocationStatusIncomplete InvocationStatus = "incomplete"
	InvocationStatusPaused     InvocationStatus = "paused"
	InvocationStatusQueued     InvocationStatus = "queued"
	InvocationStatusRunning    InvocationStatus = "running"
	InvocationStatusWaiting    InvocationStatus = "waiting"
)

// Valid indicates whether the value is a known member of the InvocationStatus enum.
func (e InvocationStatus) Valid() bool {
	switch e {
	case InvocationStatusCancelled:
		return true
	case InvocationStatusCompleted:
		return true
	case InvocationStatusFailed:
		return true
	case InvocationStatusIncomplete:
		return true
	case InvocationStatusPaused:
		return true
	case InvocationStatusQueued:
		return true
	case InvocationStatusRunning:
		return true
	case InvocationStatusWaiting:
		return true
	default:
		return false
	}
}

// Defines values for InvocationStopReason.
const (
	InvocationStopReasonDeadline         InvocationStopReason = "deadline"
	InvocationStopReasonEndTurn          InvocationStopReason = "end_turn"
	InvocationStopReasonInterrupted      InvocationStopReason = "interrupted"
	InvocationStopReasonMaxEstimatedCost InvocationStopReason = "max_estimated_cost"
	InvocationStopReasonMaxIterations    InvocationStopReason = "max_iterations"
	InvocationStopReasonMaxOutputTokens  InvocationStopReason = "max_output_tokens"
	InvocationStopReasonSessionBudget    InvocationStopReason = "session_budget"
	InvocationStopReasonSharedBudget     InvocationStopReason = "shared_budget"
)

// Valid indicates whether the value is a known member of the InvocationStopReason enum.
func (e InvocationStopReason) Valid() bool {
	switch e {
	case InvocationStopReasonDeadline:
		return true
	case InvocationStopReasonEndTurn:
		return true
	case InvocationStopReasonInterrupted:
		return true
	case InvocationStopReasonMaxEstimatedCost:
		return true
	case InvocationStopReasonMaxIterations:
		return true
	case InvocationStopReasonMaxOutputTokens:
		return true
	case InvocationStopReasonSessionBudget:
		return true
	case InvocationStopReasonSharedBudget:
		return true
	default:
		return false
	}
}

// Defines values for InvocationUpdateEventType.
const (
	EventInvocationUpdate InvocationUpdateEventType = "invocation.update"
)

// Valid indicates whether the value is a known member of the InvocationUpdateEventType enum.
func (e InvocationUpdateEventType) Valid() bool {
	switch e {
	case EventInvocationUpdate:
		return true
	default:
		return false
	}
}

// Defines values for MCPServerTransport.
const (
	TransportStreamableHTTP MCPServerTransport = "streamable_http"
)

// Valid indicates whether the value is a known member of the MCPServerTransport enum.
func (e MCPServerTransport) Valid() bool {
	switch e {
	case TransportStreamableHTTP:
		return true
	default:
		return false
	}
}

// Defines values for MCPToolExclusionReason.
const (
	InvalidName    MCPToolExclusionReason = "invalid_name"
	InvalidSchema  MCPToolExclusionReason = "invalid_schema"
	NameCollision  MCPToolExclusionReason = "name_collision"
	NotAllowlisted MCPToolExclusionReason = "not_allowlisted"
	SchemaTooDeep  MCPToolExclusionReason = "schema_too_deep"
	SchemaTooLarge MCPToolExclusionReason = "schema_too_large"
)

// Valid indicates whether the value is a known member of the MCPToolExclusionReason enum.
func (e MCPToolExclusionReason) Valid() bool {
	switch e {
	case InvalidName:
		return true
	case InvalidSchema:
		return true
	case NameCollision:
		return true
	case NotAllowlisted:
		return true
	case SchemaTooDeep:
		return true
	case SchemaTooLarge:
		return true
	default:
		return false
	}
}

// Defines values for MessagePhase.
const (
	Commentary  MessagePhase = "commentary"
	FinalAnswer MessagePhase = "final_answer"
)

// Valid indicates whether the value is a known member of the MessagePhase enum.
func (e MessagePhase) Valid() bool {
	switch e {
	case Commentary:
		return true
	case FinalAnswer:
		return true
	default:
		return false
	}
}

// Defines values for ModelPricingCurrency.
const (
	USD ModelPricingCurrency = "USD"
)

// Valid indicates whether the value is a known member of the ModelPricingCurrency enum.
func (e ModelPricingCurrency) Valid() bool {
	switch e {
	case USD:
		return true
	default:
		return false
	}
}

// Defines values for ModelPricingStatus.
const (
	Priced   ModelPricingStatus = "priced"
	Unknown  ModelPricingStatus = "unknown"
	Unpriced ModelPricingStatus = "unpriced"
)

// Valid indicates whether the value is a known member of the ModelPricingStatus enum.
func (e ModelPricingStatus) Valid() bool {
	switch e {
	case Priced:
		return true
	case Unknown:
		return true
	case Unpriced:
		return true
	default:
		return false
	}
}

// Defines values for ModelPricingUnit.
const (
	PerMillionTokens ModelPricingUnit = "per_million_tokens"
)

// Valid indicates whether the value is a known member of the ModelPricingUnit enum.
func (e ModelPricingUnit) Valid() bool {
	switch e {
	case PerMillionTokens:
		return true
	default:
		return false
	}
}

// Defines values for ModelProvenanceProviderKeySource.
const (
	ModelProvenanceProviderKeySourceAppByok         ModelProvenanceProviderKeySource = "app_byok"
	ModelProvenanceProviderKeySourceCallerEphemeral ModelProvenanceProviderKeySource = "caller_ephemeral"
	ModelProvenanceProviderKeySourceConfigByok      ModelProvenanceProviderKeySource = "config_byok"
	ModelProvenanceProviderKeySourcePlatform        ModelProvenanceProviderKeySource = "platform"
	ModelProvenanceProviderKeySourceTenantByok      ModelProvenanceProviderKeySource = "tenant_byok"
)

// Valid indicates whether the value is a known member of the ModelProvenanceProviderKeySource enum.
func (e ModelProvenanceProviderKeySource) Valid() bool {
	switch e {
	case ModelProvenanceProviderKeySourceAppByok:
		return true
	case ModelProvenanceProviderKeySourceCallerEphemeral:
		return true
	case ModelProvenanceProviderKeySourceConfigByok:
		return true
	case ModelProvenanceProviderKeySourcePlatform:
		return true
	case ModelProvenanceProviderKeySourceTenantByok:
		return true
	default:
		return false
	}
}

// Defines values for ModelToolChoiceMode.
const (
	ChoiceAuto     ModelToolChoiceMode = "auto"
	ChoiceNamed    ModelToolChoiceMode = "named"
	ChoiceNone     ModelToolChoiceMode = "none"
	ChoiceRequired ModelToolChoiceMode = "required"
)

// Valid indicates whether the value is a known member of the ModelToolChoiceMode enum.
func (e ModelToolChoiceMode) Valid() bool {
	switch e {
	case ChoiceAuto:
		return true
	case ChoiceNamed:
		return true
	case ChoiceNone:
		return true
	case ChoiceRequired:
		return true
	default:
		return false
	}
}

// Defines values for OutputTextDeltaEventType.
const (
	EventOutputTextDelta OutputTextDeltaEventType = "output_text.delta"
)

// Valid indicates whether the value is a known member of the OutputTextDeltaEventType enum.
func (e OutputTextDeltaEventType) Valid() bool {
	switch e {
	case EventOutputTextDelta:
		return true
	default:
		return false
	}
}

// Defines values for PendingInputStatus.
const (
	PendingInputStatusCancelled PendingInputStatus = "cancelled"
	PendingInputStatusDrained   PendingInputStatus = "drained"
	PendingInputStatusExpired   PendingInputStatus = "expired"
	PendingInputStatusPending   PendingInputStatus = "pending"
)

// Valid indicates whether the value is a known member of the PendingInputStatus enum.
func (e PendingInputStatus) Valid() bool {
	switch e {
	case PendingInputStatusCancelled:
		return true
	case PendingInputStatusDrained:
		return true
	case PendingInputStatusExpired:
		return true
	case PendingInputStatusPending:
		return true
	default:
		return false
	}
}

// Defines values for ProviderKeyStatus.
const (
	ProviderKeyStatusActive  ProviderKeyStatus = "active"
	ProviderKeyStatusRevoked ProviderKeyStatus = "revoked"
)

// Valid indicates whether the value is a known member of the ProviderKeyStatus enum.
func (e ProviderKeyStatus) Valid() bool {
	switch e {
	case ProviderKeyStatusActive:
		return true
	case ProviderKeyStatusRevoked:
		return true
	default:
		return false
	}
}

// Defines values for ProviderKeyVersionStatus.
const (
	ProviderKeyVersionStatusActive  ProviderKeyVersionStatus = "active"
	ProviderKeyVersionStatusExpired ProviderKeyVersionStatus = "expired"
	ProviderKeyVersionStatusOverlap ProviderKeyVersionStatus = "overlap"
	ProviderKeyVersionStatusRevoked ProviderKeyVersionStatus = "revoked"
)

// Valid indicates whether the value is a known member of the ProviderKeyVersionStatus enum.
func (e ProviderKeyVersionStatus) Valid() bool {
	switch e {
	case ProviderKeyVersionStatusActive:
		return true
	case ProviderKeyVersionStatusExpired:
		return true
	case ProviderKeyVersionStatusOverlap:
		return true
	case ProviderKeyVersionStatusRevoked:
		return true
	default:
		return false
	}
}

// Defines values for ProviderKeyScope.
const (
	ProviderKeyScopeApp    ProviderKeyScope = "app"
	ProviderKeyScopeTenant ProviderKeyScope = "tenant"
)

// Valid indicates whether the value is a known member of the ProviderKeyScope enum.
func (e ProviderKeyScope) Valid() bool {
	switch e {
	case ProviderKeyScopeApp:
		return true
	case ProviderKeyScopeTenant:
		return true
	default:
		return false
	}
}

// Defines values for ProviderKeySelection0Source.
const (
	SourceCallerEphemeral ProviderKeySelection0Source = "caller_ephemeral"
)

// Valid indicates whether the value is a known member of the ProviderKeySelection0Source enum.
func (e ProviderKeySelection0Source) Valid() bool {
	switch e {
	case SourceCallerEphemeral:
		return true
	default:
		return false
	}
}

// Defines values for ProviderKeySelection1Source.
const (
	ProviderKeySelection1SourceAppByok    ProviderKeySelection1Source = "app_byok"
	ProviderKeySelection1SourcePlatform   ProviderKeySelection1Source = "platform"
	ProviderKeySelection1SourceTenantByok ProviderKeySelection1Source = "tenant_byok"
)

// Valid indicates whether the value is a known member of the ProviderKeySelection1Source enum.
func (e ProviderKeySelection1Source) Valid() bool {
	switch e {
	case ProviderKeySelection1SourceAppByok:
		return true
	case ProviderKeySelection1SourcePlatform:
		return true
	case ProviderKeySelection1SourceTenantByok:
		return true
	default:
		return false
	}
}

// Defines values for ProviderToolType.
const (
	ProviderToolWebSearch ProviderToolType = "web_search"
)

// Valid indicates whether the value is a known member of the ProviderToolType enum.
func (e ProviderToolType) Valid() bool {
	switch e {
	case ProviderToolWebSearch:
		return true
	default:
		return false
	}
}

// Defines values for ReasoningEffort.
const (
	EffortHigh   ReasoningEffort = "high"
	EffortLow    ReasoningEffort = "low"
	EffortMax    ReasoningEffort = "max"
	EffortMedium ReasoningEffort = "medium"
	EffortXHigh  ReasoningEffort = "xhigh"
)

// Valid indicates whether the value is a known member of the ReasoningEffort enum.
func (e ReasoningEffort) Valid() bool {
	switch e {
	case EffortHigh:
		return true
	case EffortLow:
		return true
	case EffortMax:
		return true
	case EffortMedium:
		return true
	case EffortXHigh:
		return true
	default:
		return false
	}
}

// Defines values for RedactedBlockType.
const (
	TypeRedacted RedactedBlockType = "redacted"
)

// Valid indicates whether the value is a known member of the RedactedBlockType enum.
func (e RedactedBlockType) Valid() bool {
	switch e {
	case TypeRedacted:
		return true
	default:
		return false
	}
}

// Defines values for SeedMessageRole.
const (
	SeedMessageRoleAssistant SeedMessageRole = "assistant"
	SeedMessageRoleUser      SeedMessageRole = "user"
)

// Valid indicates whether the value is a known member of the SeedMessageRole enum.
func (e SeedMessageRole) Valid() bool {
	switch e {
	case SeedMessageRoleAssistant:
		return true
	case SeedMessageRoleUser:
		return true
	default:
		return false
	}
}

// Defines values for ServerToolUseBlockType.
const (
	TypeServerToolUse ServerToolUseBlockType = "server_tool_use"
)

// Valid indicates whether the value is a known member of the ServerToolUseBlockType enum.
func (e ServerToolUseBlockType) Valid() bool {
	switch e {
	case TypeServerToolUse:
		return true
	default:
		return false
	}
}

// Defines values for SessionActiveInvocationStatus.
const (
	SessionActiveInvocationStatusPaused  SessionActiveInvocationStatus = "paused"
	SessionActiveInvocationStatusQueued  SessionActiveInvocationStatus = "queued"
	SessionActiveInvocationStatusRunning SessionActiveInvocationStatus = "running"
	SessionActiveInvocationStatusWaiting SessionActiveInvocationStatus = "waiting"
)

// Valid indicates whether the value is a known member of the SessionActiveInvocationStatus enum.
func (e SessionActiveInvocationStatus) Valid() bool {
	switch e {
	case SessionActiveInvocationStatusPaused:
		return true
	case SessionActiveInvocationStatusQueued:
		return true
	case SessionActiveInvocationStatusRunning:
		return true
	case SessionActiveInvocationStatusWaiting:
		return true
	default:
		return false
	}
}

// Defines values for SessionCompactionStatus.
const (
	Applied     SessionCompactionStatus = "applied"
	FellThrough SessionCompactionStatus = "fell_through"
)

// Valid indicates whether the value is a known member of the SessionCompactionStatus enum.
func (e SessionCompactionStatus) Valid() bool {
	switch e {
	case Applied:
		return true
	case FellThrough:
		return true
	default:
		return false
	}
}

// Defines values for SessionMessageOrigin.
const (
	Seeded SessionMessageOrigin = "seeded"
)

// Valid indicates whether the value is a known member of the SessionMessageOrigin enum.
func (e SessionMessageOrigin) Valid() bool {
	switch e {
	case Seeded:
		return true
	default:
		return false
	}
}

// Defines values for SessionMessageRole.
const (
	SessionMessageRoleAssistant SessionMessageRole = "assistant"
	SessionMessageRoleTool      SessionMessageRole = "tool"
	SessionMessageRoleUser      SessionMessageRole = "user"
)

// Valid indicates whether the value is a known member of the SessionMessageRole enum.
func (e SessionMessageRole) Valid() bool {
	switch e {
	case SessionMessageRoleAssistant:
		return true
	case SessionMessageRoleTool:
		return true
	case SessionMessageRoleUser:
		return true
	default:
		return false
	}
}

// Defines values for StreamEndEventReason.
const (
	Rotate   StreamEndEventReason = "rotate"
	Terminal StreamEndEventReason = "terminal"
)

// Valid indicates whether the value is a known member of the StreamEndEventReason enum.
func (e StreamEndEventReason) Valid() bool {
	switch e {
	case Rotate:
		return true
	case Terminal:
		return true
	default:
		return false
	}
}

// Defines values for StreamEndEventType.
const (
	EventStreamEnd StreamEndEventType = "stream.end"
)

// Valid indicates whether the value is a known member of the StreamEndEventType enum.
func (e StreamEndEventType) Valid() bool {
	switch e {
	case EventStreamEnd:
		return true
	default:
		return false
	}
}

// Defines values for StreamResyncEventReason.
const (
	ReasonLiveDeliveryGap StreamResyncEventReason = "live_delivery_gap"
)

// Valid indicates whether the value is a known member of the StreamResyncEventReason enum.
func (e StreamResyncEventReason) Valid() bool {
	switch e {
	case ReasonLiveDeliveryGap:
		return true
	default:
		return false
	}
}

// Defines values for StreamResyncEventType.
const (
	EventStreamResync StreamResyncEventType = "stream.resync"
)

// Valid indicates whether the value is a known member of the StreamResyncEventType enum.
func (e StreamResyncEventType) Valid() bool {
	switch e {
	case EventStreamResync:
		return true
	default:
		return false
	}
}

// Defines values for StructuredOutputProvenanceSource.
const (
	SourceToolCall StructuredOutputProvenanceSource = "tool_call"
)

// Valid indicates whether the value is a known member of the StructuredOutputProvenanceSource enum.
func (e StructuredOutputProvenanceSource) Valid() bool {
	switch e {
	case SourceToolCall:
		return true
	default:
		return false
	}
}

// Defines values for TextBlockType.
const (
	TypeText TextBlockType = "text"
)

// Valid indicates whether the value is a known member of the TextBlockType enum.
func (e TextBlockType) Valid() bool {
	switch e {
	case TypeText:
		return true
	default:
		return false
	}
}

// Defines values for TextInputBlockType.
const (
	InputTypeText TextInputBlockType = "text"
)

// Valid indicates whether the value is a known member of the TextInputBlockType enum.
func (e TextInputBlockType) Valid() bool {
	switch e {
	case InputTypeText:
		return true
	default:
		return false
	}
}

// Defines values for ThinkingDeltaEventType.
const (
	EventThinkingDelta ThinkingDeltaEventType = "thinking.delta"
)

// Valid indicates whether the value is a known member of the ThinkingDeltaEventType enum.
func (e ThinkingDeltaEventType) Valid() bool {
	switch e {
	case EventThinkingDelta:
		return true
	default:
		return false
	}
}

// Defines values for ToolCallMode.
const (
	Builtin  ToolCallMode = "builtin"
	Callback ToolCallMode = "callback"
	Host     ToolCallMode = "host"
	Mcp      ToolCallMode = "mcp"
)

// Valid indicates whether the value is a known member of the ToolCallMode enum.
func (e ToolCallMode) Valid() bool {
	switch e {
	case Builtin:
		return true
	case Callback:
		return true
	case Host:
		return true
	case Mcp:
		return true
	default:
		return false
	}
}

// Defines values for ToolCallStatus.
const (
	ToolCallStatusCancelled ToolCallStatus = "cancelled"
	ToolCallStatusCompleted ToolCallStatus = "completed"
	ToolCallStatusFailed    ToolCallStatus = "failed"
	ToolCallStatusPending   ToolCallStatus = "pending"
	ToolCallStatusRunning   ToolCallStatus = "running"
)

// Valid indicates whether the value is a known member of the ToolCallStatus enum.
func (e ToolCallStatus) Valid() bool {
	switch e {
	case ToolCallStatusCancelled:
		return true
	case ToolCallStatusCompleted:
		return true
	case ToolCallStatusFailed:
		return true
	case ToolCallStatusPending:
		return true
	case ToolCallStatusRunning:
		return true
	default:
		return false
	}
}

// Defines values for ToolCallbackContextSchemaVersion.
const (
	N1 ToolCallbackContextSchemaVersion = 1
)

// Valid indicates whether the value is a known member of the ToolCallbackContextSchemaVersion enum.
func (e ToolCallbackContextSchemaVersion) Valid() bool {
	switch e {
	case N1:
		return true
	default:
		return false
	}
}

// Defines values for ToolResultBlockType.
const (
	TypeToolResult ToolResultBlockType = "tool_result"
)

// Valid indicates whether the value is a known member of the ToolResultBlockType enum.
func (e ToolResultBlockType) Valid() bool {
	switch e {
	case TypeToolResult:
		return true
	default:
		return false
	}
}

// Defines values for ToolUseBlockType.
const (
	TypeToolUse ToolUseBlockType = "tool_use"
)

// Valid indicates whether the value is a known member of the ToolUseBlockType enum.
func (e ToolUseBlockType) Valid() bool {
	switch e {
	case TypeToolUse:
		return true
	default:
		return false
	}
}

// Defines values for TranscriptUpdateType.
const (
	EventTranscriptUpdate TranscriptUpdateType = "transcript.update"
)

// Valid indicates whether the value is a known member of the TranscriptUpdateType enum.
func (e TranscriptUpdateType) Valid() bool {
	switch e {
	case EventTranscriptUpdate:
		return true
	default:
		return false
	}
}

// Defines values for URLCitationType.
const (
	TypeURLCitation URLCitationType = "url_citation"
)

// Valid indicates whether the value is a known member of the URLCitationType enum.
func (e URLCitationType) Valid() bool {
	switch e {
	case TypeURLCitation:
		return true
	default:
		return false
	}
}

// Defines values for WebSearchResultLocationCitationType.
const (
	TypeWebSearchResultLocation WebSearchResultLocationCitationType = "web_search_result_location"
)

// Valid indicates whether the value is a known member of the WebSearchResultLocationCitationType enum.
func (e WebSearchResultLocationCitationType) Valid() bool {
	switch e {
	case TypeWebSearchResultLocation:
		return true
	default:
		return false
	}
}

// Defines values for WebhookEvent.
const (
	WebhookEventPaused  WebhookEvent = "invocation.paused"
	WebhookEventSettled WebhookEvent = "invocation.settled"
	WebhookEventWaiting WebhookEvent = "invocation.waiting"
)

// Valid indicates whether the value is a known member of the WebhookEvent enum.
func (e WebhookEvent) Valid() bool {
	switch e {
	case WebhookEventPaused:
		return true
	case WebhookEventSettled:
		return true
	case WebhookEventWaiting:
		return true
	default:
		return false
	}
}

// Defines values for ListProviderKeysParamsStatus.
const (
	ListProviderKeysParamsStatusActive  ListProviderKeysParamsStatus = "active"
	ListProviderKeysParamsStatusRevoked ListProviderKeysParamsStatus = "revoked"
)

// Valid indicates whether the value is a known member of the ListProviderKeysParamsStatus enum.
func (e ListProviderKeysParamsStatus) Valid() bool {
	switch e {
	case ListProviderKeysParamsStatusActive:
		return true
	case ListProviderKeysParamsStatusRevoked:
		return true
	default:
		return false
	}
}

// Defines values for GetDailyUsageParamsGroupBy.
const (
	TenantKey GetDailyUsageParamsGroupBy = "tenant_key"
)

// Valid indicates whether the value is a known member of the GetDailyUsageParamsGroupBy enum.
func (e GetDailyUsageParamsGroupBy) Valid() bool {
	switch e {
	case TenantKey:
		return true
	default:
		return false
	}
}

// Defines values for ReceiveToolCallbackParamsXNvokenSignatureVersion.
const (
	V1 ReceiveToolCallbackParamsXNvokenSignatureVersion = "v1"
)

// Valid indicates whether the value is a known member of the ReceiveToolCallbackParamsXNvokenSignatureVersion enum.
func (e ReceiveToolCallbackParamsXNvokenSignatureVersion) Valid() bool {
	switch e {
	case V1:
		return true
	default:
		return false
	}
}

// APICredentialID UUIDv7 with the public `cred_` prefix.
type APICredentialID = string

// Agent Installation-wide identity anchor only. Agent behavior is not registered;
// instructions, model, tools, and provider keys travel per Invocation.
type Agent struct {
	// AgentKey Stable host-owned key, unique within the installation.
	AgentKey  string    `json:"agent_key"`
	CreatedAt time.Time `json:"created_at"`

	// ID UUIDv7 with the public `agnt_` prefix.
	ID AgentID `json:"id"`
}

// AgentDefinition Immutable launch snapshot returned on detailed Invocation reads. The
// host owns the agent definition; nvoken only records the definition
// each turn ran with. The
// create request exposes these execution fields at its top level; nvoken
// selects them, stores them as one immutable snapshot row, and re-reads
// the snapshot unchanged on every model iteration and recovery. Two
// Invocations with byte-identical execution fields share one snapshot.
// Identity, session selection, input, idempotency, `if_active`, `webhook`,
// metadata, and provider key selection are durable elsewhere and
// never appear here. Callback declarations require installation callback
// signing configuration. Remote MCP authentication headers are encrypted
// outside this snapshot and never returned. Tools or structured output
// require at least two model iterations; omission resolves to three or
// the lower installation maximum.
type AgentDefinition struct {
	// Instructions Optional model instructions. Omission adds no hidden default.
	Instructions *string `json:"instructions,omitempty"`

	// Limits Optional requested limits. Total time bounds the entire turn, active
	// time bounds model and tool execution, and waiting time bounds the
	// cumulative time parked for host or callback tool results. Installation
	// defaults supply all three time limits and the iteration limit.
	// Output-token and estimated-cost limits are unlimited when omitted.
	// Installation maxima may be lower than the schema's numeric range.
	Limits        *Limits         `json:"limits,omitempty"`
	McpServers    *[]MCPServer    `json:"mcp_servers,omitempty"`
	Model         Model           `json:"model"`
	ProviderTools *[]ProviderTool `json:"provider_tools,omitempty"`
	Reasoning     *Reasoning      `json:"reasoning,omitempty"`
	Sampling      *Sampling       `json:"sampling,omitempty"`

	// StructuredOutput Optional per-Invocation structured-output contract. nvoken exposes a
	// reserved durable submit tool and publishes only a server-validated
	// terminal object. This does not enable host-defined tools.
	StructuredOutput *StructuredOutput `json:"structured_output,omitempty"`

	// ToolChoice Portable Invocation tool selection. auto preserves normal selection;
	// none disables tools for the Invocation; required and named apply only
	// to the first durable model iteration, then return to auto. name is
	// required only for named mode. The final bounded iteration always
	// disables tools.
	ToolChoice *ToolChoice        `json:"tool_choice,omitempty"`
	Tools      *[]ToolDeclaration `json:"tools,omitempty"`
}

// AgentDefinitionID UUIDv7 with the public `def_` prefix.
type AgentDefinitionID = string

// AgentID UUIDv7 with the public `agnt_` prefix.
type AgentID = string

// AgentList defines model for AgentList.
type AgentList struct {
	HasMore    bool    `json:"has_more"`
	Items      []Agent `json:"items"`
	NextCursor *string `json:"next_cursor"`
}

// App defines model for App.
type App struct {
	CreatedAt time.Time `json:"created_at"`

	// DisplayName Human-facing label; `name` stays the unique handle.
	DisplayName *string `json:"display_name"`

	// ExternalRef Opaque owner reference used to ground console issuer tokens. One
	// reference may own many apps.
	ExternalRef *string `json:"external_ref"`

	// ID The generated nvoken app identifier.
	ID string `json:"id"`

	// Name The unique host-chosen name for this app.
	Name string `json:"name"`
}

// AppList defines model for AppList.
type AppList struct {
	Items []App `json:"items"`
}

// Budget Durable enforcement state for one fixed window. Settled and reserved
// are stored quota facts. They do not shrink when transcript evidence is
// erased and are not an invoice.
type Budget struct {
	AgentID                   *AgentID  `json:"agent_id"`
	AvailableEstimatedCostUsd float32   `json:"available_estimated_cost_usd"`
	CreatedAt                 time.Time `json:"created_at"`

	// CredentialFamilyID Root API credential ID shared by its rotation chain.
	CredentialFamilyID *APICredentialID `json:"credential_family_id"`

	// ID UUIDv7 with the public `bdgt_` prefix.
	ID                       BudgetID       `json:"id"`
	MaxEstimatedCostUsd      float32        `json:"max_estimated_cost_usd"`
	PausedInvocations        int            `json:"paused_invocations"`
	ProviderKeyID            *ProviderKeyID `json:"provider_key_id"`
	ReservedEstimatedCostUsd float32        `json:"reserved_estimated_cost_usd"`
	Scope                    BudgetScope    `json:"scope"`
	SettledEstimatedCostUsd  float32        `json:"settled_estimated_cost_usd"`
	TenantKey                *string        `json:"tenant_key"`
	UpdatedAt                time.Time      `json:"updated_at"`
	UserKey                  *string        `json:"user_key"`
	WindowEnd                time.Time      `json:"window_end"`
	WindowStart              time.Time      `json:"window_start"`
}

// BudgetBlock Exact durable budget that refused or paused work.
type BudgetBlock struct {
	// ID UUIDv7 with the public `bdgt_` prefix.
	ID    BudgetID         `json:"id"`
	Scope BudgetBlockScope `json:"scope"`
}

// BudgetBlockScope defines model for BudgetBlock.Scope.
type BudgetBlockScope string

// BudgetID UUIDv7 with the public `bdgt_` prefix.
type BudgetID = string

// BudgetScope defines model for BudgetScope.
type BudgetScope string

// BuiltinToolDeclaration defines model for BuiltinToolDeclaration.
type BuiltinToolDeclaration struct {
	Mode BuiltinToolDeclarationMode `json:"mode"`

	// Name Fixed runtime-owned public-web fetch capability.
	Name BuiltinToolDeclarationName `json:"name"`
}

// BuiltinToolDeclarationMode defines model for BuiltinToolDeclaration.Mode.
type BuiltinToolDeclarationMode string

// BuiltinToolDeclarationName Fixed runtime-owned public-web fetch capability.
type BuiltinToolDeclarationName string

// CallbackDeliveryID defines model for CallbackDeliveryID.
type CallbackDeliveryID = string

// CallbackDeliveryOutcome `pending` covers a blocked, available, or currently leased delivery.
// The other values are terminal transport outcomes.
type CallbackDeliveryOutcome string

// CallbackTarget defines model for CallbackTarget.
type CallbackTarget struct {
	// URL Public HTTPS endpoint. Userinfo and fragments are rejected. Every
	// dial revalidates resolved addresses and refuses redirects.
	URL string `json:"url"`
}

// CallbackToolDeclaration A callback tool is a host tool nvoken delivers to the host's endpoint
// instead of waiting for the host to poll.
type CallbackToolDeclaration struct {
	Callback    CallbackTarget `json:"callback"`
	Description string         `json:"description"`

	// InputSchema Object-root JSON Schema using the same bounded, self-contained
	// subset as structured output, including `uniqueItems`. Compact
	// canonical JSON is limited to 32 KiB. A rejection carries
	// `details.{code,path,keyword}` naming the offending schema position.
	InputSchema map[string]interface{}      `json:"input_schema"`
	Mode        CallbackToolDeclarationMode `json:"mode"`

	// Name Unique within the definition; the `nvoken_` prefix is reserved.
	//
	// The charset is deliberately the strictest tool-name constraint any
	// supported provider imposes, so every admitted name reaches every
	// provider unchanged. Widening it — to dotted names, for instance —
	// would require a mangling layer whose collisions nvoken would then
	// own, and would sit ambiguously beside the `__` separator MCP tool
	// projection already uses. A host whose own permission grammar uses
	// dots should map it injectively (`naming.domain.check` becomes
	// `naming_domain_check`) and state the mapping to the model once.
	Name string `json:"name"`
}

// CallbackToolDeclarationMode defines model for CallbackToolDeclaration.Mode.
type CallbackToolDeclarationMode string

// CharLocationCitation A location inside one indexed input document.
type CharLocationCitation struct {
	CitedText      *string                  `json:"cited_text,omitempty"`
	DocumentIndex  *int                     `json:"document_index,omitempty"`
	DocumentTitle  *string                  `json:"document_title,omitempty"`
	EndCharIndex   *int                     `json:"end_char_index,omitempty"`
	StartCharIndex *int                     `json:"start_char_index,omitempty"`
	Type           CharLocationCitationType `json:"type"`
}

// CharLocationCitationType defines model for CharLocationCitation.Type.
type CharLocationCitationType string

// Citation defines model for Citation.
type Citation struct {
	union json.RawMessage
}

// CompactionPolicy Durable Session context-compaction policy. Omission of model uses the
// installing Invocation's primary model. An explicit model must be
// cataloged and use the same provider. Later Invocations automatically
// use this resolved policy and the latest Session summary.
type CompactionPolicy struct {
	Model *Model `json:"model,omitempty"`

	// TriggerTokens Estimated retained-context size that triggers compaction. The
	// Session stores "auto" as an exact value materialized when the
	// policy is installed to 60% of that Invocation's primary model
	// context window, clamped to the
	// supported integer range.
	TriggerTokens CompactionPolicy_TriggerTokens `json:"trigger_tokens"`
}

// CompactionPolicyTriggerTokens0 defines model for CompactionPolicy.TriggerTokens.0.
type CompactionPolicyTriggerTokens0 = int

// CompactionPolicyTriggerTokens1 defines model for CompactionPolicy.TriggerTokens.1.
type CompactionPolicyTriggerTokens1 string

// CompactionPolicy_TriggerTokens Estimated retained-context size that triggers compaction. The
// Session stores "auto" as an exact value materialized when the
// policy is installed to 60% of that Invocation's primary model
// context window, clamped to the
// supported integer range.
type CompactionPolicy_TriggerTokens struct {
	union json.RawMessage
}

// CreateBudgetRequest The selected scope requires only its identity fields. `app` requires
// none. `customer` requires exactly one of `tenant_key` or
// `default_tenant: true`. `user` also requires `user_key`. `agent`,
// `provider_key`, and `api_credential` require `agent_id`,
// `provider_key_id`, and `credential_id` respectively. A credential ID
// resolves to its stable rotation family.
type CreateBudgetRequest struct {
	// AgentID UUIDv7 with the public `agnt_` prefix.
	AgentID *AgentID `json:"agent_id,omitempty"`

	// CredentialID UUIDv7 with the public `cred_` prefix.
	CredentialID        *APICredentialID `json:"credential_id,omitempty"`
	DefaultTenant       *bool            `json:"default_tenant,omitempty"`
	IdempotencyKey      string           `json:"idempotency_key"`
	MaxEstimatedCostUsd float32          `json:"max_estimated_cost_usd"`

	// ProviderKeyID UUIDv7 with the public `pkey_` prefix.
	ProviderKeyID *ProviderKeyID `json:"provider_key_id,omitempty"`
	Scope         BudgetScope    `json:"scope"`
	TenantKey     *string        `json:"tenant_key,omitempty"`
	UserKey       *string        `json:"user_key,omitempty"`

	// WindowEnd Exclusive UTC end.
	WindowEnd time.Time `json:"window_end"`

	// WindowStart Inclusive UTC start.
	WindowStart time.Time `json:"window_start"`
}

// CreateInvocationRequest The request carries one definition form. It either carries
// definition_id or an inline definition with model. The two forms are
// mutually exclusive.
type CreateInvocationRequest struct {
	// AgentKey Stable caller-controlled Agent key, unique within the
	// installation. The resulting Agent anchor stores identity
	// only and is shared across tenant partitions.
	AgentKey string `json:"agent_key"`

	// DefinitionID Immutable content-addressed definition previously admitted inline
	// by this app. Unknown and foreign IDs return
	// `definition_not_found`. Mutually exclusive with every inline
	// definition field. A reference and the byte-identical inline
	// definition are equal on idempotent replay.
	DefinitionID *AgentDefinitionID `json:"definition_id,omitempty"`

	// IdempotencyKey Stable admission identity scoped to (effective tenant
	// partition, agent_key). Reuse an unchanged request after any 5xx,
	// timeout, disconnect, or missing acknowledgement. Deduplication is
	// guaranteed while the original Invocation is retained.
	IdempotencyKey string `json:"idempotency_key"`

	// IfActive Policy when the resolved Session already has a nonterminal
	// Invocation. `reject` returns `session_invocation_active`.
	// `supersede` atomically applies the ordinary durable cancellation
	// transition to that Invocation before admitting this replacement,
	// dropping its work from later turns. `interrupt` stops it
	// gracefully and admits only after it has settled `completed`, so
	// the replacement builds on what the stopped turn produced: it is
	// "stop and redo" where supersede is "discard and redo". Omission
	// and explicit `reject` compare equal on replay.
	IfActive *CreateInvocationRequestIfActive `json:"if_active,omitempty"`

	// Input A plain string is shorthand for one text block; an array is ordered
	// multi-block input mixing text, images, and documents. Both normalize
	// to the same stored message and are therefore equal for idempotency.
	// At most 8 blocks may carry media, and their decoded payloads must
	// total at most 16777216 bytes.
	Input InvocationInput `json:"input"`

	// Instructions Optional model instructions. Omission adds no hidden default.
	Instructions *string `json:"instructions,omitempty"`

	// Limits Optional requested limits. Total time bounds the entire turn, active
	// time bounds model and tool execution, and waiting time bounds the
	// cumulative time parked for host or callback tool results. Installation
	// defaults supply all three time limits and the iteration limit.
	// Output-token and estimated-cost limits are unlimited when omitted.
	// Installation maxima may be lower than the schema's numeric range.
	Limits     *Limits      `json:"limits,omitempty"`
	McpServers *[]MCPServer `json:"mcp_servers,omitempty"`

	// Metadata Opaque host correlation data recorded on this Invocation. It is
	// part of the admitted input, so it is immutable and material to
	// idempotency: a replay carrying different metadata conflicts rather
	// than updating it. A genuine retry describes the same originating
	// request and so carries the same values by construction.
	//
	// Session metadata is separate and mutable — see
	// `session_options.metadata` and `PATCH /v1/sessions/{session_id}`.
	Metadata *Metadata `json:"metadata,omitempty"`

	// Model Model selection accepted at Invocation admission. The object is
	// canonical. The `provider/id` string is caller convenience: nvoken
	// splits on the first slash and always echoes the object form.
	Model *ModelInput `json:"model,omitempty"`

	// OnBudgetExhausted `settle` ends a coherent consumption-budget stop as `incomplete`.
	// `pause` parks it as resumable `paused`. Applies to iteration,
	// output-token, per-turn estimated-cost, and Session estimated-cost
	// ceilings; deadlines always settle.
	OnBudgetExhausted *CreateInvocationRequestOnBudgetExhausted `json:"on_budget_exhausted,omitempty"`

	// ProviderKeys Explicit nonsecret source selection for the model provider.
	// Omission resolves the default chain at admission: the app's
	// stored key for the provider when one exists, else a
	// self-hosted installation's environment key (config_byok), else
	// platform funding when the installation enables it. The chosen
	// source is bound durably at admission; execution never falls
	// through to a different payer afterward. The selection is stored
	// outside the reusable execution snapshot; only caller_ephemeral accepts secret
	// material, and a caller may equivalently supply it with the
	// X-Anthropic-Api-Key, X-Openai-Api-Key, X-Gemini-Api-Key, or
	// X-Xai-Api-Key request header naming the model provider. Equal
	// idempotent replay never replaces the original encrypted key. The
	// field is deliberately plural even though this
	// contract currently permits one entry: future routed invocations
	// may select keys for more than one model provider.
	ProviderKeys  *[]ProviderKeySelection `json:"provider_keys,omitempty"`
	ProviderTools *[]ProviderTool         `json:"provider_tools,omitempty"`
	Reasoning     *Reasoning              `json:"reasoning,omitempty"`
	Sampling      *Sampling               `json:"sampling,omitempty"`

	// SessionID Existing Session to continue. Mutually exclusive with session_key.
	SessionID *SessionID `json:"session_id,omitempty"`

	// SessionKey Caller key resolved within (effective tenant partition,
	// Agent, session_key). Mutually exclusive with session_id.
	SessionKey *string `json:"session_key,omitempty"`

	// SessionOptions Durable options for the resolved Session. On an existing Session,
	// every supplied value is compared with the stored value: equal is
	// accepted and different returns `session_options_conflict`.
	// Compaction is the one late-installable option: when no policy is
	// stored yet, this admission installs its model-validated policy.
	// Session options are not part of the reusable execution snapshot or
	// its idempotency comparison.
	SessionOptions *SessionOptions `json:"session_options,omitempty"`

	// StructuredOutput Optional per-Invocation structured-output contract. nvoken exposes a
	// reserved durable submit tool and publishes only a server-validated
	// terminal object. This does not enable host-defined tools.
	StructuredOutput *StructuredOutput `json:"structured_output,omitempty"`

	// TenantKey Optional tenant partition. For Session-key resolution or a new
	// Session, precedence is credential constraint, this explicit value,
	// then the default partition. For Session-ID resolution, an
	// installation-wide caller may omit it and use the stored partition.
	TenantKey *string `json:"tenant_key,omitempty"`

	// ToolChoice Portable Invocation tool selection. auto preserves normal selection;
	// none disables tools for the Invocation; required and named apply only
	// to the first durable model iteration, then return to auto. name is
	// required only for named mode. The final bounded iteration always
	// disables tools.
	ToolChoice *ToolChoice        `json:"tool_choice,omitempty"`
	Tools      *[]ToolDeclaration `json:"tools,omitempty"`

	// UserKey Optional host-owned end-user label recorded on this Invocation and
	// its input message. The Session retains the label from the request
	// that opened it, while later turns may identify different end users.
	// Filtering only; not an isolation boundary.
	UserKey *string `json:"user_key,omitempty"`

	// Webhook Optional endpoint nvoken posts a signed webhook to when this
	// Invocation parks awaiting host tool results or reaches a terminal
	// status. It is delivery configuration rather than model input, so it is
	// excluded from the reusable execution snapshot. Two otherwise identical
	// turns that differ only in endpoint therefore share one snapshot.
	//
	// Delivery is at least once and ordered within one Invocation, and the
	// webhook ID is repeated in `Idempotency-Key` on every attempt. nvoken
	// webhooks are thin: identifiers and status only, the API stays the read
	// authority. A webhook never changes, delays, or fails the Invocation it
	// describes.
	//
	// The endpoint is part of the request, so replaying an idempotency key
	// with a changed endpoint or event set is a conflict rather than a
	// silent adoption of either value. An installation without a configured
	// webhook signing key rejects this field instead of accepting it and
	// never delivering it.
	Webhook *WebhookTarget `json:"webhook,omitempty"`
}

// CreateInvocationRequestIfActive Policy when the resolved Session already has a nonterminal
// Invocation. `reject` returns `session_invocation_active`.
// `supersede` atomically applies the ordinary durable cancellation
// transition to that Invocation before admitting this replacement,
// dropping its work from later turns. `interrupt` stops it
// gracefully and admits only after it has settled `completed`, so
// the replacement builds on what the stopped turn produced: it is
// "stop and redo" where supersede is "discard and redo". Omission
// and explicit `reject` compare equal on replay.
type CreateInvocationRequestIfActive string

// CreateInvocationRequestOnBudgetExhausted `settle` ends a coherent consumption-budget stop as `incomplete`.
// `pause` parks it as resumable `paused`. Applies to iteration,
// output-token, per-turn estimated-cost, and Session estimated-cost
// ceilings; deadlines always settle.
type CreateInvocationRequestOnBudgetExhausted string

// CreateProviderKeyRequest defines model for CreateProviderKeyRequest.
type CreateProviderKeyRequest struct {
	ExpiresAt      *time.Time        `json:"expires_at,omitempty"`
	IdempotencyKey string            `json:"idempotency_key"`
	Key            ProviderStaticKey `json:"key"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider `json:"provider"`

	// Scope `app` is one provider key shared by every tenant of one app;
	// `tenant` is one provider key for a single named
	// tenant_key inside the app.
	Scope ProviderKeyScope `json:"scope"`

	// TenantKey Required for tenant scope and forbidden for app scope.
	TenantKey *string `json:"tenant_key,omitempty"`
}

// CreateSessionRequest defines model for CreateSessionRequest.
type CreateSessionRequest struct {
	// AgentKey Optional stable caller-controlled Agent key, resolved or created
	// exactly as invocation admission does. Omitting it creates an
	// unbound Session whose agent is set by its first admitted
	// Invocation.
	AgentKey *string `json:"agent_key,omitempty"`

	// SeedMessages Host-asserted starting history, accepted only while a new Session
	// is created. Requires `agent_key`. A keyed request that resolves an
	// existing Session returns it unchanged and never appends these
	// messages. The UTF-8 text across the array may total at most 524288
	// bytes.
	SeedMessages *[]SeedMessage `json:"seed_messages,omitempty"`

	// SessionKey Optional caller key resolved within (effective tenant partition,
	// Agent, session_key). Requires agent_key. Makes creation an
	// upsert: an existing keyed Session is returned unchanged.
	SessionKey *string `json:"session_key,omitempty"`

	// SessionOptions Durable Session options. Every member is optional and at least one must
	// be present. A new Session stores the supplied values. When a keyed
	// upsert or admission resolves an existing Session, supplied values are
	// comparison-only: equal values are accepted and different values return
	// `session_options_conflict` with `details.conflicting_paths`.
	//
	// `compaction` requires an Invocation because the policy is validated
	// against that turn's model. It may be installed on any Session that has
	// no compaction policy yet, including a Session created ahead of its first
	// Invocation or a long-running Session opting in later. An `auto` trigger
	// is materialized from the installing Invocation's context window. Once
	// set, the resolved policy is immutable. `POST /v1/sessions` therefore
	// still rejects `compaction`, while admissions may supply it with either
	// `session_id`, `session_key`, or a new Session.
	SessionOptions *SessionOptions `json:"session_options,omitempty"`

	// TenantKey Optional tenant partition. Precedence is credential constraint,
	// this explicit value, then the default partition.
	TenantKey *string `json:"tenant_key,omitempty"`

	// UserKey Optional host-owned end-user label recorded on the Session.
	// Filtering only; not an isolation boundary.
	UserKey *string `json:"user_key,omitempty"`
}

// DailyUsage defines model for DailyUsage.
type DailyUsage struct {
	// EndDate Inclusive last UTC day of the served window.
	EndDate openapi_types.Date `json:"end_date"`

	// Items Buckets ordered by day, app, provider, and model. Days with no usage produce no bucket.
	Items []DailyUsageBucket `json:"items"`

	// StartDate Inclusive first UTC day of the served window.
	StartDate openapi_types.Date `json:"start_date"`
}

// DailyUsageBucket defines model for DailyUsageBucket.
type DailyUsageBucket struct {
	// AppID The app whose tenants produced the usage.
	AppID                    string `json:"app_id"`
	CacheCreationInputTokens int    `json:"cache_creation_input_tokens"`
	CacheReadInputTokens     int    `json:"cache_read_input_tokens"`

	// Day UTC calendar day of the bucket.
	Day openapi_types.Date `json:"day"`

	// EstimatedCost Summed list-price estimate in USD; zero when every call in the bucket was unpriced.
	EstimatedCost float32 `json:"estimated_cost"`
	InputTokens   int     `json:"input_tokens"`

	// Invocations Distinct Invocations with at least one model call in the bucket. Compaction calls carry no Invocation.
	Invocations int `json:"invocations"`

	// Model Served model from provenance, falling back to the requested model.
	Model string `json:"model"`

	// ModelCalls Individual provider calls in the bucket.
	ModelCalls   int `json:"model_calls"`
	OutputTokens int `json:"output_tokens"`

	// Provider Provider from checkpoint provenance; empty when provenance omitted it.
	Provider        string `json:"provider"`
	ReasoningTokens int    `json:"reasoning_tokens"`

	// TenantKey Host customer group when `group_by=tenant_key`; omitted otherwise.
	TenantKey *string `json:"tenant_key,omitempty"`
}

// DocumentInputBlock An inline or URL document. A URL is fetched once during admission.
// nvoken stores the fetched bytes and never fetches it again. Admission
// applies the same sniff rules as an image with a 16777216-byte bound.
// Page limits are the provider's, so an oversized page count settles as
// input_media_rejected.
type DocumentInputBlock struct {
	// Source Exactly one source form is required. Inline sources carry media_type
	// and data. URL sources carry url and may declare media_type.
	Source DocumentInputSource `json:"source"`

	// Title Optional filename supplied to providers that require one. Omission
	// uses the adapter default and does not change acceptance.
	Title *string                `json:"title,omitempty"`
	Type  DocumentInputBlockType `json:"type"`
}

// DocumentInputBlockType defines model for DocumentInputBlock.Type.
type DocumentInputBlockType string

// DocumentInputSource Exactly one source form is required. Inline sources carry media_type
// and data. URL sources carry url and may declare media_type.
type DocumentInputSource struct {
	// Data Standard padded base64 with no whitespace.
	Data      *string                       `json:"data,omitempty"`
	MediaType *DocumentInputSourceMediaType `json:"media_type,omitempty"`

	// URL Public HTTPS URL fetched once during admission. The response is
	// size-bounded while reading and its bytes are sniffed. media_type is
	// optional for this form. When present it must be application/pdf.
	URL *string `json:"url,omitempty"`
}

// DocumentInputSourceMediaType defines model for DocumentInputSource.MediaType.
type DocumentInputSourceMediaType string

// DocumentReferenceBlock A bounded read projection of a stored input document.
type DocumentReferenceBlock struct {
	// Bytes Decoded document byte count; never the document payload.
	Bytes int `json:"bytes"`

	// Digest Digest of the decoded document bytes.
	Digest    string                          `json:"digest"`
	MediaType DocumentReferenceBlockMediaType `json:"media_type"`

	// Title Present only when the caller supplied a title.
	Title *string                    `json:"title,omitempty"`
	Type  DocumentReferenceBlockType `json:"type"`

	// URL Original URL when the document arrived by URL.
	URL *string `json:"url,omitempty"`
}

// DocumentReferenceBlockMediaType defines model for DocumentReferenceBlock.MediaType.
type DocumentReferenceBlockMediaType string

// DocumentReferenceBlockType defines model for DocumentReferenceBlock.Type.
type DocumentReferenceBlockType string

// ErrorCode defines model for ErrorCode.
type ErrorCode string

// ErrorResponse defines model for ErrorResponse.
type ErrorResponse struct {
	Code      ErrorCode               `json:"code"`
	Details   *map[string]interface{} `json:"details,omitempty"`
	Message   string                  `json:"message"`
	RequestID string                  `json:"request_id"`
}

// ForkSessionOptions Creation-only options for a forked child. The child never inherits the
// source Session's compaction policy, retention, or metadata.
type ForkSessionOptions struct {
	// Metadata Opaque host correlation data. nvoken stores it, returns it verbatim,
	// and never interprets it — it exists so a support engineer holding an
	// Invocation id can get back to the board, ticket, or surface the turn
	// came from, which nvoken deliberately knows nothing about.
	//
	// At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`,
	// `:`, and `-`; values are at most 512 bytes of UTF-8.
	//
	// There is no `title` field on a Session by design. A title is one of
	// these entries (`{"title": "Refund policy"}`), which keeps nvoken from
	// forming opinions about a string only the host renders.
	Metadata *Metadata `json:"metadata,omitempty"`

	// Retention Bounds how long an idle Session is retained. Past the window nvoken
	// erases the Session and its whole subtree, exactly as
	// `DELETE /v1/sessions/{session_id}` would.
	//
	// The window measures idle time, not lifetime: it restarts on every
	// Invocation admission and every settlement, so a turn that runs longer
	// than the window cannot expire underneath itself.
	//
	// **Automatic expiry never cancels running work.** A Session holding a
	// queued, running, or waiting Invocation is skipped and reconsidered on
	// the next sweep. An explicit `DELETE` may still stop a running turn,
	// because a person asked for it; a clock may not.
	//
	// Omitting retention retains the Session until it is deleted explicitly,
	// which stays the default.
	Retention *RetentionPolicy `json:"retention,omitempty"`
}

// ForkSessionRequest defines model for ForkSessionRequest.
type ForkSessionRequest struct {
	// FromMessage Inclusive fork point, by stable message ID or sequence.
	FromMessage ForkSessionRequest_FromMessage `json:"from_message"`

	// SessionKey Optional tenant/Agent-scoped keyed identity for the child.
	SessionKey *string `json:"session_key,omitempty"`

	// SessionOptions Creation-only options for a forked child. The child never inherits the
	// source Session's compaction policy, retention, or metadata.
	SessionOptions *ForkSessionOptions `json:"session_options,omitempty"`

	// UserKey Optional opened-by label for the child Session.
	UserKey *string `json:"user_key,omitempty"`
}

// ForkSessionRequestFromMessage1 defines model for ForkSessionRequest.FromMessage.1.
type ForkSessionRequestFromMessage1 = int64

// ForkSessionRequest_FromMessage Inclusive fork point, by stable message ID or sequence.
type ForkSessionRequest_FromMessage struct {
	union json.RawMessage
}

// HostToolDeclaration A host tool is function calling: declared with JSON Schema and executed
// by the host application.
type HostToolDeclaration struct {
	Description string `json:"description"`

	// InputSchema Object-root JSON Schema using the same bounded, self-contained
	// subset as structured output, including `uniqueItems`. Compact
	// canonical JSON is limited to 32 KiB. A rejection carries
	// `details.{code,path,keyword}` naming the offending schema position.
	InputSchema map[string]interface{}  `json:"input_schema"`
	Mode        HostToolDeclarationMode `json:"mode"`

	// Name Unique within the definition; the `nvoken_` prefix is reserved.
	//
	// The charset is deliberately the strictest tool-name constraint any
	// supported provider imposes, so every admitted name reaches every
	// provider unchanged. Widening it — to dotted names, for instance —
	// would require a mangling layer whose collisions nvoken would then
	// own, and would sit ambiguously beside the `__` separator MCP tool
	// projection already uses. A host whose own permission grammar uses
	// dots should map it injectively (`naming.domain.check` becomes
	// `naming_domain_check`) and state the mapping to the model once.
	Name string `json:"name"`
}

// HostToolDeclarationMode defines model for HostToolDeclaration.Mode.
type HostToolDeclarationMode string

// HostToolResultAcceptance defines model for HostToolResultAcceptance.
type HostToolResultAcceptance struct {
	Deduplicated bool                           `json:"deduplicated"`
	Status       HostToolResultAcceptanceStatus `json:"status"`
	ToolCallID   ToolCallID                     `json:"tool_call_id"`
}

// HostToolResultAcceptanceStatus defines model for HostToolResultAcceptance.Status.
type HostToolResultAcceptanceStatus string

// ImageInputBlock An inline or URL image. A URL is fetched once during admission. nvoken
// stores the fetched bytes and never fetches it again. Admission sniffs
// the bytes, bounds them to 5242880 bytes and 8000 by 8000 pixels, and
// requires the selected model to declare the image modality and exact
// media type.
type ImageInputBlock struct {
	// Source Exactly one source form is required. Inline sources carry media_type
	// and data. URL sources carry url and may declare media_type.
	Source ImageInputSource    `json:"source"`
	Type   ImageInputBlockType `json:"type"`
}

// ImageInputBlockType defines model for ImageInputBlock.Type.
type ImageInputBlockType string

// ImageInputSource Exactly one source form is required. Inline sources carry media_type
// and data. URL sources carry url and may declare media_type.
type ImageInputSource struct {
	// Data Standard padded base64 with no whitespace. Mutually exclusive with
	// url.
	Data      *string                    `json:"data,omitempty"`
	MediaType *ImageInputSourceMediaType `json:"media_type,omitempty"`

	// URL Public HTTPS URL fetched once during admission. The response is
	// size-bounded while reading and its bytes are sniffed. media_type is
	// optional for this form. When present it must match the sniffed type.
	URL *string `json:"url,omitempty"`
}

// ImageInputSourceMediaType defines model for ImageInputSource.MediaType.
type ImageInputSourceMediaType string

// ImageReferenceBlock A bounded read projection of a stored input image.
type ImageReferenceBlock struct {
	// Bytes Decoded media byte count; never the media payload.
	Bytes int `json:"bytes"`

	// Digest Digest of the decoded media bytes.
	Digest    string                       `json:"digest"`
	MediaType ImageReferenceBlockMediaType `json:"media_type"`
	Type      ImageReferenceBlockType      `json:"type"`

	// URL Original URL when the image arrived by URL.
	URL *string `json:"url,omitempty"`
}

// ImageReferenceBlockMediaType defines model for ImageReferenceBlock.MediaType.
type ImageReferenceBlockMediaType string

// ImageReferenceBlockType defines model for ImageReferenceBlock.Type.
type ImageReferenceBlockType string

// InputBlock One caller-input block. A block carries text, an image, or a document;
// the members of one type are never valid on another.
type InputBlock struct {
	union json.RawMessage
}

// Invocation defines model for Invocation.
type Invocation struct {
	ActiveExecutionMs int `json:"active_execution_ms"`

	// AgentID UUIDv7 with the public `agnt_` prefix.
	AgentID AgentID `json:"agent_id"`

	// Attempt Execution attempts this Invocation has been claimed for. It
	// increases on every claim, so an attempt increase across a
	// `running → queued → running` transition is the retry signal that
	// status alone cannot give, and it is the durable anchor for
	// discarding provisional output from an earlier attempt. Zero
	// before the first claim.
	Attempt int `json:"attempt"`

	// BlockingBudget Exact Session or shared Budget for a budget stop, otherwise null.
	BlockingBudget *BudgetBlock `json:"blocking_budget"`
	CreatedAt      time.Time    `json:"created_at"`

	// DeadlineAt The deadline currently enforced by the runtime. Null while the
	// Invocation is waiting without an explicit waiting timeout; the
	// explicit waiting deadline while bounded; otherwise the total-time
	// deadline for queued, running, and terminal Invocations.
	DeadlineAt *time.Time `json:"deadline_at"`

	// Deduplicated Present only in the `POST /v1/invocations` response. False for a
	// new durable admission and true when the idempotency key returned
	// an existing Invocation.
	Deduplicated *bool `json:"deduplicated,omitempty"`

	// Definition The admitted definition, returned exactly as snapshotted. Remote MCP
	// request headers were never stored and never appear here. Present
	// on `GET /v1/invocations/{id}` and on the result; null in list
	// items, where `definition_id` identifies it. `definition.limits` is what the
	// host requested; `limits` is what the installation resolved.
	Definition *AgentDefinition `json:"definition"`

	// DefinitionID UUIDv7 with the public `def_` prefix.
	DefinitionID AgentDefinitionID  `json:"definition_id"`
	EndedAt      *time.Time         `json:"ended_at"`
	Error        *InvocationFailure `json:"error"`

	// ID UUIDv7 with the public `invk_` prefix.
	ID     InvocationID   `json:"id"`
	Limits ResolvedLimits `json:"limits"`

	// Metadata Host correlation data recorded at admission, returned verbatim.
	Metadata *Metadata `json:"metadata"`

	// PendingToolCalls Present for a waiting Invocation with unresolved host calls.
	PendingToolCalls *[]PendingHostToolCall `json:"pending_tool_calls,omitempty"`
	Provenance       *ModelProvenance       `json:"provenance"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID `json:"session_id"`

	// Status `completed`, `incomplete`, `failed`, and `cancelled` are terminal and
	// immutable.
	//
	// `completed` means exactly one thing: the turn ended the way it was
	// asked to — the model finished, or the caller interrupted it.
	// `incomplete` means the runtime enforced a budget at a coherent
	// execution seam. Any budget exhaustion observed at such a seam settles
	// `incomplete` with `stop_reason` naming the budget; the transcript is
	// valid and stays in the next turn's context, exactly as a completed
	// turn's does. Exhaustion that cannot reach a seam — a deadline landing
	// mid-request, reaper settlement — still settles `failed` with `error`
	// as the authority. A schema-bearing turn that never published a
	// validated object also fails, `structured_output_unsatisfied`,
	// whichever budget stopped it.
	//
	// `waiting`—known elsewhere as `requires_action`—means the Invocation has
	// durable pending host ToolCalls and owns no execution lease. Final
	// result acceptance moves it back to
	// `queued`. Checkpoint recovery may also move `running` back to
	// `queued`; the `attempt` counter distinguishes a retry from tool-result
	// acceptance, and lifecycle revision orders every transition.
	//
	// `paused` is nonterminal dormant work stopped at a coherent consumption
	// ceiling. It owns no lease and its deadlines are suspended until the
	// exhausted turn limit is raised or the Session budget is raised or
	// removed. It still accepts interrupt, cancel, and staged nudges.
	Status InvocationStatus `json:"status"`

	// StopReason Why the turn stopped or paused. Present on `completed`,
	// `incomplete`, and `paused`; null on every other status — a failure
	// keeps `error` as the authority. Treat an unrecognized value as an
	// ordinary end.
	StopReason *InvocationStopReason `json:"stop_reason"`

	// StructuredOutput Server-validated terminal object for a schema-bearing Invocation.
	// Null until successful terminal settlement and always null when no
	// output contract was admitted.
	StructuredOutput           *map[string]interface{}     `json:"structured_output"`
	StructuredOutputProvenance *StructuredOutputProvenance `json:"structured_output_provenance"`
	UpdatedAt                  time.Time                   `json:"updated_at"`

	// Usage One normalized terminal aggregate, not a billing ledger.
	Usage *ModelUsage `json:"usage"`

	// UserKey Host-owned end-user label admitted on this turn. Filtering only;
	// not an isolation boundary.
	UserKey *string `json:"user_key"`
}

// InvocationAcceptedEvent defines model for InvocationAcceptedEvent.
type InvocationAcceptedEvent struct {
	// AgentID UUIDv7 with the public `agnt_` prefix.
	AgentID      AgentID   `json:"agent_id"`
	DeadlineAt   time.Time `json:"deadline_at"`
	Deduplicated bool      `json:"deduplicated"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID `json:"invocation_id"`

	// Limits The effective budgets this Invocation was admitted with, after
	// installation defaults and feature floors were applied. Echoed here
	// so a host sees what it actually got without a second read — an
	// omitted `max_iterations` in particular resolves to a value the
	// request never named.
	Limits ResolvedLimits `json:"limits"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID `json:"session_id"`

	// Status `completed`, `incomplete`, `failed`, and `cancelled` are terminal and
	// immutable.
	//
	// `completed` means exactly one thing: the turn ended the way it was
	// asked to — the model finished, or the caller interrupted it.
	// `incomplete` means the runtime enforced a budget at a coherent
	// execution seam. Any budget exhaustion observed at such a seam settles
	// `incomplete` with `stop_reason` naming the budget; the transcript is
	// valid and stays in the next turn's context, exactly as a completed
	// turn's does. Exhaustion that cannot reach a seam — a deadline landing
	// mid-request, reaper settlement — still settles `failed` with `error`
	// as the authority. A schema-bearing turn that never published a
	// validated object also fails, `structured_output_unsatisfied`,
	// whichever budget stopped it.
	//
	// `waiting`—known elsewhere as `requires_action`—means the Invocation has
	// durable pending host ToolCalls and owns no execution lease. Final
	// result acceptance moves it back to
	// `queued`. Checkpoint recovery may also move `running` back to
	// `queued`; the `attempt` counter distinguishes a retry from tool-result
	// acceptance, and lifecycle revision orders every transition.
	//
	// `paused` is nonterminal dormant work stopped at a coherent consumption
	// ceiling. It owns no lease and its deadlines are suspended until the
	// exhausted turn limit is raised or the Session budget is raised or
	// removed. It still accepts interrupt, cancel, and staged nudges.
	Status InvocationStatus            `json:"status"`
	Type   InvocationAcceptedEventType `json:"type"`
}

// InvocationAcceptedEventType defines model for InvocationAcceptedEvent.Type.
type InvocationAcceptedEventType string

// InvocationChange defines model for InvocationChange.
type InvocationChange struct {
	Error *InvocationFailure `json:"error"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID     `json:"invocation_id"`
	OccurredAt   time.Time        `json:"occurred_at"`
	Provenance   *ModelProvenance `json:"provenance"`
	Revision     int64            `json:"revision"`

	// Status `completed`, `incomplete`, `failed`, and `cancelled` are terminal and
	// immutable.
	//
	// `completed` means exactly one thing: the turn ended the way it was
	// asked to — the model finished, or the caller interrupted it.
	// `incomplete` means the runtime enforced a budget at a coherent
	// execution seam. Any budget exhaustion observed at such a seam settles
	// `incomplete` with `stop_reason` naming the budget; the transcript is
	// valid and stays in the next turn's context, exactly as a completed
	// turn's does. Exhaustion that cannot reach a seam — a deadline landing
	// mid-request, reaper settlement — still settles `failed` with `error`
	// as the authority. A schema-bearing turn that never published a
	// validated object also fails, `structured_output_unsatisfied`,
	// whichever budget stopped it.
	//
	// `waiting`—known elsewhere as `requires_action`—means the Invocation has
	// durable pending host ToolCalls and owns no execution lease. Final
	// result acceptance moves it back to
	// `queued`. Checkpoint recovery may also move `running` back to
	// `queued`; the `attempt` counter distinguishes a retry from tool-result
	// acceptance, and lifecycle revision orders every transition.
	//
	// `paused` is nonterminal dormant work stopped at a coherent consumption
	// ceiling. It owns no lease and its deadlines are suspended until the
	// exhausted turn limit is raised or the Session budget is raised or
	// removed. It still accepts interrupt, cancel, and staged nudges.
	Status                     InvocationStatus            `json:"status"`
	StructuredOutput           *map[string]interface{}     `json:"structured_output"`
	StructuredOutputProvenance *StructuredOutputProvenance `json:"structured_output_provenance"`
	ThroughMessageSequence     *int64                      `json:"through_message_sequence"`
	Usage                      *ModelUsage                 `json:"usage"`
}

// InvocationFailure Failed Invocations may carry paired usage and provenance when a model
// response produced safe normalized evidence before deadline or limit
// settlement. Cancellation carries any checkpointed usage and
// provenance accumulated before it was requested; a pre-response
// failure carries neither.
//
// `details` is a bounded, code-specific object. Each code names its own
// keys and nothing else reads them:
//
//   - `deadline_exceeded` carries `details.scope`, the deadline that was
//     breached, drawn from `total | active_execution | execution_segment
//     | waiting`. One condition has one code: a deadline reports this
//     wherever the runtime noticed it.
//   - `budget_exceeded` carries `details.kind`, the consumption ceiling
//     the turn overran, drawn from `output_tokens | estimated_cost |
//     estimated_cost_unavailable`. A ceiling only lands here when no
//     coherent seam absorbed it; at a seam the same ceiling settles
//     `incomplete` with the matching `stop_reason` instead.
//     `estimated_cost_unavailable` is always a failure — nothing was
//     exhausted, nvoken simply cannot price the model.
//   - `structured_output_unsatisfied` carries `details.reason`, drawn from
//     `missing | invalid | oversized`.
//   - `context_window_exceeded` carries the integer keys
//     `input_tokens`, `context_window_tokens`, and
//     `requested_output_tokens`, each present only when known.
//   - `input_media_rejected` carries `modality`, `media_type`, and
//     `reason` as the provider reported them, each present only when
//     known.
//
// A `provider_error` failure carries
// `details.provider_failure_class`, nvoken's own classification of the
// provider failure, drawn from `configuration | canceled | throttled |
// upstream_rejected | upstream_unavailable | timeout_or_transport |
// invalid_response | unknown`. `upstream_rejected` means the provider
// refused this request — do not retry it as-is. `upstream_unavailable`
// and `throttled` mean the provider could not serve it — retry with
// backoff. No raw provider error material ever crosses into this
// payload.
type InvocationFailure struct {
	Code    InvocationFailureCode   `json:"code"`
	Details *map[string]interface{} `json:"details,omitempty"`
	Message string                  `json:"message"`
}

// InvocationFailureCode defines model for InvocationFailure.Code.
type InvocationFailureCode string

// InvocationID UUIDv7 with the public `invk_` prefix.
type InvocationID = string

// InvocationInput A plain string is shorthand for one text block; an array is ordered
// multi-block input mixing text, images, and documents. Both normalize
// to the same stored message and are therefore equal for idempotency.
// At most 8 blocks may carry media, and their decoded payloads must
// total at most 16777216 bytes.
type InvocationInput struct {
	union json.RawMessage
}

// InvocationInput0 defines model for InvocationInput.0.
type InvocationInput0 = string

// InvocationInput1 defines model for InvocationInput.1.
type InvocationInput1 = []InputBlock

// InvocationList defines model for InvocationList.
type InvocationList struct {
	HasMore    bool         `json:"has_more"`
	Items      []Invocation `json:"items"`
	NextCursor *string      `json:"next_cursor"`
}

// InvocationResult defines model for InvocationResult.
type InvocationResult struct {
	Invocation Invocation `json:"invocation"`

	// Messages Every canonical SessionMessage owned by this Invocation, all
	// roles, ascending sequence, composed at read time from the
	// canonical transcript. Nothing is stored twice.
	Messages []SessionMessage `json:"messages"`

	// OutputText The text content blocks of this Invocation's assistant-role
	// messages concatenated in transcript order without separators.
	// Non-null only when the Invocation is completed and at least one
	// assistant text block exists. Failed and cancelled Invocations
	// keep their messages readable as evidence while output_text stays
	// null.
	OutputText *string `json:"output_text"`
}

// InvocationResultEvent defines model for InvocationResultEvent.
type InvocationResultEvent struct {
	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID     `json:"invocation_id"`
	Result       InvocationResult `json:"result"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID                 `json:"session_id"`
	Type      InvocationResultEventType `json:"type"`
}

// InvocationResultEventType defines model for InvocationResultEvent.Type.
type InvocationResultEventType string

// InvocationStatus `completed`, `incomplete`, `failed`, and `cancelled` are terminal and
// immutable.
//
// `completed` means exactly one thing: the turn ended the way it was
// asked to — the model finished, or the caller interrupted it.
// `incomplete` means the runtime enforced a budget at a coherent
// execution seam. Any budget exhaustion observed at such a seam settles
// `incomplete` with `stop_reason` naming the budget; the transcript is
// valid and stays in the next turn's context, exactly as a completed
// turn's does. Exhaustion that cannot reach a seam — a deadline landing
// mid-request, reaper settlement — still settles `failed` with `error`
// as the authority. A schema-bearing turn that never published a
// validated object also fails, `structured_output_unsatisfied`,
// whichever budget stopped it.
//
// `waiting`—known elsewhere as `requires_action`—means the Invocation has
// durable pending host ToolCalls and owns no execution lease. Final
// result acceptance moves it back to
// `queued`. Checkpoint recovery may also move `running` back to
// `queued`; the `attempt` counter distinguishes a retry from tool-result
// acceptance, and lifecycle revision orders every transition.
//
// `paused` is nonterminal dormant work stopped at a coherent consumption
// ceiling. It owns no lease and its deadlines are suspended until the
// exhausted turn limit is raised or the Session budget is raised or
// removed. It still accepts interrupt, cancel, and staged nudges.
type InvocationStatus string

// InvocationStopReason Why a turn stopped. It spans both non-failure terminals and is
// partitioned between them.
//
// A `completed` turn carries `end_turn` (the model finishing) or
// `interrupted` (a caller's graceful stop, taken at the next execution
// seam). An `incomplete` turn carries the budget it exhausted at a seam:
// `max_iterations`, `deadline`, `max_output_tokens`, or
// `max_estimated_cost`, `session_budget`, or `shared_budget`. A paused turn carries one of
// the consumption reasons but never `deadline`. An interrupt is deliberately not `incomplete` —
// the caller asked the turn to end there, so ending there is the
// requested result. The vocabulary is extensible.
type InvocationStopReason string

// InvocationStreamEvent Typed event from one Invocation stream. A minimal consumer prints
// `output_text.delta` and finishes on `invocation.result`.
type InvocationStreamEvent struct {
	union json.RawMessage
}

// InvocationUpdateEvent defines model for InvocationUpdateEvent.
type InvocationUpdateEvent struct {
	Invocation Invocation `json:"invocation"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID     `json:"invocation_id"`
	NewMessages  []SessionMessage `json:"new_messages"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID                 `json:"session_id"`
	Type      InvocationUpdateEventType `json:"type"`
}

// InvocationUpdateEventType defines model for InvocationUpdateEvent.Type.
type InvocationUpdateEventType string

// Limits Optional requested limits. Total time bounds the entire turn, active
// time bounds model and tool execution, and waiting time bounds the
// cumulative time parked for host or callback tool results. Installation
// defaults supply all three time limits and the iteration limit.
// Output-token and estimated-cost limits are unlimited when omitted.
// Installation maxima may be lower than the schema's numeric range.
type Limits struct {
	ActiveTimeoutSeconds *int `json:"active_timeout_seconds,omitempty"`

	// MaxEstimatedCostUsd nvoken's standard list-price guardrail, not preauthorization or a billing
	// ledger. Requires known USD pricing for the selected model and
	// otherwise fails closed with `budget_exceeded` and
	// `details.kind = estimated_cost_unavailable`. When pricing absence
	// is knowable before execution, nvoken rejects before a provider call.
	// Reaching the ceiling itself is not a failure when it is noticed at
	// an execution seam: the turn settles `incomplete` with
	// `stop_reason: max_estimated_cost` and keeps its work.
	MaxEstimatedCostUsd *float32 `json:"max_estimated_cost_usd,omitempty"`
	MaxIterations       *int     `json:"max_iterations,omitempty"`
	MaxOutputTokens     *int     `json:"max_output_tokens,omitempty"`
	TotalTimeoutSeconds *int     `json:"total_timeout_seconds,omitempty"`

	// WaitingTimeoutSeconds Optional cumulative external-waiting SLA. When omitted, host and
	// callback ToolCalls may remain pending indefinitely; waiting does
	// not consume the Invocation's total or active execution budgets.
	WaitingTimeoutSeconds *int `json:"waiting_timeout_seconds,omitempty"`
}

// MCPListToolsRequest defines model for MCPListToolsRequest.
type MCPListToolsRequest struct {
	Server MCPServer `json:"server"`
}

// MCPListToolsResponse defines model for MCPListToolsResponse.
type MCPListToolsResponse struct {
	Exclusions []MCPToolExclusion `json:"exclusions"`
	Tools      []MCPProjectedTool `json:"tools"`
}

// MCPProjectedTool defines model for MCPProjectedTool.
type MCPProjectedTool struct {
	Annotations   MCPToolAnnotations     `json:"annotations"`
	Description   string                 `json:"description"`
	InputSchema   map[string]interface{} `json:"input_schema"`
	ProjectedName string                 `json:"projected_name"`
	RemoteName    string                 `json:"remote_name"`
	ServerName    string                 `json:"server_name"`
}

// MCPServer defines model for MCPServer.
type MCPServer struct {
	// AllowedTools Optional ordered allowlist. Every named tool must be discovered and projectable.
	AllowedTools *[]string `json:"allowed_tools,omitempty"`

	// Headers Secret request headers, limited to 8 KiB encoded. Routing,
	// framing, proxy, cookie, hop-by-hop, and MCP session headers are
	// rejected. Headers are encrypted per Invocation and never returned.
	Headers *map[string]string `json:"headers,omitempty"`

	// Name Unique server name used as the projected tool-name prefix. Names
	// beginning with nvoken in any letter case are reserved.
	Name      string              `json:"name"`
	Timeouts  *MCPTimeouts        `json:"timeouts,omitempty"`
	Transport *MCPServerTransport `json:"transport,omitempty"`

	// URL Public HTTPS streamable-HTTP MCP endpoint without userinfo or a fragment.
	URL string `json:"url"`
}

// MCPServerTransport defines model for MCPServer.Transport.
type MCPServerTransport string

// MCPTimeouts defines model for MCPTimeouts.
type MCPTimeouts struct {
	CallSeconds      *int `json:"call_seconds,omitempty"`
	DiscoverySeconds *int `json:"discovery_seconds,omitempty"`
}

// MCPToolAnnotations defines model for MCPToolAnnotations.
type MCPToolAnnotations struct {
	DestructiveHint *bool `json:"destructive_hint"`
	IdempotentHint  *bool `json:"idempotent_hint"`
	ReadOnlyHint    *bool `json:"read_only_hint"`
}

// MCPToolExclusion defines model for MCPToolExclusion.
type MCPToolExclusion struct {
	Reason     MCPToolExclusionReason `json:"reason"`
	RemoteName string                 `json:"remote_name"`
	ServerName string                 `json:"server_name"`
}

// MCPToolExclusionReason defines model for MCPToolExclusion.Reason.
type MCPToolExclusionReason string

// MessagePhase `final_answer` is the assistant message that settled its turn
// `completed` with `stop_reason: end_turn` — the reply. `commentary` is
// everything else: narration between tool calls, and every message of an
// interrupted or `incomplete` turn, none of which was ever an answer. A
// turn with no `final_answer` is visibly unfinished, which is what makes
// `incomplete` directly renderable.
type MessagePhase string

// Metadata Opaque host correlation data. nvoken stores it, returns it verbatim,
// and never interprets it — it exists so a support engineer holding an
// Invocation id can get back to the board, ticket, or surface the turn
// came from, which nvoken deliberately knows nothing about.
//
// At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`,
// `:`, and `-`; values are at most 512 bytes of UTF-8.
//
// There is no `title` field on a Session by design. A title is one of
// these entries (`{"title": "Refund policy"}`), which keeps nvoken from
// forming opinions about a string only the host renders.
type Metadata map[string]string

// Model defines model for Model.
type Model struct {
	ID string `json:"id"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider `json:"provider"`
}

// ModelControlCapabilities defines model for ModelControlCapabilities.
type ModelControlCapabilities struct {
	Input     ModelInputCapabilities     `json:"input"`
	Reasoning ModelReasoningCapabilities `json:"reasoning"`
	Sampling  ModelSamplingCapabilities  `json:"sampling"`
	Tools     ModelToolCapabilities      `json:"tools"`
}

// ModelCost defines model for ModelCost.
type ModelCost struct {
	CacheRead  float32 `json:"cache_read"`
	CacheWrite float32 `json:"cache_write"`
	Currency   *string `json:"currency,omitempty"`
	Input      float32 `json:"input"`
	Model      *string `json:"model,omitempty"`
	Output     float32 `json:"output"`
	Total      float32 `json:"total"`
}

// ModelDescriptor Cataloged descriptors include all maintained metadata fields.
// Uncataloged descriptors include only provider, id, cataloged, and
// pricing; absent metadata is unknown rather than false or zero.
// A cataloged descriptor's controls are the same qualified capabilities
// used by admission; omitted controls on an uncataloged descriptor mean
// unknown, not unsupported. Deprecated models remain admissible until
// `retires_at`; after that date, new admission returns `model_retired`
// with this entry's replacement.
type ModelDescriptor struct {
	// Cataloged Whether nvoken advertises and maintains metadata for this selection.
	Cataloged           bool                      `json:"cataloged"`
	ContextWindowTokens *int                      `json:"context_window_tokens,omitempty"`
	Controls            *ModelControlCapabilities `json:"controls,omitempty"`
	Deprecated          *bool                     `json:"deprecated,omitempty"`

	// DeprecatedAt Date nvoken marked this model deprecated.
	DeprecatedAt *openapi_types.Date `json:"deprecated_at,omitempty"`
	Description  *string             `json:"description,omitempty"`
	DisplayName  *string             `json:"display_name,omitempty"`
	ID           string              `json:"id"`

	// InputModalities Input modalities admission accepts for this model selection. Media
	// modalities also publish their exact media types under
	// controls.input.media.
	InputModalities *[]string `json:"input_modalities,omitempty"`
	MaxOutputTokens *int      `json:"max_output_tokens,omitempty"`

	// Pricing Standard local price evidence used by nvoken's estimated-cost
	// guardrail. A `priced` object also carries currency, unit, input, output,
	// and update date. `unpriced` and `unknown` carry only status and the
	// opaque pricing version. Clients compare versions for equality only.
	Pricing ModelPricing `json:"pricing"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider `json:"provider"`

	// Recommended nvoken's suggested general-purpose starting point for this provider.
	Recommended *bool  `json:"recommended,omitempty"`
	Replacement *Model `json:"replacement,omitempty"`

	// RetiresAt Date nvoken starts refusing new admission for this model.
	RetiresAt *openapi_types.Date `json:"retires_at,omitempty"`
}

// ModelInput Model selection accepted at Invocation admission. The object is
// canonical. The `provider/id` string is caller convenience: nvoken
// splits on the first slash and always echoes the object form.
type ModelInput struct {
	union json.RawMessage
}

// ModelInput1 defines model for ModelInput.1.
type ModelInput1 = string

// ModelInputCapabilities defines model for ModelInputCapabilities.
type ModelInputCapabilities struct {
	Media ModelMediaCapabilities `json:"media"`
}

// ModelList defines model for ModelList.
type ModelList struct {
	// CatalogVersion Opaque version of the complete unfiltered catalog and pricing snapshot.
	CatalogVersion string            `json:"catalog_version"`
	Items          []ModelDescriptor `json:"items"`
}

// ModelMediaCapabilities defines model for ModelMediaCapabilities.
type ModelMediaCapabilities struct {
	Document ModelMediaKindCapabilities `json:"document"`
	Image    ModelMediaKindCapabilities `json:"image"`
}

// ModelMediaKindCapabilities defines model for ModelMediaKindCapabilities.
type ModelMediaKindCapabilities struct {
	// MediaTypes Exact media types admission accepts for this model. An empty list
	// means the modality is unsupported.
	MediaTypes []string `json:"media_types"`
	Supported  bool     `json:"supported"`
}

// ModelPricing Standard local price evidence used by nvoken's estimated-cost
// guardrail. A `priced` object also carries currency, unit, input, output,
// and update date. `unpriced` and `unknown` carry only status and the
// opaque pricing version. Clients compare versions for equality only.
type ModelPricing struct {
	CacheRead  *string               `json:"cache_read,omitempty"`
	CacheWrite *string               `json:"cache_write,omitempty"`
	Currency   *ModelPricingCurrency `json:"currency,omitempty"`
	Input      *string               `json:"input,omitempty"`
	Output     *string               `json:"output,omitempty"`

	// PricingVersion Opaque nvoken-owned identifier for the local pricing snapshot.
	// Values never encode an upstream dependency name or version.
	PricingVersion string              `json:"pricing_version"`
	Status         ModelPricingStatus  `json:"status"`
	Unit           *ModelPricingUnit   `json:"unit,omitempty"`
	UpdatedAt      *openapi_types.Date `json:"updated_at,omitempty"`
}

// ModelPricingCurrency defines model for ModelPricing.Currency.
type ModelPricingCurrency string

// ModelPricingStatus defines model for ModelPricing.Status.
type ModelPricingStatus string

// ModelPricingUnit defines model for ModelPricing.Unit.
type ModelPricingUnit string

// ModelProvenance Reusable BYOK sources include safe provider key and version IDs. Caller,
// platform, and config sources omit them. No secret material is
// included.
type ModelProvenance struct {
	Provider string `json:"provider"`

	// ProviderKeyID UUIDv7 with the public `pkey_` prefix.
	ProviderKeyID     *ProviderKeyID                   `json:"provider_key_id,omitempty"`
	ProviderKeySource ModelProvenanceProviderKeySource `json:"provider_key_source"`

	// ProviderKeyVersionID UUIDv7 with the public `pkeyv_` prefix.
	ProviderKeyVersionID *ProviderKeyVersionID `json:"provider_key_version_id,omitempty"`
	RequestedModel       string                `json:"requested_model"`
	ServedModel          string                `json:"served_model"`
}

// ModelProvenanceProviderKeySource defines model for ModelProvenance.ProviderKeySource.
type ModelProvenanceProviderKeySource string

// ModelProvider Extensible canonical provider identifier. Consumers must preserve
// unknown values so adding a provider does not break decoding. Request
// positions still reject providers not registered by the installation.
type ModelProvider = string

// ModelReasoningBudgetCapabilities defines model for ModelReasoningBudgetCapabilities.
type ModelReasoningBudgetCapabilities struct {
	Maximum         *int `json:"maximum,omitempty"`
	Minimum         *int `json:"minimum,omitempty"`
	Supported       bool `json:"supported"`
	WithTemperature bool `json:"with_temperature"`
}

// ModelReasoningCapabilities defines model for ModelReasoningCapabilities.
type ModelReasoningCapabilities struct {
	BudgetTokens           ModelReasoningBudgetCapabilities `json:"budget_tokens"`
	Effort                 ModelReasoningEffortCapabilities `json:"effort"`
	EffortBudgetCompatible bool                             `json:"effort_budget_compatible"`
}

// ModelReasoningEffortCapabilities defines model for ModelReasoningEffortCapabilities.
type ModelReasoningEffortCapabilities struct {
	Supported       bool              `json:"supported"`
	Values          []ReasoningEffort `json:"values"`
	WithTemperature bool              `json:"with_temperature"`
}

// ModelSamplingCapabilities defines model for ModelSamplingCapabilities.
type ModelSamplingCapabilities struct {
	// Temperature Whether nvoken can guarantee an explicit temperature reaches this exact model path.
	Temperature bool `json:"temperature"`
}

// ModelToolCapabilities defines model for ModelToolCapabilities.
type ModelToolCapabilities struct {
	Choice ModelToolChoiceCapabilities `json:"choice"`

	// WebSearch Whether this exact model supports the provider's server-side web
	// search. Admission fails closed on it, so `provider_tools` is
	// refused on a model that reports false — including any uncataloged
	// model, which reports nothing.
	WebSearch bool `json:"web_search"`
}

// ModelToolChoiceCapabilities defines model for ModelToolChoiceCapabilities.
type ModelToolChoiceCapabilities struct {
	Modes []ModelToolChoiceMode `json:"modes"`

	// WithReasoning Modes that remain exact when explicit reasoning is active.
	WithReasoning []ModelToolChoiceMode `json:"with_reasoning"`
}

// ModelToolChoiceMode defines model for ModelToolChoiceMode.
type ModelToolChoiceMode string

// ModelUsage defines model for ModelUsage.
type ModelUsage struct {
	CacheCreationInputTokens *int       `json:"cache_creation_input_tokens,omitempty"`
	CacheReadInputTokens     *int       `json:"cache_read_input_tokens,omitempty"`
	EstimatedCost            *ModelCost `json:"estimated_cost,omitempty"`
	InputTokens              int        `json:"input_tokens"`

	// ModelCalls Model requests represented by this aggregate. Older retained evidence may omit it.
	ModelCalls      *int `json:"model_calls,omitempty"`
	OutputTokens    int  `json:"output_tokens"`
	ReasoningTokens *int `json:"reasoning_tokens,omitempty"`
}

// NudgeAcknowledgement defines model for NudgeAcknowledgement.
type NudgeAcknowledgement struct {
	// AfterSequence Transcript position to watch from for the promoted message. It is
	// the Session cursor as of this call for input that has not been
	// drained, and the position immediately before the promoted message
	// for input that already has, so reading after it finds the message
	// either way.
	AfterSequence int64 `json:"after_sequence"`
	Deduped       bool  `json:"deduped"`

	// PendingInputID UUIDv7 with the public `npin_` prefix.
	PendingInputID PendingInputID `json:"pending_input_id"`

	// State `pending` is the only state the turn consumes. `drained` means the
	// executor promoted the content into the transcript, `expired` that the
	// Invocation settled without taking it, and `cancelled` that the caller
	// withdrew it first.
	State PendingInputStatus `json:"state"`
}

// NudgeInvocationRequest defines model for NudgeInvocationRequest.
type NudgeInvocationRequest struct {
	// Content Text guidance for the running turn: a string, or an array of text
	// blocks. Media blocks are refused here.
	Content InvocationInput `json:"content"`

	// IdempotencyKey Per-Invocation retry key. The same key with the same content
	// returns the original acknowledgement with `deduped: true`; the
	// same key with different content is refused, so a key is never
	// reused with different words.
	IdempotencyKey *string `json:"idempotency_key,omitempty"`
}

// OutputTextDeltaEvent defines model for OutputTextDeltaEvent.
type OutputTextDeltaEvent struct {
	// Attempt Execution attempt that emitted this preview. Discard provisional
	// output from earlier attempts when this value increases.
	Attempt      int64     `json:"attempt"`
	ContentIndex int       `json:"content_index"`
	EmittedAt    time.Time `json:"emitted_at"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID `json:"invocation_id"`

	// Iteration Model iteration that owns the assistant message.
	Iteration int `json:"iteration"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID                `json:"session_id"`
	Text      string                   `json:"text"`
	Type      OutputTextDeltaEventType `json:"type"`
}

// OutputTextDeltaEventType defines model for OutputTextDeltaEvent.Type.
type OutputTextDeltaEventType string

// PendingHostToolCall defines model for PendingHostToolCall.
type PendingHostToolCall struct {
	// DeadlineAt The Invocation's explicit waiting deadline, or null when external
	// waiting is unbounded.
	DeadlineAt *time.Time  `json:"deadline_at"`
	ID         ToolCallID  `json:"id"`
	Input      interface{} `json:"input"`
	Name       string      `json:"name"`
}

// PendingInput defines model for PendingInput.
type PendingInput struct {
	// Content A plain string is shorthand for one text block; an array is ordered
	// multi-block input mixing text, images, and documents. Both normalize
	// to the same stored message and are therefore equal for idempotency.
	// At most 8 blocks may carry media, and their decoded payloads must
	// total at most 16777216 bytes.
	Content   InvocationInput `json:"content"`
	CreatedAt time.Time       `json:"created_at"`
	DrainedAt *time.Time      `json:"drained_at,omitempty"`

	// DrainedMessageSequence Transcript sequence the content was promoted into. Present only on
	// a drained input, and the receipt that the model saw it.
	DrainedMessageSequence *int64 `json:"drained_message_sequence,omitempty"`

	// ID UUIDv7 with the public `npin_` prefix.
	ID             PendingInputID `json:"id"`
	IdempotencyKey *string        `json:"idempotency_key,omitempty"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID `json:"invocation_id"`
	SettledAt    *time.Time   `json:"settled_at,omitempty"`

	// Status `pending` is the only state the turn consumes. `drained` means the
	// executor promoted the content into the transcript, `expired` that the
	// Invocation settled without taking it, and `cancelled` that the caller
	// withdrew it first.
	Status PendingInputStatus `json:"status"`
}

// PendingInputID UUIDv7 with the public `npin_` prefix.
type PendingInputID = string

// PendingInputList defines model for PendingInputList.
type PendingInputList struct {
	HasMore    bool           `json:"has_more"`
	Items      []PendingInput `json:"items"`
	NextCursor *string        `json:"next_cursor"`
}

// PendingInputStatus `pending` is the only state the turn consumes. `drained` means the
// executor promoted the content into the transcript, `expired` that the
// Invocation settled without taking it, and `cancelled` that the caller
// withdrew it first.
type PendingInputStatus string

// ProviderKey Safe metadata only; secret material is never represented.
type ProviderKey struct {
	CreatedAt time.Time  `json:"created_at"`
	CreatedBy string     `json:"created_by"`
	ExpiresAt *time.Time `json:"expires_at"`

	// ID UUIDv7 with the public `pkey_` prefix.
	ID                ProviderKeyID         `json:"id"`
	OverlapExpiresAt  *time.Time            `json:"overlap_expires_at"`
	PreviousVersionID *ProviderKeyVersionID `json:"previous_version_id"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider  ModelProvider `json:"provider"`
	RevokedAt *time.Time    `json:"revoked_at"`

	// Scope `app` is one provider key shared by every tenant of one app;
	// `tenant` is one provider key for a single named
	// tenant_key inside the app.
	Scope ProviderKeyScope `json:"scope"`

	// Status Active roots remain rotatable and revocable even when their current version has expired.
	Status    ProviderKeyStatus `json:"status"`
	TenantKey *string           `json:"tenant_key"`
	UpdatedAt time.Time         `json:"updated_at"`
	Version   int               `json:"version"`

	// VersionID UUIDv7 with the public `pkeyv_` prefix.
	VersionID ProviderKeyVersionID `json:"version_id"`

	// VersionStatus An expired current version is unusable; rotate the active provider key before any admission or model call can use it.
	VersionStatus ProviderKeyVersionStatus `json:"version_status"`
}

// ProviderKeyStatus Active roots remain rotatable and revocable even when their current version has expired.
type ProviderKeyStatus string

// ProviderKeyVersionStatus An expired current version is unusable; rotate the active provider key before any admission or model call can use it.
type ProviderKeyVersionStatus string

// ProviderKeyID UUIDv7 with the public `pkey_` prefix.
type ProviderKeyID = string

// ProviderKeyList defines model for ProviderKeyList.
type ProviderKeyList struct {
	HasMore    bool          `json:"has_more"`
	Items      []ProviderKey `json:"items"`
	NextCursor *string       `json:"next_cursor"`
}

// ProviderKeyScope `app` is one provider key shared by every tenant of one app;
// `tenant` is one provider key for a single named
// tenant_key inside the app.
type ProviderKeyScope string

// ProviderKeySelection Exactly one selection matching the canonical model provider. Omitting
// `provider_keys` entirely resolves the default chain instead:
// the app's stored key when one exists, else the self-hosted
// installation's environment key (config_byok), else platform funding
// when the installation enables it.
type ProviderKeySelection struct {
	union json.RawMessage
}

// ProviderKeySelection0 defines model for ProviderKeySelection.0.
type ProviderKeySelection0 struct {
	Key ProviderStaticKey `json:"key"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider               `json:"provider"`
	Source   ProviderKeySelection0Source `json:"source"`
}

// ProviderKeySelection0Source defines model for ProviderKeySelection.0.Source.
type ProviderKeySelection0Source string

// ProviderKeySelection1 defines model for ProviderKeySelection.1.
type ProviderKeySelection1 struct {
	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider               `json:"provider"`
	Source   ProviderKeySelection1Source `json:"source"`
}

// ProviderKeySelection1Source defines model for ProviderKeySelection.1.Source.
type ProviderKeySelection1Source string

// ProviderKeyUsage defines model for ProviderKeyUsage.
type ProviderKeyUsage struct {
	// ID UUIDv7 with the public `pkey_` prefix.
	ID ProviderKeyID `json:"id"`

	// Invocations Invocations that bound this provider key at admission.
	Invocations int `json:"invocations"`

	// LastUsedAt Latest durable model checkpoint under any bound Invocation, null before the first model call.
	LastUsedAt *time.Time `json:"last_used_at"`

	// Provider Extensible canonical provider identifier. Consumers must preserve
	// unknown values so adding a provider does not break decoding. Request
	// positions still reject providers not registered by the installation.
	Provider ModelProvider `json:"provider"`

	// Scope `app` is one provider key shared by every tenant of one app;
	// `tenant` is one provider key for a single named
	// tenant_key inside the app.
	Scope ProviderKeyScope `json:"scope"`

	// Usage Summed checkpoint usage, null before the first model call.
	Usage *ModelUsage `json:"usage"`
}

// ProviderKeyVersionID UUIDv7 with the public `pkeyv_` prefix.
type ProviderKeyVersionID = string

// ProviderStaticKey defines model for ProviderStaticKey.
type ProviderStaticKey struct {
	// APIKey Encrypted before persistence and never returned.
	APIKey *string `json:"api_key,omitempty"`
}

// ProviderTool Selects one server-side tool the provider runs inside the generation
// call. Unlike a host tool the turn does not park for it, and unlike a
// builtin nvoken does not execute it — the provider does, and bills it on
// the same provider key the model runs on. That is deliberate: a
// runtime-operated search would make nvoken hold a search vendor
// relationship and answer "who pays per query" for every installation,
// while this uses the billing relationship the host already has.
//
// **Anthropic only, for now.** The providers' search controls differ
// materially, and a "common" option set would either be the empty
// intersection or would silently drop fields on some providers — which is
// exactly what nvoken's fail-closed posture forbids. Another provider is
// rejected at admission rather than served a degraded search. Widening is
// per-provider capability work.
//
// A model that does not declare `controls.tools.web_search` is refused
// too, including any uncataloged model: a tool configuration the provider
// quietly ignored would bill a turn that did no searching and return an
// answer the host believes was researched.
//
// **Cost accounting.** Search charges ride the provider's bill outside
// nvoken's estimate. `limits.max_estimated_cost_usd` and the usage
// reporting cover model tokens; the provider does not report a
// per-search fee through the interface nvoken reads, so a host that needs
// a hard ceiling on search spend should set `max_uses`.
//
// **What appears in the transcript.** The model's `server_tool_use` block
// is public, so a host can show what was searched for. The matching
// `web_search_tool_result` is not: it carries the provider's encrypted
// continuation bytes, which the next turn must echo back verbatim and
// which mean nothing to a host. The sources behind a claim arrive as
// citations on the visible text blocks.
type ProviderTool struct {
	Type ProviderToolType `json:"type"`

	// WebSearch Anthropic web search options, passed through as the provider defines
	// them.
	WebSearch WebSearchTool `json:"web_search"`
}

// ProviderToolType defines model for ProviderTool.Type.
type ProviderToolType string

// Reasoning defines model for Reasoning.
type Reasoning struct {
	// BudgetTokens Provider reasoning target, not a separate Invocation hard budget.
	// Requires an explicit, larger limits.max_output_tokens and exact
	// support in the selected model descriptor.
	BudgetTokens *int             `json:"budget_tokens,omitempty"`
	Effort       *ReasoningEffort `json:"effort,omitempty"`
}

// ReasoningEffort defines model for ReasoningEffort.
type ReasoningEffort string

// RedactedBlock Placeholder for an assistant message whose content is private.
type RedactedBlock struct {
	Type RedactedBlockType `json:"type"`
}

// RedactedBlockType defines model for RedactedBlock.Type.
type RedactedBlockType string

// RegisterAppRequest defines model for RegisterAppRequest.
type RegisterAppRequest struct {
	// DisplayName Optional human-facing label.
	DisplayName *string `json:"display_name,omitempty"`

	// ExternalRef Optional opaque owner reference.
	ExternalRef *string `json:"external_ref,omitempty"`

	// Name The unique name identifying this app. Registering a name that
	// already exists is rejected.
	Name string `json:"name"`
}

// ResolvedLimits defines model for ResolvedLimits.
type ResolvedLimits struct {
	ActiveTimeoutSeconds int `json:"active_timeout_seconds"`

	// MaxEstimatedCostUsd Resolved USD list-price guardrail. It is present only when the host
	// requested a cost limit; unknown price metadata fails closed.
	MaxEstimatedCostUsd *float32 `json:"max_estimated_cost_usd,omitempty"`
	MaxIterations       int      `json:"max_iterations"`
	MaxOutputTokens     *int     `json:"max_output_tokens,omitempty"`
	TotalTimeoutSeconds int      `json:"total_timeout_seconds"`

	// WaitingTimeoutSeconds The explicit cumulative external-waiting SLA, or null when waits
	// are durable and unbounded.
	WaitingTimeoutSeconds *int `json:"waiting_timeout_seconds"`
}

// ResumeInvocationRequest Replacement for the one turn-level consumption ceiling named by the
// paused Invocation's stop_reason. Exactly that field must be present
// and must be raised above both the previous ceiling and usage so far.
type ResumeInvocationRequest struct {
	// Limits Optional requested limits. Total time bounds the entire turn, active
	// time bounds model and tool execution, and waiting time bounds the
	// cumulative time parked for host or callback tool results. Installation
	// defaults supply all three time limits and the iteration limit.
	// Output-token and estimated-cost limits are unlimited when omitted.
	// Installation maxima may be lower than the schema's numeric range.
	Limits Limits `json:"limits"`
}

// RetentionPolicy Bounds how long an idle Session is retained. Past the window nvoken
// erases the Session and its whole subtree, exactly as
// `DELETE /v1/sessions/{session_id}` would.
//
// The window measures idle time, not lifetime: it restarts on every
// Invocation admission and every settlement, so a turn that runs longer
// than the window cannot expire underneath itself.
//
// **Automatic expiry never cancels running work.** A Session holding a
// queued, running, or waiting Invocation is skipped and reconsidered on
// the next sweep. An explicit `DELETE` may still stop a running turn,
// because a person asked for it; a clock may not.
//
// Omitting retention retains the Session until it is deleted explicitly,
// which stays the default.
type RetentionPolicy struct {
	// TTLSeconds Idle window, from one hour to thirty days.
	TTLSeconds int `json:"ttl_seconds"`
}

// RotateProviderKeyRequest defines model for RotateProviderKeyRequest.
type RotateProviderKeyRequest struct {
	ExpiresAt      *time.Time        `json:"expires_at,omitempty"`
	IdempotencyKey string            `json:"idempotency_key"`
	Key            ProviderStaticKey `json:"key"`
	OverlapSeconds *int              `json:"overlap_seconds,omitempty"`
}

// Sampling defines model for Sampling.
type Sampling struct {
	// Temperature Portable sampling temperature. Omit sampling to preserve the
	// provider default. An explicit zero remains explicit. Admission
	// fails closed unless the selected model descriptor positively
	// declares support.
	Temperature float64 `json:"temperature"`
}

// SeedMessage defines model for SeedMessage.
type SeedMessage struct {
	// Content Text-only canonical history. A string is shorthand for one text block;
	// both forms normalize to the same stored content-block array.
	Content SeedMessageContent `json:"content"`
	Role    SeedMessageRole    `json:"role"`
}

// SeedMessageRole defines model for SeedMessage.Role.
type SeedMessageRole string

// SeedMessageContent Text-only canonical history. A string is shorthand for one text block;
// both forms normalize to the same stored content-block array.
type SeedMessageContent struct {
	union json.RawMessage
}

// SeedMessageContent0 defines model for SeedMessageContent.0.
type SeedMessageContent0 = string

// SeedMessageContent1 defines model for SeedMessageContent.1.
type SeedMessageContent1 = []TextInputBlock

// ServerToolUseBlock A provider-run tool call retained in the public transcript.
type ServerToolUseBlock struct {
	ID    string                 `json:"id"`
	Input interface{}            `json:"input"`
	Name  string                 `json:"name"`
	Type  ServerToolUseBlockType `json:"type"`
}

// ServerToolUseBlockType defines model for ServerToolUseBlock.Type.
type ServerToolUseBlockType string

// Session defines model for Session.
type Session struct {
	// ActiveInvocationID The queued, running, waiting, or paused Invocation, if one exists.
	ActiveInvocationID *InvocationID `json:"active_invocation_id"`

	// ActiveInvocationStatus Status of active_invocation_id; null exactly when that ID is null.
	ActiveInvocationStatus *SessionActiveInvocationStatus `json:"active_invocation_status"`

	// AgentID Null only for a Session created ahead of its first Invocation.
	// The first admitted Invocation binds it; once set the binding is
	// immutable.
	AgentID *AgentID `json:"agent_id"`

	// BlockingBudget Budget blocking the active paused Invocation, otherwise null.
	BlockingBudget *BudgetBlock `json:"blocking_budget"`

	// Budget The current mutable Session-wide estimated-cost guardrail.
	Budget *SessionBudget `json:"budget"`

	// Compaction The automatic compaction policy this Session actually applies, or
	// null when it compacts nothing. It is echoed resolved: a request
	// that asked for `trigger_tokens: auto` reads back the integer that
	// resolved to, and a request that named no model reads back the
	// model the policy bound. Nothing here is ever the unresolved
	// request.
	Compaction *CompactionPolicy `json:"compaction"`

	// Context Read-time retained-context estimate and the model window it is
	// measured against. Null until the Session has either a compaction
	// model or an Invocation primary model. The object remains present
	// for an uncataloged model, with `context_window_tokens: null`.
	Context   *SessionContext `json:"context"`
	CreatedAt time.Time       `json:"created_at"`

	// ExpiresAt When this Session becomes eligible for automatic erasure, or null
	// when it has no retention window. It moves forward on every
	// Invocation admission and settlement, so a Session in use stays
	// ahead of it.
	ExpiresAt *time.Time `json:"expires_at"`

	// ForkedFrom Durable source prefix lineage, or null for an original Session.
	ForkedFrom *SessionForkLineage `json:"forked_from"`

	// ID UUIDv7 with the public `sesn_` prefix.
	ID SessionID `json:"id"`

	// Metadata Host correlation data, returned verbatim. Set at creation through
	// `session_options.metadata` and changed with
	// `PATCH /v1/sessions/{session_id}`.
	Metadata *Metadata `json:"metadata"`

	// PendingToolCalls Pending host calls for the active waiting Invocation.
	PendingToolCalls *[]PendingHostToolCall `json:"pending_tool_calls,omitempty"`

	// Retention The idle retention window this Session was created with, or null
	// when it is retained until deleted explicitly. A window outside the
	// supported range is refused at creation rather than clamped, so
	// what is read back is always exactly what applies.
	Retention  *RetentionPolicy `json:"retention"`
	SessionKey *string          `json:"session_key"`

	// TenantKey Immutable effective tenant partition reference.
	TenantKey *string   `json:"tenant_key"`
	UpdatedAt time.Time `json:"updated_at"`

	// Usage Read-time sum of this Session's non-null Invocation usage and
	// committed private compaction usage. Null until either exists. This
	// normalized estimate is not a billing ledger.
	Usage *ModelUsage `json:"usage"`

	// UserKey Host-owned end-user label recorded when this Session was opened.
	// Filtering only; not an isolation boundary.
	UserKey *string `json:"user_key"`
}

// SessionActiveInvocationStatus defines model for Session.ActiveInvocationStatus.
type SessionActiveInvocationStatus string

// SessionBudget Mutable Session-wide USD list-price guardrail. The estimate is derived
// from durable model-call evidence and is operational control, not a
// billing ledger. Models without catalog pricing fail closed.
type SessionBudget struct {
	MaxEstimatedCostUsd float32 `json:"max_estimated_cost_usd"`
}

// SessionCompaction defines model for SessionCompaction.
type SessionCompaction struct {
	// CoversThrough Last canonical Session message sequence included in this pass.
	CoversThrough int64     `json:"covers_through"`
	CreatedAt     time.Time `json:"created_at"`

	// FailureClass nvoken's stable failure classification for a fell-through pass;
	// null when status is applied.
	FailureClass *string `json:"failure_class"`

	// ID UUIDv7-shaped diagnostic identifier with the public `cmp_` prefix.
	ID SessionCompactionID `json:"id"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID `json:"invocation_id"`

	// Status `applied` means the summary became the Session's private context
	// projection. `fell_through` means the turn continued with the prior
	// projection after a failed or invalid summary pass.
	Status SessionCompactionStatus `json:"status"`

	// Summary Diagnostic private context projection for an applied pass; null
	// for a fell-through pass. It is derived from the host transcript
	// and is not a canonical Session message.
	Summary *string `json:"summary"`

	// Usage Summary-model usage when trustworthy evidence exists. This counts
	// toward Session usage but never toward the creating Invocation's
	// usage.
	Usage *ModelUsage `json:"usage"`
}

// SessionCompactionID UUIDv7-shaped diagnostic identifier with the public `cmp_` prefix.
type SessionCompactionID = string

// SessionCompactionList defines model for SessionCompactionList.
type SessionCompactionList struct {
	HasMore    bool                `json:"has_more"`
	Items      []SessionCompaction `json:"items"`
	NextCursor *string             `json:"next_cursor"`
}

// SessionCompactionStatus `applied` means the summary became the Session's private context
// projection. `fell_through` means the turn continued with the prior
// projection after a failed or invalid summary pass.
type SessionCompactionStatus string

// SessionContentBlock One public, provider-neutral transcript block. Stored media bytes are
// projected on reads as `type`, `media_type`, optional `title`, byte
// count in `bytes`, and a `sha256:` digest.
//
// New block types will appear as features ship. Render the types you know
// and skip the rest.
type SessionContentBlock struct {
	union json.RawMessage
}

// SessionContext defines model for SessionContext.
type SessionContext struct {
	// ContextWindowTokens Cataloged context window for `model`, or null when the selected
	// model is not in this nvoken installation's catalog.
	ContextWindowTokens *int `json:"context_window_tokens"`

	// EstimatedTokens Estimated tokens in the retained generation context: the latest
	// applied summary plus the generation-eligible messages after its
	// watermark. This is the same estimate the compaction trigger uses.
	EstimatedTokens int64 `json:"estimated_tokens"`

	// Model Model whose context window applies: the Session compaction model
	// when configured, otherwise the most recent Invocation's primary
	// model.
	Model Model `json:"model"`
}

// SessionForkLineage The source Session and inclusive source message of a fork. These IDs
// remain as lineage after the source Session is erased.
type SessionForkLineage struct {
	// Message UUIDv7 with the public `smsg_` prefix.
	Message SessionMessageID `json:"message"`

	// Session UUIDv7 with the public `sesn_` prefix.
	Session SessionID `json:"session"`
}

// SessionID UUIDv7 with the public `sesn_` prefix.
type SessionID = string

// SessionList defines model for SessionList.
type SessionList struct {
	HasMore    bool      `json:"has_more"`
	Items      []Session `json:"items"`
	NextCursor *string   `json:"next_cursor"`
}

// SessionMessage defines model for SessionMessage.
type SessionMessage struct {
	// AgentID UUIDv7 with the public `agnt_` prefix.
	AgentID AgentID               `json:"agent_id"`
	Content []SessionContentBlock `json:"content"`

	// CopiedFromMessageID Present only on a fork copy; names its source message.
	CopiedFromMessageID *SessionMessageID `json:"copied_from_message_id,omitempty"`
	CreatedAt           time.Time         `json:"created_at"`

	// ID UUIDv7 with the public `smsg_` prefix.
	ID SessionMessageID `json:"id"`

	// InvocationID The Invocation that wrote this row, or null for seeded and copied
	// history. `origin` and `copied_from_message_id` explain that null.
	InvocationID *InvocationID `json:"invocation_id"`

	// Origin Present only when the message content was asserted by the host at
	// Session creation. Messages nvoken wrote carry no origin marker;
	// fork copies preserve the source marker.
	Origin *SessionMessageOrigin `json:"origin,omitempty"`

	// Phase What this assistant message was in its turn. Absent on user and
	// tool messages, which have no phase.
	//
	// Derived at read time from the settling facts on an
	// Invocation-owned message and frozen onto a fork copy. It never
	// takes host input. One consequence: a live source message read
	// before its turn settles reads `commentary`, because nothing had
	// settled yet. The same source message read after settlement reads
	// its final phase, and the stream's `invocation_changes` are what
	// announce the settlement in between.
	Phase    *MessagePhase      `json:"phase,omitempty"`
	Role     SessionMessageRole `json:"role"`
	Sequence int64              `json:"sequence"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID `json:"session_id"`

	// UserKey Per-turn host end-user attribution. Copied rows preserve it;
	// seeded history may omit it.
	UserKey *string `json:"user_key"`
}

// SessionMessageOrigin Present only when the message content was asserted by the host at
// Session creation. Messages nvoken wrote carry no origin marker;
// fork copies preserve the source marker.
type SessionMessageOrigin string

// SessionMessageID UUIDv7 with the public `smsg_` prefix.
type SessionMessageID = string

// SessionMessageList defines model for SessionMessageList.
type SessionMessageList struct {
	HasMore    bool             `json:"has_more"`
	Items      []SessionMessage `json:"items"`
	NextCursor *string          `json:"next_cursor"`
}

// SessionMessageRole defines model for SessionMessageRole.
type SessionMessageRole string

// SessionOptions Durable Session options. Every member is optional and at least one must
// be present. A new Session stores the supplied values. When a keyed
// upsert or admission resolves an existing Session, supplied values are
// comparison-only: equal values are accepted and different values return
// `session_options_conflict` with `details.conflicting_paths`.
//
// `compaction` requires an Invocation because the policy is validated
// against that turn's model. It may be installed on any Session that has
// no compaction policy yet, including a Session created ahead of its first
// Invocation or a long-running Session opting in later. An `auto` trigger
// is materialized from the installing Invocation's context window. Once
// set, the resolved policy is immutable. `POST /v1/sessions` therefore
// still rejects `compaction`, while admissions may supply it with either
// `session_id`, `session_key`, or a new Session.
type SessionOptions struct {
	// Budget Mutable Session-wide USD list-price guardrail. The estimate is derived
	// from durable model-call evidence and is operational control, not a
	// billing ledger. Models without catalog pricing fail closed.
	Budget *SessionBudget `json:"budget,omitempty"`

	// Compaction Durable Session context-compaction policy. Omission of model uses the
	// installing Invocation's primary model. An explicit model must be
	// cataloged and use the same provider. Later Invocations automatically
	// use this resolved policy and the latest Session summary.
	Compaction *CompactionPolicy `json:"compaction,omitempty"`

	// Metadata Opaque host correlation data. nvoken stores it, returns it verbatim,
	// and never interprets it — it exists so a support engineer holding an
	// Invocation id can get back to the board, ticket, or surface the turn
	// came from, which nvoken deliberately knows nothing about.
	//
	// At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`,
	// `:`, and `-`; values are at most 512 bytes of UTF-8.
	//
	// There is no `title` field on a Session by design. A title is one of
	// these entries (`{"title": "Refund policy"}`), which keeps nvoken from
	// forming opinions about a string only the host renders.
	Metadata *Metadata `json:"metadata,omitempty"`

	// Retention Bounds how long an idle Session is retained. Past the window nvoken
	// erases the Session and its whole subtree, exactly as
	// `DELETE /v1/sessions/{session_id}` would.
	//
	// The window measures idle time, not lifetime: it restarts on every
	// Invocation admission and every settlement, so a turn that runs longer
	// than the window cannot expire underneath itself.
	//
	// **Automatic expiry never cancels running work.** A Session holding a
	// queued, running, or waiting Invocation is skipped and reconsidered on
	// the next sweep. An explicit `DELETE` may still stop a running turn,
	// because a person asked for it; a clock may not.
	//
	// Omitting retention retains the Session until it is deleted explicitly,
	// which stays the default.
	Retention *RetentionPolicy `json:"retention,omitempty"`
}

// StreamEndEvent defines model for StreamEndEvent.
type StreamEndEvent struct {
	InvocationID *InvocationID `json:"invocation_id"`

	// Reason `rotate` means reconnect with `resume_cursor`. `terminal` means
	// the scoped Invocation settled, or the Session became idle for a
	// Session-scoped stream.
	Reason       StreamEndEventReason `json:"reason"`
	ResumeCursor string               `json:"resume_cursor"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID          `json:"session_id"`
	Type      StreamEndEventType `json:"type"`
}

// StreamEndEventReason `rotate` means reconnect with `resume_cursor`. `terminal` means
// the scoped Invocation settled, or the Session became idle for a
// Session-scoped stream.
type StreamEndEventReason string

// StreamEndEventType defines model for StreamEndEvent.Type.
type StreamEndEventType string

// StreamResyncEvent defines model for StreamResyncEvent.
type StreamResyncEvent struct {
	InvocationID *InvocationID           `json:"invocation_id"`
	Reason       StreamResyncEventReason `json:"reason"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID             `json:"session_id"`
	Type      StreamResyncEventType `json:"type"`
}

// StreamResyncEventReason defines model for StreamResyncEvent.Reason.
type StreamResyncEventReason string

// StreamResyncEventType defines model for StreamResyncEvent.Type.
type StreamResyncEventType string

// StructuredOutput Optional per-Invocation structured-output contract. nvoken exposes a
// reserved durable submit tool and publishes only a server-validated
// terminal object. This does not enable host-defined tools.
type StructuredOutput struct {
	// Schema Self-contained JSON Schema for an object result. Compact canonical
	// JSON is limited to 32 KiB and 16 schema positions. Supported
	// keywords are type, title, description, properties, required,
	// additionalProperties, items, enum, pattern, minLength, maxLength,
	// minItems, maxItems, uniqueItems, minimum, and maximum. Every schema
	// position has one string type; pattern values are limited to 1,024
	// UTF-8 bytes; references and other keywords are rejected. Numeric
	// bounds are read as values, not spellings: 10, 10.0, and 1e1 are the
	// same bound.
	Schema map[string]interface{} `json:"schema"`
}

// StructuredOutputProvenance Immutable proof that output equals the accepted reserved durable
// ToolCall request under the admitted schema digest.
type StructuredOutputProvenance struct {
	SchemaSha256 string                           `json:"schema_sha256"`
	Source       StructuredOutputProvenanceSource `json:"source"`
	ToolCallID   *string                          `json:"tool_call_id,omitempty"`
}

// StructuredOutputProvenanceSource defines model for StructuredOutputProvenance.Source.
type StructuredOutputProvenanceSource string

// SubmitHostToolResultsRequest defines model for SubmitHostToolResultsRequest.
type SubmitHostToolResultsRequest struct {
	Results []struct {
		Content    interface{} `json:"content"`
		IsError    *bool       `json:"is_error,omitempty"`
		ToolCallID ToolCallID  `json:"tool_call_id"`
	} `json:"results"`
}

// SubmitHostToolResultsResponse defines model for SubmitHostToolResultsResponse.
type SubmitHostToolResultsResponse struct {
	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID     InvocationID               `json:"invocation_id"`
	PendingToolCalls []PendingHostToolCall      `json:"pending_tool_calls"`
	Results          []HostToolResultAcceptance `json:"results"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID `json:"session_id"`

	// Status `completed`, `incomplete`, `failed`, and `cancelled` are terminal and
	// immutable.
	//
	// `completed` means exactly one thing: the turn ended the way it was
	// asked to — the model finished, or the caller interrupted it.
	// `incomplete` means the runtime enforced a budget at a coherent
	// execution seam. Any budget exhaustion observed at such a seam settles
	// `incomplete` with `stop_reason` naming the budget; the transcript is
	// valid and stays in the next turn's context, exactly as a completed
	// turn's does. Exhaustion that cannot reach a seam — a deadline landing
	// mid-request, reaper settlement — still settles `failed` with `error`
	// as the authority. A schema-bearing turn that never published a
	// validated object also fails, `structured_output_unsatisfied`,
	// whichever budget stopped it.
	//
	// `waiting`—known elsewhere as `requires_action`—means the Invocation has
	// durable pending host ToolCalls and owns no execution lease. Final
	// result acceptance moves it back to
	// `queued`. Checkpoint recovery may also move `running` back to
	// `queued`; the `attempt` counter distinguishes a retry from tool-result
	// acceptance, and lifecycle revision orders every transition.
	//
	// `paused` is nonterminal dormant work stopped at a coherent consumption
	// ceiling. It owns no lease and its deadlines are suspended until the
	// exhausted turn limit is raised or the Session budget is raised or
	// removed. It still accepts interrupt, cancel, and staged nudges.
	Status InvocationStatus `json:"status"`
}

// TextBlock defines model for TextBlock.
type TextBlock struct {
	// Citations Source locations attached to generated text.
	Citations *[]Citation   `json:"citations,omitempty"`
	Text      string        `json:"text"`
	Type      TextBlockType `json:"type"`
}

// TextBlockType defines model for TextBlock.Type.
type TextBlockType string

// TextInputBlock defines model for TextInputBlock.
type TextInputBlock struct {
	Text string             `json:"text"`
	Type TextInputBlockType `json:"type"`
}

// TextInputBlockType defines model for TextInputBlock.Type.
type TextInputBlockType string

// ThinkingDeltaEvent defines model for ThinkingDeltaEvent.
type ThinkingDeltaEvent struct {
	// Attempt Execution attempt that emitted this preview. Discard provisional
	// output from earlier attempts when this value increases.
	Attempt      int64     `json:"attempt"`
	ContentIndex int       `json:"content_index"`
	EmittedAt    time.Time `json:"emitted_at"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID InvocationID `json:"invocation_id"`

	// Iteration Model iteration that owns the assistant message.
	Iteration int `json:"iteration"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID              `json:"session_id"`
	Thinking  string                 `json:"thinking"`
	Type      ThinkingDeltaEventType `json:"type"`
}

// ThinkingDeltaEventType defines model for ThinkingDeltaEvent.Type.
type ThinkingDeltaEventType string

// ToolCall defines model for ToolCall.
type ToolCall struct {
	// Attempts Number of runtime execution attempts for this ToolCall.
	Attempts  int       `json:"attempts"`
	CreatedAt time.Time `json:"created_at"`

	// Delivery Present only when mode is callback.
	Delivery  *ToolCallDelivery `json:"delivery,omitempty"`
	ID        ToolCallID        `json:"id"`
	Iteration int               `json:"iteration"`
	Mode      ToolCallMode      `json:"mode"`
	Name      string            `json:"name"`
	SettledAt *time.Time        `json:"settled_at"`
	Status    ToolCallStatus    `json:"status"`
}

// ToolCallDelivery defines model for ToolCallDelivery.
type ToolCallDelivery struct {
	// Attempts Number of leased HTTP delivery attempts.
	Attempts int64 `json:"attempts"`

	// LastHTTPStatus Last HTTP status returned by the endpoint, or null before any response.
	LastHTTPStatus *int `json:"last_http_status"`

	// Outcome `pending` covers a blocked, available, or currently leased delivery.
	// The other values are terminal transport outcomes.
	Outcome CallbackDeliveryOutcome `json:"outcome"`
}

// ToolCallID defines model for ToolCallID.
type ToolCallID = string

// ToolCallList defines model for ToolCallList.
type ToolCallList struct {
	HasMore    bool       `json:"has_more"`
	Items      []ToolCall `json:"items"`
	NextCursor *string    `json:"next_cursor"`
}

// ToolCallMode defines model for ToolCallMode.
type ToolCallMode string

// ToolCallStatus defines model for ToolCallStatus.
type ToolCallStatus string

// ToolCallbackContext defines model for ToolCallbackContext.
type ToolCallbackContext struct {
	AgentKey   string             `json:"agent_key"`
	DeliveryID CallbackDeliveryID `json:"delivery_id"`

	// InvocationID UUIDv7 with the public `invk_` prefix.
	InvocationID  InvocationID                     `json:"invocation_id"`
	SchemaVersion ToolCallbackContextSchemaVersion `json:"schema_version"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID `json:"session_id"`

	// TenantKey Absent for the app's default tenant.
	TenantKey  *string    `json:"tenant_key,omitempty"`
	ToolCallID ToolCallID `json:"tool_call_id"`
}

// ToolCallbackContextSchemaVersion defines model for ToolCallbackContext.SchemaVersion.
type ToolCallbackContextSchemaVersion int

// ToolCallbackRequest defines model for ToolCallbackRequest.
type ToolCallbackRequest struct {
	Input  interface{}         `json:"input"`
	Nvoken ToolCallbackContext `json:"nvoken"`
}

// ToolCallbackResponse Strict callback reply. The encoded content is limited to 256 KiB and
// 32 nesting levels. `is_error` defaults to false when omitted.
type ToolCallbackResponse struct {
	Content interface{} `json:"content"`
	IsError *bool       `json:"is_error,omitempty"`
}

// ToolChoice Portable Invocation tool selection. auto preserves normal selection;
// none disables tools for the Invocation; required and named apply only
// to the first durable model iteration, then return to auto. name is
// required only for named mode. The final bounded iteration always
// disables tools.
type ToolChoice struct {
	Mode ModelToolChoiceMode `json:"mode"`
	Name *string             `json:"name,omitempty"`
}

// ToolDeclaration defines model for ToolDeclaration.
type ToolDeclaration struct {
	union json.RawMessage
}

// ToolResultBlock The durable result of one tool call.
type ToolResultBlock struct {
	// Content The host's own JSON, retained verbatim. Its canonical encoding is
	// limited to 262144 bytes and 32 nesting levels; the complete result
	// submission body is limited to 1048576 bytes.
	Content   interface{}         `json:"content"`
	IsError   *bool               `json:"is_error,omitempty"`
	ToolUseID string              `json:"tool_use_id"`
	Type      ToolResultBlockType `json:"type"`
}

// ToolResultBlockType defines model for ToolResultBlock.Type.
type ToolResultBlockType string

// ToolUseBlock A host, builtin, MCP, or callback tool call.
type ToolUseBlock struct {
	ID    string           `json:"id"`
	Input interface{}      `json:"input"`
	Name  string           `json:"name"`
	Type  ToolUseBlockType `json:"type"`
}

// ToolUseBlockType defines model for ToolUseBlock.Type.
type ToolUseBlockType string

// TranscriptSnapshot defines model for TranscriptSnapshot.
type TranscriptSnapshot struct {
	HasMore           bool               `json:"has_more"`
	InvocationChanges []InvocationChange `json:"invocation_changes"`
	Messages          []SessionMessage   `json:"messages"`
	NextPageToken     *string            `json:"next_page_token"`

	// ResumeCursor Delivered composite message-sequence and lifecycle-revision watermark.
	ResumeCursor string `json:"resume_cursor"`
}

// TranscriptStreamEvent The JSON value carried by one Session-stream SSE `data:` field.
// Durable `transcript.update` frames carry the resume cursor as both
// payload data and SSE `id`; preview and control frames never carry IDs.
type TranscriptStreamEvent struct {
	union json.RawMessage
}

// TranscriptUpdate defines model for TranscriptUpdate.
type TranscriptUpdate struct {
	InvocationChanges []InvocationChange `json:"invocation_changes"`
	Messages          []SessionMessage   `json:"messages"`

	// ResumeCursor Delivered composite message-sequence and lifecycle-revision watermark.
	ResumeCursor string `json:"resume_cursor"`

	// SessionID UUIDv7 with the public `sesn_` prefix.
	SessionID SessionID            `json:"session_id"`
	Type      TranscriptUpdateType `json:"type"`
}

// TranscriptUpdateType defines model for TranscriptUpdate.Type.
type TranscriptUpdateType string

// URLCitation A URL citation positioned within the generated text.
type URLCitation struct {
	EndIndex   int             `json:"end_index"`
	StartIndex int             `json:"start_index"`
	Title      string          `json:"title"`
	Type       URLCitationType `json:"type"`
	URL        string          `json:"url"`
}

// URLCitationType defines model for URLCitation.Type.
type URLCitationType string

// UpdateAppRequest defines model for UpdateAppRequest.
type UpdateAppRequest struct {
	// DisplayName New human-facing label for the app.
	DisplayName string `json:"display_name"`
}

// UpdateBudgetRequest defines model for UpdateBudgetRequest.
type UpdateBudgetRequest struct {
	MaxEstimatedCostUsd float32 `json:"max_estimated_cost_usd"`
}

// UpdateSessionRequest defines model for UpdateSessionRequest.
type UpdateSessionRequest struct {
	// Budget Replaces the Session budget; null removes it. A finite ceiling may
	// not be lower than estimated cost already consumed. Raising or
	// removing the ceiling atomically requeues a turn paused on it.
	Budget *SessionBudget `json:"budget,omitempty"`

	// Metadata Metadata merge patch. A string value sets the key, `null` deletes
	// it, and an absent key is left alone. Bounds are enforced on the
	// merged result.
	Metadata *map[string]*string `json:"metadata,omitempty"`
}

// WebSearchLocation Approximate location used to bias results. Every member is optional;
// the host decides how precise to be about its end user.
type WebSearchLocation struct {
	City     *string `json:"city,omitempty"`
	Country  *string `json:"country,omitempty"`
	Region   *string `json:"region,omitempty"`
	Timezone *string `json:"timezone,omitempty"`
}

// WebSearchResultLocationCitation A provider search result cited by the generated text.
type WebSearchResultLocationCitation struct {
	CitedText      *string                             `json:"cited_text,omitempty"`
	EncryptedIndex *string                             `json:"encrypted_index,omitempty"`
	Title          string                              `json:"title"`
	Type           WebSearchResultLocationCitationType `json:"type"`
	URL            string                              `json:"url"`
}

// WebSearchResultLocationCitationType defines model for WebSearchResultLocationCitation.Type.
type WebSearchResultLocationCitationType string

// WebSearchTool Anthropic web search options, passed through as the provider defines
// them.
type WebSearchTool struct {
	// AllowedDomains Restrict results to these hosts. Bare hostnames only — a scheme,
	// path, or port is rejected rather than reinterpreted. Mutually
	// exclusive with `blocked_domains`, which is the provider's rule.
	AllowedDomains *[]string `json:"allowed_domains,omitempty"`

	// BlockedDomains Exclude these hosts. Same format as `allowed_domains`, and mutually
	// exclusive with it.
	BlockedDomains *[]string `json:"blocked_domains,omitempty"`

	// MaxUses Searches this turn may run. Omitted means the provider default.
	// This is the only bound nvoken can place on search spend, because
	// the provider does not report a per-search fee it can meter.
	MaxUses *int `json:"max_uses,omitempty"`

	// UserLocation Approximate location used to bias results. Every member is optional;
	// the host decides how precise to be about its end user.
	UserLocation *WebSearchLocation `json:"user_location,omitempty"`
}

// WebhookEvent `invocation.waiting` fires when the Invocation parks with at least one
// unresolved host ToolCall the caller must satisfy; a wait made entirely
// of callback tools emits nothing, because nvoken delivers those itself.
// `invocation.paused` fires when an opt-in consumption budget parks the
// turn and carries the exhausted `stop_reason`.
// `invocation.settled` fires once when the Invocation reaches
// `completed`, `incomplete`, `failed`, or `cancelled`; completed and
// incomplete payloads carry `stop_reason` next to `failure_code`.
type WebhookEvent string

// WebhookTarget Optional endpoint nvoken posts a signed webhook to when this
// Invocation parks awaiting host tool results or reaches a terminal
// status. It is delivery configuration rather than model input, so it is
// excluded from the reusable execution snapshot. Two otherwise identical
// turns that differ only in endpoint therefore share one snapshot.
//
// Delivery is at least once and ordered within one Invocation, and the
// webhook ID is repeated in `Idempotency-Key` on every attempt. nvoken
// webhooks are thin: identifiers and status only, the API stays the read
// authority. A webhook never changes, delays, or fails the Invocation it
// describes.
//
// The endpoint is part of the request, so replaying an idempotency key
// with a changed endpoint or event set is a conflict rather than a
// silent adoption of either value. An installation without a configured
// webhook signing key rejects this field instead of accepting it and
// never delivering it.
type WebhookTarget struct {
	// Events Events to deliver. Omission selects every event, because the
	// harmful default is the one that quietly drops
	// `invocation.waiting` and leaves a parked tool loop with nobody
	// listening. Order does not affect replay comparison.
	Events *[]WebhookEvent `json:"events,omitempty"`

	// URL Absolute HTTPS endpoint with no userinfo and no fragment. Private,
	// loopback, and link-local destinations are refused by the same
	// guarded public egress the callback transport uses.
	URL string `json:"url"`
}

// AgentKeyFilter defines model for AgentKeyFilter.
type AgentKeyFilter = string

// Cursor defines model for Cursor.
type Cursor = string

// DefaultTenantFilter defines model for DefaultTenantFilter.
type DefaultTenantFilter = bool

// IfNoneMatch defines model for IfNoneMatch.
type IfNoneMatch = string

// Limit defines model for Limit.
type Limit = int

// TenantKeyFilter defines model for TenantKeyFilter.
type TenantKeyFilter = string

// UserKeyFilter defines model for UserKeyFilter.
type UserKeyFilter = string

// Conflict defines model for Conflict.
type Conflict = ErrorResponse

// Forbidden defines model for Forbidden.
type Forbidden = ErrorResponse

// Internal defines model for Internal.
type Internal = ErrorResponse

// InvalidRequest defines model for InvalidRequest.
type InvalidRequest = ErrorResponse

// MCPDiscoveryFailed defines model for MCPDiscoveryFailed.
type MCPDiscoveryFailed = ErrorResponse

// NotFound defines model for NotFound.
type NotFound = ErrorResponse

// RateLimited defines model for RateLimited.
type RateLimited = ErrorResponse

// Unauthenticated defines model for Unauthenticated.
type Unauthenticated = ErrorResponse

// Unavailable defines model for Unavailable.
type Unavailable = ErrorResponse

// ListAgentsParams defines parameters for ListAgents.
type ListAgentsParams struct {
	// AgentKey Exact host-owned Agent key. On Session and Invocation lists this is
	// mutually exclusive with agent_id.
	AgentKey *AgentKeyFilter `form:"agent_key,omitempty" json:"agent_key,omitempty"`

	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListAppsParams defines parameters for ListApps.
type ListAppsParams struct {
	// ExternalRef Return only apps whose `external_ref` equals this value.
	ExternalRef *string `form:"external_ref,omitempty" json:"external_ref,omitempty"`
}

// ListInvocationsParams defines parameters for ListInvocations.
type ListInvocationsParams struct {
	// TenantKey Exact non-default tenant partition reference.
	TenantKey *TenantKeyFilter `form:"tenant_key,omitempty" json:"tenant_key,omitempty"`

	// DefaultTenant Select only the default tenant partition. Mutually exclusive with tenant_key.
	DefaultTenant *DefaultTenantFilter `form:"default_tenant,omitempty" json:"default_tenant,omitempty"`

	// UserKey Exact host-owned end-user reference. Filters to rows whose Session
	// carries this label.
	UserKey   *UserKeyFilter `form:"user_key,omitempty" json:"user_key,omitempty"`
	SessionID *SessionID     `form:"session_id,omitempty" json:"session_id,omitempty"`

	// AgentID Mutually exclusive with agent_key.
	AgentID *AgentID `form:"agent_id,omitempty" json:"agent_id,omitempty"`

	// AgentKey Exact host-owned Agent key. On Session and Invocation lists this is
	// mutually exclusive with agent_id.
	AgentKey *AgentKeyFilter `form:"agent_key,omitempty" json:"agent_key,omitempty"`

	// Status Repeat to select a union of statuses. Order and duplicates are
	// normalized before cursor binding.
	Status *[]InvocationStatus `form:"status,omitempty" json:"status,omitempty"`

	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// CreateInvocationParams defines parameters for CreateInvocation.
type CreateInvocationParams struct {
	// XAnthropicAPIKey Caller-supplied Anthropic API key, equivalent to a
	// caller_ephemeral `provider_keys` selection. The header
	// must name the model provider and cannot be combined with
	// the body field. Siblings: X-Openai-Api-Key, X-Gemini-Api-Key,
	// X-Xai-Api-Key.
	XAnthropicAPIKey *string `json:"X-Anthropic-Api-Key,omitempty"`
	XOpenaiAPIKey    *string `json:"X-Openai-Api-Key,omitempty"`
	XGeminiAPIKey    *string `json:"X-Gemini-Api-Key,omitempty"`
	XXaiAPIKey       *string `json:"X-Xai-Api-Key,omitempty"`
}

// ListPendingInputsParams defines parameters for ListPendingInputs.
type ListPendingInputsParams struct {
	// Status Restrict to one status.
	Status *PendingInputStatus `form:"status,omitempty" json:"status,omitempty"`

	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 20.
	Limit *int `form:"limit,omitempty" json:"limit,omitempty"`
}

// StreamInvocationParams defines parameters for StreamInvocation.
type StreamInvocationParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Deltas Include id-less output and thinking preview frames. Defaults to true.
	Deltas *bool `form:"deltas,omitempty" json:"deltas,omitempty"`

	// LastEventID Opaque cursor from the last durable frame; ignored when `cursor` is supplied.
	LastEventID *string `json:"Last-Event-ID,omitempty"`
}

// ListToolCallsParams defines parameters for ListToolCalls.
type ListToolCallsParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListModelsParams defines parameters for ListModels.
type ListModelsParams struct {
	// Provider Limit results to one installed canonical provider.
	Provider *ModelProvider `form:"provider,omitempty" json:"provider,omitempty"`

	// IncludeDeprecated Include catalog entries nvoken marks as deprecated.
	IncludeDeprecated *bool `form:"include_deprecated,omitempty" json:"include_deprecated,omitempty"`

	// IfNoneMatch ETag from a prior response for this exact representation.
	IfNoneMatch *IfNoneMatch `json:"If-None-Match,omitempty"`
}

// GetModelParams defines parameters for GetModel.
type GetModelParams struct {
	// IfNoneMatch ETag from a prior response for this exact representation.
	IfNoneMatch *IfNoneMatch `json:"If-None-Match,omitempty"`
}

// ListProviderKeysParams defines parameters for ListProviderKeys.
type ListProviderKeysParams struct {
	Provider  *ModelProvider                `form:"provider,omitempty" json:"provider,omitempty"`
	Scope     *ProviderKeyScope             `form:"scope,omitempty" json:"scope,omitempty"`
	Status    *ListProviderKeysParamsStatus `form:"status,omitempty" json:"status,omitempty"`
	TenantKey *string                       `form:"tenant_key,omitempty" json:"tenant_key,omitempty"`

	// Cursor Opaque continuation cursor bound to the effective scope, filters, and limit.
	Cursor *string `form:"cursor,omitempty" json:"cursor,omitempty"`
	Limit  *int    `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListProviderKeysParamsStatus defines parameters for ListProviderKeys.
type ListProviderKeysParamsStatus string

// ListSessionsParams defines parameters for ListSessions.
type ListSessionsParams struct {
	// TenantKey Exact non-default tenant partition reference.
	TenantKey *TenantKeyFilter `form:"tenant_key,omitempty" json:"tenant_key,omitempty"`

	// DefaultTenant Select only the default tenant partition. Mutually exclusive with tenant_key.
	DefaultTenant *DefaultTenantFilter `form:"default_tenant,omitempty" json:"default_tenant,omitempty"`

	// UserKey Exact host-owned end-user reference. Filters to rows whose Session
	// carries this label.
	UserKey *UserKeyFilter `form:"user_key,omitempty" json:"user_key,omitempty"`

	// AgentID Mutually exclusive with agent_key.
	AgentID *AgentID `form:"agent_id,omitempty" json:"agent_id,omitempty"`

	// AgentKey Exact host-owned Agent key. On Session and Invocation lists this is
	// mutually exclusive with agent_id.
	AgentKey   *AgentKeyFilter `form:"agent_key,omitempty" json:"agent_key,omitempty"`
	SessionKey *string         `form:"session_key,omitempty" json:"session_key,omitempty"`

	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListSessionCompactionsParams defines parameters for ListSessionCompactions.
type ListSessionCompactionsParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListSessionMessagesParams defines parameters for ListSessionMessages.
type ListSessionMessagesParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// GetSessionTranscriptParams defines parameters for GetSessionTranscript.
type GetSessionTranscriptParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// PageToken Opaque fixed-cut continuation from `next_page_token`.
	PageToken *string `form:"page_token,omitempty" json:"page_token,omitempty"`

	// Limit Maximum items in this page. Defaults to 100.
	Limit *Limit `form:"limit,omitempty" json:"limit,omitempty"`
}

// StreamSessionTranscriptParams defines parameters for StreamSessionTranscript.
type StreamSessionTranscriptParams struct {
	// Cursor Opaque cursor returned by the same operation and filter set.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// Deltas Include id-less output and thinking preview frames. Defaults to true.
	Deltas *bool `form:"deltas,omitempty" json:"deltas,omitempty"`

	// LastEventID Opaque `resume_cursor` from the last durable update frame; ignored when `cursor` is supplied.
	LastEventID *string `json:"Last-Event-ID,omitempty"`
}

// GetDailyUsageParams defines parameters for GetDailyUsage.
type GetDailyUsageParams struct {
	// StartDate Inclusive first UTC day. Defaults to 29 days before `end_date`.
	StartDate *openapi_types.Date `form:"start_date,omitempty" json:"start_date,omitempty"`

	// EndDate Inclusive last UTC day. Defaults to the current UTC day.
	EndDate *openapi_types.Date `form:"end_date,omitempty" json:"end_date,omitempty"`

	// TenantKey Return only usage attributed to this host customer.
	TenantKey *string `form:"tenant_key,omitempty" json:"tenant_key,omitempty"`

	// UserKey Return only usage attributed to this host end user.
	UserKey *string `form:"user_key,omitempty" json:"user_key,omitempty"`

	// GroupBy Split otherwise identical buckets by host customer.
	GroupBy *GetDailyUsageParamsGroupBy `form:"group_by,omitempty" json:"group_by,omitempty"`
}

// GetDailyUsageParamsGroupBy defines parameters for GetDailyUsage.
type GetDailyUsageParamsGroupBy string

// ReceiveToolCallbackParams defines parameters for ReceiveToolCallback.
type ReceiveToolCallbackParams struct {
	// XNvokenSignature HMAC-SHA256 signature of the canonical delivery string.
	XNvokenSignature        string                                           `json:"X-Nvoken-Signature"`
	XNvokenSignatureVersion ReceiveToolCallbackParamsXNvokenSignatureVersion `json:"X-Nvoken-Signature-Version"`

	// XNvokenTimestamp Unix timestamp used in the signature.
	XNvokenTimestamp         int64              `json:"X-Nvoken-Timestamp"`
	XNvokenDeliveryID        CallbackDeliveryID `json:"X-Nvoken-Delivery-Id"`
	XNvokenSigningKeyID      string             `json:"X-Nvoken-Signing-Key-Id"`
	XNvokenSigningKeyVersion int64              `json:"X-Nvoken-Signing-Key-Version"`

	// IdempotencyKey The stable ToolCall ID. It is unchanged across retries.
	IdempotencyKey ToolCallID `json:"Idempotency-Key"`
}

// ReceiveToolCallbackParamsXNvokenSignatureVersion defines parameters for ReceiveToolCallback.
type ReceiveToolCallbackParamsXNvokenSignatureVersion string

// RegisterAppJSONRequestBody defines body for RegisterApp for application/json ContentType.
type RegisterAppJSONRequestBody = RegisterAppRequest

// UpdateAppJSONRequestBody defines body for UpdateApp for application/json ContentType.
type UpdateAppJSONRequestBody = UpdateAppRequest

// CreateBudgetJSONRequestBody defines body for CreateBudget for application/json ContentType.
type CreateBudgetJSONRequestBody = CreateBudgetRequest

// UpdateBudgetJSONRequestBody defines body for UpdateBudget for application/json ContentType.
type UpdateBudgetJSONRequestBody = UpdateBudgetRequest

// CreateInvocationJSONRequestBody defines body for CreateInvocation for application/json ContentType.
type CreateInvocationJSONRequestBody = CreateInvocationRequest

// NudgeInvocationJSONRequestBody defines body for NudgeInvocation for application/json ContentType.
type NudgeInvocationJSONRequestBody = NudgeInvocationRequest

// ResumeInvocationJSONRequestBody defines body for ResumeInvocation for application/json ContentType.
type ResumeInvocationJSONRequestBody = ResumeInvocationRequest

// SubmitHostToolResultsJSONRequestBody defines body for SubmitHostToolResults for application/json ContentType.
type SubmitHostToolResultsJSONRequestBody = SubmitHostToolResultsRequest

// ListMCPToolsJSONRequestBody defines body for ListMCPTools for application/json ContentType.
type ListMCPToolsJSONRequestBody = MCPListToolsRequest

// CreateProviderKeyJSONRequestBody defines body for CreateProviderKey for application/json ContentType.
type CreateProviderKeyJSONRequestBody = CreateProviderKeyRequest

// RotateProviderKeyJSONRequestBody defines body for RotateProviderKey for application/json ContentType.
type RotateProviderKeyJSONRequestBody = RotateProviderKeyRequest

// CreateSessionJSONRequestBody defines body for CreateSession for application/json ContentType.
type CreateSessionJSONRequestBody = CreateSessionRequest

// UpdateSessionJSONRequestBody defines body for UpdateSession for application/json ContentType.
type UpdateSessionJSONRequestBody = UpdateSessionRequest

// ForkSessionJSONRequestBody defines body for ForkSession for application/json ContentType.
type ForkSessionJSONRequestBody = ForkSessionRequest

// ReceiveToolCallbackApplicationVndNvokenToolCallbackPlusJSONVersion1RequestBody defines body for ReceiveToolCallback for application/vnd.nvoken.tool-callback+json; version=1 ContentType.
type ReceiveToolCallbackApplicationVndNvokenToolCallbackPlusJSONVersion1RequestBody = ToolCallbackRequest

// AsCharLocationCitation returns the union data inside the Citation as a CharLocationCitation
func (t Citation) AsCharLocationCitation() (CharLocationCitation, error) {
	var body CharLocationCitation
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCharLocationCitation overwrites any union data inside the Citation as the provided CharLocationCitation
func (t *Citation) FromCharLocationCitation(v CharLocationCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"char_location"}`))
	t.union = b
	return err
}

// MergeCharLocationCitation performs a merge with any union data inside the Citation, using the provided CharLocationCitation
func (t *Citation) MergeCharLocationCitation(v CharLocationCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"char_location"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsWebSearchResultLocationCitation returns the union data inside the Citation as a WebSearchResultLocationCitation
func (t Citation) AsWebSearchResultLocationCitation() (WebSearchResultLocationCitation, error) {
	var body WebSearchResultLocationCitation
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromWebSearchResultLocationCitation overwrites any union data inside the Citation as the provided WebSearchResultLocationCitation
func (t *Citation) FromWebSearchResultLocationCitation(v WebSearchResultLocationCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"web_search_result_location"}`))
	t.union = b
	return err
}

// MergeWebSearchResultLocationCitation performs a merge with any union data inside the Citation, using the provided WebSearchResultLocationCitation
func (t *Citation) MergeWebSearchResultLocationCitation(v WebSearchResultLocationCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"web_search_result_location"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsURLCitation returns the union data inside the Citation as a URLCitation
func (t Citation) AsURLCitation() (URLCitation, error) {
	var body URLCitation
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromURLCitation overwrites any union data inside the Citation as the provided URLCitation
func (t *Citation) FromURLCitation(v URLCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"url_citation"}`))
	t.union = b
	return err
}

// MergeURLCitation performs a merge with any union data inside the Citation, using the provided URLCitation
func (t *Citation) MergeURLCitation(v URLCitation) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"url_citation"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t Citation) Discriminator() (string, error) {
	var discriminator struct {
		Discriminator string `json:"type"`
	}
	err := json.Unmarshal(t.union, &discriminator)
	return discriminator.Discriminator, err
}

func (t Citation) ValueByDiscriminator() (interface{}, error) {
	discriminator, err := t.Discriminator()
	if err != nil {
		return nil, err
	}
	switch discriminator {
	case "char_location":
		return t.AsCharLocationCitation()
	case "url_citation":
		return t.AsURLCitation()
	case "web_search_result_location":
		return t.AsWebSearchResultLocationCitation()
	default:
		return nil, errors.New("unknown discriminator value: " + discriminator)
	}
}

func (t Citation) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *Citation) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsCompactionPolicyTriggerTokens0 returns the union data inside the CompactionPolicy_TriggerTokens as a CompactionPolicyTriggerTokens0
func (t CompactionPolicy_TriggerTokens) AsCompactionPolicyTriggerTokens0() (CompactionPolicyTriggerTokens0, error) {
	var body CompactionPolicyTriggerTokens0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCompactionPolicyTriggerTokens0 overwrites any union data inside the CompactionPolicy_TriggerTokens as the provided CompactionPolicyTriggerTokens0
func (t *CompactionPolicy_TriggerTokens) FromCompactionPolicyTriggerTokens0(v CompactionPolicyTriggerTokens0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeCompactionPolicyTriggerTokens0 performs a merge with any union data inside the CompactionPolicy_TriggerTokens, using the provided CompactionPolicyTriggerTokens0
func (t *CompactionPolicy_TriggerTokens) MergeCompactionPolicyTriggerTokens0(v CompactionPolicyTriggerTokens0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsCompactionPolicyTriggerTokens1 returns the union data inside the CompactionPolicy_TriggerTokens as a CompactionPolicyTriggerTokens1
func (t CompactionPolicy_TriggerTokens) AsCompactionPolicyTriggerTokens1() (CompactionPolicyTriggerTokens1, error) {
	var body CompactionPolicyTriggerTokens1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCompactionPolicyTriggerTokens1 overwrites any union data inside the CompactionPolicy_TriggerTokens as the provided CompactionPolicyTriggerTokens1
func (t *CompactionPolicy_TriggerTokens) FromCompactionPolicyTriggerTokens1(v CompactionPolicyTriggerTokens1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeCompactionPolicyTriggerTokens1 performs a merge with any union data inside the CompactionPolicy_TriggerTokens, using the provided CompactionPolicyTriggerTokens1
func (t *CompactionPolicy_TriggerTokens) MergeCompactionPolicyTriggerTokens1(v CompactionPolicyTriggerTokens1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t CompactionPolicy_TriggerTokens) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *CompactionPolicy_TriggerTokens) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsSessionMessageID returns the union data inside the ForkSessionRequest_FromMessage as a SessionMessageID
func (t ForkSessionRequest_FromMessage) AsSessionMessageID() (SessionMessageID, error) {
	var body SessionMessageID
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSessionMessageID overwrites any union data inside the ForkSessionRequest_FromMessage as the provided SessionMessageID
func (t *ForkSessionRequest_FromMessage) FromSessionMessageID(v SessionMessageID) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSessionMessageID performs a merge with any union data inside the ForkSessionRequest_FromMessage, using the provided SessionMessageID
func (t *ForkSessionRequest_FromMessage) MergeSessionMessageID(v SessionMessageID) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsForkSessionRequestFromMessage1 returns the union data inside the ForkSessionRequest_FromMessage as a ForkSessionRequestFromMessage1
func (t ForkSessionRequest_FromMessage) AsForkSessionRequestFromMessage1() (ForkSessionRequestFromMessage1, error) {
	var body ForkSessionRequestFromMessage1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromForkSessionRequestFromMessage1 overwrites any union data inside the ForkSessionRequest_FromMessage as the provided ForkSessionRequestFromMessage1
func (t *ForkSessionRequest_FromMessage) FromForkSessionRequestFromMessage1(v ForkSessionRequestFromMessage1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeForkSessionRequestFromMessage1 performs a merge with any union data inside the ForkSessionRequest_FromMessage, using the provided ForkSessionRequestFromMessage1
func (t *ForkSessionRequest_FromMessage) MergeForkSessionRequestFromMessage1(v ForkSessionRequestFromMessage1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t ForkSessionRequest_FromMessage) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *ForkSessionRequest_FromMessage) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsTextInputBlock returns the union data inside the InputBlock as a TextInputBlock
func (t InputBlock) AsTextInputBlock() (TextInputBlock, error) {
	var body TextInputBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromTextInputBlock overwrites any union data inside the InputBlock as the provided TextInputBlock
func (t *InputBlock) FromTextInputBlock(v TextInputBlock) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeTextInputBlock performs a merge with any union data inside the InputBlock, using the provided TextInputBlock
func (t *InputBlock) MergeTextInputBlock(v TextInputBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsImageInputBlock returns the union data inside the InputBlock as a ImageInputBlock
func (t InputBlock) AsImageInputBlock() (ImageInputBlock, error) {
	var body ImageInputBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromImageInputBlock overwrites any union data inside the InputBlock as the provided ImageInputBlock
func (t *InputBlock) FromImageInputBlock(v ImageInputBlock) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeImageInputBlock performs a merge with any union data inside the InputBlock, using the provided ImageInputBlock
func (t *InputBlock) MergeImageInputBlock(v ImageInputBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsDocumentInputBlock returns the union data inside the InputBlock as a DocumentInputBlock
func (t InputBlock) AsDocumentInputBlock() (DocumentInputBlock, error) {
	var body DocumentInputBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromDocumentInputBlock overwrites any union data inside the InputBlock as the provided DocumentInputBlock
func (t *InputBlock) FromDocumentInputBlock(v DocumentInputBlock) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeDocumentInputBlock performs a merge with any union data inside the InputBlock, using the provided DocumentInputBlock
func (t *InputBlock) MergeDocumentInputBlock(v DocumentInputBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t InputBlock) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *InputBlock) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsInvocationInput0 returns the union data inside the InvocationInput as a InvocationInput0
func (t InvocationInput) AsInvocationInput0() (InvocationInput0, error) {
	var body InvocationInput0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromInvocationInput0 overwrites any union data inside the InvocationInput as the provided InvocationInput0
func (t *InvocationInput) FromInvocationInput0(v InvocationInput0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeInvocationInput0 performs a merge with any union data inside the InvocationInput, using the provided InvocationInput0
func (t *InvocationInput) MergeInvocationInput0(v InvocationInput0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsInvocationInput1 returns the union data inside the InvocationInput as a InvocationInput1
func (t InvocationInput) AsInvocationInput1() (InvocationInput1, error) {
	var body InvocationInput1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromInvocationInput1 overwrites any union data inside the InvocationInput as the provided InvocationInput1
func (t *InvocationInput) FromInvocationInput1(v InvocationInput1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeInvocationInput1 performs a merge with any union data inside the InvocationInput, using the provided InvocationInput1
func (t *InvocationInput) MergeInvocationInput1(v InvocationInput1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t InvocationInput) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *InvocationInput) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsInvocationAcceptedEvent returns the union data inside the InvocationStreamEvent as a InvocationAcceptedEvent
func (t InvocationStreamEvent) AsInvocationAcceptedEvent() (InvocationAcceptedEvent, error) {
	var body InvocationAcceptedEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromInvocationAcceptedEvent overwrites any union data inside the InvocationStreamEvent as the provided InvocationAcceptedEvent
func (t *InvocationStreamEvent) FromInvocationAcceptedEvent(v InvocationAcceptedEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeInvocationAcceptedEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided InvocationAcceptedEvent
func (t *InvocationStreamEvent) MergeInvocationAcceptedEvent(v InvocationAcceptedEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsOutputTextDeltaEvent returns the union data inside the InvocationStreamEvent as a OutputTextDeltaEvent
func (t InvocationStreamEvent) AsOutputTextDeltaEvent() (OutputTextDeltaEvent, error) {
	var body OutputTextDeltaEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromOutputTextDeltaEvent overwrites any union data inside the InvocationStreamEvent as the provided OutputTextDeltaEvent
func (t *InvocationStreamEvent) FromOutputTextDeltaEvent(v OutputTextDeltaEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeOutputTextDeltaEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided OutputTextDeltaEvent
func (t *InvocationStreamEvent) MergeOutputTextDeltaEvent(v OutputTextDeltaEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsThinkingDeltaEvent returns the union data inside the InvocationStreamEvent as a ThinkingDeltaEvent
func (t InvocationStreamEvent) AsThinkingDeltaEvent() (ThinkingDeltaEvent, error) {
	var body ThinkingDeltaEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromThinkingDeltaEvent overwrites any union data inside the InvocationStreamEvent as the provided ThinkingDeltaEvent
func (t *InvocationStreamEvent) FromThinkingDeltaEvent(v ThinkingDeltaEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeThinkingDeltaEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided ThinkingDeltaEvent
func (t *InvocationStreamEvent) MergeThinkingDeltaEvent(v ThinkingDeltaEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsInvocationUpdateEvent returns the union data inside the InvocationStreamEvent as a InvocationUpdateEvent
func (t InvocationStreamEvent) AsInvocationUpdateEvent() (InvocationUpdateEvent, error) {
	var body InvocationUpdateEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromInvocationUpdateEvent overwrites any union data inside the InvocationStreamEvent as the provided InvocationUpdateEvent
func (t *InvocationStreamEvent) FromInvocationUpdateEvent(v InvocationUpdateEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeInvocationUpdateEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided InvocationUpdateEvent
func (t *InvocationStreamEvent) MergeInvocationUpdateEvent(v InvocationUpdateEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsInvocationResultEvent returns the union data inside the InvocationStreamEvent as a InvocationResultEvent
func (t InvocationStreamEvent) AsInvocationResultEvent() (InvocationResultEvent, error) {
	var body InvocationResultEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromInvocationResultEvent overwrites any union data inside the InvocationStreamEvent as the provided InvocationResultEvent
func (t *InvocationStreamEvent) FromInvocationResultEvent(v InvocationResultEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeInvocationResultEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided InvocationResultEvent
func (t *InvocationStreamEvent) MergeInvocationResultEvent(v InvocationResultEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsStreamResyncEvent returns the union data inside the InvocationStreamEvent as a StreamResyncEvent
func (t InvocationStreamEvent) AsStreamResyncEvent() (StreamResyncEvent, error) {
	var body StreamResyncEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromStreamResyncEvent overwrites any union data inside the InvocationStreamEvent as the provided StreamResyncEvent
func (t *InvocationStreamEvent) FromStreamResyncEvent(v StreamResyncEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeStreamResyncEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided StreamResyncEvent
func (t *InvocationStreamEvent) MergeStreamResyncEvent(v StreamResyncEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsStreamEndEvent returns the union data inside the InvocationStreamEvent as a StreamEndEvent
func (t InvocationStreamEvent) AsStreamEndEvent() (StreamEndEvent, error) {
	var body StreamEndEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromStreamEndEvent overwrites any union data inside the InvocationStreamEvent as the provided StreamEndEvent
func (t *InvocationStreamEvent) FromStreamEndEvent(v StreamEndEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeStreamEndEvent performs a merge with any union data inside the InvocationStreamEvent, using the provided StreamEndEvent
func (t *InvocationStreamEvent) MergeStreamEndEvent(v StreamEndEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t InvocationStreamEvent) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *InvocationStreamEvent) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsModel returns the union data inside the ModelInput as a Model
func (t ModelInput) AsModel() (Model, error) {
	var body Model
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromModel overwrites any union data inside the ModelInput as the provided Model
func (t *ModelInput) FromModel(v Model) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeModel performs a merge with any union data inside the ModelInput, using the provided Model
func (t *ModelInput) MergeModel(v Model) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsModelInput1 returns the union data inside the ModelInput as a ModelInput1
func (t ModelInput) AsModelInput1() (ModelInput1, error) {
	var body ModelInput1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromModelInput1 overwrites any union data inside the ModelInput as the provided ModelInput1
func (t *ModelInput) FromModelInput1(v ModelInput1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeModelInput1 performs a merge with any union data inside the ModelInput, using the provided ModelInput1
func (t *ModelInput) MergeModelInput1(v ModelInput1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t ModelInput) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *ModelInput) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsProviderKeySelection0 returns the union data inside the ProviderKeySelection as a ProviderKeySelection0
func (t ProviderKeySelection) AsProviderKeySelection0() (ProviderKeySelection0, error) {
	var body ProviderKeySelection0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromProviderKeySelection0 overwrites any union data inside the ProviderKeySelection as the provided ProviderKeySelection0
func (t *ProviderKeySelection) FromProviderKeySelection0(v ProviderKeySelection0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeProviderKeySelection0 performs a merge with any union data inside the ProviderKeySelection, using the provided ProviderKeySelection0
func (t *ProviderKeySelection) MergeProviderKeySelection0(v ProviderKeySelection0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsProviderKeySelection1 returns the union data inside the ProviderKeySelection as a ProviderKeySelection1
func (t ProviderKeySelection) AsProviderKeySelection1() (ProviderKeySelection1, error) {
	var body ProviderKeySelection1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromProviderKeySelection1 overwrites any union data inside the ProviderKeySelection as the provided ProviderKeySelection1
func (t *ProviderKeySelection) FromProviderKeySelection1(v ProviderKeySelection1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeProviderKeySelection1 performs a merge with any union data inside the ProviderKeySelection, using the provided ProviderKeySelection1
func (t *ProviderKeySelection) MergeProviderKeySelection1(v ProviderKeySelection1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t ProviderKeySelection) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *ProviderKeySelection) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsSeedMessageContent0 returns the union data inside the SeedMessageContent as a SeedMessageContent0
func (t SeedMessageContent) AsSeedMessageContent0() (SeedMessageContent0, error) {
	var body SeedMessageContent0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSeedMessageContent0 overwrites any union data inside the SeedMessageContent as the provided SeedMessageContent0
func (t *SeedMessageContent) FromSeedMessageContent0(v SeedMessageContent0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSeedMessageContent0 performs a merge with any union data inside the SeedMessageContent, using the provided SeedMessageContent0
func (t *SeedMessageContent) MergeSeedMessageContent0(v SeedMessageContent0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsSeedMessageContent1 returns the union data inside the SeedMessageContent as a SeedMessageContent1
func (t SeedMessageContent) AsSeedMessageContent1() (SeedMessageContent1, error) {
	var body SeedMessageContent1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSeedMessageContent1 overwrites any union data inside the SeedMessageContent as the provided SeedMessageContent1
func (t *SeedMessageContent) FromSeedMessageContent1(v SeedMessageContent1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSeedMessageContent1 performs a merge with any union data inside the SeedMessageContent, using the provided SeedMessageContent1
func (t *SeedMessageContent) MergeSeedMessageContent1(v SeedMessageContent1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t SeedMessageContent) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *SeedMessageContent) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsTextBlock returns the union data inside the SessionContentBlock as a TextBlock
func (t SessionContentBlock) AsTextBlock() (TextBlock, error) {
	var body TextBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromTextBlock overwrites any union data inside the SessionContentBlock as the provided TextBlock
func (t *SessionContentBlock) FromTextBlock(v TextBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"text"}`))
	t.union = b
	return err
}

// MergeTextBlock performs a merge with any union data inside the SessionContentBlock, using the provided TextBlock
func (t *SessionContentBlock) MergeTextBlock(v TextBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"text"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsImageReferenceBlock returns the union data inside the SessionContentBlock as a ImageReferenceBlock
func (t SessionContentBlock) AsImageReferenceBlock() (ImageReferenceBlock, error) {
	var body ImageReferenceBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromImageReferenceBlock overwrites any union data inside the SessionContentBlock as the provided ImageReferenceBlock
func (t *SessionContentBlock) FromImageReferenceBlock(v ImageReferenceBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"image"}`))
	t.union = b
	return err
}

// MergeImageReferenceBlock performs a merge with any union data inside the SessionContentBlock, using the provided ImageReferenceBlock
func (t *SessionContentBlock) MergeImageReferenceBlock(v ImageReferenceBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"image"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsDocumentReferenceBlock returns the union data inside the SessionContentBlock as a DocumentReferenceBlock
func (t SessionContentBlock) AsDocumentReferenceBlock() (DocumentReferenceBlock, error) {
	var body DocumentReferenceBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromDocumentReferenceBlock overwrites any union data inside the SessionContentBlock as the provided DocumentReferenceBlock
func (t *SessionContentBlock) FromDocumentReferenceBlock(v DocumentReferenceBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"document"}`))
	t.union = b
	return err
}

// MergeDocumentReferenceBlock performs a merge with any union data inside the SessionContentBlock, using the provided DocumentReferenceBlock
func (t *SessionContentBlock) MergeDocumentReferenceBlock(v DocumentReferenceBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"document"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsToolUseBlock returns the union data inside the SessionContentBlock as a ToolUseBlock
func (t SessionContentBlock) AsToolUseBlock() (ToolUseBlock, error) {
	var body ToolUseBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromToolUseBlock overwrites any union data inside the SessionContentBlock as the provided ToolUseBlock
func (t *SessionContentBlock) FromToolUseBlock(v ToolUseBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"tool_use"}`))
	t.union = b
	return err
}

// MergeToolUseBlock performs a merge with any union data inside the SessionContentBlock, using the provided ToolUseBlock
func (t *SessionContentBlock) MergeToolUseBlock(v ToolUseBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"tool_use"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsServerToolUseBlock returns the union data inside the SessionContentBlock as a ServerToolUseBlock
func (t SessionContentBlock) AsServerToolUseBlock() (ServerToolUseBlock, error) {
	var body ServerToolUseBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromServerToolUseBlock overwrites any union data inside the SessionContentBlock as the provided ServerToolUseBlock
func (t *SessionContentBlock) FromServerToolUseBlock(v ServerToolUseBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"server_tool_use"}`))
	t.union = b
	return err
}

// MergeServerToolUseBlock performs a merge with any union data inside the SessionContentBlock, using the provided ServerToolUseBlock
func (t *SessionContentBlock) MergeServerToolUseBlock(v ServerToolUseBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"server_tool_use"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsToolResultBlock returns the union data inside the SessionContentBlock as a ToolResultBlock
func (t SessionContentBlock) AsToolResultBlock() (ToolResultBlock, error) {
	var body ToolResultBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromToolResultBlock overwrites any union data inside the SessionContentBlock as the provided ToolResultBlock
func (t *SessionContentBlock) FromToolResultBlock(v ToolResultBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"tool_result"}`))
	t.union = b
	return err
}

// MergeToolResultBlock performs a merge with any union data inside the SessionContentBlock, using the provided ToolResultBlock
func (t *SessionContentBlock) MergeToolResultBlock(v ToolResultBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"tool_result"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsRedactedBlock returns the union data inside the SessionContentBlock as a RedactedBlock
func (t SessionContentBlock) AsRedactedBlock() (RedactedBlock, error) {
	var body RedactedBlock
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromRedactedBlock overwrites any union data inside the SessionContentBlock as the provided RedactedBlock
func (t *SessionContentBlock) FromRedactedBlock(v RedactedBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"redacted"}`))
	t.union = b
	return err
}

// MergeRedactedBlock performs a merge with any union data inside the SessionContentBlock, using the provided RedactedBlock
func (t *SessionContentBlock) MergeRedactedBlock(v RedactedBlock) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"type":"redacted"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t SessionContentBlock) Discriminator() (string, error) {
	var discriminator struct {
		Discriminator string `json:"type"`
	}
	err := json.Unmarshal(t.union, &discriminator)
	return discriminator.Discriminator, err
}

func (t SessionContentBlock) ValueByDiscriminator() (interface{}, error) {
	discriminator, err := t.Discriminator()
	if err != nil {
		return nil, err
	}
	switch discriminator {
	case "document":
		return t.AsDocumentReferenceBlock()
	case "image":
		return t.AsImageReferenceBlock()
	case "redacted":
		return t.AsRedactedBlock()
	case "server_tool_use":
		return t.AsServerToolUseBlock()
	case "text":
		return t.AsTextBlock()
	case "tool_result":
		return t.AsToolResultBlock()
	case "tool_use":
		return t.AsToolUseBlock()
	default:
		return nil, errors.New("unknown discriminator value: " + discriminator)
	}
}

func (t SessionContentBlock) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *SessionContentBlock) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsBuiltinToolDeclaration returns the union data inside the ToolDeclaration as a BuiltinToolDeclaration
func (t ToolDeclaration) AsBuiltinToolDeclaration() (BuiltinToolDeclaration, error) {
	var body BuiltinToolDeclaration
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromBuiltinToolDeclaration overwrites any union data inside the ToolDeclaration as the provided BuiltinToolDeclaration
func (t *ToolDeclaration) FromBuiltinToolDeclaration(v BuiltinToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"builtin"}`))
	t.union = b
	return err
}

// MergeBuiltinToolDeclaration performs a merge with any union data inside the ToolDeclaration, using the provided BuiltinToolDeclaration
func (t *ToolDeclaration) MergeBuiltinToolDeclaration(v BuiltinToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"builtin"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsHostToolDeclaration returns the union data inside the ToolDeclaration as a HostToolDeclaration
func (t ToolDeclaration) AsHostToolDeclaration() (HostToolDeclaration, error) {
	var body HostToolDeclaration
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromHostToolDeclaration overwrites any union data inside the ToolDeclaration as the provided HostToolDeclaration
func (t *ToolDeclaration) FromHostToolDeclaration(v HostToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"host"}`))
	t.union = b
	return err
}

// MergeHostToolDeclaration performs a merge with any union data inside the ToolDeclaration, using the provided HostToolDeclaration
func (t *ToolDeclaration) MergeHostToolDeclaration(v HostToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"host"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsCallbackToolDeclaration returns the union data inside the ToolDeclaration as a CallbackToolDeclaration
func (t ToolDeclaration) AsCallbackToolDeclaration() (CallbackToolDeclaration, error) {
	var body CallbackToolDeclaration
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCallbackToolDeclaration overwrites any union data inside the ToolDeclaration as the provided CallbackToolDeclaration
func (t *ToolDeclaration) FromCallbackToolDeclaration(v CallbackToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"callback"}`))
	t.union = b
	return err
}

// MergeCallbackToolDeclaration performs a merge with any union data inside the ToolDeclaration, using the provided CallbackToolDeclaration
func (t *ToolDeclaration) MergeCallbackToolDeclaration(v CallbackToolDeclaration) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	b, err = runtime.JSONMerge(b, []byte(`{"mode":"callback"}`))
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t ToolDeclaration) Discriminator() (string, error) {
	var discriminator struct {
		Discriminator string `json:"mode"`
	}
	err := json.Unmarshal(t.union, &discriminator)
	return discriminator.Discriminator, err
}

func (t ToolDeclaration) ValueByDiscriminator() (interface{}, error) {
	discriminator, err := t.Discriminator()
	if err != nil {
		return nil, err
	}
	switch discriminator {
	case "builtin":
		return t.AsBuiltinToolDeclaration()
	case "callback":
		return t.AsCallbackToolDeclaration()
	case "host":
		return t.AsHostToolDeclaration()
	default:
		return nil, errors.New("unknown discriminator value: " + discriminator)
	}
}

func (t ToolDeclaration) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *ToolDeclaration) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsTranscriptUpdate returns the union data inside the TranscriptStreamEvent as a TranscriptUpdate
func (t TranscriptStreamEvent) AsTranscriptUpdate() (TranscriptUpdate, error) {
	var body TranscriptUpdate
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromTranscriptUpdate overwrites any union data inside the TranscriptStreamEvent as the provided TranscriptUpdate
func (t *TranscriptStreamEvent) FromTranscriptUpdate(v TranscriptUpdate) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeTranscriptUpdate performs a merge with any union data inside the TranscriptStreamEvent, using the provided TranscriptUpdate
func (t *TranscriptStreamEvent) MergeTranscriptUpdate(v TranscriptUpdate) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsOutputTextDeltaEvent returns the union data inside the TranscriptStreamEvent as a OutputTextDeltaEvent
func (t TranscriptStreamEvent) AsOutputTextDeltaEvent() (OutputTextDeltaEvent, error) {
	var body OutputTextDeltaEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromOutputTextDeltaEvent overwrites any union data inside the TranscriptStreamEvent as the provided OutputTextDeltaEvent
func (t *TranscriptStreamEvent) FromOutputTextDeltaEvent(v OutputTextDeltaEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeOutputTextDeltaEvent performs a merge with any union data inside the TranscriptStreamEvent, using the provided OutputTextDeltaEvent
func (t *TranscriptStreamEvent) MergeOutputTextDeltaEvent(v OutputTextDeltaEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsThinkingDeltaEvent returns the union data inside the TranscriptStreamEvent as a ThinkingDeltaEvent
func (t TranscriptStreamEvent) AsThinkingDeltaEvent() (ThinkingDeltaEvent, error) {
	var body ThinkingDeltaEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromThinkingDeltaEvent overwrites any union data inside the TranscriptStreamEvent as the provided ThinkingDeltaEvent
func (t *TranscriptStreamEvent) FromThinkingDeltaEvent(v ThinkingDeltaEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeThinkingDeltaEvent performs a merge with any union data inside the TranscriptStreamEvent, using the provided ThinkingDeltaEvent
func (t *TranscriptStreamEvent) MergeThinkingDeltaEvent(v ThinkingDeltaEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsStreamResyncEvent returns the union data inside the TranscriptStreamEvent as a StreamResyncEvent
func (t TranscriptStreamEvent) AsStreamResyncEvent() (StreamResyncEvent, error) {
	var body StreamResyncEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromStreamResyncEvent overwrites any union data inside the TranscriptStreamEvent as the provided StreamResyncEvent
func (t *TranscriptStreamEvent) FromStreamResyncEvent(v StreamResyncEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeStreamResyncEvent performs a merge with any union data inside the TranscriptStreamEvent, using the provided StreamResyncEvent
func (t *TranscriptStreamEvent) MergeStreamResyncEvent(v StreamResyncEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsStreamEndEvent returns the union data inside the TranscriptStreamEvent as a StreamEndEvent
func (t TranscriptStreamEvent) AsStreamEndEvent() (StreamEndEvent, error) {
	var body StreamEndEvent
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromStreamEndEvent overwrites any union data inside the TranscriptStreamEvent as the provided StreamEndEvent
func (t *TranscriptStreamEvent) FromStreamEndEvent(v StreamEndEvent) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeStreamEndEvent performs a merge with any union data inside the TranscriptStreamEvent, using the provided StreamEndEvent
func (t *TranscriptStreamEvent) MergeStreamEndEvent(v StreamEndEvent) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t TranscriptStreamEvent) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *TranscriptStreamEvent) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// ListAgents List Agent identity anchors
	//
	// Returns newest-first identity anchors scoped to the caller's app. An
	// Agent stores only its nvoken ID, the host-owned `agent_key`, and
	// creation time; instructions, models, tools, and provider keys still
	// travel on each Invocation. An exact `agent_key` filter returns zero or
	// one item.
	//
	// Unconstrained credentials see the anchors referenced by a Session in
	// one of their app's tenants. Tenant-constrained credentials see only
	// anchors referenced by a Session in their effective partition.
	// Session-constrained credentials see only that Session's anchor. The
	// opaque cursor is bound to the authenticated caller, credential
	// constraint, and exact key filter.
	//
	// Corresponds with GET /v1/agents (the `ListAgents` operationId).
	ListAgents(ctx context.Context, params *ListAgentsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAgent Read one Agent identity anchor
	//
	// Reads identity without admitting work. Out-of-scope and undisclosable
	// constrained resources use `not_found`.
	//
	// Corresponds with GET /v1/agents/{agent_id} (the `GetAgent` operationId).
	GetAgent(ctx context.Context, agentID AgentID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListApps List registered apps
	//
	// Returns the apps this credential can see. A credential associated with
	// an app sees only that app; a credential with no app association sees
	// every registered app. An exact `external_ref` filter narrows either
	// view to apps owned by that reference.
	//
	// Corresponds with GET /v1/apps (the `ListApps` operationId).
	ListApps(ctx context.Context, params *ListAppsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RegisterAppWithBody Register an app
	//
	// Registers one host application and creates its default tenant,
	// returning the generated `app_id`.
	//
	// Registration is installation management rather than work inside an app,
	// so it requires a credential that is *not* associated with an app; a
	// credential bound to one app cannot mint siblings for itself. Names
	// identify apps and are unique, so re-registering an existing name is
	// rejected rather than returning the existing app.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
	RegisterAppWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RegisterApp Register an app
	//
	// Registers one host application and creates its default tenant,
	// returning the generated `app_id`.
	//
	// Registration is installation management rather than work inside an app,
	// so it requires a credential that is *not* associated with an app; a
	// credential bound to one app cannot mint siblings for itself. Names
	// identify apps and are unique, so re-registering an existing name is
	// rejected rather than returning the existing app.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
	RegisterApp(ctx context.Context, body RegisterAppJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetApp Get one app
	//
	// Returns one registered app. A credential associated with a different
	// app receives `404`, so an app identifier cannot be probed across the
	// isolation boundary.
	//
	// Corresponds with GET /v1/apps/{app_id} (the `GetApp` operationId).
	GetApp(ctx context.Context, appID string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAppWithBody Update an app
	//
	// Updates an app's mutable presentation fields. The unique `name` and the
	// ownership-grounding `external_ref` cannot be changed. A credential
	// associated with a different app receives `404`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
	UpdateAppWithBody(ctx context.Context, appID string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateApp Update an app
	//
	// Updates an app's mutable presentation fields. The unique `name` and the
	// ownership-grounding `external_ref` cannot be changed. A credential
	// associated with a different app receives `404`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
	UpdateApp(ctx context.Context, appID string, body UpdateAppJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBudgetWithBody Create one fixed usage budget window
	//
	// Creates an estimated-list-price guardrail inside the authenticated App.
	// Windows for the same scope identity cannot overlap. The idempotency key
	// makes an identical retry return the first Budget.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
	CreateBudgetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBudget Create one fixed usage budget window
	//
	// Creates an estimated-list-price guardrail inside the authenticated App.
	// Windows for the same scope identity cannot overlap. The idempotency key
	// makes an identical retry return the first Budget.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
	CreateBudget(ctx context.Context, body CreateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteBudget Remove a Budget guardrail
	//
	// Removes enforcement without deleting its accounting history.
	//
	// Corresponds with DELETE /v1/budgets/{budget_id} (the `DeleteBudget` operationId).
	DeleteBudget(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBudget Read one usage Budget
	//
	// Corresponds with GET /v1/budgets/{budget_id} (the `GetBudget` operationId).
	GetBudget(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBudgetWithBody Raise or lower a Budget cap
	//
	// Replaces the cap. It cannot be lower than settled plus reserved cost.
	// Raising it reevaluates paused work in bounded batches.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
	UpdateBudgetWithBody(ctx context.Context, budgetID BudgetID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBudget Raise or lower a Budget cap
	//
	// Replaces the cap. It cannot be lower than settled plus reserved cost.
	// Raising it reevaluates paused work in bounded batches.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
	UpdateBudget(ctx context.Context, budgetID BudgetID, body UpdateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListInvocations List authoritative Invocations
	//
	// Returns newest-first durable Invocation state. Exact filters combine
	// with AND. An installation-wide caller may list all tenant partitions, one
	// named partition with `tenant_key`, or the default partition with
	// `default_tenant=true`. A tenant-constrained credential is always scoped
	// to its partition. The opaque cursor is bound to the normalized filter
	// set and credential tenant scope. `agent_id` and `agent_key` are
	// mutually exclusive; both normalize to the resolved Agent ID for cursor
	// binding, so an equivalent cursor may resume under either spelling.
	//
	// Corresponds with GET /v1/invocations (the `ListInvocations` operationId).
	ListInvocations(ctx context.Context, params *ListInvocationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateInvocationWithBody Durably admit one background agent turn
	//
	// Resolves or creates the installation-wide Agent identity anchor,
	// resolves or creates a Session, resolves the inline or referenced
	// definition, appends exactly one caller-input message, and creates one
	// queued Invocation in a single transaction. The response is sent only
	// after that transaction commits. The request handler never executes the
	// model.
	//
	// `session_id` and `session_key` are mutually exclusive. A Session ID must
	// belong to the named Agent, or be a Session created without one — the
	// first admitted Invocation binds the Agent permanently. An installation-wide
	// credential may omit `tenant_key` and use the Session's stored partition.
	// A tenant-constrained credential cannot cross its partition; an explicit
	// mismatch is rejected with `403 forbidden` before resource lookup.
	//
	// `idempotency_key` is scoped to the effective
	// tenant partition, and `agent_key`. A same-key replay is checked before
	// the Session's one-nonterminal-Invocation rule. It returns the original
	// records without appending input, even when the Invocation is terminal.
	// Material equality covers the Session selector kind and value, the
	// resolved definition, and input. A `definition_id` and its byte-identical
	// inline definition compare equal. Requested limits are compared before
	// default resolution, so an explicit default differs from omission. JSON
	// object member order is ignored, array order is significant, and strings
	// are not rewritten. A changed material field returns
	// `idempotency_conflict`.
	//
	// `if_active` defaults to `reject`, preserving
	// `session_invocation_active`. Explicit `supersede` requires both create
	// and cancel authority. While holding the Session lock, it durably
	// cancels any nonterminal Invocation and admits this replacement in the
	// same transaction. With no active work it admits normally. Equal replay
	// returns the original work before supersession and never cancels newer
	// active work.
	//
	// `interrupt` requires the same authority and keeps the same lock
	// ordering, but stops the active Invocation gracefully instead of
	// cancelling it: work already at a seam settles and the replacement is
	// admitted in the same transaction, while work under a live lease has
	// the interrupt recorded and this request waits for it to settle. If it
	// has not settled within the wait, the response is
	// `session_invocation_active` with `details.interrupt_requested = true`
	// and the request may be reissued.
	//
	// A cataloged model continues to admit while deprecated. On and after
	// its `retires_at` date, a new admission is refused with `422
	// model_retired`; `details` carries the refused `model`, `retires_at`,
	// the exact `replacement` provider/id pair, and the request `path`.
	// Equal idempotent replay still returns work admitted before retirement.
	//
	// A text-only request body is limited to 1 MiB. A body carrying image or
	// document blocks is limited to 24 MiB, within which decoded media is
	// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
	// 5 MiB per image, and 16 MiB per document. Requests over any of these
	// limits are rejected before admission. URL media is fetched after an
	// idempotent replay check and before any row is admitted. The guarded
	// fetch accepts public HTTPS only, enforces the item limit while reading,
	// and sniffs the returned bytes. nvoken stores those bytes and does not
	// fetch the URL again.
	//
	// The supported streaming pattern is admit-then-stream: admit with a
	// plain JSON POST, then follow the Invocation over
	// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
	// connection without re-admitting and does not depend on front-end
	// streaming behavior. `Accept: text/event-stream` on this request is a
	// deployment-dependent convenience, not a first-class mode: the
	// committed admission is the first `invocation.accepted` frame and the
	// connection tails that Invocation through `invocation.result`, but
	// delivery requires the deployment front end to stream a non-`200`
	// POST response unbuffered. Some managed front ends (including Cloud
	// Run's) buffer it until the Invocation settles, which makes a
	// host-tool turn's `waiting` state invisible on this connection.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
	CreateInvocationWithBody(ctx context.Context, params *CreateInvocationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateInvocation Durably admit one background agent turn
	//
	// Resolves or creates the installation-wide Agent identity anchor,
	// resolves or creates a Session, resolves the inline or referenced
	// definition, appends exactly one caller-input message, and creates one
	// queued Invocation in a single transaction. The response is sent only
	// after that transaction commits. The request handler never executes the
	// model.
	//
	// `session_id` and `session_key` are mutually exclusive. A Session ID must
	// belong to the named Agent, or be a Session created without one — the
	// first admitted Invocation binds the Agent permanently. An installation-wide
	// credential may omit `tenant_key` and use the Session's stored partition.
	// A tenant-constrained credential cannot cross its partition; an explicit
	// mismatch is rejected with `403 forbidden` before resource lookup.
	//
	// `idempotency_key` is scoped to the effective
	// tenant partition, and `agent_key`. A same-key replay is checked before
	// the Session's one-nonterminal-Invocation rule. It returns the original
	// records without appending input, even when the Invocation is terminal.
	// Material equality covers the Session selector kind and value, the
	// resolved definition, and input. A `definition_id` and its byte-identical
	// inline definition compare equal. Requested limits are compared before
	// default resolution, so an explicit default differs from omission. JSON
	// object member order is ignored, array order is significant, and strings
	// are not rewritten. A changed material field returns
	// `idempotency_conflict`.
	//
	// `if_active` defaults to `reject`, preserving
	// `session_invocation_active`. Explicit `supersede` requires both create
	// and cancel authority. While holding the Session lock, it durably
	// cancels any nonterminal Invocation and admits this replacement in the
	// same transaction. With no active work it admits normally. Equal replay
	// returns the original work before supersession and never cancels newer
	// active work.
	//
	// `interrupt` requires the same authority and keeps the same lock
	// ordering, but stops the active Invocation gracefully instead of
	// cancelling it: work already at a seam settles and the replacement is
	// admitted in the same transaction, while work under a live lease has
	// the interrupt recorded and this request waits for it to settle. If it
	// has not settled within the wait, the response is
	// `session_invocation_active` with `details.interrupt_requested = true`
	// and the request may be reissued.
	//
	// A cataloged model continues to admit while deprecated. On and after
	// its `retires_at` date, a new admission is refused with `422
	// model_retired`; `details` carries the refused `model`, `retires_at`,
	// the exact `replacement` provider/id pair, and the request `path`.
	// Equal idempotent replay still returns work admitted before retirement.
	//
	// A text-only request body is limited to 1 MiB. A body carrying image or
	// document blocks is limited to 24 MiB, within which decoded media is
	// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
	// 5 MiB per image, and 16 MiB per document. Requests over any of these
	// limits are rejected before admission. URL media is fetched after an
	// idempotent replay check and before any row is admitted. The guarded
	// fetch accepts public HTTPS only, enforces the item limit while reading,
	// and sniffs the returned bytes. nvoken stores those bytes and does not
	// fetch the URL again.
	//
	// The supported streaming pattern is admit-then-stream: admit with a
	// plain JSON POST, then follow the Invocation over
	// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
	// connection without re-admitting and does not depend on front-end
	// streaming behavior. `Accept: text/event-stream` on this request is a
	// deployment-dependent convenience, not a first-class mode: the
	// committed admission is the first `invocation.accepted` frame and the
	// connection tails that Invocation through `invocation.result`, but
	// delivery requires the deployment front end to stream a non-`200`
	// POST response unbuffered. Some managed front ends (including Cloud
	// Run's) buffer it until the Invocation settles, which makes a
	// host-tool turn's `waiting` state invisible on this connection.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
	CreateInvocation(ctx context.Context, params *CreateInvocationParams, body CreateInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetInvocation Read authoritative Invocation identity and state
	//
	// Returns the durable current state. Post-admission failures appear here.
	// A credential authenticated for Runtime but denied this read operation
	// receives `forbidden`.
	// A resource outside the caller's tenant constraint is
	// reported as `not_found` when disclosure would reveal its existence.
	//
	// Corresponds with GET /v1/invocations/{invocation_id} (the `GetInvocation` operationId).
	GetInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CancelInvocation Idempotently cancel an Invocation
	//
	// Atomically makes nonterminal work `cancelled`. Repeating the request,
	// or cancelling an Invocation that already completed or failed, returns
	// the unchanged authoritative terminal row. A successful response means
	// cancellation is durable; provider work stops cooperatively and may
	// have already incurred external cost. The request body must be empty.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/cancel (the `CancelInvocation` operationId).
	CancelInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InterruptInvocation Gracefully stop an Invocation and keep its work
	//
	// Asks the turn to stop at its next execution seam and settle
	// `completed` with `stop_reason = interrupted`, so its assistant and
	// tool messages stay in the next turn's generation context. That is the
	// whole difference from cancellation, which drops the turn's work from
	// every later turn.
	//
	// The request is durable and idempotent. Work already at a seam —
	// `queued`, `waiting`, or `running` with no live owner — settles before
	// this call returns, closing any pending tool calls with synthetic
	// results; a subsequently submitted tool result then gets the ordinary
	// terminal `409`. Work under a live lease records the request and
	// returns the Invocation unchanged, still `running`: its executor stops
	// at the next checkpoint boundary, at worst one provider call away.
	// Follow the Invocation stream or re-read it to observe settlement.
	//
	// Interrupting terminal work is a no-op that returns the unchanged
	// terminal row. A turn whose definition carries `structured_output` and that
	// never published a validated object fails
	// `structured_output_unsatisfied` instead of completing — the
	// obligation was not met. Usage and settlement evidence are complete:
	// the work was kept, so it is charged. The request body must be empty.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/interrupt (the `InterruptInvocation` operationId).
	InterruptInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// NudgeInvocationWithBody Append steering to a running Invocation
	//
	// Stages caller guidance against a turn that is already running — "focus
	// on the marine segment" — without ending it and without discarding the
	// work being steered. This is the difference from
	// `if_active: supersede`, which rewinds: a superseded turn's assistant
	// and tool work product is dropped from the next generation, so steering
	// a long turn by supersession throws away exactly what is being steered.
	// Unlike Claude's `system.message`, a nudge is durable staged input
	// consumed at an execution seam, not an event injected immediately into
	// a managed session.
	//
	// A nudge is not an interrupt. The earliest injection point is the next
	// execution seam: the start of the next execution segment, which is a
	// host-tool turn's park boundary, or the stop seam where a turn that
	// considers itself finished re-enters its loop to answer the input in
	// place. An in-flight provider call or tool run is never aborted for it,
	// and an Invocation that has been interrupted is never given more to do —
	// the interrupt wins the seam and the staged input expires.
	//
	// A nudge is never implicitly minted and never implicitly converted.
	// `POST /v1/invocations` against a busy Session keeps its `if_active`
	// semantics; admission is never silently turned into a nudge, and a
	// nudge is never silently turned into an Invocation. nvoken holds no
	// agent configuration — the definition travels inline on every admission — so
	// there is nothing it could legitimately run converted input with.
	//
	// Input that the turn never takes is settled `expired` when the
	// Invocation settles, in the same transaction and under the same Session
	// lock this request takes. It never influences a later Invocation.
	// Re-sending missed direction as the next Invocation's input is the
	// host's call to make, and `GET .../pending-inputs` is where the miss is
	// visible.
	//
	// `content` is text: a string, or an array of text blocks. Image and
	// document blocks are accepted on an Invocation's own input but not
	// here, because the stop seam carries a continuation as text and staging
	// content one seam would silently drop is worse than refusing it.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
	NudgeInvocationWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// NudgeInvocation Append steering to a running Invocation
	//
	// Stages caller guidance against a turn that is already running — "focus
	// on the marine segment" — without ending it and without discarding the
	// work being steered. This is the difference from
	// `if_active: supersede`, which rewinds: a superseded turn's assistant
	// and tool work product is dropped from the next generation, so steering
	// a long turn by supersession throws away exactly what is being steered.
	// Unlike Claude's `system.message`, a nudge is durable staged input
	// consumed at an execution seam, not an event injected immediately into
	// a managed session.
	//
	// A nudge is not an interrupt. The earliest injection point is the next
	// execution seam: the start of the next execution segment, which is a
	// host-tool turn's park boundary, or the stop seam where a turn that
	// considers itself finished re-enters its loop to answer the input in
	// place. An in-flight provider call or tool run is never aborted for it,
	// and an Invocation that has been interrupted is never given more to do —
	// the interrupt wins the seam and the staged input expires.
	//
	// A nudge is never implicitly minted and never implicitly converted.
	// `POST /v1/invocations` against a busy Session keeps its `if_active`
	// semantics; admission is never silently turned into a nudge, and a
	// nudge is never silently turned into an Invocation. nvoken holds no
	// agent configuration — the definition travels inline on every admission — so
	// there is nothing it could legitimately run converted input with.
	//
	// Input that the turn never takes is settled `expired` when the
	// Invocation settles, in the same transaction and under the same Session
	// lock this request takes. It never influences a later Invocation.
	// Re-sending missed direction as the next Invocation's input is the
	// host's call to make, and `GET .../pending-inputs` is where the miss is
	// visible.
	//
	// `content` is text: a string, or an array of text blocks. Image and
	// document blocks are accepted on an Invocation's own input but not
	// here, because the stop seam carries a continuation as text and staging
	// content one seam would silently drop is worse than refusing it.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
	NudgeInvocation(ctx context.Context, invocationID InvocationID, body NudgeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListPendingInputs List staged input for an Invocation
	//
	// Returns the staged queue in `(created_at, id)` ascending order — the
	// order the turn will consume it — with settled rows retained so a
	// caller can answer "what did the user say, and did the model see it".
	// This, not an event vocabulary, is the reconciliation source for a
	// surface that shows queued direction: a drained input also appears on
	// the transcript stream as an ordinary `user` message.
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/pending-inputs (the `ListPendingInputs` operationId).
	ListPendingInputs(ctx context.Context, invocationID InvocationID, params *ListPendingInputsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CancelPendingInput Withdraw staged input the turn has not taken
	//
	// Cancels input that is still `pending`. Cancelling input that is
	// already `cancelled` returns it unchanged, so a retry is safe.
	//
	// Cancel and drain serialize on the row, so whichever commits first
	// wins outright: a cancelled input is never seen by the model, and input
	// the executor already drained is reported `409 pending_input_settled`
	// with the current resource in `details.pending_input` rather than being
	// withdrawn from a transcript it is already part of. The request body
	// must be empty.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/pending-inputs/{pending_input_id}/cancel (the `CancelPendingInput` operationId).
	CancelPendingInput(ctx context.Context, invocationID InvocationID, pendingInputID PendingInputID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetInvocationResult Read the composed Invocation result
	//
	// Returns one InvocationResult at any status: the authoritative
	// Invocation, this Invocation's canonical messages composed at read
	// time, and the output_text convenience projection. The Invocation and
	// its messages are read in one repeatable-read snapshot, so the payload
	// never shows a terminal status with a missing message tail.
	// Authentication, tenant scoping, and the nondisclosing not_found rule
	// match the plain Invocation read exactly.
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/result (the `GetInvocationResult` operationId).
	GetInvocationResult(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ResumeInvocationWithBody Raise the exhausted turn ceiling and resume a paused Invocation
	//
	// Requeues an Invocation paused on a turn-level consumption ceiling.
	// `limits` must contain only the exhausted field, raised above both its
	// prior value and the amount already consumed, and the new value must
	// remain within installation policy. A Session-budget pause resumes by
	// raising or removing the Session budget instead. Deadlines never pause.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
	ResumeInvocationWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ResumeInvocation Raise the exhausted turn ceiling and resume a paused Invocation
	//
	// Requeues an Invocation paused on a turn-level consumption ceiling.
	// `limits` must contain only the exhausted field, raised above both its
	// prior value and the amount already consumed, and the new value must
	// remain within installation policy. A Session-budget pause resumes by
	// raising or removing the Session budget instead. Deadlines never pause.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
	ResumeInvocation(ctx context.Context, invocationID InvocationID, body ResumeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// StreamInvocation Resume and tail one Invocation over SSE
	//
	// Replays durable updates after `cursor`, optionally forwards ephemeral
	// output previews for this Invocation, and ends only when this Invocation
	// settles or the connection rotates. Durable frames carry an SSE `id`;
	// previews and control frames do not. After `stream.resync`, discard
	// provisional output and wait for durable state.
	//
	// The explicit `cursor` query parameter takes precedence over
	// `Last-Event-ID`. `stream.end` reason `rotate` means reconnect with the
	// last durable ID. `deltas=false` skips preview fan-out without changing
	// replay, polling, cursor, or terminal semantics. Disconnecting never
	// cancels the Invocation.
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/stream (the `StreamInvocation` operationId).
	StreamInvocation(ctx context.Context, invocationID InvocationID, params *StreamInvocationParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListToolCalls Page through durable ToolCall execution records
	//
	// Returns ToolCalls in execution discovery order. Every execution mode is
	// included. The records contain lifecycle and timing facts only. Tool
	// inputs and results remain in the canonical Session transcript.
	//
	// Callback records include a delivery object. Its terminal outcome,
	// attempt count, and last HTTP status remain available after the bounded
	// delivery transport row is pruned. These records use the same
	// authentication, tenant scope, Session constraint, and nondisclosing
	// not_found behavior as the Invocation read. Deleting the Session deletes
	// these records with the rest of its subtree.
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/tool-calls (the `ListToolCalls` operationId).
	ListToolCalls(ctx context.Context, invocationID InvocationID, params *ListToolCallsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHostToolResultsWithBody Submit durable results for pending host ToolCalls
	//
	// Atomically accepts one bounded batch for a waiting Invocation. The
	// first committed result for each ToolCall wins. An equal replay is
	// acknowledged as deduplicated; a changed replay conflicts. Partial
	// batches leave the Invocation waiting. Closing the final pending call
	// queues the same Invocation and its successor execution dispatch before
	// returning `202`.
	//
	// This command accepts only host-mode calls owned by the path
	// Invocation and authenticated tenant scope. It is not a generic
	// Session append endpoint. The body is limited to 1 MiB; each result
	// content value is valid JSON limited to 256 KiB and 32 nesting levels.
	//
	// `content` accepts any JSON value and the stored transcript retains it
	// verbatim. Before a result reaches the model, a string or an array of
	// content blocks passes through unchanged; any other value is
	// serialized to its compact JSON text and sent as a string, so the
	// model sees the same bytes a host that pre-stringifies would send.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
	SubmitHostToolResultsWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHostToolResults Submit durable results for pending host ToolCalls
	//
	// Atomically accepts one bounded batch for a waiting Invocation. The
	// first committed result for each ToolCall wins. An equal replay is
	// acknowledged as deduplicated; a changed replay conflicts. Partial
	// batches leave the Invocation waiting. Closing the final pending call
	// queues the same Invocation and its successor execution dispatch before
	// returning `202`.
	//
	// This command accepts only host-mode calls owned by the path
	// Invocation and authenticated tenant scope. It is not a generic
	// Session append endpoint. The body is limited to 1 MiB; each result
	// content value is valid JSON limited to 256 KiB and 32 nesting levels.
	//
	// `content` accepts any JSON value and the stored transcript retains it
	// verbatim. Before a result reaches the model, a string or an array of
	// content blocks passes through unchanged; any other value is
	// serialized to its compact JSON text and sent as a string, so the
	// model sees the same bytes a host that pre-stringifies would send.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
	SubmitHostToolResults(ctx context.Context, invocationID InvocationID, body SubmitHostToolResultsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListMCPToolsWithBody Discover and project one remote MCP server's tools
	//
	// Opens one short-lived streamable-HTTP MCP session through guarded
	// public-only egress, drains tools/list pagination, applies the same
	// allowlist and projection rules used by Invocation execution, and then
	// closes the session. Supplied headers are used only for this request,
	// are never logged or returned, and are not persisted.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
	ListMCPToolsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListMCPTools Discover and project one remote MCP server's tools
	//
	// Opens one short-lived streamable-HTTP MCP session through guarded
	// public-only egress, drains tools/list pagination, applies the same
	// allowlist and projection rules used by Invocation execution, and then
	// closes the session. Supplied headers are used only for this request,
	// are never logged or returned, and are not persisted.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
	ListMCPTools(ctx context.Context, body ListMCPToolsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListModels List nvoken's curated model catalog
	//
	// Returns the complete bounded set of text-generation models nvoken
	// intentionally advertises. Catalog membership means nvoken maintains
	// metadata for the exact provider/model selection. It does not prove that
	// the caller's provider account, region, or selected provider key can access
	// the model. Ordering is deterministic but has no semantic meaning.
	//
	// Corresponds with GET /v1/models (the `ListModels` operationId).
	ListModels(ctx context.Context, params *ListModelsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetModel Inspect one exact provider/model selection
	//
	// Returns a descriptor for any valid installed provider and exact model
	// ID, including IDs outside the curated catalog. `cataloged` distinguishes
	// maintained metadata from tolerant inspection. Pricing is the standard
	// local USD estimate used by nvoken's estimated-cost guardrail, not
	// provider billing or proof of account access.
	//
	// Corresponds with GET /v1/models/{provider}/{model_id} (the `GetModel` operationId).
	GetModel(ctx context.Context, provider ModelProvider, modelID string, params *GetModelParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListProviderKeys List reusable provider key metadata
	//
	// Viewer, Runtime, and Operator profiles may read metadata within their exact scope; secret material is never readable.
	//
	// Corresponds with GET /v1/provider-keys (the `ListProviderKeys` operationId).
	ListProviderKeys(ctx context.Context, params *ListProviderKeysParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateProviderKeyWithBody Create one reusable provider key
	//
	// Creates encrypted app or tenant BYOK. App scope requires an
	// unconstrained Operator credential inside the app; the credential is
	// shared by every tenant of that app and by no other app. A
	// tenant-constrained Runtime credential may manage only its exact tenant
	// partition. The response contains metadata only.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
	CreateProviderKeyWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateProviderKey Create one reusable provider key
	//
	// Creates encrypted app or tenant BYOK. App scope requires an
	// unconstrained Operator credential inside the app; the credential is
	// shared by every tenant of that app and by no other app. A
	// tenant-constrained Runtime credential may manage only its exact tenant
	// partition. The response contains metadata only.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
	CreateProviderKey(ctx context.Context, body CreateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RevokeProviderKey Revoke a provider key and destroy live encrypted versions
	//
	// Corresponds with DELETE /v1/provider-keys/{provider_key_id} (the `RevokeProviderKey` operationId).
	RevokeProviderKey(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetProviderKey Read provider key metadata
	//
	// Viewer, Runtime, and Operator profiles may read secret-free metadata within their exact scope.
	//
	// Corresponds with GET /v1/provider-keys/{provider_key_id} (the `GetProviderKey` operationId).
	GetProviderKey(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RotateProviderKeyWithBody Rotate a provider key
	//
	// Replaces the active version, optionally preserving a bounded overlap.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
	RotateProviderKeyWithBody(ctx context.Context, providerKeyID ProviderKeyID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RotateProviderKey Rotate a provider key
	//
	// Replaces the active version, optionally preserving a bounded overlap.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
	RotateProviderKey(ctx context.Context, providerKeyID ProviderKeyID, body RotateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetProviderKeyUsage Read the token usage rollup for one provider key
	//
	// Aggregates durable checkpoint evidence across every Invocation that
	// bound the provider key: how many Invocations used it, when a model call
	// last used it, and the summed model usage. Derived at read time from the
	// transcript record, never from a separately maintained counter.
	// Authorization matches reading the provider key itself.
	//
	// Corresponds with GET /v1/provider-keys/{provider_key_id}/usage (the `GetProviderKeyUsage` operationId).
	GetProviderKeyUsage(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListSessions List authoritative Sessions
	//
	// Returns newest-first Session identity and current nonterminal
	// Invocation state. Exact filters combine with AND. Tenant filtering and
	// cursor binding follow the Invocation-list rules. `agent_id` and
	// `agent_key` are mutually exclusive and normalize to the same cursor
	// filter.
	//
	// Corresponds with GET /v1/sessions (the `ListSessions` operationId).
	ListSessions(ctx context.Context, params *ListSessionsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateSessionWithBody Create or seed a Session without admitting an Invocation
	//
	// Creates a Session with zero Invocations and optional host-asserted
	// starting history. Every body field is optional. An omitted `agent_key` leaves the
	// Session unbound: `agent_id` is null until the first admitted
	// Invocation binds it, and the binding is immutable once set either way;
	// `seed_messages` therefore requires `agent_key`. A `session_key`
	// requires an `agent_key`, because key uniqueness
	// and lookup are scoped per (tenant partition, Agent); a keyed create
	// is an upsert that accepts equal supplied Session options and returns
	// `session_options_conflict` when a supplied value differs.
	// Without a `session_key`, every call creates a fresh Session.
	// Precedence for the tenant partition is credential constraint,
	// explicit `tenant_key`, then the default partition.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
	CreateSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateSession Create or seed a Session without admitting an Invocation
	//
	// Creates a Session with zero Invocations and optional host-asserted
	// starting history. Every body field is optional. An omitted `agent_key` leaves the
	// Session unbound: `agent_id` is null until the first admitted
	// Invocation binds it, and the binding is immutable once set either way;
	// `seed_messages` therefore requires `agent_key`. A `session_key`
	// requires an `agent_key`, because key uniqueness
	// and lookup are scoped per (tenant partition, Agent); a keyed create
	// is an upsert that accepts equal supplied Session options and returns
	// `session_options_conflict` when a supplied value differs.
	// Without a `session_key`, every call creates a fresh Session.
	// Precedence for the tenant partition is credential constraint,
	// explicit `tenant_key`, then the default partition.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
	CreateSession(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteSession Erase a Session and everything under it
	//
	// Removes the Session, its Invocations, transcript messages, checkpoints,
	// tool calls, provider artifacts, compactions, provider-key and MCP
	// bindings, and undelivered webhooks. The erasure is immediate and
	// irreversible; a subsequent read is `not_found`.
	//
	// An Invocation still running is stopped. No cancellation is recorded,
	// because there is nothing left to record it against — the Invocation is
	// removed rather than settled, and no `invocation.settled` webhook
	// is emitted for it. A host that needs a settled record should cancel and
	// observe the terminal state before deleting.
	//
	// Erasure is scoped like every other Session operation: an unknown or
	// out-of-scope `session_id` returns `not_found`, so a retry after a lost
	// response can treat `404` as already-done. The `delete_session`
	// operation belongs to the Runtime and Operator profiles; a Viewer
	// credential cannot erase a transcript.
	//
	// **This is not account deletion by itself.** nvoken keeps no account
	// tombstone, so a host honouring a deletion request must first stop
	// admitting work for that tenant, then page `GET /v1/sessions` and delete
	// until the list is empty. Otherwise a concurrent request creates a new
	// Session behind the sweep.
	//
	// Two consequences worth planning for. Usage reporting shrinks
	// retroactively: `GET /v1/usage/daily` is computed at read time from
	// Invocation evidence that erasure removes, which is why the endpoint is
	// documented as operational visibility rather than a billing ledger — a
	// billing host records usage at settlement, keyed by Invocation id.
	// And the erased Invocations' idempotency keys become free for reuse,
	// consistent with the documented guarantee that deduplication holds while
	// the original Invocation is retained.
	//
	// Corresponds with DELETE /v1/sessions/{session_id} (the `DeleteSession` operationId).
	DeleteSession(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSession Read authoritative Session identity and current state
	//
	// An installation-wide credential may resolve a Session in any tenant
	// partition. A tenant-constrained credential resolves only Sessions
	// in its partition. Missing, incompatible, and undisclosable resources use
	// `not_found`; a credential denied the read operation itself receives
	// `forbidden`.
	//
	// Corresponds with GET /v1/sessions/{session_id} (the `GetSession` operationId).
	GetSession(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateSessionWithBody Merge host metadata into a Session
	//
	// Merges a metadata patch: a present key replaces its value, an explicit
	// `null` deletes that key, and a key the patch does not mention survives.
	//
	// Merge rather than replace, because independent writers share this map —
	// a conversation UI writing a title, correlation tooling writing a trace
	// id — and a full replacement would make each silently discard the
	// other's keys. The merge happens under the Session lock, so two
	// concurrent patches compose instead of one overwriting the other's read.
	//
	// `"metadata": null` is refused rather than guessed at: it could mean
	// "clear everything" or "leave it alone", and either reading is
	// destructive or silent. Delete keys one at a time.
	//
	// Bounds apply to the merged result, not to the patch, so a patch that
	// deletes as many keys as it adds is not refused for a count it never
	// produces. Requires the `update_session` operation.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
	UpdateSessionWithBody(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateSession Merge host metadata into a Session
	//
	// Merges a metadata patch: a present key replaces its value, an explicit
	// `null` deletes that key, and a key the patch does not mention survives.
	//
	// Merge rather than replace, because independent writers share this map —
	// a conversation UI writing a title, correlation tooling writing a trace
	// id — and a full replacement would make each silently discard the
	// other's keys. The merge happens under the Session lock, so two
	// concurrent patches compose instead of one overwriting the other's read.
	//
	// `"metadata": null` is refused rather than guessed at: it could mean
	// "clear everything" or "leave it alone", and either reading is
	// destructive or silent. Delete keys one at a time.
	//
	// Bounds apply to the merged result, not to the patch, so a patch that
	// deletes as many keys as it adds is not refused for a count it never
	// produces. Requires the `update_session` operation.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
	UpdateSession(ctx context.Context, sessionID SessionID, body UpdateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListSessionCompactions Page through immutable Session compaction records
	//
	// Returns newest-first diagnostic records for every attempted compaction
	// pass. `applied` records include the private summary projection and its
	// model usage. `fell_through` records identify the failure class and may
	// include usage when the provider returned trustworthy evidence. Only
	// applied records change future model context; neither kind changes the
	// canonical Session transcript.
	//
	// The summary is derived from the caller's own transcript and therefore
	// uses the same authorization and Session scoping as transcript reads.
	// The opaque cursor is bound to the authenticated app and Session.
	//
	// Corresponds with GET /v1/sessions/{session_id}/compactions (the `ListSessionCompactions` operationId).
	ListSessionCompactions(ctx context.Context, sessionID SessionID, params *ListSessionCompactionsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ForkSessionWithBody Copy a Session prefix into a new Session
	//
	// Creates a new Session in the source Session's tenant and Agent scope,
	// copying every canonical message through `from_message` inclusively.
	// The source is untouched. The child stores durable Session and message
	// lineage, but copied messages no longer belong to the source
	// Invocations. Their `origin`, per-turn `user_key`, and resolved message
	// phase are preserved.
	//
	// Usage and compaction summaries are not copied. Child usage starts at
	// zero and the child starts uncompacted. Retention and metadata come only
	// from `session_options` on this request; no Session option is inherited.
	// A `session_key` has the same tenant/Agent-scoped upsert behavior as
	// Session creation.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
	ForkSessionWithBody(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ForkSession Copy a Session prefix into a new Session
	//
	// Creates a new Session in the source Session's tenant and Agent scope,
	// copying every canonical message through `from_message` inclusively.
	// The source is untouched. The child stores durable Session and message
	// lineage, but copied messages no longer belong to the source
	// Invocations. Their `origin`, per-turn `user_key`, and resolved message
	// phase are preserved.
	//
	// Usage and compaction summaries are not copied. Child usage starts at
	// zero and the child starts uncompacted. Retention and metadata come only
	// from `session_options` on this request; no Session option is inherited.
	// A `session_key` has the same tenant/Agent-scoped upsert behavior as
	// Session creation.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
	ForkSession(ctx context.Context, sessionID SessionID, body ForkSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListSessionMessages Page through the canonical Session transcript
	//
	// Returns persisted SessionMessage rows in ascending sequence order.
	// The opaque forward cursor is bound to the authenticated caller and
	// Session. This history endpoint contains no lifecycle or live-preview
	// copies.
	//
	// Corresponds with GET /v1/sessions/{session_id}/messages (the `ListSessionMessages` operationId).
	ListSessionMessages(ctx context.Context, sessionID SessionID, params *ListSessionMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSessionTranscript Drain a fixed-cut incremental transcript snapshot
	//
	// Projects canonical messages and append-only Invocation lifecycle
	// changes. Supply a prior `resume_cursor` as `cursor` to drain newer
	// durable state. Continue a multi-page fixed cut with `page_token` until
	// `has_more` is false. Each page contains one phase: all message pages are
	// delivered before lifecycle-change pages, so terminal state cannot
	// precede the transcript rows committed with it. Omitting both positions
	// starts at the beginning of the retained Session.
	//
	// Corresponds with GET /v1/sessions/{session_id}/transcript (the `GetSessionTranscript` operationId).
	GetSessionTranscript(ctx context.Context, sessionID SessionID, params *GetSessionTranscriptParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// StreamSessionTranscript Replay and tail one Session transcript over SSE
	//
	// Opens a resumable Server-Sent Events projection over the same fixed-cut
	// transcript read model as the JSON endpoint. With deltas enabled, the
	// server subscribes to live fan-out before its first Postgres drain. In
	// either mode it re-drains Postgres on a bounded poll and closes after
	// authoritative terminal reconciliation or deliberate rotation.
	// Disconnecting never cancels the Invocation.
	//
	// Every nonempty `transcript.update` frame carries
	// `id: <resume_cursor>`; that opaque ID is the only replay position
	// clients persist. `output_text.delta`, `thinking.delta`,
	// `stream.resync`, and `stream.end` never carry an `id`. Deltas are
	// ephemeral and may be lost; after `stream.resync`, discard provisional
	// output and wait for canonical messages. `stream.end` reason `terminal`
	// means the final Postgres drain observed no nonterminal Invocation.
	// Reason `rotate` means reconnect with the last durable ID. An abnormal
	// close has no terminal meaning. `deltas=false` skips preview fan-out
	// without changing replay, polling, cursor, or terminal semantics.
	//
	// The explicit `cursor` query parameter takes precedence over
	// `Last-Event-ID`. Bearer authentication requires an SSE-capable HTTP
	// client that can set the `Authorization` header; the browser EventSource
	// constructor alone cannot do so. The server emits `retry: 1000` as its
	// default reconnect delay.
	//
	// Corresponds with GET /v1/sessions/{session_id}/transcript/stream (the `StreamSessionTranscript` operationId).
	StreamSessionTranscript(ctx context.Context, sessionID SessionID, params *StreamSessionTranscriptParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetDailyUsage Read daily usage rollups
	//
	// Aggregates durable model-call evidence — invocation checkpoints plus
	// session compaction calls — into per-day buckets keyed by app,
	// provider, and model, summing token counts and estimated cost. Derived
	// at read time from the transcript record, never from a separately
	// maintained counter.
	//
	// An app-bound credential reads its own app. An app-less presentation
	// spans every registered app; for a console issuer token that requires
	// the admin claim.
	//
	// Corresponds with GET /v1/usage/daily (the `GetDailyUsage` operationId).
	GetDailyUsage(ctx context.Context, params *GetDailyUsageParams, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// ListAgents List Agent identity anchors
//
// Returns newest-first identity anchors scoped to the caller's app. An
// Agent stores only its nvoken ID, the host-owned `agent_key`, and
// creation time; instructions, models, tools, and provider keys still
// travel on each Invocation. An exact `agent_key` filter returns zero or
// one item.
//
// Unconstrained credentials see the anchors referenced by a Session in
// one of their app's tenants. Tenant-constrained credentials see only
// anchors referenced by a Session in their effective partition.
// Session-constrained credentials see only that Session's anchor. The
// opaque cursor is bound to the authenticated caller, credential
// constraint, and exact key filter.
//
// Corresponds with GET /v1/agents (the `ListAgents` operationId).
func (c *Client) ListAgents(ctx context.Context, params *ListAgentsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAgentsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAgent Read one Agent identity anchor
//
// Reads identity without admitting work. Out-of-scope and undisclosable
// constrained resources use `not_found`.
//
// Corresponds with GET /v1/agents/{agent_id} (the `GetAgent` operationId).
func (c *Client) GetAgent(ctx context.Context, agentID AgentID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAgentRequest(c.Server, agentID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListApps List registered apps
//
// Returns the apps this credential can see. A credential associated with
// an app sees only that app; a credential with no app association sees
// every registered app. An exact `external_ref` filter narrows either
// view to apps owned by that reference.
//
// Corresponds with GET /v1/apps (the `ListApps` operationId).
func (c *Client) ListApps(ctx context.Context, params *ListAppsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAppsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RegisterAppWithBody Register an app
//
// Registers one host application and creates its default tenant,
// returning the generated `app_id`.
//
// Registration is installation management rather than work inside an app,
// so it requires a credential that is *not* associated with an app; a
// credential bound to one app cannot mint siblings for itself. Names
// identify apps and are unique, so re-registering an existing name is
// rejected rather than returning the existing app.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
func (c *Client) RegisterAppWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRegisterAppRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RegisterApp Register an app
//
// Registers one host application and creates its default tenant,
// returning the generated `app_id`.
//
// Registration is installation management rather than work inside an app,
// so it requires a credential that is *not* associated with an app; a
// credential bound to one app cannot mint siblings for itself. Names
// identify apps and are unique, so re-registering an existing name is
// rejected rather than returning the existing app.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
func (c *Client) RegisterApp(ctx context.Context, body RegisterAppJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRegisterAppRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetApp Get one app
//
// Returns one registered app. A credential associated with a different
// app receives `404`, so an app identifier cannot be probed across the
// isolation boundary.
//
// Corresponds with GET /v1/apps/{app_id} (the `GetApp` operationId).
func (c *Client) GetApp(ctx context.Context, appID string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAppRequest(c.Server, appID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAppWithBody Update an app
//
// Updates an app's mutable presentation fields. The unique `name` and the
// ownership-grounding `external_ref` cannot be changed. A credential
// associated with a different app receives `404`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
func (c *Client) UpdateAppWithBody(ctx context.Context, appID string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAppRequestWithBody(c.Server, appID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateApp Update an app
//
// Updates an app's mutable presentation fields. The unique `name` and the
// ownership-grounding `external_ref` cannot be changed. A credential
// associated with a different app receives `404`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
func (c *Client) UpdateApp(ctx context.Context, appID string, body UpdateAppJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAppRequest(c.Server, appID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBudgetWithBody Create one fixed usage budget window
//
// Creates an estimated-list-price guardrail inside the authenticated App.
// Windows for the same scope identity cannot overlap. The idempotency key
// makes an identical retry return the first Budget.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
func (c *Client) CreateBudgetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBudgetRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBudget Create one fixed usage budget window
//
// Creates an estimated-list-price guardrail inside the authenticated App.
// Windows for the same scope identity cannot overlap. The idempotency key
// makes an identical retry return the first Budget.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
func (c *Client) CreateBudget(ctx context.Context, body CreateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBudgetRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteBudget Remove a Budget guardrail
//
// Removes enforcement without deleting its accounting history.
//
// Corresponds with DELETE /v1/budgets/{budget_id} (the `DeleteBudget` operationId).
func (c *Client) DeleteBudget(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteBudgetRequest(c.Server, budgetID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBudget Read one usage Budget
//
// Corresponds with GET /v1/budgets/{budget_id} (the `GetBudget` operationId).
func (c *Client) GetBudget(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBudgetRequest(c.Server, budgetID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBudgetWithBody Raise or lower a Budget cap
//
// Replaces the cap. It cannot be lower than settled plus reserved cost.
// Raising it reevaluates paused work in bounded batches.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
func (c *Client) UpdateBudgetWithBody(ctx context.Context, budgetID BudgetID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBudgetRequestWithBody(c.Server, budgetID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBudget Raise or lower a Budget cap
//
// Replaces the cap. It cannot be lower than settled plus reserved cost.
// Raising it reevaluates paused work in bounded batches.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
func (c *Client) UpdateBudget(ctx context.Context, budgetID BudgetID, body UpdateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBudgetRequest(c.Server, budgetID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListInvocations List authoritative Invocations
//
// Returns newest-first durable Invocation state. Exact filters combine
// with AND. An installation-wide caller may list all tenant partitions, one
// named partition with `tenant_key`, or the default partition with
// `default_tenant=true`. A tenant-constrained credential is always scoped
// to its partition. The opaque cursor is bound to the normalized filter
// set and credential tenant scope. `agent_id` and `agent_key` are
// mutually exclusive; both normalize to the resolved Agent ID for cursor
// binding, so an equivalent cursor may resume under either spelling.
//
// Corresponds with GET /v1/invocations (the `ListInvocations` operationId).
func (c *Client) ListInvocations(ctx context.Context, params *ListInvocationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListInvocationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateInvocationWithBody Durably admit one background agent turn
//
// Resolves or creates the installation-wide Agent identity anchor,
// resolves or creates a Session, resolves the inline or referenced
// definition, appends exactly one caller-input message, and creates one
// queued Invocation in a single transaction. The response is sent only
// after that transaction commits. The request handler never executes the
// model.
//
// `session_id` and `session_key` are mutually exclusive. A Session ID must
// belong to the named Agent, or be a Session created without one — the
// first admitted Invocation binds the Agent permanently. An installation-wide
// credential may omit `tenant_key` and use the Session's stored partition.
// A tenant-constrained credential cannot cross its partition; an explicit
// mismatch is rejected with `403 forbidden` before resource lookup.
//
// `idempotency_key` is scoped to the effective
// tenant partition, and `agent_key`. A same-key replay is checked before
// the Session's one-nonterminal-Invocation rule. It returns the original
// records without appending input, even when the Invocation is terminal.
// Material equality covers the Session selector kind and value, the
// resolved definition, and input. A `definition_id` and its byte-identical
// inline definition compare equal. Requested limits are compared before
// default resolution, so an explicit default differs from omission. JSON
// object member order is ignored, array order is significant, and strings
// are not rewritten. A changed material field returns
// `idempotency_conflict`.
//
// `if_active` defaults to `reject`, preserving
// `session_invocation_active`. Explicit `supersede` requires both create
// and cancel authority. While holding the Session lock, it durably
// cancels any nonterminal Invocation and admits this replacement in the
// same transaction. With no active work it admits normally. Equal replay
// returns the original work before supersession and never cancels newer
// active work.
//
// `interrupt` requires the same authority and keeps the same lock
// ordering, but stops the active Invocation gracefully instead of
// cancelling it: work already at a seam settles and the replacement is
// admitted in the same transaction, while work under a live lease has
// the interrupt recorded and this request waits for it to settle. If it
// has not settled within the wait, the response is
// `session_invocation_active` with `details.interrupt_requested = true`
// and the request may be reissued.
//
// A cataloged model continues to admit while deprecated. On and after
// its `retires_at` date, a new admission is refused with `422
// model_retired`; `details` carries the refused `model`, `retires_at`,
// the exact `replacement` provider/id pair, and the request `path`.
// Equal idempotent replay still returns work admitted before retirement.
//
// A text-only request body is limited to 1 MiB. A body carrying image or
// document blocks is limited to 24 MiB, within which decoded media is
// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
// 5 MiB per image, and 16 MiB per document. Requests over any of these
// limits are rejected before admission. URL media is fetched after an
// idempotent replay check and before any row is admitted. The guarded
// fetch accepts public HTTPS only, enforces the item limit while reading,
// and sniffs the returned bytes. nvoken stores those bytes and does not
// fetch the URL again.
//
// The supported streaming pattern is admit-then-stream: admit with a
// plain JSON POST, then follow the Invocation over
// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
// connection without re-admitting and does not depend on front-end
// streaming behavior. `Accept: text/event-stream` on this request is a
// deployment-dependent convenience, not a first-class mode: the
// committed admission is the first `invocation.accepted` frame and the
// connection tails that Invocation through `invocation.result`, but
// delivery requires the deployment front end to stream a non-`200`
// POST response unbuffered. Some managed front ends (including Cloud
// Run's) buffer it until the Invocation settles, which makes a
// host-tool turn's `waiting` state invisible on this connection.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
func (c *Client) CreateInvocationWithBody(ctx context.Context, params *CreateInvocationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateInvocationRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateInvocation Durably admit one background agent turn
//
// Resolves or creates the installation-wide Agent identity anchor,
// resolves or creates a Session, resolves the inline or referenced
// definition, appends exactly one caller-input message, and creates one
// queued Invocation in a single transaction. The response is sent only
// after that transaction commits. The request handler never executes the
// model.
//
// `session_id` and `session_key` are mutually exclusive. A Session ID must
// belong to the named Agent, or be a Session created without one — the
// first admitted Invocation binds the Agent permanently. An installation-wide
// credential may omit `tenant_key` and use the Session's stored partition.
// A tenant-constrained credential cannot cross its partition; an explicit
// mismatch is rejected with `403 forbidden` before resource lookup.
//
// `idempotency_key` is scoped to the effective
// tenant partition, and `agent_key`. A same-key replay is checked before
// the Session's one-nonterminal-Invocation rule. It returns the original
// records without appending input, even when the Invocation is terminal.
// Material equality covers the Session selector kind and value, the
// resolved definition, and input. A `definition_id` and its byte-identical
// inline definition compare equal. Requested limits are compared before
// default resolution, so an explicit default differs from omission. JSON
// object member order is ignored, array order is significant, and strings
// are not rewritten. A changed material field returns
// `idempotency_conflict`.
//
// `if_active` defaults to `reject`, preserving
// `session_invocation_active`. Explicit `supersede` requires both create
// and cancel authority. While holding the Session lock, it durably
// cancels any nonterminal Invocation and admits this replacement in the
// same transaction. With no active work it admits normally. Equal replay
// returns the original work before supersession and never cancels newer
// active work.
//
// `interrupt` requires the same authority and keeps the same lock
// ordering, but stops the active Invocation gracefully instead of
// cancelling it: work already at a seam settles and the replacement is
// admitted in the same transaction, while work under a live lease has
// the interrupt recorded and this request waits for it to settle. If it
// has not settled within the wait, the response is
// `session_invocation_active` with `details.interrupt_requested = true`
// and the request may be reissued.
//
// A cataloged model continues to admit while deprecated. On and after
// its `retires_at` date, a new admission is refused with `422
// model_retired`; `details` carries the refused `model`, `retires_at`,
// the exact `replacement` provider/id pair, and the request `path`.
// Equal idempotent replay still returns work admitted before retirement.
//
// A text-only request body is limited to 1 MiB. A body carrying image or
// document blocks is limited to 24 MiB, within which decoded media is
// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
// 5 MiB per image, and 16 MiB per document. Requests over any of these
// limits are rejected before admission. URL media is fetched after an
// idempotent replay check and before any row is admitted. The guarded
// fetch accepts public HTTPS only, enforces the item limit while reading,
// and sniffs the returned bytes. nvoken stores those bytes and does not
// fetch the URL again.
//
// The supported streaming pattern is admit-then-stream: admit with a
// plain JSON POST, then follow the Invocation over
// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
// connection without re-admitting and does not depend on front-end
// streaming behavior. `Accept: text/event-stream` on this request is a
// deployment-dependent convenience, not a first-class mode: the
// committed admission is the first `invocation.accepted` frame and the
// connection tails that Invocation through `invocation.result`, but
// delivery requires the deployment front end to stream a non-`200`
// POST response unbuffered. Some managed front ends (including Cloud
// Run's) buffer it until the Invocation settles, which makes a
// host-tool turn's `waiting` state invisible on this connection.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
func (c *Client) CreateInvocation(ctx context.Context, params *CreateInvocationParams, body CreateInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateInvocationRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetInvocation Read authoritative Invocation identity and state
//
// Returns the durable current state. Post-admission failures appear here.
// A credential authenticated for Runtime but denied this read operation
// receives `forbidden`.
// A resource outside the caller's tenant constraint is
// reported as `not_found` when disclosure would reveal its existence.
//
// Corresponds with GET /v1/invocations/{invocation_id} (the `GetInvocation` operationId).
func (c *Client) GetInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetInvocationRequest(c.Server, invocationID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CancelInvocation Idempotently cancel an Invocation
//
// Atomically makes nonterminal work `cancelled`. Repeating the request,
// or cancelling an Invocation that already completed or failed, returns
// the unchanged authoritative terminal row. A successful response means
// cancellation is durable; provider work stops cooperatively and may
// have already incurred external cost. The request body must be empty.
//
// Corresponds with POST /v1/invocations/{invocation_id}/cancel (the `CancelInvocation` operationId).
func (c *Client) CancelInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCancelInvocationRequest(c.Server, invocationID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InterruptInvocation Gracefully stop an Invocation and keep its work
//
// Asks the turn to stop at its next execution seam and settle
// `completed` with `stop_reason = interrupted`, so its assistant and
// tool messages stay in the next turn's generation context. That is the
// whole difference from cancellation, which drops the turn's work from
// every later turn.
//
// The request is durable and idempotent. Work already at a seam —
// `queued`, `waiting`, or `running` with no live owner — settles before
// this call returns, closing any pending tool calls with synthetic
// results; a subsequently submitted tool result then gets the ordinary
// terminal `409`. Work under a live lease records the request and
// returns the Invocation unchanged, still `running`: its executor stops
// at the next checkpoint boundary, at worst one provider call away.
// Follow the Invocation stream or re-read it to observe settlement.
//
// Interrupting terminal work is a no-op that returns the unchanged
// terminal row. A turn whose definition carries `structured_output` and that
// never published a validated object fails
// `structured_output_unsatisfied` instead of completing — the
// obligation was not met. Usage and settlement evidence are complete:
// the work was kept, so it is charged. The request body must be empty.
//
// Corresponds with POST /v1/invocations/{invocation_id}/interrupt (the `InterruptInvocation` operationId).
func (c *Client) InterruptInvocation(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInterruptInvocationRequest(c.Server, invocationID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NudgeInvocationWithBody Append steering to a running Invocation
//
// Stages caller guidance against a turn that is already running — "focus
// on the marine segment" — without ending it and without discarding the
// work being steered. This is the difference from
// `if_active: supersede`, which rewinds: a superseded turn's assistant
// and tool work product is dropped from the next generation, so steering
// a long turn by supersession throws away exactly what is being steered.
// Unlike Claude's `system.message`, a nudge is durable staged input
// consumed at an execution seam, not an event injected immediately into
// a managed session.
//
// A nudge is not an interrupt. The earliest injection point is the next
// execution seam: the start of the next execution segment, which is a
// host-tool turn's park boundary, or the stop seam where a turn that
// considers itself finished re-enters its loop to answer the input in
// place. An in-flight provider call or tool run is never aborted for it,
// and an Invocation that has been interrupted is never given more to do —
// the interrupt wins the seam and the staged input expires.
//
// A nudge is never implicitly minted and never implicitly converted.
// `POST /v1/invocations` against a busy Session keeps its `if_active`
// semantics; admission is never silently turned into a nudge, and a
// nudge is never silently turned into an Invocation. nvoken holds no
// agent configuration — the definition travels inline on every admission — so
// there is nothing it could legitimately run converted input with.
//
// Input that the turn never takes is settled `expired` when the
// Invocation settles, in the same transaction and under the same Session
// lock this request takes. It never influences a later Invocation.
// Re-sending missed direction as the next Invocation's input is the
// host's call to make, and `GET .../pending-inputs` is where the miss is
// visible.
//
// `content` is text: a string, or an array of text blocks. Image and
// document blocks are accepted on an Invocation's own input but not
// here, because the stop seam carries a continuation as text and staging
// content one seam would silently drop is worse than refusing it.
//
// Requires the same authority as cancelling the Invocation.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
func (c *Client) NudgeInvocationWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewNudgeInvocationRequestWithBody(c.Server, invocationID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NudgeInvocation Append steering to a running Invocation
//
// Stages caller guidance against a turn that is already running — "focus
// on the marine segment" — without ending it and without discarding the
// work being steered. This is the difference from
// `if_active: supersede`, which rewinds: a superseded turn's assistant
// and tool work product is dropped from the next generation, so steering
// a long turn by supersession throws away exactly what is being steered.
// Unlike Claude's `system.message`, a nudge is durable staged input
// consumed at an execution seam, not an event injected immediately into
// a managed session.
//
// A nudge is not an interrupt. The earliest injection point is the next
// execution seam: the start of the next execution segment, which is a
// host-tool turn's park boundary, or the stop seam where a turn that
// considers itself finished re-enters its loop to answer the input in
// place. An in-flight provider call or tool run is never aborted for it,
// and an Invocation that has been interrupted is never given more to do —
// the interrupt wins the seam and the staged input expires.
//
// A nudge is never implicitly minted and never implicitly converted.
// `POST /v1/invocations` against a busy Session keeps its `if_active`
// semantics; admission is never silently turned into a nudge, and a
// nudge is never silently turned into an Invocation. nvoken holds no
// agent configuration — the definition travels inline on every admission — so
// there is nothing it could legitimately run converted input with.
//
// Input that the turn never takes is settled `expired` when the
// Invocation settles, in the same transaction and under the same Session
// lock this request takes. It never influences a later Invocation.
// Re-sending missed direction as the next Invocation's input is the
// host's call to make, and `GET .../pending-inputs` is where the miss is
// visible.
//
// `content` is text: a string, or an array of text blocks. Image and
// document blocks are accepted on an Invocation's own input but not
// here, because the stop seam carries a continuation as text and staging
// content one seam would silently drop is worse than refusing it.
//
// Requires the same authority as cancelling the Invocation.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
func (c *Client) NudgeInvocation(ctx context.Context, invocationID InvocationID, body NudgeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewNudgeInvocationRequest(c.Server, invocationID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListPendingInputs List staged input for an Invocation
//
// Returns the staged queue in `(created_at, id)` ascending order — the
// order the turn will consume it — with settled rows retained so a
// caller can answer "what did the user say, and did the model see it".
// This, not an event vocabulary, is the reconciliation source for a
// surface that shows queued direction: a drained input also appears on
// the transcript stream as an ordinary `user` message.
//
// Corresponds with GET /v1/invocations/{invocation_id}/pending-inputs (the `ListPendingInputs` operationId).
func (c *Client) ListPendingInputs(ctx context.Context, invocationID InvocationID, params *ListPendingInputsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListPendingInputsRequest(c.Server, invocationID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CancelPendingInput Withdraw staged input the turn has not taken
//
// Cancels input that is still `pending`. Cancelling input that is
// already `cancelled` returns it unchanged, so a retry is safe.
//
// Cancel and drain serialize on the row, so whichever commits first
// wins outright: a cancelled input is never seen by the model, and input
// the executor already drained is reported `409 pending_input_settled`
// with the current resource in `details.pending_input` rather than being
// withdrawn from a transcript it is already part of. The request body
// must be empty.
//
// Requires the same authority as cancelling the Invocation.
//
// Corresponds with POST /v1/invocations/{invocation_id}/pending-inputs/{pending_input_id}/cancel (the `CancelPendingInput` operationId).
func (c *Client) CancelPendingInput(ctx context.Context, invocationID InvocationID, pendingInputID PendingInputID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCancelPendingInputRequest(c.Server, invocationID, pendingInputID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetInvocationResult Read the composed Invocation result
//
// Returns one InvocationResult at any status: the authoritative
// Invocation, this Invocation's canonical messages composed at read
// time, and the output_text convenience projection. The Invocation and
// its messages are read in one repeatable-read snapshot, so the payload
// never shows a terminal status with a missing message tail.
// Authentication, tenant scoping, and the nondisclosing not_found rule
// match the plain Invocation read exactly.
//
// Corresponds with GET /v1/invocations/{invocation_id}/result (the `GetInvocationResult` operationId).
func (c *Client) GetInvocationResult(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetInvocationResultRequest(c.Server, invocationID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ResumeInvocationWithBody Raise the exhausted turn ceiling and resume a paused Invocation
//
// Requeues an Invocation paused on a turn-level consumption ceiling.
// `limits` must contain only the exhausted field, raised above both its
// prior value and the amount already consumed, and the new value must
// remain within installation policy. A Session-budget pause resumes by
// raising or removing the Session budget instead. Deadlines never pause.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
func (c *Client) ResumeInvocationWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewResumeInvocationRequestWithBody(c.Server, invocationID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ResumeInvocation Raise the exhausted turn ceiling and resume a paused Invocation
//
// Requeues an Invocation paused on a turn-level consumption ceiling.
// `limits` must contain only the exhausted field, raised above both its
// prior value and the amount already consumed, and the new value must
// remain within installation policy. A Session-budget pause resumes by
// raising or removing the Session budget instead. Deadlines never pause.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
func (c *Client) ResumeInvocation(ctx context.Context, invocationID InvocationID, body ResumeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewResumeInvocationRequest(c.Server, invocationID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// StreamInvocation Resume and tail one Invocation over SSE
//
// Replays durable updates after `cursor`, optionally forwards ephemeral
// output previews for this Invocation, and ends only when this Invocation
// settles or the connection rotates. Durable frames carry an SSE `id`;
// previews and control frames do not. After `stream.resync`, discard
// provisional output and wait for durable state.
//
// The explicit `cursor` query parameter takes precedence over
// `Last-Event-ID`. `stream.end` reason `rotate` means reconnect with the
// last durable ID. `deltas=false` skips preview fan-out without changing
// replay, polling, cursor, or terminal semantics. Disconnecting never
// cancels the Invocation.
//
// Corresponds with GET /v1/invocations/{invocation_id}/stream (the `StreamInvocation` operationId).
func (c *Client) StreamInvocation(ctx context.Context, invocationID InvocationID, params *StreamInvocationParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewStreamInvocationRequest(c.Server, invocationID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListToolCalls Page through durable ToolCall execution records
//
// Returns ToolCalls in execution discovery order. Every execution mode is
// included. The records contain lifecycle and timing facts only. Tool
// inputs and results remain in the canonical Session transcript.
//
// Callback records include a delivery object. Its terminal outcome,
// attempt count, and last HTTP status remain available after the bounded
// delivery transport row is pruned. These records use the same
// authentication, tenant scope, Session constraint, and nondisclosing
// not_found behavior as the Invocation read. Deleting the Session deletes
// these records with the rest of its subtree.
//
// Corresponds with GET /v1/invocations/{invocation_id}/tool-calls (the `ListToolCalls` operationId).
func (c *Client) ListToolCalls(ctx context.Context, invocationID InvocationID, params *ListToolCallsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListToolCallsRequest(c.Server, invocationID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHostToolResultsWithBody Submit durable results for pending host ToolCalls
//
// Atomically accepts one bounded batch for a waiting Invocation. The
// first committed result for each ToolCall wins. An equal replay is
// acknowledged as deduplicated; a changed replay conflicts. Partial
// batches leave the Invocation waiting. Closing the final pending call
// queues the same Invocation and its successor execution dispatch before
// returning `202`.
//
// This command accepts only host-mode calls owned by the path
// Invocation and authenticated tenant scope. It is not a generic
// Session append endpoint. The body is limited to 1 MiB; each result
// content value is valid JSON limited to 256 KiB and 32 nesting levels.
//
// `content` accepts any JSON value and the stored transcript retains it
// verbatim. Before a result reaches the model, a string or an array of
// content blocks passes through unchanged; any other value is
// serialized to its compact JSON text and sent as a string, so the
// model sees the same bytes a host that pre-stringifies would send.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
func (c *Client) SubmitHostToolResultsWithBody(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHostToolResultsRequestWithBody(c.Server, invocationID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHostToolResults Submit durable results for pending host ToolCalls
//
// Atomically accepts one bounded batch for a waiting Invocation. The
// first committed result for each ToolCall wins. An equal replay is
// acknowledged as deduplicated; a changed replay conflicts. Partial
// batches leave the Invocation waiting. Closing the final pending call
// queues the same Invocation and its successor execution dispatch before
// returning `202`.
//
// This command accepts only host-mode calls owned by the path
// Invocation and authenticated tenant scope. It is not a generic
// Session append endpoint. The body is limited to 1 MiB; each result
// content value is valid JSON limited to 256 KiB and 32 nesting levels.
//
// `content` accepts any JSON value and the stored transcript retains it
// verbatim. Before a result reaches the model, a string or an array of
// content blocks passes through unchanged; any other value is
// serialized to its compact JSON text and sent as a string, so the
// model sees the same bytes a host that pre-stringifies would send.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
func (c *Client) SubmitHostToolResults(ctx context.Context, invocationID InvocationID, body SubmitHostToolResultsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHostToolResultsRequest(c.Server, invocationID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListMCPToolsWithBody Discover and project one remote MCP server's tools
//
// Opens one short-lived streamable-HTTP MCP session through guarded
// public-only egress, drains tools/list pagination, applies the same
// allowlist and projection rules used by Invocation execution, and then
// closes the session. Supplied headers are used only for this request,
// are never logged or returned, and are not persisted.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
func (c *Client) ListMCPToolsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListMCPToolsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListMCPTools Discover and project one remote MCP server's tools
//
// Opens one short-lived streamable-HTTP MCP session through guarded
// public-only egress, drains tools/list pagination, applies the same
// allowlist and projection rules used by Invocation execution, and then
// closes the session. Supplied headers are used only for this request,
// are never logged or returned, and are not persisted.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
func (c *Client) ListMCPTools(ctx context.Context, body ListMCPToolsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListMCPToolsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListModels List nvoken's curated model catalog
//
// Returns the complete bounded set of text-generation models nvoken
// intentionally advertises. Catalog membership means nvoken maintains
// metadata for the exact provider/model selection. It does not prove that
// the caller's provider account, region, or selected provider key can access
// the model. Ordering is deterministic but has no semantic meaning.
//
// Corresponds with GET /v1/models (the `ListModels` operationId).
func (c *Client) ListModels(ctx context.Context, params *ListModelsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListModelsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetModel Inspect one exact provider/model selection
//
// Returns a descriptor for any valid installed provider and exact model
// ID, including IDs outside the curated catalog. `cataloged` distinguishes
// maintained metadata from tolerant inspection. Pricing is the standard
// local USD estimate used by nvoken's estimated-cost guardrail, not
// provider billing or proof of account access.
//
// Corresponds with GET /v1/models/{provider}/{model_id} (the `GetModel` operationId).
func (c *Client) GetModel(ctx context.Context, provider ModelProvider, modelID string, params *GetModelParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetModelRequest(c.Server, provider, modelID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListProviderKeys List reusable provider key metadata
//
// Viewer, Runtime, and Operator profiles may read metadata within their exact scope; secret material is never readable.
//
// Corresponds with GET /v1/provider-keys (the `ListProviderKeys` operationId).
func (c *Client) ListProviderKeys(ctx context.Context, params *ListProviderKeysParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListProviderKeysRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateProviderKeyWithBody Create one reusable provider key
//
// Creates encrypted app or tenant BYOK. App scope requires an
// unconstrained Operator credential inside the app; the credential is
// shared by every tenant of that app and by no other app. A
// tenant-constrained Runtime credential may manage only its exact tenant
// partition. The response contains metadata only.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
func (c *Client) CreateProviderKeyWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateProviderKeyRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateProviderKey Create one reusable provider key
//
// Creates encrypted app or tenant BYOK. App scope requires an
// unconstrained Operator credential inside the app; the credential is
// shared by every tenant of that app and by no other app. A
// tenant-constrained Runtime credential may manage only its exact tenant
// partition. The response contains metadata only.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
func (c *Client) CreateProviderKey(ctx context.Context, body CreateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateProviderKeyRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RevokeProviderKey Revoke a provider key and destroy live encrypted versions
//
// Corresponds with DELETE /v1/provider-keys/{provider_key_id} (the `RevokeProviderKey` operationId).
func (c *Client) RevokeProviderKey(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRevokeProviderKeyRequest(c.Server, providerKeyID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetProviderKey Read provider key metadata
//
// Viewer, Runtime, and Operator profiles may read secret-free metadata within their exact scope.
//
// Corresponds with GET /v1/provider-keys/{provider_key_id} (the `GetProviderKey` operationId).
func (c *Client) GetProviderKey(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetProviderKeyRequest(c.Server, providerKeyID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RotateProviderKeyWithBody Rotate a provider key
//
// Replaces the active version, optionally preserving a bounded overlap.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
func (c *Client) RotateProviderKeyWithBody(ctx context.Context, providerKeyID ProviderKeyID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRotateProviderKeyRequestWithBody(c.Server, providerKeyID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RotateProviderKey Rotate a provider key
//
// Replaces the active version, optionally preserving a bounded overlap.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
func (c *Client) RotateProviderKey(ctx context.Context, providerKeyID ProviderKeyID, body RotateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRotateProviderKeyRequest(c.Server, providerKeyID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetProviderKeyUsage Read the token usage rollup for one provider key
//
// Aggregates durable checkpoint evidence across every Invocation that
// bound the provider key: how many Invocations used it, when a model call
// last used it, and the summed model usage. Derived at read time from the
// transcript record, never from a separately maintained counter.
// Authorization matches reading the provider key itself.
//
// Corresponds with GET /v1/provider-keys/{provider_key_id}/usage (the `GetProviderKeyUsage` operationId).
func (c *Client) GetProviderKeyUsage(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetProviderKeyUsageRequest(c.Server, providerKeyID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListSessions List authoritative Sessions
//
// Returns newest-first Session identity and current nonterminal
// Invocation state. Exact filters combine with AND. Tenant filtering and
// cursor binding follow the Invocation-list rules. `agent_id` and
// `agent_key` are mutually exclusive and normalize to the same cursor
// filter.
//
// Corresponds with GET /v1/sessions (the `ListSessions` operationId).
func (c *Client) ListSessions(ctx context.Context, params *ListSessionsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListSessionsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateSessionWithBody Create or seed a Session without admitting an Invocation
//
// Creates a Session with zero Invocations and optional host-asserted
// starting history. Every body field is optional. An omitted `agent_key` leaves the
// Session unbound: `agent_id` is null until the first admitted
// Invocation binds it, and the binding is immutable once set either way;
// `seed_messages` therefore requires `agent_key`. A `session_key`
// requires an `agent_key`, because key uniqueness
// and lookup are scoped per (tenant partition, Agent); a keyed create
// is an upsert that accepts equal supplied Session options and returns
// `session_options_conflict` when a supplied value differs.
// Without a `session_key`, every call creates a fresh Session.
// Precedence for the tenant partition is credential constraint,
// explicit `tenant_key`, then the default partition.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
func (c *Client) CreateSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateSessionRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateSession Create or seed a Session without admitting an Invocation
//
// Creates a Session with zero Invocations and optional host-asserted
// starting history. Every body field is optional. An omitted `agent_key` leaves the
// Session unbound: `agent_id` is null until the first admitted
// Invocation binds it, and the binding is immutable once set either way;
// `seed_messages` therefore requires `agent_key`. A `session_key`
// requires an `agent_key`, because key uniqueness
// and lookup are scoped per (tenant partition, Agent); a keyed create
// is an upsert that accepts equal supplied Session options and returns
// `session_options_conflict` when a supplied value differs.
// Without a `session_key`, every call creates a fresh Session.
// Precedence for the tenant partition is credential constraint,
// explicit `tenant_key`, then the default partition.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
func (c *Client) CreateSession(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateSessionRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteSession Erase a Session and everything under it
//
// Removes the Session, its Invocations, transcript messages, checkpoints,
// tool calls, provider artifacts, compactions, provider-key and MCP
// bindings, and undelivered webhooks. The erasure is immediate and
// irreversible; a subsequent read is `not_found`.
//
// An Invocation still running is stopped. No cancellation is recorded,
// because there is nothing left to record it against — the Invocation is
// removed rather than settled, and no `invocation.settled` webhook
// is emitted for it. A host that needs a settled record should cancel and
// observe the terminal state before deleting.
//
// Erasure is scoped like every other Session operation: an unknown or
// out-of-scope `session_id` returns `not_found`, so a retry after a lost
// response can treat `404` as already-done. The `delete_session`
// operation belongs to the Runtime and Operator profiles; a Viewer
// credential cannot erase a transcript.
//
// **This is not account deletion by itself.** nvoken keeps no account
// tombstone, so a host honouring a deletion request must first stop
// admitting work for that tenant, then page `GET /v1/sessions` and delete
// until the list is empty. Otherwise a concurrent request creates a new
// Session behind the sweep.
//
// Two consequences worth planning for. Usage reporting shrinks
// retroactively: `GET /v1/usage/daily` is computed at read time from
// Invocation evidence that erasure removes, which is why the endpoint is
// documented as operational visibility rather than a billing ledger — a
// billing host records usage at settlement, keyed by Invocation id.
// And the erased Invocations' idempotency keys become free for reuse,
// consistent with the documented guarantee that deduplication holds while
// the original Invocation is retained.
//
// Corresponds with DELETE /v1/sessions/{session_id} (the `DeleteSession` operationId).
func (c *Client) DeleteSession(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteSessionRequest(c.Server, sessionID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSession Read authoritative Session identity and current state
//
// An installation-wide credential may resolve a Session in any tenant
// partition. A tenant-constrained credential resolves only Sessions
// in its partition. Missing, incompatible, and undisclosable resources use
// `not_found`; a credential denied the read operation itself receives
// `forbidden`.
//
// Corresponds with GET /v1/sessions/{session_id} (the `GetSession` operationId).
func (c *Client) GetSession(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSessionRequest(c.Server, sessionID)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateSessionWithBody Merge host metadata into a Session
//
// Merges a metadata patch: a present key replaces its value, an explicit
// `null` deletes that key, and a key the patch does not mention survives.
//
// Merge rather than replace, because independent writers share this map —
// a conversation UI writing a title, correlation tooling writing a trace
// id — and a full replacement would make each silently discard the
// other's keys. The merge happens under the Session lock, so two
// concurrent patches compose instead of one overwriting the other's read.
//
// `"metadata": null` is refused rather than guessed at: it could mean
// "clear everything" or "leave it alone", and either reading is
// destructive or silent. Delete keys one at a time.
//
// Bounds apply to the merged result, not to the patch, so a patch that
// deletes as many keys as it adds is not refused for a count it never
// produces. Requires the `update_session` operation.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
func (c *Client) UpdateSessionWithBody(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateSessionRequestWithBody(c.Server, sessionID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateSession Merge host metadata into a Session
//
// Merges a metadata patch: a present key replaces its value, an explicit
// `null` deletes that key, and a key the patch does not mention survives.
//
// Merge rather than replace, because independent writers share this map —
// a conversation UI writing a title, correlation tooling writing a trace
// id — and a full replacement would make each silently discard the
// other's keys. The merge happens under the Session lock, so two
// concurrent patches compose instead of one overwriting the other's read.
//
// `"metadata": null` is refused rather than guessed at: it could mean
// "clear everything" or "leave it alone", and either reading is
// destructive or silent. Delete keys one at a time.
//
// Bounds apply to the merged result, not to the patch, so a patch that
// deletes as many keys as it adds is not refused for a count it never
// produces. Requires the `update_session` operation.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
func (c *Client) UpdateSession(ctx context.Context, sessionID SessionID, body UpdateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateSessionRequest(c.Server, sessionID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListSessionCompactions Page through immutable Session compaction records
//
// Returns newest-first diagnostic records for every attempted compaction
// pass. `applied` records include the private summary projection and its
// model usage. `fell_through` records identify the failure class and may
// include usage when the provider returned trustworthy evidence. Only
// applied records change future model context; neither kind changes the
// canonical Session transcript.
//
// The summary is derived from the caller's own transcript and therefore
// uses the same authorization and Session scoping as transcript reads.
// The opaque cursor is bound to the authenticated app and Session.
//
// Corresponds with GET /v1/sessions/{session_id}/compactions (the `ListSessionCompactions` operationId).
func (c *Client) ListSessionCompactions(ctx context.Context, sessionID SessionID, params *ListSessionCompactionsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListSessionCompactionsRequest(c.Server, sessionID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ForkSessionWithBody Copy a Session prefix into a new Session
//
// Creates a new Session in the source Session's tenant and Agent scope,
// copying every canonical message through `from_message` inclusively.
// The source is untouched. The child stores durable Session and message
// lineage, but copied messages no longer belong to the source
// Invocations. Their `origin`, per-turn `user_key`, and resolved message
// phase are preserved.
//
// Usage and compaction summaries are not copied. Child usage starts at
// zero and the child starts uncompacted. Retention and metadata come only
// from `session_options` on this request; no Session option is inherited.
// A `session_key` has the same tenant/Agent-scoped upsert behavior as
// Session creation.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
func (c *Client) ForkSessionWithBody(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewForkSessionRequestWithBody(c.Server, sessionID, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ForkSession Copy a Session prefix into a new Session
//
// Creates a new Session in the source Session's tenant and Agent scope,
// copying every canonical message through `from_message` inclusively.
// The source is untouched. The child stores durable Session and message
// lineage, but copied messages no longer belong to the source
// Invocations. Their `origin`, per-turn `user_key`, and resolved message
// phase are preserved.
//
// Usage and compaction summaries are not copied. Child usage starts at
// zero and the child starts uncompacted. Retention and metadata come only
// from `session_options` on this request; no Session option is inherited.
// A `session_key` has the same tenant/Agent-scoped upsert behavior as
// Session creation.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
func (c *Client) ForkSession(ctx context.Context, sessionID SessionID, body ForkSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewForkSessionRequest(c.Server, sessionID, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListSessionMessages Page through the canonical Session transcript
//
// Returns persisted SessionMessage rows in ascending sequence order.
// The opaque forward cursor is bound to the authenticated caller and
// Session. This history endpoint contains no lifecycle or live-preview
// copies.
//
// Corresponds with GET /v1/sessions/{session_id}/messages (the `ListSessionMessages` operationId).
func (c *Client) ListSessionMessages(ctx context.Context, sessionID SessionID, params *ListSessionMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListSessionMessagesRequest(c.Server, sessionID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSessionTranscript Drain a fixed-cut incremental transcript snapshot
//
// Projects canonical messages and append-only Invocation lifecycle
// changes. Supply a prior `resume_cursor` as `cursor` to drain newer
// durable state. Continue a multi-page fixed cut with `page_token` until
// `has_more` is false. Each page contains one phase: all message pages are
// delivered before lifecycle-change pages, so terminal state cannot
// precede the transcript rows committed with it. Omitting both positions
// starts at the beginning of the retained Session.
//
// Corresponds with GET /v1/sessions/{session_id}/transcript (the `GetSessionTranscript` operationId).
func (c *Client) GetSessionTranscript(ctx context.Context, sessionID SessionID, params *GetSessionTranscriptParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSessionTranscriptRequest(c.Server, sessionID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// StreamSessionTranscript Replay and tail one Session transcript over SSE
//
// Opens a resumable Server-Sent Events projection over the same fixed-cut
// transcript read model as the JSON endpoint. With deltas enabled, the
// server subscribes to live fan-out before its first Postgres drain. In
// either mode it re-drains Postgres on a bounded poll and closes after
// authoritative terminal reconciliation or deliberate rotation.
// Disconnecting never cancels the Invocation.
//
// Every nonempty `transcript.update` frame carries
// `id: <resume_cursor>`; that opaque ID is the only replay position
// clients persist. `output_text.delta`, `thinking.delta`,
// `stream.resync`, and `stream.end` never carry an `id`. Deltas are
// ephemeral and may be lost; after `stream.resync`, discard provisional
// output and wait for canonical messages. `stream.end` reason `terminal`
// means the final Postgres drain observed no nonterminal Invocation.
// Reason `rotate` means reconnect with the last durable ID. An abnormal
// close has no terminal meaning. `deltas=false` skips preview fan-out
// without changing replay, polling, cursor, or terminal semantics.
//
// The explicit `cursor` query parameter takes precedence over
// `Last-Event-ID`. Bearer authentication requires an SSE-capable HTTP
// client that can set the `Authorization` header; the browser EventSource
// constructor alone cannot do so. The server emits `retry: 1000` as its
// default reconnect delay.
//
// Corresponds with GET /v1/sessions/{session_id}/transcript/stream (the `StreamSessionTranscript` operationId).
func (c *Client) StreamSessionTranscript(ctx context.Context, sessionID SessionID, params *StreamSessionTranscriptParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewStreamSessionTranscriptRequest(c.Server, sessionID, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetDailyUsage Read daily usage rollups
//
// Aggregates durable model-call evidence — invocation checkpoints plus
// session compaction calls — into per-day buckets keyed by app,
// provider, and model, summing token counts and estimated cost. Derived
// at read time from the transcript record, never from a separately
// maintained counter.
//
// An app-bound credential reads its own app. An app-less presentation
// spans every registered app; for a console issuer token that requires
// the admin claim.
//
// Corresponds with GET /v1/usage/daily (the `GetDailyUsage` operationId).
func (c *Client) GetDailyUsage(ctx context.Context, params *GetDailyUsageParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetDailyUsageRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewListAgentsRequest constructs an http.Request for the ListAgents method
func NewListAgentsRequest(server string, params *ListAgentsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/agents")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AgentKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "agent_key", *params.AgentKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAgentRequest constructs an http.Request for the GetAgent method
func NewGetAgentRequest(server string, agentID AgentID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "agent_id", agentID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/agents/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAppsRequest constructs an http.Request for the ListApps method
func NewListAppsRequest(server string, params *ListAppsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/apps")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ExternalRef != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "external_ref", *params.ExternalRef, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRegisterAppRequest calls the generic RegisterApp builder with application/json body
func NewRegisterAppRequest(server string, body RegisterAppJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewRegisterAppRequestWithBody(server, "application/json", bodyReader)
}

// NewRegisterAppRequestWithBody constructs an http.Request for the RegisterApp method, with any body, and a specified content type
func NewRegisterAppRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/apps")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAppRequest constructs an http.Request for the GetApp method
func NewGetAppRequest(server string, appID string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "app_id", appID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/apps/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAppRequest calls the generic UpdateApp builder with application/json body
func NewUpdateAppRequest(server string, appID string, body UpdateAppJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAppRequestWithBody(server, appID, "application/json", bodyReader)
}

// NewUpdateAppRequestWithBody constructs an http.Request for the UpdateApp method, with any body, and a specified content type
func NewUpdateAppRequestWithBody(server string, appID string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "app_id", appID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/apps/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCreateBudgetRequest calls the generic CreateBudget builder with application/json body
func NewCreateBudgetRequest(server string, body CreateBudgetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateBudgetRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateBudgetRequestWithBody constructs an http.Request for the CreateBudget method, with any body, and a specified content type
func NewCreateBudgetRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/budgets")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteBudgetRequest constructs an http.Request for the DeleteBudget method
func NewDeleteBudgetRequest(server string, budgetID BudgetID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "budget_id", budgetID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/budgets/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBudgetRequest constructs an http.Request for the GetBudget method
func NewGetBudgetRequest(server string, budgetID BudgetID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "budget_id", budgetID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/budgets/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateBudgetRequest calls the generic UpdateBudget builder with application/json body
func NewUpdateBudgetRequest(server string, budgetID BudgetID, body UpdateBudgetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateBudgetRequestWithBody(server, budgetID, "application/json", bodyReader)
}

// NewUpdateBudgetRequestWithBody constructs an http.Request for the UpdateBudget method, with any body, and a specified content type
func NewUpdateBudgetRequestWithBody(server string, budgetID BudgetID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "budget_id", budgetID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/budgets/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListInvocationsRequest constructs an http.Request for the ListInvocations method
func NewListInvocationsRequest(server string, params *ListInvocationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.TenantKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "tenant_key", *params.TenantKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.DefaultTenant != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "default_tenant", *params.DefaultTenant, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.UserKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "user_key", *params.UserKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.SessionID != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "session_id", *params.SessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.AgentID != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "agent_id", *params.AgentID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.AgentKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "agent_key", *params.AgentKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "array", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateInvocationRequest calls the generic CreateInvocation builder with application/json body
func NewCreateInvocationRequest(server string, params *CreateInvocationParams, body CreateInvocationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateInvocationRequestWithBody(server, params, "application/json", bodyReader)
}

// NewCreateInvocationRequestWithBody constructs an http.Request for the CreateInvocation method, with any body, and a specified content type
func NewCreateInvocationRequestWithBody(server string, params *CreateInvocationParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.XAnthropicAPIKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Anthropic-Api-Key", *params.XAnthropicAPIKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Anthropic-Api-Key", headerParam0)
		}

		if params.XOpenaiAPIKey != nil {
			var headerParam1 string

			headerParam1, err = runtime.StyleParamWithOptions("simple", false, "X-Openai-Api-Key", *params.XOpenaiAPIKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Openai-Api-Key", headerParam1)
		}

		if params.XGeminiAPIKey != nil {
			var headerParam2 string

			headerParam2, err = runtime.StyleParamWithOptions("simple", false, "X-Gemini-Api-Key", *params.XGeminiAPIKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Gemini-Api-Key", headerParam2)
		}

		if params.XXaiAPIKey != nil {
			var headerParam3 string

			headerParam3, err = runtime.StyleParamWithOptions("simple", false, "X-Xai-Api-Key", *params.XXaiAPIKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Xai-Api-Key", headerParam3)
		}

	}

	return req, nil
}

// NewGetInvocationRequest constructs an http.Request for the GetInvocation method
func NewGetInvocationRequest(server string, invocationID InvocationID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCancelInvocationRequest constructs an http.Request for the CancelInvocation method
func NewCancelInvocationRequest(server string, invocationID InvocationID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/cancel", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewInterruptInvocationRequest constructs an http.Request for the InterruptInvocation method
func NewInterruptInvocationRequest(server string, invocationID InvocationID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/interrupt", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewNudgeInvocationRequest calls the generic NudgeInvocation builder with application/json body
func NewNudgeInvocationRequest(server string, invocationID InvocationID, body NudgeInvocationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewNudgeInvocationRequestWithBody(server, invocationID, "application/json", bodyReader)
}

// NewNudgeInvocationRequestWithBody constructs an http.Request for the NudgeInvocation method, with any body, and a specified content type
func NewNudgeInvocationRequestWithBody(server string, invocationID InvocationID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/nudge", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListPendingInputsRequest constructs an http.Request for the ListPendingInputs method
func NewListPendingInputsRequest(server string, invocationID InvocationID, params *ListPendingInputsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/pending-inputs", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCancelPendingInputRequest constructs an http.Request for the CancelPendingInput method
func NewCancelPendingInputRequest(server string, invocationID InvocationID, pendingInputID PendingInputID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "pending_input_id", pendingInputID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/pending-inputs/%s/cancel", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetInvocationResultRequest constructs an http.Request for the GetInvocationResult method
func NewGetInvocationResultRequest(server string, invocationID InvocationID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/result", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewResumeInvocationRequest calls the generic ResumeInvocation builder with application/json body
func NewResumeInvocationRequest(server string, invocationID InvocationID, body ResumeInvocationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewResumeInvocationRequestWithBody(server, invocationID, "application/json", bodyReader)
}

// NewResumeInvocationRequestWithBody constructs an http.Request for the ResumeInvocation method, with any body, and a specified content type
func NewResumeInvocationRequestWithBody(server string, invocationID InvocationID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/resume", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewStreamInvocationRequest constructs an http.Request for the StreamInvocation method
func NewStreamInvocationRequest(server string, invocationID InvocationID, params *StreamInvocationParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/stream", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Deltas != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "deltas", *params.Deltas, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.LastEventID != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Last-Event-ID", *params.LastEventID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Last-Event-ID", headerParam0)
		}

	}

	return req, nil
}

// NewListToolCallsRequest constructs an http.Request for the ListToolCalls method
func NewListToolCallsRequest(server string, invocationID InvocationID, params *ListToolCallsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/tool-calls", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSubmitHostToolResultsRequest calls the generic SubmitHostToolResults builder with application/json body
func NewSubmitHostToolResultsRequest(server string, invocationID InvocationID, body SubmitHostToolResultsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitHostToolResultsRequestWithBody(server, invocationID, "application/json", bodyReader)
}

// NewSubmitHostToolResultsRequestWithBody constructs an http.Request for the SubmitHostToolResults method, with any body, and a specified content type
func NewSubmitHostToolResultsRequestWithBody(server string, invocationID InvocationID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "invocation_id", invocationID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/invocations/%s/tool-results", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListMCPToolsRequest calls the generic ListMCPTools builder with application/json body
func NewListMCPToolsRequest(server string, body ListMCPToolsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewListMCPToolsRequestWithBody(server, "application/json", bodyReader)
}

// NewListMCPToolsRequestWithBody constructs an http.Request for the ListMCPTools method, with any body, and a specified content type
func NewListMCPToolsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/mcp/list-tools")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListModelsRequest constructs an http.Request for the ListModels method
func NewListModelsRequest(server string, params *ListModelsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/models")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Provider != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "provider", *params.Provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeDeprecated != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_deprecated", *params.IncludeDeprecated, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.IfNoneMatch != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "If-None-Match", *params.IfNoneMatch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("If-None-Match", headerParam0)
		}

	}

	return req, nil
}

// NewGetModelRequest constructs an http.Request for the GetModel method
func NewGetModelRequest(server string, provider ModelProvider, modelID string, params *GetModelParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider", provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "model_id", modelID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/models/%s/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.IfNoneMatch != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "If-None-Match", *params.IfNoneMatch, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("If-None-Match", headerParam0)
		}

	}

	return req, nil
}

// NewListProviderKeysRequest constructs an http.Request for the ListProviderKeys method
func NewListProviderKeysRequest(server string, params *ListProviderKeysParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Provider != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "provider", *params.Provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.TenantKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "tenant_key", *params.TenantKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateProviderKeyRequest calls the generic CreateProviderKey builder with application/json body
func NewCreateProviderKeyRequest(server string, body CreateProviderKeyJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateProviderKeyRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateProviderKeyRequestWithBody constructs an http.Request for the CreateProviderKey method, with any body, and a specified content type
func NewCreateProviderKeyRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewRevokeProviderKeyRequest constructs an http.Request for the RevokeProviderKey method
func NewRevokeProviderKeyRequest(server string, providerKeyID ProviderKeyID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider_key_id", providerKeyID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetProviderKeyRequest constructs an http.Request for the GetProviderKey method
func NewGetProviderKeyRequest(server string, providerKeyID ProviderKeyID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider_key_id", providerKeyID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRotateProviderKeyRequest calls the generic RotateProviderKey builder with application/json body
func NewRotateProviderKeyRequest(server string, providerKeyID ProviderKeyID, body RotateProviderKeyJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewRotateProviderKeyRequestWithBody(server, providerKeyID, "application/json", bodyReader)
}

// NewRotateProviderKeyRequestWithBody constructs an http.Request for the RotateProviderKey method, with any body, and a specified content type
func NewRotateProviderKeyRequestWithBody(server string, providerKeyID ProviderKeyID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider_key_id", providerKeyID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys/%s/rotate", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetProviderKeyUsageRequest constructs an http.Request for the GetProviderKeyUsage method
func NewGetProviderKeyUsageRequest(server string, providerKeyID ProviderKeyID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider_key_id", providerKeyID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/provider-keys/%s/usage", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListSessionsRequest constructs an http.Request for the ListSessions method
func NewListSessionsRequest(server string, params *ListSessionsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.TenantKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "tenant_key", *params.TenantKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.DefaultTenant != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "default_tenant", *params.DefaultTenant, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.UserKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "user_key", *params.UserKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.AgentID != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "agent_id", *params.AgentID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.AgentKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "agent_key", *params.AgentKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.SessionKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "session_key", *params.SessionKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateSessionRequest calls the generic CreateSession builder with application/json body
func NewCreateSessionRequest(server string, body CreateSessionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateSessionRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateSessionRequestWithBody constructs an http.Request for the CreateSession method, with any body, and a specified content type
func NewCreateSessionRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteSessionRequest constructs an http.Request for the DeleteSession method
func NewDeleteSessionRequest(server string, sessionID SessionID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetSessionRequest constructs an http.Request for the GetSession method
func NewGetSessionRequest(server string, sessionID SessionID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateSessionRequest calls the generic UpdateSession builder with application/json body
func NewUpdateSessionRequest(server string, sessionID SessionID, body UpdateSessionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateSessionRequestWithBody(server, sessionID, "application/json", bodyReader)
}

// NewUpdateSessionRequestWithBody constructs an http.Request for the UpdateSession method, with any body, and a specified content type
func NewUpdateSessionRequestWithBody(server string, sessionID SessionID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListSessionCompactionsRequest constructs an http.Request for the ListSessionCompactions method
func NewListSessionCompactionsRequest(server string, sessionID SessionID, params *ListSessionCompactionsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s/compactions", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewForkSessionRequest calls the generic ForkSession builder with application/json body
func NewForkSessionRequest(server string, sessionID SessionID, body ForkSessionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewForkSessionRequestWithBody(server, sessionID, "application/json", bodyReader)
}

// NewForkSessionRequestWithBody constructs an http.Request for the ForkSession method, with any body, and a specified content type
func NewForkSessionRequestWithBody(server string, sessionID SessionID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s/fork", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListSessionMessagesRequest constructs an http.Request for the ListSessionMessages method
func NewListSessionMessagesRequest(server string, sessionID SessionID, params *ListSessionMessagesParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s/messages", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetSessionTranscriptRequest constructs an http.Request for the GetSessionTranscript method
func NewGetSessionTranscriptRequest(server string, sessionID SessionID, params *GetSessionTranscriptParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s/transcript", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.PageToken != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "page_token", *params.PageToken, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewStreamSessionTranscriptRequest constructs an http.Request for the StreamSessionTranscript method
func NewStreamSessionTranscriptRequest(server string, sessionID SessionID, params *StreamSessionTranscriptParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "session_id", sessionID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/sessions/%s/transcript/stream", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Deltas != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "deltas", *params.Deltas, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.LastEventID != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Last-Event-ID", *params.LastEventID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Last-Event-ID", headerParam0)
		}

	}

	return req, nil
}

// NewGetDailyUsageRequest constructs an http.Request for the GetDailyUsage method
func NewGetDailyUsageRequest(server string, params *GetDailyUsageParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/usage/daily")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "start_date", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "end_date", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.TenantKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "tenant_key", *params.TenantKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.UserKey != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "user_key", *params.UserKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.GroupBy != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "group_by", *params.GroupBy, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// ListAgentsWithResponse List Agent identity anchors
	//
	// Returns newest-first identity anchors scoped to the caller's app. An
	// Agent stores only its nvoken ID, the host-owned `agent_key`, and
	// creation time; instructions, models, tools, and provider keys still
	// travel on each Invocation. An exact `agent_key` filter returns zero or
	// one item.
	//
	// Unconstrained credentials see the anchors referenced by a Session in
	// one of their app's tenants. Tenant-constrained credentials see only
	// anchors referenced by a Session in their effective partition.
	// Session-constrained credentials see only that Session's anchor. The
	// opaque cursor is bound to the authenticated caller, credential
	// constraint, and exact key filter.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/agents (the `ListAgents` operationId).
	ListAgentsWithResponse(ctx context.Context, params *ListAgentsParams, reqEditors ...RequestEditorFn) (*ListAgentsHTTPResponse, error)

	// GetAgentWithResponse Read one Agent identity anchor
	//
	// Reads identity without admitting work. Out-of-scope and undisclosable
	// constrained resources use `not_found`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/agents/{agent_id} (the `GetAgent` operationId).
	GetAgentWithResponse(ctx context.Context, agentID AgentID, reqEditors ...RequestEditorFn) (*GetAgentHTTPResponse, error)

	// ListAppsWithResponse List registered apps
	//
	// Returns the apps this credential can see. A credential associated with
	// an app sees only that app; a credential with no app association sees
	// every registered app. An exact `external_ref` filter narrows either
	// view to apps owned by that reference.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/apps (the `ListApps` operationId).
	ListAppsWithResponse(ctx context.Context, params *ListAppsParams, reqEditors ...RequestEditorFn) (*ListAppsHTTPResponse, error)

	// RegisterAppWithBodyWithResponse Register an app
	//
	// Registers one host application and creates its default tenant,
	// returning the generated `app_id`.
	//
	// Registration is installation management rather than work inside an app,
	// so it requires a credential that is *not* associated with an app; a
	// credential bound to one app cannot mint siblings for itself. Names
	// identify apps and are unique, so re-registering an existing name is
	// rejected rather than returning the existing app.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
	RegisterAppWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RegisterAppHTTPResponse, error)

	// RegisterAppWithResponse Register an app
	//
	// Registers one host application and creates its default tenant,
	// returning the generated `app_id`.
	//
	// Registration is installation management rather than work inside an app,
	// so it requires a credential that is *not* associated with an app; a
	// credential bound to one app cannot mint siblings for itself. Names
	// identify apps and are unique, so re-registering an existing name is
	// rejected rather than returning the existing app.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
	RegisterAppWithResponse(ctx context.Context, body RegisterAppJSONRequestBody, reqEditors ...RequestEditorFn) (*RegisterAppHTTPResponse, error)

	// GetAppWithResponse Get one app
	//
	// Returns one registered app. A credential associated with a different
	// app receives `404`, so an app identifier cannot be probed across the
	// isolation boundary.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/apps/{app_id} (the `GetApp` operationId).
	GetAppWithResponse(ctx context.Context, appID string, reqEditors ...RequestEditorFn) (*GetAppHTTPResponse, error)

	// UpdateAppWithBodyWithResponse Update an app
	//
	// Updates an app's mutable presentation fields. The unique `name` and the
	// ownership-grounding `external_ref` cannot be changed. A credential
	// associated with a different app receives `404`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
	UpdateAppWithBodyWithResponse(ctx context.Context, appID string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAppHTTPResponse, error)

	// UpdateAppWithResponse Update an app
	//
	// Updates an app's mutable presentation fields. The unique `name` and the
	// ownership-grounding `external_ref` cannot be changed. A credential
	// associated with a different app receives `404`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
	UpdateAppWithResponse(ctx context.Context, appID string, body UpdateAppJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAppHTTPResponse, error)

	// CreateBudgetWithBodyWithResponse Create one fixed usage budget window
	//
	// Creates an estimated-list-price guardrail inside the authenticated App.
	// Windows for the same scope identity cannot overlap. The idempotency key
	// makes an identical retry return the first Budget.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
	CreateBudgetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBudgetHTTPResponse, error)

	// CreateBudgetWithResponse Create one fixed usage budget window
	//
	// Creates an estimated-list-price guardrail inside the authenticated App.
	// Windows for the same scope identity cannot overlap. The idempotency key
	// makes an identical retry return the first Budget.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
	CreateBudgetWithResponse(ctx context.Context, body CreateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBudgetHTTPResponse, error)

	// DeleteBudgetWithResponse Remove a Budget guardrail
	//
	// Removes enforcement without deleting its accounting history.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/budgets/{budget_id} (the `DeleteBudget` operationId).
	DeleteBudgetWithResponse(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*DeleteBudgetHTTPResponse, error)

	// GetBudgetWithResponse Read one usage Budget
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/budgets/{budget_id} (the `GetBudget` operationId).
	GetBudgetWithResponse(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*GetBudgetHTTPResponse, error)

	// UpdateBudgetWithBodyWithResponse Raise or lower a Budget cap
	//
	// Replaces the cap. It cannot be lower than settled plus reserved cost.
	// Raising it reevaluates paused work in bounded batches.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
	UpdateBudgetWithBodyWithResponse(ctx context.Context, budgetID BudgetID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBudgetHTTPResponse, error)

	// UpdateBudgetWithResponse Raise or lower a Budget cap
	//
	// Replaces the cap. It cannot be lower than settled plus reserved cost.
	// Raising it reevaluates paused work in bounded batches.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
	UpdateBudgetWithResponse(ctx context.Context, budgetID BudgetID, body UpdateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBudgetHTTPResponse, error)

	// ListInvocationsWithResponse List authoritative Invocations
	//
	// Returns newest-first durable Invocation state. Exact filters combine
	// with AND. An installation-wide caller may list all tenant partitions, one
	// named partition with `tenant_key`, or the default partition with
	// `default_tenant=true`. A tenant-constrained credential is always scoped
	// to its partition. The opaque cursor is bound to the normalized filter
	// set and credential tenant scope. `agent_id` and `agent_key` are
	// mutually exclusive; both normalize to the resolved Agent ID for cursor
	// binding, so an equivalent cursor may resume under either spelling.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations (the `ListInvocations` operationId).
	ListInvocationsWithResponse(ctx context.Context, params *ListInvocationsParams, reqEditors ...RequestEditorFn) (*ListInvocationsHTTPResponse, error)

	// CreateInvocationWithBodyWithResponse Durably admit one background agent turn
	//
	// Resolves or creates the installation-wide Agent identity anchor,
	// resolves or creates a Session, resolves the inline or referenced
	// definition, appends exactly one caller-input message, and creates one
	// queued Invocation in a single transaction. The response is sent only
	// after that transaction commits. The request handler never executes the
	// model.
	//
	// `session_id` and `session_key` are mutually exclusive. A Session ID must
	// belong to the named Agent, or be a Session created without one — the
	// first admitted Invocation binds the Agent permanently. An installation-wide
	// credential may omit `tenant_key` and use the Session's stored partition.
	// A tenant-constrained credential cannot cross its partition; an explicit
	// mismatch is rejected with `403 forbidden` before resource lookup.
	//
	// `idempotency_key` is scoped to the effective
	// tenant partition, and `agent_key`. A same-key replay is checked before
	// the Session's one-nonterminal-Invocation rule. It returns the original
	// records without appending input, even when the Invocation is terminal.
	// Material equality covers the Session selector kind and value, the
	// resolved definition, and input. A `definition_id` and its byte-identical
	// inline definition compare equal. Requested limits are compared before
	// default resolution, so an explicit default differs from omission. JSON
	// object member order is ignored, array order is significant, and strings
	// are not rewritten. A changed material field returns
	// `idempotency_conflict`.
	//
	// `if_active` defaults to `reject`, preserving
	// `session_invocation_active`. Explicit `supersede` requires both create
	// and cancel authority. While holding the Session lock, it durably
	// cancels any nonterminal Invocation and admits this replacement in the
	// same transaction. With no active work it admits normally. Equal replay
	// returns the original work before supersession and never cancels newer
	// active work.
	//
	// `interrupt` requires the same authority and keeps the same lock
	// ordering, but stops the active Invocation gracefully instead of
	// cancelling it: work already at a seam settles and the replacement is
	// admitted in the same transaction, while work under a live lease has
	// the interrupt recorded and this request waits for it to settle. If it
	// has not settled within the wait, the response is
	// `session_invocation_active` with `details.interrupt_requested = true`
	// and the request may be reissued.
	//
	// A cataloged model continues to admit while deprecated. On and after
	// its `retires_at` date, a new admission is refused with `422
	// model_retired`; `details` carries the refused `model`, `retires_at`,
	// the exact `replacement` provider/id pair, and the request `path`.
	// Equal idempotent replay still returns work admitted before retirement.
	//
	// A text-only request body is limited to 1 MiB. A body carrying image or
	// document blocks is limited to 24 MiB, within which decoded media is
	// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
	// 5 MiB per image, and 16 MiB per document. Requests over any of these
	// limits are rejected before admission. URL media is fetched after an
	// idempotent replay check and before any row is admitted. The guarded
	// fetch accepts public HTTPS only, enforces the item limit while reading,
	// and sniffs the returned bytes. nvoken stores those bytes and does not
	// fetch the URL again.
	//
	// The supported streaming pattern is admit-then-stream: admit with a
	// plain JSON POST, then follow the Invocation over
	// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
	// connection without re-admitting and does not depend on front-end
	// streaming behavior. `Accept: text/event-stream` on this request is a
	// deployment-dependent convenience, not a first-class mode: the
	// committed admission is the first `invocation.accepted` frame and the
	// connection tails that Invocation through `invocation.result`, but
	// delivery requires the deployment front end to stream a non-`200`
	// POST response unbuffered. Some managed front ends (including Cloud
	// Run's) buffer it until the Invocation settles, which makes a
	// host-tool turn's `waiting` state invisible on this connection.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
	CreateInvocationWithBodyWithResponse(ctx context.Context, params *CreateInvocationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateInvocationHTTPResponse, error)

	// CreateInvocationWithResponse Durably admit one background agent turn
	//
	// Resolves or creates the installation-wide Agent identity anchor,
	// resolves or creates a Session, resolves the inline or referenced
	// definition, appends exactly one caller-input message, and creates one
	// queued Invocation in a single transaction. The response is sent only
	// after that transaction commits. The request handler never executes the
	// model.
	//
	// `session_id` and `session_key` are mutually exclusive. A Session ID must
	// belong to the named Agent, or be a Session created without one — the
	// first admitted Invocation binds the Agent permanently. An installation-wide
	// credential may omit `tenant_key` and use the Session's stored partition.
	// A tenant-constrained credential cannot cross its partition; an explicit
	// mismatch is rejected with `403 forbidden` before resource lookup.
	//
	// `idempotency_key` is scoped to the effective
	// tenant partition, and `agent_key`. A same-key replay is checked before
	// the Session's one-nonterminal-Invocation rule. It returns the original
	// records without appending input, even when the Invocation is terminal.
	// Material equality covers the Session selector kind and value, the
	// resolved definition, and input. A `definition_id` and its byte-identical
	// inline definition compare equal. Requested limits are compared before
	// default resolution, so an explicit default differs from omission. JSON
	// object member order is ignored, array order is significant, and strings
	// are not rewritten. A changed material field returns
	// `idempotency_conflict`.
	//
	// `if_active` defaults to `reject`, preserving
	// `session_invocation_active`. Explicit `supersede` requires both create
	// and cancel authority. While holding the Session lock, it durably
	// cancels any nonterminal Invocation and admits this replacement in the
	// same transaction. With no active work it admits normally. Equal replay
	// returns the original work before supersession and never cancels newer
	// active work.
	//
	// `interrupt` requires the same authority and keeps the same lock
	// ordering, but stops the active Invocation gracefully instead of
	// cancelling it: work already at a seam settles and the replacement is
	// admitted in the same transaction, while work under a live lease has
	// the interrupt recorded and this request waits for it to settle. If it
	// has not settled within the wait, the response is
	// `session_invocation_active` with `details.interrupt_requested = true`
	// and the request may be reissued.
	//
	// A cataloged model continues to admit while deprecated. On and after
	// its `retires_at` date, a new admission is refused with `422
	// model_retired`; `details` carries the refused `model`, `retires_at`,
	// the exact `replacement` provider/id pair, and the request `path`.
	// Equal idempotent replay still returns work admitted before retirement.
	//
	// A text-only request body is limited to 1 MiB. A body carrying image or
	// document blocks is limited to 24 MiB, within which decoded media is
	// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
	// 5 MiB per image, and 16 MiB per document. Requests over any of these
	// limits are rejected before admission. URL media is fetched after an
	// idempotent replay check and before any row is admitted. The guarded
	// fetch accepts public HTTPS only, enforces the item limit while reading,
	// and sniffs the returned bytes. nvoken stores those bytes and does not
	// fetch the URL again.
	//
	// The supported streaming pattern is admit-then-stream: admit with a
	// plain JSON POST, then follow the Invocation over
	// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
	// connection without re-admitting and does not depend on front-end
	// streaming behavior. `Accept: text/event-stream` on this request is a
	// deployment-dependent convenience, not a first-class mode: the
	// committed admission is the first `invocation.accepted` frame and the
	// connection tails that Invocation through `invocation.result`, but
	// delivery requires the deployment front end to stream a non-`200`
	// POST response unbuffered. Some managed front ends (including Cloud
	// Run's) buffer it until the Invocation settles, which makes a
	// host-tool turn's `waiting` state invisible on this connection.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
	CreateInvocationWithResponse(ctx context.Context, params *CreateInvocationParams, body CreateInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateInvocationHTTPResponse, error)

	// GetInvocationWithResponse Read authoritative Invocation identity and state
	//
	// Returns the durable current state. Post-admission failures appear here.
	// A credential authenticated for Runtime but denied this read operation
	// receives `forbidden`.
	// A resource outside the caller's tenant constraint is
	// reported as `not_found` when disclosure would reveal its existence.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations/{invocation_id} (the `GetInvocation` operationId).
	GetInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*GetInvocationHTTPResponse, error)

	// CancelInvocationWithResponse Idempotently cancel an Invocation
	//
	// Atomically makes nonterminal work `cancelled`. Repeating the request,
	// or cancelling an Invocation that already completed or failed, returns
	// the unchanged authoritative terminal row. A successful response means
	// cancellation is durable; provider work stops cooperatively and may
	// have already incurred external cost. The request body must be empty.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/cancel (the `CancelInvocation` operationId).
	CancelInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*CancelInvocationHTTPResponse, error)

	// InterruptInvocationWithResponse Gracefully stop an Invocation and keep its work
	//
	// Asks the turn to stop at its next execution seam and settle
	// `completed` with `stop_reason = interrupted`, so its assistant and
	// tool messages stay in the next turn's generation context. That is the
	// whole difference from cancellation, which drops the turn's work from
	// every later turn.
	//
	// The request is durable and idempotent. Work already at a seam —
	// `queued`, `waiting`, or `running` with no live owner — settles before
	// this call returns, closing any pending tool calls with synthetic
	// results; a subsequently submitted tool result then gets the ordinary
	// terminal `409`. Work under a live lease records the request and
	// returns the Invocation unchanged, still `running`: its executor stops
	// at the next checkpoint boundary, at worst one provider call away.
	// Follow the Invocation stream or re-read it to observe settlement.
	//
	// Interrupting terminal work is a no-op that returns the unchanged
	// terminal row. A turn whose definition carries `structured_output` and that
	// never published a validated object fails
	// `structured_output_unsatisfied` instead of completing — the
	// obligation was not met. Usage and settlement evidence are complete:
	// the work was kept, so it is charged. The request body must be empty.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/interrupt (the `InterruptInvocation` operationId).
	InterruptInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*InterruptInvocationHTTPResponse, error)

	// NudgeInvocationWithBodyWithResponse Append steering to a running Invocation
	//
	// Stages caller guidance against a turn that is already running — "focus
	// on the marine segment" — without ending it and without discarding the
	// work being steered. This is the difference from
	// `if_active: supersede`, which rewinds: a superseded turn's assistant
	// and tool work product is dropped from the next generation, so steering
	// a long turn by supersession throws away exactly what is being steered.
	// Unlike Claude's `system.message`, a nudge is durable staged input
	// consumed at an execution seam, not an event injected immediately into
	// a managed session.
	//
	// A nudge is not an interrupt. The earliest injection point is the next
	// execution seam: the start of the next execution segment, which is a
	// host-tool turn's park boundary, or the stop seam where a turn that
	// considers itself finished re-enters its loop to answer the input in
	// place. An in-flight provider call or tool run is never aborted for it,
	// and an Invocation that has been interrupted is never given more to do —
	// the interrupt wins the seam and the staged input expires.
	//
	// A nudge is never implicitly minted and never implicitly converted.
	// `POST /v1/invocations` against a busy Session keeps its `if_active`
	// semantics; admission is never silently turned into a nudge, and a
	// nudge is never silently turned into an Invocation. nvoken holds no
	// agent configuration — the definition travels inline on every admission — so
	// there is nothing it could legitimately run converted input with.
	//
	// Input that the turn never takes is settled `expired` when the
	// Invocation settles, in the same transaction and under the same Session
	// lock this request takes. It never influences a later Invocation.
	// Re-sending missed direction as the next Invocation's input is the
	// host's call to make, and `GET .../pending-inputs` is where the miss is
	// visible.
	//
	// `content` is text: a string, or an array of text blocks. Image and
	// document blocks are accepted on an Invocation's own input but not
	// here, because the stop seam carries a continuation as text and staging
	// content one seam would silently drop is worse than refusing it.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
	NudgeInvocationWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*NudgeInvocationHTTPResponse, error)

	// NudgeInvocationWithResponse Append steering to a running Invocation
	//
	// Stages caller guidance against a turn that is already running — "focus
	// on the marine segment" — without ending it and without discarding the
	// work being steered. This is the difference from
	// `if_active: supersede`, which rewinds: a superseded turn's assistant
	// and tool work product is dropped from the next generation, so steering
	// a long turn by supersession throws away exactly what is being steered.
	// Unlike Claude's `system.message`, a nudge is durable staged input
	// consumed at an execution seam, not an event injected immediately into
	// a managed session.
	//
	// A nudge is not an interrupt. The earliest injection point is the next
	// execution seam: the start of the next execution segment, which is a
	// host-tool turn's park boundary, or the stop seam where a turn that
	// considers itself finished re-enters its loop to answer the input in
	// place. An in-flight provider call or tool run is never aborted for it,
	// and an Invocation that has been interrupted is never given more to do —
	// the interrupt wins the seam and the staged input expires.
	//
	// A nudge is never implicitly minted and never implicitly converted.
	// `POST /v1/invocations` against a busy Session keeps its `if_active`
	// semantics; admission is never silently turned into a nudge, and a
	// nudge is never silently turned into an Invocation. nvoken holds no
	// agent configuration — the definition travels inline on every admission — so
	// there is nothing it could legitimately run converted input with.
	//
	// Input that the turn never takes is settled `expired` when the
	// Invocation settles, in the same transaction and under the same Session
	// lock this request takes. It never influences a later Invocation.
	// Re-sending missed direction as the next Invocation's input is the
	// host's call to make, and `GET .../pending-inputs` is where the miss is
	// visible.
	//
	// `content` is text: a string, or an array of text blocks. Image and
	// document blocks are accepted on an Invocation's own input but not
	// here, because the stop seam carries a continuation as text and staging
	// content one seam would silently drop is worse than refusing it.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
	NudgeInvocationWithResponse(ctx context.Context, invocationID InvocationID, body NudgeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*NudgeInvocationHTTPResponse, error)

	// ListPendingInputsWithResponse List staged input for an Invocation
	//
	// Returns the staged queue in `(created_at, id)` ascending order — the
	// order the turn will consume it — with settled rows retained so a
	// caller can answer "what did the user say, and did the model see it".
	// This, not an event vocabulary, is the reconciliation source for a
	// surface that shows queued direction: a drained input also appears on
	// the transcript stream as an ordinary `user` message.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/pending-inputs (the `ListPendingInputs` operationId).
	ListPendingInputsWithResponse(ctx context.Context, invocationID InvocationID, params *ListPendingInputsParams, reqEditors ...RequestEditorFn) (*ListPendingInputsHTTPResponse, error)

	// CancelPendingInputWithResponse Withdraw staged input the turn has not taken
	//
	// Cancels input that is still `pending`. Cancelling input that is
	// already `cancelled` returns it unchanged, so a retry is safe.
	//
	// Cancel and drain serialize on the row, so whichever commits first
	// wins outright: a cancelled input is never seen by the model, and input
	// the executor already drained is reported `409 pending_input_settled`
	// with the current resource in `details.pending_input` rather than being
	// withdrawn from a transcript it is already part of. The request body
	// must be empty.
	//
	// Requires the same authority as cancelling the Invocation.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/pending-inputs/{pending_input_id}/cancel (the `CancelPendingInput` operationId).
	CancelPendingInputWithResponse(ctx context.Context, invocationID InvocationID, pendingInputID PendingInputID, reqEditors ...RequestEditorFn) (*CancelPendingInputHTTPResponse, error)

	// GetInvocationResultWithResponse Read the composed Invocation result
	//
	// Returns one InvocationResult at any status: the authoritative
	// Invocation, this Invocation's canonical messages composed at read
	// time, and the output_text convenience projection. The Invocation and
	// its messages are read in one repeatable-read snapshot, so the payload
	// never shows a terminal status with a missing message tail.
	// Authentication, tenant scoping, and the nondisclosing not_found rule
	// match the plain Invocation read exactly.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/result (the `GetInvocationResult` operationId).
	GetInvocationResultWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*GetInvocationResultHTTPResponse, error)

	// ResumeInvocationWithBodyWithResponse Raise the exhausted turn ceiling and resume a paused Invocation
	//
	// Requeues an Invocation paused on a turn-level consumption ceiling.
	// `limits` must contain only the exhausted field, raised above both its
	// prior value and the amount already consumed, and the new value must
	// remain within installation policy. A Session-budget pause resumes by
	// raising or removing the Session budget instead. Deadlines never pause.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
	ResumeInvocationWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ResumeInvocationHTTPResponse, error)

	// ResumeInvocationWithResponse Raise the exhausted turn ceiling and resume a paused Invocation
	//
	// Requeues an Invocation paused on a turn-level consumption ceiling.
	// `limits` must contain only the exhausted field, raised above both its
	// prior value and the amount already consumed, and the new value must
	// remain within installation policy. A Session-budget pause resumes by
	// raising or removing the Session budget instead. Deadlines never pause.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
	ResumeInvocationWithResponse(ctx context.Context, invocationID InvocationID, body ResumeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*ResumeInvocationHTTPResponse, error)

	// StreamInvocationWithResponse Resume and tail one Invocation over SSE
	//
	// Replays durable updates after `cursor`, optionally forwards ephemeral
	// output previews for this Invocation, and ends only when this Invocation
	// settles or the connection rotates. Durable frames carry an SSE `id`;
	// previews and control frames do not. After `stream.resync`, discard
	// provisional output and wait for durable state.
	//
	// The explicit `cursor` query parameter takes precedence over
	// `Last-Event-ID`. `stream.end` reason `rotate` means reconnect with the
	// last durable ID. `deltas=false` skips preview fan-out without changing
	// replay, polling, cursor, or terminal semantics. Disconnecting never
	// cancels the Invocation.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/stream (the `StreamInvocation` operationId).
	StreamInvocationWithResponse(ctx context.Context, invocationID InvocationID, params *StreamInvocationParams, reqEditors ...RequestEditorFn) (*StreamInvocationHTTPResponse, error)

	// ListToolCallsWithResponse Page through durable ToolCall execution records
	//
	// Returns ToolCalls in execution discovery order. Every execution mode is
	// included. The records contain lifecycle and timing facts only. Tool
	// inputs and results remain in the canonical Session transcript.
	//
	// Callback records include a delivery object. Its terminal outcome,
	// attempt count, and last HTTP status remain available after the bounded
	// delivery transport row is pruned. These records use the same
	// authentication, tenant scope, Session constraint, and nondisclosing
	// not_found behavior as the Invocation read. Deleting the Session deletes
	// these records with the rest of its subtree.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/invocations/{invocation_id}/tool-calls (the `ListToolCalls` operationId).
	ListToolCallsWithResponse(ctx context.Context, invocationID InvocationID, params *ListToolCallsParams, reqEditors ...RequestEditorFn) (*ListToolCallsHTTPResponse, error)

	// SubmitHostToolResultsWithBodyWithResponse Submit durable results for pending host ToolCalls
	//
	// Atomically accepts one bounded batch for a waiting Invocation. The
	// first committed result for each ToolCall wins. An equal replay is
	// acknowledged as deduplicated; a changed replay conflicts. Partial
	// batches leave the Invocation waiting. Closing the final pending call
	// queues the same Invocation and its successor execution dispatch before
	// returning `202`.
	//
	// This command accepts only host-mode calls owned by the path
	// Invocation and authenticated tenant scope. It is not a generic
	// Session append endpoint. The body is limited to 1 MiB; each result
	// content value is valid JSON limited to 256 KiB and 32 nesting levels.
	//
	// `content` accepts any JSON value and the stored transcript retains it
	// verbatim. Before a result reaches the model, a string or an array of
	// content blocks passes through unchanged; any other value is
	// serialized to its compact JSON text and sent as a string, so the
	// model sees the same bytes a host that pre-stringifies would send.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
	SubmitHostToolResultsWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHostToolResultsHTTPResponse, error)

	// SubmitHostToolResultsWithResponse Submit durable results for pending host ToolCalls
	//
	// Atomically accepts one bounded batch for a waiting Invocation. The
	// first committed result for each ToolCall wins. An equal replay is
	// acknowledged as deduplicated; a changed replay conflicts. Partial
	// batches leave the Invocation waiting. Closing the final pending call
	// queues the same Invocation and its successor execution dispatch before
	// returning `202`.
	//
	// This command accepts only host-mode calls owned by the path
	// Invocation and authenticated tenant scope. It is not a generic
	// Session append endpoint. The body is limited to 1 MiB; each result
	// content value is valid JSON limited to 256 KiB and 32 nesting levels.
	//
	// `content` accepts any JSON value and the stored transcript retains it
	// verbatim. Before a result reaches the model, a string or an array of
	// content blocks passes through unchanged; any other value is
	// serialized to its compact JSON text and sent as a string, so the
	// model sees the same bytes a host that pre-stringifies would send.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
	SubmitHostToolResultsWithResponse(ctx context.Context, invocationID InvocationID, body SubmitHostToolResultsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHostToolResultsHTTPResponse, error)

	// ListMCPToolsWithBodyWithResponse Discover and project one remote MCP server's tools
	//
	// Opens one short-lived streamable-HTTP MCP session through guarded
	// public-only egress, drains tools/list pagination, applies the same
	// allowlist and projection rules used by Invocation execution, and then
	// closes the session. Supplied headers are used only for this request,
	// are never logged or returned, and are not persisted.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
	ListMCPToolsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ListMCPToolsHTTPResponse, error)

	// ListMCPToolsWithResponse Discover and project one remote MCP server's tools
	//
	// Opens one short-lived streamable-HTTP MCP session through guarded
	// public-only egress, drains tools/list pagination, applies the same
	// allowlist and projection rules used by Invocation execution, and then
	// closes the session. Supplied headers are used only for this request,
	// are never logged or returned, and are not persisted.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
	ListMCPToolsWithResponse(ctx context.Context, body ListMCPToolsJSONRequestBody, reqEditors ...RequestEditorFn) (*ListMCPToolsHTTPResponse, error)

	// ListModelsWithResponse List nvoken's curated model catalog
	//
	// Returns the complete bounded set of text-generation models nvoken
	// intentionally advertises. Catalog membership means nvoken maintains
	// metadata for the exact provider/model selection. It does not prove that
	// the caller's provider account, region, or selected provider key can access
	// the model. Ordering is deterministic but has no semantic meaning.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/models (the `ListModels` operationId).
	ListModelsWithResponse(ctx context.Context, params *ListModelsParams, reqEditors ...RequestEditorFn) (*ListModelsHTTPResponse, error)

	// GetModelWithResponse Inspect one exact provider/model selection
	//
	// Returns a descriptor for any valid installed provider and exact model
	// ID, including IDs outside the curated catalog. `cataloged` distinguishes
	// maintained metadata from tolerant inspection. Pricing is the standard
	// local USD estimate used by nvoken's estimated-cost guardrail, not
	// provider billing or proof of account access.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/models/{provider}/{model_id} (the `GetModel` operationId).
	GetModelWithResponse(ctx context.Context, provider ModelProvider, modelID string, params *GetModelParams, reqEditors ...RequestEditorFn) (*GetModelHTTPResponse, error)

	// ListProviderKeysWithResponse List reusable provider key metadata
	//
	// Viewer, Runtime, and Operator profiles may read metadata within their exact scope; secret material is never readable.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/provider-keys (the `ListProviderKeys` operationId).
	ListProviderKeysWithResponse(ctx context.Context, params *ListProviderKeysParams, reqEditors ...RequestEditorFn) (*ListProviderKeysHTTPResponse, error)

	// CreateProviderKeyWithBodyWithResponse Create one reusable provider key
	//
	// Creates encrypted app or tenant BYOK. App scope requires an
	// unconstrained Operator credential inside the app; the credential is
	// shared by every tenant of that app and by no other app. A
	// tenant-constrained Runtime credential may manage only its exact tenant
	// partition. The response contains metadata only.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
	CreateProviderKeyWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateProviderKeyHTTPResponse, error)

	// CreateProviderKeyWithResponse Create one reusable provider key
	//
	// Creates encrypted app or tenant BYOK. App scope requires an
	// unconstrained Operator credential inside the app; the credential is
	// shared by every tenant of that app and by no other app. A
	// tenant-constrained Runtime credential may manage only its exact tenant
	// partition. The response contains metadata only.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
	CreateProviderKeyWithResponse(ctx context.Context, body CreateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateProviderKeyHTTPResponse, error)

	// RevokeProviderKeyWithResponse Revoke a provider key and destroy live encrypted versions
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/provider-keys/{provider_key_id} (the `RevokeProviderKey` operationId).
	RevokeProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*RevokeProviderKeyHTTPResponse, error)

	// GetProviderKeyWithResponse Read provider key metadata
	//
	// Viewer, Runtime, and Operator profiles may read secret-free metadata within their exact scope.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/provider-keys/{provider_key_id} (the `GetProviderKey` operationId).
	GetProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*GetProviderKeyHTTPResponse, error)

	// RotateProviderKeyWithBodyWithResponse Rotate a provider key
	//
	// Replaces the active version, optionally preserving a bounded overlap.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
	RotateProviderKeyWithBodyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RotateProviderKeyHTTPResponse, error)

	// RotateProviderKeyWithResponse Rotate a provider key
	//
	// Replaces the active version, optionally preserving a bounded overlap.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
	RotateProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, body RotateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*RotateProviderKeyHTTPResponse, error)

	// GetProviderKeyUsageWithResponse Read the token usage rollup for one provider key
	//
	// Aggregates durable checkpoint evidence across every Invocation that
	// bound the provider key: how many Invocations used it, when a model call
	// last used it, and the summed model usage. Derived at read time from the
	// transcript record, never from a separately maintained counter.
	// Authorization matches reading the provider key itself.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/provider-keys/{provider_key_id}/usage (the `GetProviderKeyUsage` operationId).
	GetProviderKeyUsageWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*GetProviderKeyUsageHTTPResponse, error)

	// ListSessionsWithResponse List authoritative Sessions
	//
	// Returns newest-first Session identity and current nonterminal
	// Invocation state. Exact filters combine with AND. Tenant filtering and
	// cursor binding follow the Invocation-list rules. `agent_id` and
	// `agent_key` are mutually exclusive and normalize to the same cursor
	// filter.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions (the `ListSessions` operationId).
	ListSessionsWithResponse(ctx context.Context, params *ListSessionsParams, reqEditors ...RequestEditorFn) (*ListSessionsHTTPResponse, error)

	// CreateSessionWithBodyWithResponse Create or seed a Session without admitting an Invocation
	//
	// Creates a Session with zero Invocations and optional host-asserted
	// starting history. Every body field is optional. An omitted `agent_key` leaves the
	// Session unbound: `agent_id` is null until the first admitted
	// Invocation binds it, and the binding is immutable once set either way;
	// `seed_messages` therefore requires `agent_key`. A `session_key`
	// requires an `agent_key`, because key uniqueness
	// and lookup are scoped per (tenant partition, Agent); a keyed create
	// is an upsert that accepts equal supplied Session options and returns
	// `session_options_conflict` when a supplied value differs.
	// Without a `session_key`, every call creates a fresh Session.
	// Precedence for the tenant partition is credential constraint,
	// explicit `tenant_key`, then the default partition.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
	CreateSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateSessionHTTPResponse, error)

	// CreateSessionWithResponse Create or seed a Session without admitting an Invocation
	//
	// Creates a Session with zero Invocations and optional host-asserted
	// starting history. Every body field is optional. An omitted `agent_key` leaves the
	// Session unbound: `agent_id` is null until the first admitted
	// Invocation binds it, and the binding is immutable once set either way;
	// `seed_messages` therefore requires `agent_key`. A `session_key`
	// requires an `agent_key`, because key uniqueness
	// and lookup are scoped per (tenant partition, Agent); a keyed create
	// is an upsert that accepts equal supplied Session options and returns
	// `session_options_conflict` when a supplied value differs.
	// Without a `session_key`, every call creates a fresh Session.
	// Precedence for the tenant partition is credential constraint,
	// explicit `tenant_key`, then the default partition.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
	CreateSessionWithResponse(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateSessionHTTPResponse, error)

	// DeleteSessionWithResponse Erase a Session and everything under it
	//
	// Removes the Session, its Invocations, transcript messages, checkpoints,
	// tool calls, provider artifacts, compactions, provider-key and MCP
	// bindings, and undelivered webhooks. The erasure is immediate and
	// irreversible; a subsequent read is `not_found`.
	//
	// An Invocation still running is stopped. No cancellation is recorded,
	// because there is nothing left to record it against — the Invocation is
	// removed rather than settled, and no `invocation.settled` webhook
	// is emitted for it. A host that needs a settled record should cancel and
	// observe the terminal state before deleting.
	//
	// Erasure is scoped like every other Session operation: an unknown or
	// out-of-scope `session_id` returns `not_found`, so a retry after a lost
	// response can treat `404` as already-done. The `delete_session`
	// operation belongs to the Runtime and Operator profiles; a Viewer
	// credential cannot erase a transcript.
	//
	// **This is not account deletion by itself.** nvoken keeps no account
	// tombstone, so a host honouring a deletion request must first stop
	// admitting work for that tenant, then page `GET /v1/sessions` and delete
	// until the list is empty. Otherwise a concurrent request creates a new
	// Session behind the sweep.
	//
	// Two consequences worth planning for. Usage reporting shrinks
	// retroactively: `GET /v1/usage/daily` is computed at read time from
	// Invocation evidence that erasure removes, which is why the endpoint is
	// documented as operational visibility rather than a billing ledger — a
	// billing host records usage at settlement, keyed by Invocation id.
	// And the erased Invocations' idempotency keys become free for reuse,
	// consistent with the documented guarantee that deduplication holds while
	// the original Invocation is retained.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/sessions/{session_id} (the `DeleteSession` operationId).
	DeleteSessionWithResponse(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*DeleteSessionHTTPResponse, error)

	// GetSessionWithResponse Read authoritative Session identity and current state
	//
	// An installation-wide credential may resolve a Session in any tenant
	// partition. A tenant-constrained credential resolves only Sessions
	// in its partition. Missing, incompatible, and undisclosable resources use
	// `not_found`; a credential denied the read operation itself receives
	// `forbidden`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions/{session_id} (the `GetSession` operationId).
	GetSessionWithResponse(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*GetSessionHTTPResponse, error)

	// UpdateSessionWithBodyWithResponse Merge host metadata into a Session
	//
	// Merges a metadata patch: a present key replaces its value, an explicit
	// `null` deletes that key, and a key the patch does not mention survives.
	//
	// Merge rather than replace, because independent writers share this map —
	// a conversation UI writing a title, correlation tooling writing a trace
	// id — and a full replacement would make each silently discard the
	// other's keys. The merge happens under the Session lock, so two
	// concurrent patches compose instead of one overwriting the other's read.
	//
	// `"metadata": null` is refused rather than guessed at: it could mean
	// "clear everything" or "leave it alone", and either reading is
	// destructive or silent. Delete keys one at a time.
	//
	// Bounds apply to the merged result, not to the patch, so a patch that
	// deletes as many keys as it adds is not refused for a count it never
	// produces. Requires the `update_session` operation.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
	UpdateSessionWithBodyWithResponse(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateSessionHTTPResponse, error)

	// UpdateSessionWithResponse Merge host metadata into a Session
	//
	// Merges a metadata patch: a present key replaces its value, an explicit
	// `null` deletes that key, and a key the patch does not mention survives.
	//
	// Merge rather than replace, because independent writers share this map —
	// a conversation UI writing a title, correlation tooling writing a trace
	// id — and a full replacement would make each silently discard the
	// other's keys. The merge happens under the Session lock, so two
	// concurrent patches compose instead of one overwriting the other's read.
	//
	// `"metadata": null` is refused rather than guessed at: it could mean
	// "clear everything" or "leave it alone", and either reading is
	// destructive or silent. Delete keys one at a time.
	//
	// Bounds apply to the merged result, not to the patch, so a patch that
	// deletes as many keys as it adds is not refused for a count it never
	// produces. Requires the `update_session` operation.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
	UpdateSessionWithResponse(ctx context.Context, sessionID SessionID, body UpdateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateSessionHTTPResponse, error)

	// ListSessionCompactionsWithResponse Page through immutable Session compaction records
	//
	// Returns newest-first diagnostic records for every attempted compaction
	// pass. `applied` records include the private summary projection and its
	// model usage. `fell_through` records identify the failure class and may
	// include usage when the provider returned trustworthy evidence. Only
	// applied records change future model context; neither kind changes the
	// canonical Session transcript.
	//
	// The summary is derived from the caller's own transcript and therefore
	// uses the same authorization and Session scoping as transcript reads.
	// The opaque cursor is bound to the authenticated app and Session.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions/{session_id}/compactions (the `ListSessionCompactions` operationId).
	ListSessionCompactionsWithResponse(ctx context.Context, sessionID SessionID, params *ListSessionCompactionsParams, reqEditors ...RequestEditorFn) (*ListSessionCompactionsHTTPResponse, error)

	// ForkSessionWithBodyWithResponse Copy a Session prefix into a new Session
	//
	// Creates a new Session in the source Session's tenant and Agent scope,
	// copying every canonical message through `from_message` inclusively.
	// The source is untouched. The child stores durable Session and message
	// lineage, but copied messages no longer belong to the source
	// Invocations. Their `origin`, per-turn `user_key`, and resolved message
	// phase are preserved.
	//
	// Usage and compaction summaries are not copied. Child usage starts at
	// zero and the child starts uncompacted. Retention and metadata come only
	// from `session_options` on this request; no Session option is inherited.
	// A `session_key` has the same tenant/Agent-scoped upsert behavior as
	// Session creation.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
	ForkSessionWithBodyWithResponse(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ForkSessionHTTPResponse, error)

	// ForkSessionWithResponse Copy a Session prefix into a new Session
	//
	// Creates a new Session in the source Session's tenant and Agent scope,
	// copying every canonical message through `from_message` inclusively.
	// The source is untouched. The child stores durable Session and message
	// lineage, but copied messages no longer belong to the source
	// Invocations. Their `origin`, per-turn `user_key`, and resolved message
	// phase are preserved.
	//
	// Usage and compaction summaries are not copied. Child usage starts at
	// zero and the child starts uncompacted. Retention and metadata come only
	// from `session_options` on this request; no Session option is inherited.
	// A `session_key` has the same tenant/Agent-scoped upsert behavior as
	// Session creation.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
	ForkSessionWithResponse(ctx context.Context, sessionID SessionID, body ForkSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*ForkSessionHTTPResponse, error)

	// ListSessionMessagesWithResponse Page through the canonical Session transcript
	//
	// Returns persisted SessionMessage rows in ascending sequence order.
	// The opaque forward cursor is bound to the authenticated caller and
	// Session. This history endpoint contains no lifecycle or live-preview
	// copies.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions/{session_id}/messages (the `ListSessionMessages` operationId).
	ListSessionMessagesWithResponse(ctx context.Context, sessionID SessionID, params *ListSessionMessagesParams, reqEditors ...RequestEditorFn) (*ListSessionMessagesHTTPResponse, error)

	// GetSessionTranscriptWithResponse Drain a fixed-cut incremental transcript snapshot
	//
	// Projects canonical messages and append-only Invocation lifecycle
	// changes. Supply a prior `resume_cursor` as `cursor` to drain newer
	// durable state. Continue a multi-page fixed cut with `page_token` until
	// `has_more` is false. Each page contains one phase: all message pages are
	// delivered before lifecycle-change pages, so terminal state cannot
	// precede the transcript rows committed with it. Omitting both positions
	// starts at the beginning of the retained Session.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions/{session_id}/transcript (the `GetSessionTranscript` operationId).
	GetSessionTranscriptWithResponse(ctx context.Context, sessionID SessionID, params *GetSessionTranscriptParams, reqEditors ...RequestEditorFn) (*GetSessionTranscriptHTTPResponse, error)

	// StreamSessionTranscriptWithResponse Replay and tail one Session transcript over SSE
	//
	// Opens a resumable Server-Sent Events projection over the same fixed-cut
	// transcript read model as the JSON endpoint. With deltas enabled, the
	// server subscribes to live fan-out before its first Postgres drain. In
	// either mode it re-drains Postgres on a bounded poll and closes after
	// authoritative terminal reconciliation or deliberate rotation.
	// Disconnecting never cancels the Invocation.
	//
	// Every nonempty `transcript.update` frame carries
	// `id: <resume_cursor>`; that opaque ID is the only replay position
	// clients persist. `output_text.delta`, `thinking.delta`,
	// `stream.resync`, and `stream.end` never carry an `id`. Deltas are
	// ephemeral and may be lost; after `stream.resync`, discard provisional
	// output and wait for canonical messages. `stream.end` reason `terminal`
	// means the final Postgres drain observed no nonterminal Invocation.
	// Reason `rotate` means reconnect with the last durable ID. An abnormal
	// close has no terminal meaning. `deltas=false` skips preview fan-out
	// without changing replay, polling, cursor, or terminal semantics.
	//
	// The explicit `cursor` query parameter takes precedence over
	// `Last-Event-ID`. Bearer authentication requires an SSE-capable HTTP
	// client that can set the `Authorization` header; the browser EventSource
	// constructor alone cannot do so. The server emits `retry: 1000` as its
	// default reconnect delay.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/sessions/{session_id}/transcript/stream (the `StreamSessionTranscript` operationId).
	StreamSessionTranscriptWithResponse(ctx context.Context, sessionID SessionID, params *StreamSessionTranscriptParams, reqEditors ...RequestEditorFn) (*StreamSessionTranscriptHTTPResponse, error)

	// GetDailyUsageWithResponse Read daily usage rollups
	//
	// Aggregates durable model-call evidence — invocation checkpoints plus
	// session compaction calls — into per-day buckets keyed by app,
	// provider, and model, summing token counts and estimated cost. Derived
	// at read time from the transcript record, never from a separately
	// maintained counter.
	//
	// An app-bound credential reads its own app. An app-less presentation
	// spans every registered app; for a console issuer token that requires
	// the admin claim.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/usage/daily (the `GetDailyUsage` operationId).
	GetDailyUsageWithResponse(ctx context.Context, params *GetDailyUsageParams, reqEditors ...RequestEditorFn) (*GetDailyUsageHTTPResponse, error)
}

// ListAgentsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListAgents
type ListAgentsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListAgentsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AgentList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListAgentsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON200() *AgentList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListAgentsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListAgentsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAgentsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAgentsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAgentsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetAgentHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetAgent
type GetAgentHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetAgentHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Agent
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetAgentHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAgentHTTPResponse) GetJSON200() *Agent {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetAgentHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAgentHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAgentHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAgentHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetAgentHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAgentHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetAgentHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetAgentHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAgentHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAgentHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAgentHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListAppsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListApps
type ListAppsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListAppsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AppList
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListAppsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAppsHTTPResponse) GetJSON200() *AppList {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAppsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAppsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListAppsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAppsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAppsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAppsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAppsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAppsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// RegisterAppHTTPResponse429Headers the declared response headers of an HTTP 429 response for RegisterApp
type RegisterAppHTTPResponse429Headers struct {
	RetryAfter *int
}

type RegisterAppHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *App
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *RegisterAppHTTPResponse429Headers
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON201() *App {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r RegisterAppHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r RegisterAppHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RegisterAppHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RegisterAppHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RegisterAppHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetAppHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetApp
type GetAppHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetAppHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *App
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetAppHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAppHTTPResponse) GetJSON200() *App {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAppHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAppHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAppHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetAppHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAppHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAppHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAppHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAppHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAppHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// UpdateAppHTTPResponse429Headers the declared response headers of an HTTP 429 response for UpdateApp
type UpdateAppHTTPResponse429Headers struct {
	RetryAfter *int
}

type UpdateAppHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *App
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *UpdateAppHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON200() *App {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAppHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateAppHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAppHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAppHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAppHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateBudgetHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *Budget
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON201() *Budget {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CreateBudgetHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CreateBudgetHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateBudgetHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateBudgetHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateBudgetHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteBudgetHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteBudgetHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteBudgetHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteBudgetHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeleteBudgetHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r DeleteBudgetHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r DeleteBudgetHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteBudgetHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteBudgetHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteBudgetHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBudgetHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Budget
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON200() *Budget {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetBudgetHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetBudgetHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBudgetHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBudgetHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBudgetHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateBudgetHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Budget
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON200() *Budget {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r UpdateBudgetHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r UpdateBudgetHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateBudgetHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateBudgetHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateBudgetHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListInvocationsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListInvocations
type ListInvocationsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListInvocationsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *InvocationList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListInvocationsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON200() *InvocationList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListInvocationsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListInvocationsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListInvocationsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListInvocationsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListInvocationsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// CreateInvocationHTTPResponse429Headers the declared response headers of an HTTP 429 response for CreateInvocation
type CreateInvocationHTTPResponse429Headers struct {
	RetryAfter *int
}

type CreateInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *Invocation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *ErrorResponse
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *ErrorResponse
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *CreateInvocationHTTPResponse429Headers
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON202() *Invocation {
	return r.JSON202
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON404() *ErrorResponse {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON422() *ErrorResponse {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CreateInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CreateInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetInvocationHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetInvocation
type GetInvocationHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Invocation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetInvocationHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON200() *Invocation {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CancelInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Invocation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON200() *Invocation {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CancelInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CancelInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CancelInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CancelInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CancelInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type InterruptInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Invocation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON200() *Invocation {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r InterruptInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r InterruptInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r InterruptInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r InterruptInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r InterruptInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type NudgeInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *NudgeAcknowledgement
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON202() *NudgeAcknowledgement {
	return r.JSON202
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r NudgeInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r NudgeInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r NudgeInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r NudgeInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r NudgeInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListPendingInputsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PendingInputList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON200() *PendingInputList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListPendingInputsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListPendingInputsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListPendingInputsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListPendingInputsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListPendingInputsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CancelPendingInputHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PendingInput
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON200() *PendingInput {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CancelPendingInputHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CancelPendingInputHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CancelPendingInputHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CancelPendingInputHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CancelPendingInputHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetInvocationResultHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetInvocationResult
type GetInvocationResultHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetInvocationResultHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *InvocationResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetInvocationResultHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON200() *InvocationResult {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetInvocationResultHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetInvocationResultHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetInvocationResultHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetInvocationResultHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetInvocationResultHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ResumeInvocationHTTPResponse429Headers the declared response headers of an HTTP 429 response for ResumeInvocation
type ResumeInvocationHTTPResponse429Headers struct {
	RetryAfter *int
}

type ResumeInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *Invocation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ResumeInvocationHTTPResponse429Headers
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON202() *Invocation {
	return r.JSON202
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ResumeInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ResumeInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ResumeInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ResumeInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ResumeInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// StreamInvocationHTTPResponse429Headers the declared response headers of an HTTP 429 response for StreamInvocation
type StreamInvocationHTTPResponse429Headers struct {
	RetryAfter *int
}

type StreamInvocationHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *StreamInvocationHTTPResponse429Headers
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r StreamInvocationHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r StreamInvocationHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r StreamInvocationHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r StreamInvocationHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r StreamInvocationHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListToolCallsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListToolCalls
type ListToolCallsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListToolCallsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ToolCallList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListToolCallsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON200() *ToolCallList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListToolCallsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListToolCallsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListToolCallsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListToolCallsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListToolCallsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitHostToolResultsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *SubmitHostToolResultsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON202() *SubmitHostToolResultsResponse {
	return r.JSON202
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r SubmitHostToolResultsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r SubmitHostToolResultsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitHostToolResultsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitHostToolResultsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitHostToolResultsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListMCPToolsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListMCPTools
type ListMCPToolsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListMCPToolsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MCPListToolsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *MCPDiscoveryFailed
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListMCPToolsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON200() *MCPListToolsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON502() *MCPDiscoveryFailed {
	return r.JSON502
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListMCPToolsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListMCPToolsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListMCPToolsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListMCPToolsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListMCPToolsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListModelsHTTPResponse200Headers the declared response headers of an HTTP 200 response for ListModels
type ListModelsHTTPResponse200Headers struct {
	ETag *string
}

// ListModelsHTTPResponse304Headers the declared response headers of an HTTP 304 response for ListModels
type ListModelsHTTPResponse304Headers struct {
	ETag *string
}

// ListModelsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListModels
type ListModelsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListModelsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ModelList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *ListModelsHTTPResponse200Headers
	// Headers304 the parsed response headers for an HTTP 304 response
	Headers304 *ListModelsHTTPResponse304Headers
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListModelsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListModelsHTTPResponse) GetJSON200() *ModelList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListModelsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListModelsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListModelsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListModelsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListModelsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListModelsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListModelsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListModelsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListModelsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetModelHTTPResponse200Headers the declared response headers of an HTTP 200 response for GetModel
type GetModelHTTPResponse200Headers struct {
	ETag *string
}

// GetModelHTTPResponse304Headers the declared response headers of an HTTP 304 response for GetModel
type GetModelHTTPResponse304Headers struct {
	ETag *string
}

// GetModelHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetModel
type GetModelHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetModelHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ModelDescriptor
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers200 the parsed response headers for an HTTP 200 response
	Headers200 *GetModelHTTPResponse200Headers
	// Headers304 the parsed response headers for an HTTP 304 response
	Headers304 *GetModelHTTPResponse304Headers
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetModelHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetModelHTTPResponse) GetJSON200() *ModelDescriptor {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetModelHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetModelHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetModelHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetModelHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetModelHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetModelHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetModelHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetModelHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetModelHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListProviderKeysHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ProviderKeyList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON200() *ProviderKeyList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListProviderKeysHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListProviderKeysHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListProviderKeysHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListProviderKeysHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListProviderKeysHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateProviderKeyHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ProviderKey
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON201() *ProviderKey {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CreateProviderKeyHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CreateProviderKeyHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateProviderKeyHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateProviderKeyHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateProviderKeyHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RevokeProviderKeyHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ProviderKey
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON200() *ProviderKey {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r RevokeProviderKeyHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r RevokeProviderKeyHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RevokeProviderKeyHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RevokeProviderKeyHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RevokeProviderKeyHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetProviderKeyHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ProviderKey
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON200() *ProviderKey {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetProviderKeyHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetProviderKeyHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetProviderKeyHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetProviderKeyHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetProviderKeyHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RotateProviderKeyHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ProviderKey
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON200() *ProviderKey {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r RotateProviderKeyHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r RotateProviderKeyHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RotateProviderKeyHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RotateProviderKeyHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RotateProviderKeyHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetProviderKeyUsageHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ProviderKeyUsage
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON200() *ProviderKeyUsage {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetProviderKeyUsageHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetProviderKeyUsageHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetProviderKeyUsageHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetProviderKeyUsageHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetProviderKeyUsageHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListSessionsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListSessions
type ListSessionsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListSessionsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListSessionsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON200() *SessionList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListSessionsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListSessionsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListSessionsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListSessionsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListSessionsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// CreateSessionHTTPResponse429Headers the declared response headers of an HTTP 429 response for CreateSession
type CreateSessionHTTPResponse429Headers struct {
	RetryAfter *int
}

type CreateSessionHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *Session
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *ErrorResponse
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *CreateSessionHTTPResponse429Headers
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON201() *Session {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON409() *ErrorResponse {
	return r.JSON409
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r CreateSessionHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r CreateSessionHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateSessionHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateSessionHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateSessionHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// DeleteSessionHTTPResponse429Headers the declared response headers of an HTTP 429 response for DeleteSession
type DeleteSessionHTTPResponse429Headers struct {
	RetryAfter *int
}

type DeleteSessionHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *DeleteSessionHTTPResponse429Headers
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r DeleteSessionHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r DeleteSessionHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteSessionHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteSessionHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteSessionHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetSessionHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetSession
type GetSessionHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetSessionHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Session
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetSessionHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSessionHTTPResponse) GetJSON200() *Session {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetSessionHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSessionHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetSessionHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetSessionHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetSessionHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetSessionHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetSessionHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetSessionHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSessionHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSessionHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSessionHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// UpdateSessionHTTPResponse429Headers the declared response headers of an HTTP 429 response for UpdateSession
type UpdateSessionHTTPResponse429Headers struct {
	RetryAfter *int
}

type UpdateSessionHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Session
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *UpdateSessionHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON200() *Session {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r UpdateSessionHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r UpdateSessionHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateSessionHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateSessionHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateSessionHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListSessionCompactionsHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListSessionCompactions
type ListSessionCompactionsHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListSessionCompactionsHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionCompactionList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListSessionCompactionsHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON200() *SessionCompactionList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListSessionCompactionsHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListSessionCompactionsHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListSessionCompactionsHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListSessionCompactionsHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListSessionCompactionsHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ForkSessionHTTPResponse429Headers the declared response headers of an HTTP 429 response for ForkSession
type ForkSessionHTTPResponse429Headers struct {
	RetryAfter *int
}

type ForkSessionHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *Session
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ForkSessionHTTPResponse429Headers
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON201() *Session {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ForkSessionHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ForkSessionHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ForkSessionHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ForkSessionHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ForkSessionHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListSessionMessagesHTTPResponse429Headers the declared response headers of an HTTP 429 response for ListSessionMessages
type ListSessionMessagesHTTPResponse429Headers struct {
	RetryAfter *int
}

type ListSessionMessagesHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionMessageList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *ListSessionMessagesHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON200() *SessionMessageList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r ListSessionMessagesHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r ListSessionMessagesHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListSessionMessagesHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListSessionMessagesHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListSessionMessagesHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetSessionTranscriptHTTPResponse429Headers the declared response headers of an HTTP 429 response for GetSessionTranscript
type GetSessionTranscriptHTTPResponse429Headers struct {
	RetryAfter *int
}

type GetSessionTranscriptHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *TranscriptSnapshot
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetSessionTranscriptHTTPResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON200() *TranscriptSnapshot {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetSessionTranscriptHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetSessionTranscriptHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSessionTranscriptHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSessionTranscriptHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSessionTranscriptHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// StreamSessionTranscriptHTTPResponse429Headers the declared response headers of an HTTP 429 response for StreamSessionTranscript
type StreamSessionTranscriptHTTPResponse429Headers struct {
	RetryAfter *int
}

type StreamSessionTranscriptHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *RateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *StreamSessionTranscriptHTTPResponse429Headers
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON429() *RateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r StreamSessionTranscriptHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r StreamSessionTranscriptHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r StreamSessionTranscriptHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r StreamSessionTranscriptHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r StreamSessionTranscriptHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetDailyUsageHTTPResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *DailyUsage
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *InvalidRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthenticated
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Internal
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Unavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON200() *DailyUsage {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON400() *InvalidRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON401() *Unauthenticated {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON500() *Internal {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r GetDailyUsageHTTPResponse) GetJSON503() *Unavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r GetDailyUsageHTTPResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetDailyUsageHTTPResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetDailyUsageHTTPResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetDailyUsageHTTPResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListAgentsWithResponse List Agent identity anchors
//
// Returns newest-first identity anchors scoped to the caller's app. An
// Agent stores only its nvoken ID, the host-owned `agent_key`, and
// creation time; instructions, models, tools, and provider keys still
// travel on each Invocation. An exact `agent_key` filter returns zero or
// one item.
//
// Unconstrained credentials see the anchors referenced by a Session in
// one of their app's tenants. Tenant-constrained credentials see only
// anchors referenced by a Session in their effective partition.
// Session-constrained credentials see only that Session's anchor. The
// opaque cursor is bound to the authenticated caller, credential
// constraint, and exact key filter.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/agents (the `ListAgents` operationId).
func (c *ClientWithResponses) ListAgentsWithResponse(ctx context.Context, params *ListAgentsParams, reqEditors ...RequestEditorFn) (*ListAgentsHTTPResponse, error) {
	rsp, err := c.ListAgents(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAgentsHTTPResponse(rsp)
}

// GetAgentWithResponse Read one Agent identity anchor
//
// Reads identity without admitting work. Out-of-scope and undisclosable
// constrained resources use `not_found`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/agents/{agent_id} (the `GetAgent` operationId).
func (c *ClientWithResponses) GetAgentWithResponse(ctx context.Context, agentID AgentID, reqEditors ...RequestEditorFn) (*GetAgentHTTPResponse, error) {
	rsp, err := c.GetAgent(ctx, agentID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAgentHTTPResponse(rsp)
}

// ListAppsWithResponse List registered apps
//
// Returns the apps this credential can see. A credential associated with
// an app sees only that app; a credential with no app association sees
// every registered app. An exact `external_ref` filter narrows either
// view to apps owned by that reference.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/apps (the `ListApps` operationId).
func (c *ClientWithResponses) ListAppsWithResponse(ctx context.Context, params *ListAppsParams, reqEditors ...RequestEditorFn) (*ListAppsHTTPResponse, error) {
	rsp, err := c.ListApps(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAppsHTTPResponse(rsp)
}

// RegisterAppWithBodyWithResponse Register an app
//
// Registers one host application and creates its default tenant,
// returning the generated `app_id`.
//
// Registration is installation management rather than work inside an app,
// so it requires a credential that is *not* associated with an app; a
// credential bound to one app cannot mint siblings for itself. Names
// identify apps and are unique, so re-registering an existing name is
// rejected rather than returning the existing app.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
func (c *ClientWithResponses) RegisterAppWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RegisterAppHTTPResponse, error) {
	rsp, err := c.RegisterAppWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRegisterAppHTTPResponse(rsp)
}

// RegisterAppWithResponse Register an app
//
// Registers one host application and creates its default tenant,
// returning the generated `app_id`.
//
// Registration is installation management rather than work inside an app,
// so it requires a credential that is *not* associated with an app; a
// credential bound to one app cannot mint siblings for itself. Names
// identify apps and are unique, so re-registering an existing name is
// rejected rather than returning the existing app.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/apps (the `RegisterApp` operationId).
func (c *ClientWithResponses) RegisterAppWithResponse(ctx context.Context, body RegisterAppJSONRequestBody, reqEditors ...RequestEditorFn) (*RegisterAppHTTPResponse, error) {
	rsp, err := c.RegisterApp(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRegisterAppHTTPResponse(rsp)
}

// GetAppWithResponse Get one app
//
// Returns one registered app. A credential associated with a different
// app receives `404`, so an app identifier cannot be probed across the
// isolation boundary.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/apps/{app_id} (the `GetApp` operationId).
func (c *ClientWithResponses) GetAppWithResponse(ctx context.Context, appID string, reqEditors ...RequestEditorFn) (*GetAppHTTPResponse, error) {
	rsp, err := c.GetApp(ctx, appID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAppHTTPResponse(rsp)
}

// UpdateAppWithBodyWithResponse Update an app
//
// Updates an app's mutable presentation fields. The unique `name` and the
// ownership-grounding `external_ref` cannot be changed. A credential
// associated with a different app receives `404`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
func (c *ClientWithResponses) UpdateAppWithBodyWithResponse(ctx context.Context, appID string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAppHTTPResponse, error) {
	rsp, err := c.UpdateAppWithBody(ctx, appID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAppHTTPResponse(rsp)
}

// UpdateAppWithResponse Update an app
//
// Updates an app's mutable presentation fields. The unique `name` and the
// ownership-grounding `external_ref` cannot be changed. A credential
// associated with a different app receives `404`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/apps/{app_id} (the `UpdateApp` operationId).
func (c *ClientWithResponses) UpdateAppWithResponse(ctx context.Context, appID string, body UpdateAppJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAppHTTPResponse, error) {
	rsp, err := c.UpdateApp(ctx, appID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAppHTTPResponse(rsp)
}

// CreateBudgetWithBodyWithResponse Create one fixed usage budget window
//
// Creates an estimated-list-price guardrail inside the authenticated App.
// Windows for the same scope identity cannot overlap. The idempotency key
// makes an identical retry return the first Budget.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
func (c *ClientWithResponses) CreateBudgetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBudgetHTTPResponse, error) {
	rsp, err := c.CreateBudgetWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBudgetHTTPResponse(rsp)
}

// CreateBudgetWithResponse Create one fixed usage budget window
//
// Creates an estimated-list-price guardrail inside the authenticated App.
// Windows for the same scope identity cannot overlap. The idempotency key
// makes an identical retry return the first Budget.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/budgets (the `CreateBudget` operationId).
func (c *ClientWithResponses) CreateBudgetWithResponse(ctx context.Context, body CreateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBudgetHTTPResponse, error) {
	rsp, err := c.CreateBudget(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBudgetHTTPResponse(rsp)
}

// DeleteBudgetWithResponse Remove a Budget guardrail
//
// Removes enforcement without deleting its accounting history.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/budgets/{budget_id} (the `DeleteBudget` operationId).
func (c *ClientWithResponses) DeleteBudgetWithResponse(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*DeleteBudgetHTTPResponse, error) {
	rsp, err := c.DeleteBudget(ctx, budgetID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteBudgetHTTPResponse(rsp)
}

// GetBudgetWithResponse Read one usage Budget
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/budgets/{budget_id} (the `GetBudget` operationId).
func (c *ClientWithResponses) GetBudgetWithResponse(ctx context.Context, budgetID BudgetID, reqEditors ...RequestEditorFn) (*GetBudgetHTTPResponse, error) {
	rsp, err := c.GetBudget(ctx, budgetID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBudgetHTTPResponse(rsp)
}

// UpdateBudgetWithBodyWithResponse Raise or lower a Budget cap
//
// Replaces the cap. It cannot be lower than settled plus reserved cost.
// Raising it reevaluates paused work in bounded batches.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
func (c *ClientWithResponses) UpdateBudgetWithBodyWithResponse(ctx context.Context, budgetID BudgetID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBudgetHTTPResponse, error) {
	rsp, err := c.UpdateBudgetWithBody(ctx, budgetID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBudgetHTTPResponse(rsp)
}

// UpdateBudgetWithResponse Raise or lower a Budget cap
//
// Replaces the cap. It cannot be lower than settled plus reserved cost.
// Raising it reevaluates paused work in bounded batches.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/budgets/{budget_id} (the `UpdateBudget` operationId).
func (c *ClientWithResponses) UpdateBudgetWithResponse(ctx context.Context, budgetID BudgetID, body UpdateBudgetJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBudgetHTTPResponse, error) {
	rsp, err := c.UpdateBudget(ctx, budgetID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBudgetHTTPResponse(rsp)
}

// ListInvocationsWithResponse List authoritative Invocations
//
// Returns newest-first durable Invocation state. Exact filters combine
// with AND. An installation-wide caller may list all tenant partitions, one
// named partition with `tenant_key`, or the default partition with
// `default_tenant=true`. A tenant-constrained credential is always scoped
// to its partition. The opaque cursor is bound to the normalized filter
// set and credential tenant scope. `agent_id` and `agent_key` are
// mutually exclusive; both normalize to the resolved Agent ID for cursor
// binding, so an equivalent cursor may resume under either spelling.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations (the `ListInvocations` operationId).
func (c *ClientWithResponses) ListInvocationsWithResponse(ctx context.Context, params *ListInvocationsParams, reqEditors ...RequestEditorFn) (*ListInvocationsHTTPResponse, error) {
	rsp, err := c.ListInvocations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListInvocationsHTTPResponse(rsp)
}

// CreateInvocationWithBodyWithResponse Durably admit one background agent turn
//
// Resolves or creates the installation-wide Agent identity anchor,
// resolves or creates a Session, resolves the inline or referenced
// definition, appends exactly one caller-input message, and creates one
// queued Invocation in a single transaction. The response is sent only
// after that transaction commits. The request handler never executes the
// model.
//
// `session_id` and `session_key` are mutually exclusive. A Session ID must
// belong to the named Agent, or be a Session created without one — the
// first admitted Invocation binds the Agent permanently. An installation-wide
// credential may omit `tenant_key` and use the Session's stored partition.
// A tenant-constrained credential cannot cross its partition; an explicit
// mismatch is rejected with `403 forbidden` before resource lookup.
//
// `idempotency_key` is scoped to the effective
// tenant partition, and `agent_key`. A same-key replay is checked before
// the Session's one-nonterminal-Invocation rule. It returns the original
// records without appending input, even when the Invocation is terminal.
// Material equality covers the Session selector kind and value, the
// resolved definition, and input. A `definition_id` and its byte-identical
// inline definition compare equal. Requested limits are compared before
// default resolution, so an explicit default differs from omission. JSON
// object member order is ignored, array order is significant, and strings
// are not rewritten. A changed material field returns
// `idempotency_conflict`.
//
// `if_active` defaults to `reject`, preserving
// `session_invocation_active`. Explicit `supersede` requires both create
// and cancel authority. While holding the Session lock, it durably
// cancels any nonterminal Invocation and admits this replacement in the
// same transaction. With no active work it admits normally. Equal replay
// returns the original work before supersession and never cancels newer
// active work.
//
// `interrupt` requires the same authority and keeps the same lock
// ordering, but stops the active Invocation gracefully instead of
// cancelling it: work already at a seam settles and the replacement is
// admitted in the same transaction, while work under a live lease has
// the interrupt recorded and this request waits for it to settle. If it
// has not settled within the wait, the response is
// `session_invocation_active` with `details.interrupt_requested = true`
// and the request may be reissued.
//
// A cataloged model continues to admit while deprecated. On and after
// its `retires_at` date, a new admission is refused with `422
// model_retired`; `details` carries the refused `model`, `retires_at`,
// the exact `replacement` provider/id pair, and the request `path`.
// Equal idempotent replay still returns work admitted before retirement.
//
// A text-only request body is limited to 1 MiB. A body carrying image or
// document blocks is limited to 24 MiB, within which decoded media is
// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
// 5 MiB per image, and 16 MiB per document. Requests over any of these
// limits are rejected before admission. URL media is fetched after an
// idempotent replay check and before any row is admitted. The guarded
// fetch accepts public HTTPS only, enforces the item limit while reading,
// and sniffs the returned bytes. nvoken stores those bytes and does not
// fetch the URL again.
//
// The supported streaming pattern is admit-then-stream: admit with a
// plain JSON POST, then follow the Invocation over
// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
// connection without re-admitting and does not depend on front-end
// streaming behavior. `Accept: text/event-stream` on this request is a
// deployment-dependent convenience, not a first-class mode: the
// committed admission is the first `invocation.accepted` frame and the
// connection tails that Invocation through `invocation.result`, but
// delivery requires the deployment front end to stream a non-`200`
// POST response unbuffered. Some managed front ends (including Cloud
// Run's) buffer it until the Invocation settles, which makes a
// host-tool turn's `waiting` state invisible on this connection.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
func (c *ClientWithResponses) CreateInvocationWithBodyWithResponse(ctx context.Context, params *CreateInvocationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateInvocationHTTPResponse, error) {
	rsp, err := c.CreateInvocationWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateInvocationHTTPResponse(rsp)
}

// CreateInvocationWithResponse Durably admit one background agent turn
//
// Resolves or creates the installation-wide Agent identity anchor,
// resolves or creates a Session, resolves the inline or referenced
// definition, appends exactly one caller-input message, and creates one
// queued Invocation in a single transaction. The response is sent only
// after that transaction commits. The request handler never executes the
// model.
//
// `session_id` and `session_key` are mutually exclusive. A Session ID must
// belong to the named Agent, or be a Session created without one — the
// first admitted Invocation binds the Agent permanently. An installation-wide
// credential may omit `tenant_key` and use the Session's stored partition.
// A tenant-constrained credential cannot cross its partition; an explicit
// mismatch is rejected with `403 forbidden` before resource lookup.
//
// `idempotency_key` is scoped to the effective
// tenant partition, and `agent_key`. A same-key replay is checked before
// the Session's one-nonterminal-Invocation rule. It returns the original
// records without appending input, even when the Invocation is terminal.
// Material equality covers the Session selector kind and value, the
// resolved definition, and input. A `definition_id` and its byte-identical
// inline definition compare equal. Requested limits are compared before
// default resolution, so an explicit default differs from omission. JSON
// object member order is ignored, array order is significant, and strings
// are not rewritten. A changed material field returns
// `idempotency_conflict`.
//
// `if_active` defaults to `reject`, preserving
// `session_invocation_active`. Explicit `supersede` requires both create
// and cancel authority. While holding the Session lock, it durably
// cancels any nonterminal Invocation and admits this replacement in the
// same transaction. With no active work it admits normally. Equal replay
// returns the original work before supersession and never cancels newer
// active work.
//
// `interrupt` requires the same authority and keeps the same lock
// ordering, but stops the active Invocation gracefully instead of
// cancelling it: work already at a seam settles and the replacement is
// admitted in the same transaction, while work under a live lease has
// the interrupt recorded and this request waits for it to settle. If it
// has not settled within the wait, the response is
// `session_invocation_active` with `details.interrupt_requested = true`
// and the request may be reissued.
//
// A cataloged model continues to admit while deprecated. On and after
// its `retires_at` date, a new admission is refused with `422
// model_retired`; `details` carries the refused `model`, `retires_at`,
// the exact `replacement` provider/id pair, and the request `path`.
// Equal idempotent replay still returns work admitted before retirement.
//
// A text-only request body is limited to 1 MiB. A body carrying image or
// document blocks is limited to 24 MiB, within which decoded media is
// bounded separately: at most 8 media blocks, 16 MiB decoded in total,
// 5 MiB per image, and 16 MiB per document. Requests over any of these
// limits are rejected before admission. URL media is fetched after an
// idempotent replay check and before any row is admitted. The guarded
// fetch accepts public HTTPS only, enforces the item limit while reading,
// and sniffs the returned bytes. nvoken stores those bytes and does not
// fetch the URL again.
//
// The supported streaming pattern is admit-then-stream: admit with a
// plain JSON POST, then follow the Invocation over
// `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped
// connection without re-admitting and does not depend on front-end
// streaming behavior. `Accept: text/event-stream` on this request is a
// deployment-dependent convenience, not a first-class mode: the
// committed admission is the first `invocation.accepted` frame and the
// connection tails that Invocation through `invocation.result`, but
// delivery requires the deployment front end to stream a non-`200`
// POST response unbuffered. Some managed front ends (including Cloud
// Run's) buffer it until the Invocation settles, which makes a
// host-tool turn's `waiting` state invisible on this connection.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations (the `CreateInvocation` operationId).
func (c *ClientWithResponses) CreateInvocationWithResponse(ctx context.Context, params *CreateInvocationParams, body CreateInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateInvocationHTTPResponse, error) {
	rsp, err := c.CreateInvocation(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateInvocationHTTPResponse(rsp)
}

// GetInvocationWithResponse Read authoritative Invocation identity and state
//
// Returns the durable current state. Post-admission failures appear here.
// A credential authenticated for Runtime but denied this read operation
// receives `forbidden`.
// A resource outside the caller's tenant constraint is
// reported as `not_found` when disclosure would reveal its existence.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations/{invocation_id} (the `GetInvocation` operationId).
func (c *ClientWithResponses) GetInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*GetInvocationHTTPResponse, error) {
	rsp, err := c.GetInvocation(ctx, invocationID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetInvocationHTTPResponse(rsp)
}

// CancelInvocationWithResponse Idempotently cancel an Invocation
//
// Atomically makes nonterminal work `cancelled`. Repeating the request,
// or cancelling an Invocation that already completed or failed, returns
// the unchanged authoritative terminal row. A successful response means
// cancellation is durable; provider work stops cooperatively and may
// have already incurred external cost. The request body must be empty.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/cancel (the `CancelInvocation` operationId).
func (c *ClientWithResponses) CancelInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*CancelInvocationHTTPResponse, error) {
	rsp, err := c.CancelInvocation(ctx, invocationID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCancelInvocationHTTPResponse(rsp)
}

// InterruptInvocationWithResponse Gracefully stop an Invocation and keep its work
//
// Asks the turn to stop at its next execution seam and settle
// `completed` with `stop_reason = interrupted`, so its assistant and
// tool messages stay in the next turn's generation context. That is the
// whole difference from cancellation, which drops the turn's work from
// every later turn.
//
// The request is durable and idempotent. Work already at a seam —
// `queued`, `waiting`, or `running` with no live owner — settles before
// this call returns, closing any pending tool calls with synthetic
// results; a subsequently submitted tool result then gets the ordinary
// terminal `409`. Work under a live lease records the request and
// returns the Invocation unchanged, still `running`: its executor stops
// at the next checkpoint boundary, at worst one provider call away.
// Follow the Invocation stream or re-read it to observe settlement.
//
// Interrupting terminal work is a no-op that returns the unchanged
// terminal row. A turn whose definition carries `structured_output` and that
// never published a validated object fails
// `structured_output_unsatisfied` instead of completing — the
// obligation was not met. Usage and settlement evidence are complete:
// the work was kept, so it is charged. The request body must be empty.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/interrupt (the `InterruptInvocation` operationId).
func (c *ClientWithResponses) InterruptInvocationWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*InterruptInvocationHTTPResponse, error) {
	rsp, err := c.InterruptInvocation(ctx, invocationID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInterruptInvocationHTTPResponse(rsp)
}

// NudgeInvocationWithBodyWithResponse Append steering to a running Invocation
//
// Stages caller guidance against a turn that is already running — "focus
// on the marine segment" — without ending it and without discarding the
// work being steered. This is the difference from
// `if_active: supersede`, which rewinds: a superseded turn's assistant
// and tool work product is dropped from the next generation, so steering
// a long turn by supersession throws away exactly what is being steered.
// Unlike Claude's `system.message`, a nudge is durable staged input
// consumed at an execution seam, not an event injected immediately into
// a managed session.
//
// A nudge is not an interrupt. The earliest injection point is the next
// execution seam: the start of the next execution segment, which is a
// host-tool turn's park boundary, or the stop seam where a turn that
// considers itself finished re-enters its loop to answer the input in
// place. An in-flight provider call or tool run is never aborted for it,
// and an Invocation that has been interrupted is never given more to do —
// the interrupt wins the seam and the staged input expires.
//
// A nudge is never implicitly minted and never implicitly converted.
// `POST /v1/invocations` against a busy Session keeps its `if_active`
// semantics; admission is never silently turned into a nudge, and a
// nudge is never silently turned into an Invocation. nvoken holds no
// agent configuration — the definition travels inline on every admission — so
// there is nothing it could legitimately run converted input with.
//
// Input that the turn never takes is settled `expired` when the
// Invocation settles, in the same transaction and under the same Session
// lock this request takes. It never influences a later Invocation.
// Re-sending missed direction as the next Invocation's input is the
// host's call to make, and `GET .../pending-inputs` is where the miss is
// visible.
//
// `content` is text: a string, or an array of text blocks. Image and
// document blocks are accepted on an Invocation's own input but not
// here, because the stop seam carries a continuation as text and staging
// content one seam would silently drop is worse than refusing it.
//
// Requires the same authority as cancelling the Invocation.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
func (c *ClientWithResponses) NudgeInvocationWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*NudgeInvocationHTTPResponse, error) {
	rsp, err := c.NudgeInvocationWithBody(ctx, invocationID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseNudgeInvocationHTTPResponse(rsp)
}

// NudgeInvocationWithResponse Append steering to a running Invocation
//
// Stages caller guidance against a turn that is already running — "focus
// on the marine segment" — without ending it and without discarding the
// work being steered. This is the difference from
// `if_active: supersede`, which rewinds: a superseded turn's assistant
// and tool work product is dropped from the next generation, so steering
// a long turn by supersession throws away exactly what is being steered.
// Unlike Claude's `system.message`, a nudge is durable staged input
// consumed at an execution seam, not an event injected immediately into
// a managed session.
//
// A nudge is not an interrupt. The earliest injection point is the next
// execution seam: the start of the next execution segment, which is a
// host-tool turn's park boundary, or the stop seam where a turn that
// considers itself finished re-enters its loop to answer the input in
// place. An in-flight provider call or tool run is never aborted for it,
// and an Invocation that has been interrupted is never given more to do —
// the interrupt wins the seam and the staged input expires.
//
// A nudge is never implicitly minted and never implicitly converted.
// `POST /v1/invocations` against a busy Session keeps its `if_active`
// semantics; admission is never silently turned into a nudge, and a
// nudge is never silently turned into an Invocation. nvoken holds no
// agent configuration — the definition travels inline on every admission — so
// there is nothing it could legitimately run converted input with.
//
// Input that the turn never takes is settled `expired` when the
// Invocation settles, in the same transaction and under the same Session
// lock this request takes. It never influences a later Invocation.
// Re-sending missed direction as the next Invocation's input is the
// host's call to make, and `GET .../pending-inputs` is where the miss is
// visible.
//
// `content` is text: a string, or an array of text blocks. Image and
// document blocks are accepted on an Invocation's own input but not
// here, because the stop seam carries a continuation as text and staging
// content one seam would silently drop is worse than refusing it.
//
// Requires the same authority as cancelling the Invocation.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/nudge (the `NudgeInvocation` operationId).
func (c *ClientWithResponses) NudgeInvocationWithResponse(ctx context.Context, invocationID InvocationID, body NudgeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*NudgeInvocationHTTPResponse, error) {
	rsp, err := c.NudgeInvocation(ctx, invocationID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseNudgeInvocationHTTPResponse(rsp)
}

// ListPendingInputsWithResponse List staged input for an Invocation
//
// Returns the staged queue in `(created_at, id)` ascending order — the
// order the turn will consume it — with settled rows retained so a
// caller can answer "what did the user say, and did the model see it".
// This, not an event vocabulary, is the reconciliation source for a
// surface that shows queued direction: a drained input also appears on
// the transcript stream as an ordinary `user` message.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations/{invocation_id}/pending-inputs (the `ListPendingInputs` operationId).
func (c *ClientWithResponses) ListPendingInputsWithResponse(ctx context.Context, invocationID InvocationID, params *ListPendingInputsParams, reqEditors ...RequestEditorFn) (*ListPendingInputsHTTPResponse, error) {
	rsp, err := c.ListPendingInputs(ctx, invocationID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListPendingInputsHTTPResponse(rsp)
}

// CancelPendingInputWithResponse Withdraw staged input the turn has not taken
//
// Cancels input that is still `pending`. Cancelling input that is
// already `cancelled` returns it unchanged, so a retry is safe.
//
// Cancel and drain serialize on the row, so whichever commits first
// wins outright: a cancelled input is never seen by the model, and input
// the executor already drained is reported `409 pending_input_settled`
// with the current resource in `details.pending_input` rather than being
// withdrawn from a transcript it is already part of. The request body
// must be empty.
//
// Requires the same authority as cancelling the Invocation.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/pending-inputs/{pending_input_id}/cancel (the `CancelPendingInput` operationId).
func (c *ClientWithResponses) CancelPendingInputWithResponse(ctx context.Context, invocationID InvocationID, pendingInputID PendingInputID, reqEditors ...RequestEditorFn) (*CancelPendingInputHTTPResponse, error) {
	rsp, err := c.CancelPendingInput(ctx, invocationID, pendingInputID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCancelPendingInputHTTPResponse(rsp)
}

// GetInvocationResultWithResponse Read the composed Invocation result
//
// Returns one InvocationResult at any status: the authoritative
// Invocation, this Invocation's canonical messages composed at read
// time, and the output_text convenience projection. The Invocation and
// its messages are read in one repeatable-read snapshot, so the payload
// never shows a terminal status with a missing message tail.
// Authentication, tenant scoping, and the nondisclosing not_found rule
// match the plain Invocation read exactly.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations/{invocation_id}/result (the `GetInvocationResult` operationId).
func (c *ClientWithResponses) GetInvocationResultWithResponse(ctx context.Context, invocationID InvocationID, reqEditors ...RequestEditorFn) (*GetInvocationResultHTTPResponse, error) {
	rsp, err := c.GetInvocationResult(ctx, invocationID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetInvocationResultHTTPResponse(rsp)
}

// ResumeInvocationWithBodyWithResponse Raise the exhausted turn ceiling and resume a paused Invocation
//
// Requeues an Invocation paused on a turn-level consumption ceiling.
// `limits` must contain only the exhausted field, raised above both its
// prior value and the amount already consumed, and the new value must
// remain within installation policy. A Session-budget pause resumes by
// raising or removing the Session budget instead. Deadlines never pause.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
func (c *ClientWithResponses) ResumeInvocationWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ResumeInvocationHTTPResponse, error) {
	rsp, err := c.ResumeInvocationWithBody(ctx, invocationID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseResumeInvocationHTTPResponse(rsp)
}

// ResumeInvocationWithResponse Raise the exhausted turn ceiling and resume a paused Invocation
//
// Requeues an Invocation paused on a turn-level consumption ceiling.
// `limits` must contain only the exhausted field, raised above both its
// prior value and the amount already consumed, and the new value must
// remain within installation policy. A Session-budget pause resumes by
// raising or removing the Session budget instead. Deadlines never pause.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/resume (the `ResumeInvocation` operationId).
func (c *ClientWithResponses) ResumeInvocationWithResponse(ctx context.Context, invocationID InvocationID, body ResumeInvocationJSONRequestBody, reqEditors ...RequestEditorFn) (*ResumeInvocationHTTPResponse, error) {
	rsp, err := c.ResumeInvocation(ctx, invocationID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseResumeInvocationHTTPResponse(rsp)
}

// StreamInvocationWithResponse Resume and tail one Invocation over SSE
//
// Replays durable updates after `cursor`, optionally forwards ephemeral
// output previews for this Invocation, and ends only when this Invocation
// settles or the connection rotates. Durable frames carry an SSE `id`;
// previews and control frames do not. After `stream.resync`, discard
// provisional output and wait for durable state.
//
// The explicit `cursor` query parameter takes precedence over
// `Last-Event-ID`. `stream.end` reason `rotate` means reconnect with the
// last durable ID. `deltas=false` skips preview fan-out without changing
// replay, polling, cursor, or terminal semantics. Disconnecting never
// cancels the Invocation.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations/{invocation_id}/stream (the `StreamInvocation` operationId).
func (c *ClientWithResponses) StreamInvocationWithResponse(ctx context.Context, invocationID InvocationID, params *StreamInvocationParams, reqEditors ...RequestEditorFn) (*StreamInvocationHTTPResponse, error) {
	rsp, err := c.StreamInvocation(ctx, invocationID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseStreamInvocationHTTPResponse(rsp)
}

// ListToolCallsWithResponse Page through durable ToolCall execution records
//
// Returns ToolCalls in execution discovery order. Every execution mode is
// included. The records contain lifecycle and timing facts only. Tool
// inputs and results remain in the canonical Session transcript.
//
// Callback records include a delivery object. Its terminal outcome,
// attempt count, and last HTTP status remain available after the bounded
// delivery transport row is pruned. These records use the same
// authentication, tenant scope, Session constraint, and nondisclosing
// not_found behavior as the Invocation read. Deleting the Session deletes
// these records with the rest of its subtree.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/invocations/{invocation_id}/tool-calls (the `ListToolCalls` operationId).
func (c *ClientWithResponses) ListToolCallsWithResponse(ctx context.Context, invocationID InvocationID, params *ListToolCallsParams, reqEditors ...RequestEditorFn) (*ListToolCallsHTTPResponse, error) {
	rsp, err := c.ListToolCalls(ctx, invocationID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListToolCallsHTTPResponse(rsp)
}

// SubmitHostToolResultsWithBodyWithResponse Submit durable results for pending host ToolCalls
//
// Atomically accepts one bounded batch for a waiting Invocation. The
// first committed result for each ToolCall wins. An equal replay is
// acknowledged as deduplicated; a changed replay conflicts. Partial
// batches leave the Invocation waiting. Closing the final pending call
// queues the same Invocation and its successor execution dispatch before
// returning `202`.
//
// This command accepts only host-mode calls owned by the path
// Invocation and authenticated tenant scope. It is not a generic
// Session append endpoint. The body is limited to 1 MiB; each result
// content value is valid JSON limited to 256 KiB and 32 nesting levels.
//
// `content` accepts any JSON value and the stored transcript retains it
// verbatim. Before a result reaches the model, a string or an array of
// content blocks passes through unchanged; any other value is
// serialized to its compact JSON text and sent as a string, so the
// model sees the same bytes a host that pre-stringifies would send.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
func (c *ClientWithResponses) SubmitHostToolResultsWithBodyWithResponse(ctx context.Context, invocationID InvocationID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHostToolResultsHTTPResponse, error) {
	rsp, err := c.SubmitHostToolResultsWithBody(ctx, invocationID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHostToolResultsHTTPResponse(rsp)
}

// SubmitHostToolResultsWithResponse Submit durable results for pending host ToolCalls
//
// Atomically accepts one bounded batch for a waiting Invocation. The
// first committed result for each ToolCall wins. An equal replay is
// acknowledged as deduplicated; a changed replay conflicts. Partial
// batches leave the Invocation waiting. Closing the final pending call
// queues the same Invocation and its successor execution dispatch before
// returning `202`.
//
// This command accepts only host-mode calls owned by the path
// Invocation and authenticated tenant scope. It is not a generic
// Session append endpoint. The body is limited to 1 MiB; each result
// content value is valid JSON limited to 256 KiB and 32 nesting levels.
//
// `content` accepts any JSON value and the stored transcript retains it
// verbatim. Before a result reaches the model, a string or an array of
// content blocks passes through unchanged; any other value is
// serialized to its compact JSON text and sent as a string, so the
// model sees the same bytes a host that pre-stringifies would send.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/invocations/{invocation_id}/tool-results (the `SubmitHostToolResults` operationId).
func (c *ClientWithResponses) SubmitHostToolResultsWithResponse(ctx context.Context, invocationID InvocationID, body SubmitHostToolResultsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHostToolResultsHTTPResponse, error) {
	rsp, err := c.SubmitHostToolResults(ctx, invocationID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHostToolResultsHTTPResponse(rsp)
}

// ListMCPToolsWithBodyWithResponse Discover and project one remote MCP server's tools
//
// Opens one short-lived streamable-HTTP MCP session through guarded
// public-only egress, drains tools/list pagination, applies the same
// allowlist and projection rules used by Invocation execution, and then
// closes the session. Supplied headers are used only for this request,
// are never logged or returned, and are not persisted.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
func (c *ClientWithResponses) ListMCPToolsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ListMCPToolsHTTPResponse, error) {
	rsp, err := c.ListMCPToolsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListMCPToolsHTTPResponse(rsp)
}

// ListMCPToolsWithResponse Discover and project one remote MCP server's tools
//
// Opens one short-lived streamable-HTTP MCP session through guarded
// public-only egress, drains tools/list pagination, applies the same
// allowlist and projection rules used by Invocation execution, and then
// closes the session. Supplied headers are used only for this request,
// are never logged or returned, and are not persisted.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/mcp/list-tools (the `ListMCPTools` operationId).
func (c *ClientWithResponses) ListMCPToolsWithResponse(ctx context.Context, body ListMCPToolsJSONRequestBody, reqEditors ...RequestEditorFn) (*ListMCPToolsHTTPResponse, error) {
	rsp, err := c.ListMCPTools(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListMCPToolsHTTPResponse(rsp)
}

// ListModelsWithResponse List nvoken's curated model catalog
//
// Returns the complete bounded set of text-generation models nvoken
// intentionally advertises. Catalog membership means nvoken maintains
// metadata for the exact provider/model selection. It does not prove that
// the caller's provider account, region, or selected provider key can access
// the model. Ordering is deterministic but has no semantic meaning.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/models (the `ListModels` operationId).
func (c *ClientWithResponses) ListModelsWithResponse(ctx context.Context, params *ListModelsParams, reqEditors ...RequestEditorFn) (*ListModelsHTTPResponse, error) {
	rsp, err := c.ListModels(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListModelsHTTPResponse(rsp)
}

// GetModelWithResponse Inspect one exact provider/model selection
//
// Returns a descriptor for any valid installed provider and exact model
// ID, including IDs outside the curated catalog. `cataloged` distinguishes
// maintained metadata from tolerant inspection. Pricing is the standard
// local USD estimate used by nvoken's estimated-cost guardrail, not
// provider billing or proof of account access.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/models/{provider}/{model_id} (the `GetModel` operationId).
func (c *ClientWithResponses) GetModelWithResponse(ctx context.Context, provider ModelProvider, modelID string, params *GetModelParams, reqEditors ...RequestEditorFn) (*GetModelHTTPResponse, error) {
	rsp, err := c.GetModel(ctx, provider, modelID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetModelHTTPResponse(rsp)
}

// ListProviderKeysWithResponse List reusable provider key metadata
//
// Viewer, Runtime, and Operator profiles may read metadata within their exact scope; secret material is never readable.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/provider-keys (the `ListProviderKeys` operationId).
func (c *ClientWithResponses) ListProviderKeysWithResponse(ctx context.Context, params *ListProviderKeysParams, reqEditors ...RequestEditorFn) (*ListProviderKeysHTTPResponse, error) {
	rsp, err := c.ListProviderKeys(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListProviderKeysHTTPResponse(rsp)
}

// CreateProviderKeyWithBodyWithResponse Create one reusable provider key
//
// Creates encrypted app or tenant BYOK. App scope requires an
// unconstrained Operator credential inside the app; the credential is
// shared by every tenant of that app and by no other app. A
// tenant-constrained Runtime credential may manage only its exact tenant
// partition. The response contains metadata only.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
func (c *ClientWithResponses) CreateProviderKeyWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateProviderKeyHTTPResponse, error) {
	rsp, err := c.CreateProviderKeyWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateProviderKeyHTTPResponse(rsp)
}

// CreateProviderKeyWithResponse Create one reusable provider key
//
// Creates encrypted app or tenant BYOK. App scope requires an
// unconstrained Operator credential inside the app; the credential is
// shared by every tenant of that app and by no other app. A
// tenant-constrained Runtime credential may manage only its exact tenant
// partition. The response contains metadata only.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/provider-keys (the `CreateProviderKey` operationId).
func (c *ClientWithResponses) CreateProviderKeyWithResponse(ctx context.Context, body CreateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateProviderKeyHTTPResponse, error) {
	rsp, err := c.CreateProviderKey(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateProviderKeyHTTPResponse(rsp)
}

// RevokeProviderKeyWithResponse Revoke a provider key and destroy live encrypted versions
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/provider-keys/{provider_key_id} (the `RevokeProviderKey` operationId).
func (c *ClientWithResponses) RevokeProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*RevokeProviderKeyHTTPResponse, error) {
	rsp, err := c.RevokeProviderKey(ctx, providerKeyID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRevokeProviderKeyHTTPResponse(rsp)
}

// GetProviderKeyWithResponse Read provider key metadata
//
// Viewer, Runtime, and Operator profiles may read secret-free metadata within their exact scope.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/provider-keys/{provider_key_id} (the `GetProviderKey` operationId).
func (c *ClientWithResponses) GetProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*GetProviderKeyHTTPResponse, error) {
	rsp, err := c.GetProviderKey(ctx, providerKeyID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetProviderKeyHTTPResponse(rsp)
}

// RotateProviderKeyWithBodyWithResponse Rotate a provider key
//
// Replaces the active version, optionally preserving a bounded overlap.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
func (c *ClientWithResponses) RotateProviderKeyWithBodyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RotateProviderKeyHTTPResponse, error) {
	rsp, err := c.RotateProviderKeyWithBody(ctx, providerKeyID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRotateProviderKeyHTTPResponse(rsp)
}

// RotateProviderKeyWithResponse Rotate a provider key
//
// Replaces the active version, optionally preserving a bounded overlap.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/provider-keys/{provider_key_id}/rotate (the `RotateProviderKey` operationId).
func (c *ClientWithResponses) RotateProviderKeyWithResponse(ctx context.Context, providerKeyID ProviderKeyID, body RotateProviderKeyJSONRequestBody, reqEditors ...RequestEditorFn) (*RotateProviderKeyHTTPResponse, error) {
	rsp, err := c.RotateProviderKey(ctx, providerKeyID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRotateProviderKeyHTTPResponse(rsp)
}

// GetProviderKeyUsageWithResponse Read the token usage rollup for one provider key
//
// Aggregates durable checkpoint evidence across every Invocation that
// bound the provider key: how many Invocations used it, when a model call
// last used it, and the summed model usage. Derived at read time from the
// transcript record, never from a separately maintained counter.
// Authorization matches reading the provider key itself.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/provider-keys/{provider_key_id}/usage (the `GetProviderKeyUsage` operationId).
func (c *ClientWithResponses) GetProviderKeyUsageWithResponse(ctx context.Context, providerKeyID ProviderKeyID, reqEditors ...RequestEditorFn) (*GetProviderKeyUsageHTTPResponse, error) {
	rsp, err := c.GetProviderKeyUsage(ctx, providerKeyID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetProviderKeyUsageHTTPResponse(rsp)
}

// ListSessionsWithResponse List authoritative Sessions
//
// Returns newest-first Session identity and current nonterminal
// Invocation state. Exact filters combine with AND. Tenant filtering and
// cursor binding follow the Invocation-list rules. `agent_id` and
// `agent_key` are mutually exclusive and normalize to the same cursor
// filter.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions (the `ListSessions` operationId).
func (c *ClientWithResponses) ListSessionsWithResponse(ctx context.Context, params *ListSessionsParams, reqEditors ...RequestEditorFn) (*ListSessionsHTTPResponse, error) {
	rsp, err := c.ListSessions(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListSessionsHTTPResponse(rsp)
}

// CreateSessionWithBodyWithResponse Create or seed a Session without admitting an Invocation
//
// Creates a Session with zero Invocations and optional host-asserted
// starting history. Every body field is optional. An omitted `agent_key` leaves the
// Session unbound: `agent_id` is null until the first admitted
// Invocation binds it, and the binding is immutable once set either way;
// `seed_messages` therefore requires `agent_key`. A `session_key`
// requires an `agent_key`, because key uniqueness
// and lookup are scoped per (tenant partition, Agent); a keyed create
// is an upsert that accepts equal supplied Session options and returns
// `session_options_conflict` when a supplied value differs.
// Without a `session_key`, every call creates a fresh Session.
// Precedence for the tenant partition is credential constraint,
// explicit `tenant_key`, then the default partition.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
func (c *ClientWithResponses) CreateSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateSessionHTTPResponse, error) {
	rsp, err := c.CreateSessionWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateSessionHTTPResponse(rsp)
}

// CreateSessionWithResponse Create or seed a Session without admitting an Invocation
//
// Creates a Session with zero Invocations and optional host-asserted
// starting history. Every body field is optional. An omitted `agent_key` leaves the
// Session unbound: `agent_id` is null until the first admitted
// Invocation binds it, and the binding is immutable once set either way;
// `seed_messages` therefore requires `agent_key`. A `session_key`
// requires an `agent_key`, because key uniqueness
// and lookup are scoped per (tenant partition, Agent); a keyed create
// is an upsert that accepts equal supplied Session options and returns
// `session_options_conflict` when a supplied value differs.
// Without a `session_key`, every call creates a fresh Session.
// Precedence for the tenant partition is credential constraint,
// explicit `tenant_key`, then the default partition.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/sessions (the `CreateSession` operationId).
func (c *ClientWithResponses) CreateSessionWithResponse(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateSessionHTTPResponse, error) {
	rsp, err := c.CreateSession(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateSessionHTTPResponse(rsp)
}

// DeleteSessionWithResponse Erase a Session and everything under it
//
// Removes the Session, its Invocations, transcript messages, checkpoints,
// tool calls, provider artifacts, compactions, provider-key and MCP
// bindings, and undelivered webhooks. The erasure is immediate and
// irreversible; a subsequent read is `not_found`.
//
// An Invocation still running is stopped. No cancellation is recorded,
// because there is nothing left to record it against — the Invocation is
// removed rather than settled, and no `invocation.settled` webhook
// is emitted for it. A host that needs a settled record should cancel and
// observe the terminal state before deleting.
//
// Erasure is scoped like every other Session operation: an unknown or
// out-of-scope `session_id` returns `not_found`, so a retry after a lost
// response can treat `404` as already-done. The `delete_session`
// operation belongs to the Runtime and Operator profiles; a Viewer
// credential cannot erase a transcript.
//
// **This is not account deletion by itself.** nvoken keeps no account
// tombstone, so a host honouring a deletion request must first stop
// admitting work for that tenant, then page `GET /v1/sessions` and delete
// until the list is empty. Otherwise a concurrent request creates a new
// Session behind the sweep.
//
// Two consequences worth planning for. Usage reporting shrinks
// retroactively: `GET /v1/usage/daily` is computed at read time from
// Invocation evidence that erasure removes, which is why the endpoint is
// documented as operational visibility rather than a billing ledger — a
// billing host records usage at settlement, keyed by Invocation id.
// And the erased Invocations' idempotency keys become free for reuse,
// consistent with the documented guarantee that deduplication holds while
// the original Invocation is retained.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/sessions/{session_id} (the `DeleteSession` operationId).
func (c *ClientWithResponses) DeleteSessionWithResponse(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*DeleteSessionHTTPResponse, error) {
	rsp, err := c.DeleteSession(ctx, sessionID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteSessionHTTPResponse(rsp)
}

// GetSessionWithResponse Read authoritative Session identity and current state
//
// An installation-wide credential may resolve a Session in any tenant
// partition. A tenant-constrained credential resolves only Sessions
// in its partition. Missing, incompatible, and undisclosable resources use
// `not_found`; a credential denied the read operation itself receives
// `forbidden`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions/{session_id} (the `GetSession` operationId).
func (c *ClientWithResponses) GetSessionWithResponse(ctx context.Context, sessionID SessionID, reqEditors ...RequestEditorFn) (*GetSessionHTTPResponse, error) {
	rsp, err := c.GetSession(ctx, sessionID, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSessionHTTPResponse(rsp)
}

// UpdateSessionWithBodyWithResponse Merge host metadata into a Session
//
// Merges a metadata patch: a present key replaces its value, an explicit
// `null` deletes that key, and a key the patch does not mention survives.
//
// Merge rather than replace, because independent writers share this map —
// a conversation UI writing a title, correlation tooling writing a trace
// id — and a full replacement would make each silently discard the
// other's keys. The merge happens under the Session lock, so two
// concurrent patches compose instead of one overwriting the other's read.
//
// `"metadata": null` is refused rather than guessed at: it could mean
// "clear everything" or "leave it alone", and either reading is
// destructive or silent. Delete keys one at a time.
//
// Bounds apply to the merged result, not to the patch, so a patch that
// deletes as many keys as it adds is not refused for a count it never
// produces. Requires the `update_session` operation.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
func (c *ClientWithResponses) UpdateSessionWithBodyWithResponse(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateSessionHTTPResponse, error) {
	rsp, err := c.UpdateSessionWithBody(ctx, sessionID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateSessionHTTPResponse(rsp)
}

// UpdateSessionWithResponse Merge host metadata into a Session
//
// Merges a metadata patch: a present key replaces its value, an explicit
// `null` deletes that key, and a key the patch does not mention survives.
//
// Merge rather than replace, because independent writers share this map —
// a conversation UI writing a title, correlation tooling writing a trace
// id — and a full replacement would make each silently discard the
// other's keys. The merge happens under the Session lock, so two
// concurrent patches compose instead of one overwriting the other's read.
//
// `"metadata": null` is refused rather than guessed at: it could mean
// "clear everything" or "leave it alone", and either reading is
// destructive or silent. Delete keys one at a time.
//
// Bounds apply to the merged result, not to the patch, so a patch that
// deletes as many keys as it adds is not refused for a count it never
// produces. Requires the `update_session` operation.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/sessions/{session_id} (the `UpdateSession` operationId).
func (c *ClientWithResponses) UpdateSessionWithResponse(ctx context.Context, sessionID SessionID, body UpdateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateSessionHTTPResponse, error) {
	rsp, err := c.UpdateSession(ctx, sessionID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateSessionHTTPResponse(rsp)
}

// ListSessionCompactionsWithResponse Page through immutable Session compaction records
//
// Returns newest-first diagnostic records for every attempted compaction
// pass. `applied` records include the private summary projection and its
// model usage. `fell_through` records identify the failure class and may
// include usage when the provider returned trustworthy evidence. Only
// applied records change future model context; neither kind changes the
// canonical Session transcript.
//
// The summary is derived from the caller's own transcript and therefore
// uses the same authorization and Session scoping as transcript reads.
// The opaque cursor is bound to the authenticated app and Session.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions/{session_id}/compactions (the `ListSessionCompactions` operationId).
func (c *ClientWithResponses) ListSessionCompactionsWithResponse(ctx context.Context, sessionID SessionID, params *ListSessionCompactionsParams, reqEditors ...RequestEditorFn) (*ListSessionCompactionsHTTPResponse, error) {
	rsp, err := c.ListSessionCompactions(ctx, sessionID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListSessionCompactionsHTTPResponse(rsp)
}

// ForkSessionWithBodyWithResponse Copy a Session prefix into a new Session
//
// Creates a new Session in the source Session's tenant and Agent scope,
// copying every canonical message through `from_message` inclusively.
// The source is untouched. The child stores durable Session and message
// lineage, but copied messages no longer belong to the source
// Invocations. Their `origin`, per-turn `user_key`, and resolved message
// phase are preserved.
//
// Usage and compaction summaries are not copied. Child usage starts at
// zero and the child starts uncompacted. Retention and metadata come only
// from `session_options` on this request; no Session option is inherited.
// A `session_key` has the same tenant/Agent-scoped upsert behavior as
// Session creation.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
func (c *ClientWithResponses) ForkSessionWithBodyWithResponse(ctx context.Context, sessionID SessionID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ForkSessionHTTPResponse, error) {
	rsp, err := c.ForkSessionWithBody(ctx, sessionID, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseForkSessionHTTPResponse(rsp)
}

// ForkSessionWithResponse Copy a Session prefix into a new Session
//
// Creates a new Session in the source Session's tenant and Agent scope,
// copying every canonical message through `from_message` inclusively.
// The source is untouched. The child stores durable Session and message
// lineage, but copied messages no longer belong to the source
// Invocations. Their `origin`, per-turn `user_key`, and resolved message
// phase are preserved.
//
// Usage and compaction summaries are not copied. Child usage starts at
// zero and the child starts uncompacted. Retention and metadata come only
// from `session_options` on this request; no Session option is inherited.
// A `session_key` has the same tenant/Agent-scoped upsert behavior as
// Session creation.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/sessions/{session_id}/fork (the `ForkSession` operationId).
func (c *ClientWithResponses) ForkSessionWithResponse(ctx context.Context, sessionID SessionID, body ForkSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*ForkSessionHTTPResponse, error) {
	rsp, err := c.ForkSession(ctx, sessionID, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseForkSessionHTTPResponse(rsp)
}

// ListSessionMessagesWithResponse Page through the canonical Session transcript
//
// Returns persisted SessionMessage rows in ascending sequence order.
// The opaque forward cursor is bound to the authenticated caller and
// Session. This history endpoint contains no lifecycle or live-preview
// copies.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions/{session_id}/messages (the `ListSessionMessages` operationId).
func (c *ClientWithResponses) ListSessionMessagesWithResponse(ctx context.Context, sessionID SessionID, params *ListSessionMessagesParams, reqEditors ...RequestEditorFn) (*ListSessionMessagesHTTPResponse, error) {
	rsp, err := c.ListSessionMessages(ctx, sessionID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListSessionMessagesHTTPResponse(rsp)
}

// GetSessionTranscriptWithResponse Drain a fixed-cut incremental transcript snapshot
//
// Projects canonical messages and append-only Invocation lifecycle
// changes. Supply a prior `resume_cursor` as `cursor` to drain newer
// durable state. Continue a multi-page fixed cut with `page_token` until
// `has_more` is false. Each page contains one phase: all message pages are
// delivered before lifecycle-change pages, so terminal state cannot
// precede the transcript rows committed with it. Omitting both positions
// starts at the beginning of the retained Session.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions/{session_id}/transcript (the `GetSessionTranscript` operationId).
func (c *ClientWithResponses) GetSessionTranscriptWithResponse(ctx context.Context, sessionID SessionID, params *GetSessionTranscriptParams, reqEditors ...RequestEditorFn) (*GetSessionTranscriptHTTPResponse, error) {
	rsp, err := c.GetSessionTranscript(ctx, sessionID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSessionTranscriptHTTPResponse(rsp)
}

// StreamSessionTranscriptWithResponse Replay and tail one Session transcript over SSE
//
// Opens a resumable Server-Sent Events projection over the same fixed-cut
// transcript read model as the JSON endpoint. With deltas enabled, the
// server subscribes to live fan-out before its first Postgres drain. In
// either mode it re-drains Postgres on a bounded poll and closes after
// authoritative terminal reconciliation or deliberate rotation.
// Disconnecting never cancels the Invocation.
//
// Every nonempty `transcript.update` frame carries
// `id: <resume_cursor>`; that opaque ID is the only replay position
// clients persist. `output_text.delta`, `thinking.delta`,
// `stream.resync`, and `stream.end` never carry an `id`. Deltas are
// ephemeral and may be lost; after `stream.resync`, discard provisional
// output and wait for canonical messages. `stream.end` reason `terminal`
// means the final Postgres drain observed no nonterminal Invocation.
// Reason `rotate` means reconnect with the last durable ID. An abnormal
// close has no terminal meaning. `deltas=false` skips preview fan-out
// without changing replay, polling, cursor, or terminal semantics.
//
// The explicit `cursor` query parameter takes precedence over
// `Last-Event-ID`. Bearer authentication requires an SSE-capable HTTP
// client that can set the `Authorization` header; the browser EventSource
// constructor alone cannot do so. The server emits `retry: 1000` as its
// default reconnect delay.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/sessions/{session_id}/transcript/stream (the `StreamSessionTranscript` operationId).
func (c *ClientWithResponses) StreamSessionTranscriptWithResponse(ctx context.Context, sessionID SessionID, params *StreamSessionTranscriptParams, reqEditors ...RequestEditorFn) (*StreamSessionTranscriptHTTPResponse, error) {
	rsp, err := c.StreamSessionTranscript(ctx, sessionID, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseStreamSessionTranscriptHTTPResponse(rsp)
}

// GetDailyUsageWithResponse Read daily usage rollups
//
// Aggregates durable model-call evidence — invocation checkpoints plus
// session compaction calls — into per-day buckets keyed by app,
// provider, and model, summing token counts and estimated cost. Derived
// at read time from the transcript record, never from a separately
// maintained counter.
//
// An app-bound credential reads its own app. An app-less presentation
// spans every registered app; for a console issuer token that requires
// the admin claim.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/usage/daily (the `GetDailyUsage` operationId).
func (c *ClientWithResponses) GetDailyUsageWithResponse(ctx context.Context, params *GetDailyUsageParams, reqEditors ...RequestEditorFn) (*GetDailyUsageHTTPResponse, error) {
	rsp, err := c.GetDailyUsage(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetDailyUsageHTTPResponse(rsp)
}

// ParseListAgentsHTTPResponse parses an HTTP response from a ListAgentsWithResponse call
func ParseListAgentsHTTPResponse(rsp *http.Response) (*ListAgentsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAgentsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AgentList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListAgentsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetAgentHTTPResponse parses an HTTP response from a GetAgentWithResponse call
func ParseGetAgentHTTPResponse(rsp *http.Response) (*GetAgentHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAgentHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Agent
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetAgentHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListAppsHTTPResponse parses an HTTP response from a ListAppsWithResponse call
func ParseListAppsHTTPResponse(rsp *http.Response) (*ListAppsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAppsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AppList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListAppsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseRegisterAppHTTPResponse parses an HTTP response from a RegisterAppWithResponse call
func ParseRegisterAppHTTPResponse(rsp *http.Response) (*RegisterAppHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RegisterAppHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest App
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers RegisterAppHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetAppHTTPResponse parses an HTTP response from a GetAppWithResponse call
func ParseGetAppHTTPResponse(rsp *http.Response) (*GetAppHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAppHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest App
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetAppHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseUpdateAppHTTPResponse parses an HTTP response from a UpdateAppWithResponse call
func ParseUpdateAppHTTPResponse(rsp *http.Response) (*UpdateAppHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAppHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest App
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers UpdateAppHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseCreateBudgetHTTPResponse parses an HTTP response from a CreateBudgetWithResponse call
func ParseCreateBudgetHTTPResponse(rsp *http.Response) (*CreateBudgetHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateBudgetHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest Budget
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseDeleteBudgetHTTPResponse parses an HTTP response from a DeleteBudgetWithResponse call
func ParseDeleteBudgetHTTPResponse(rsp *http.Response) (*DeleteBudgetHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteBudgetHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseGetBudgetHTTPResponse parses an HTTP response from a GetBudgetWithResponse call
func ParseGetBudgetHTTPResponse(rsp *http.Response) (*GetBudgetHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBudgetHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Budget
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseUpdateBudgetHTTPResponse parses an HTTP response from a UpdateBudgetWithResponse call
func ParseUpdateBudgetHTTPResponse(rsp *http.Response) (*UpdateBudgetHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateBudgetHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Budget
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseListInvocationsHTTPResponse parses an HTTP response from a ListInvocationsWithResponse call
func ParseListInvocationsHTTPResponse(rsp *http.Response) (*ListInvocationsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListInvocationsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest InvocationList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListInvocationsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseCreateInvocationHTTPResponse parses an HTTP response from a CreateInvocationWithResponse call
func ParseCreateInvocationHTTPResponse(rsp *http.Response) (*CreateInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest Invocation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	case rsp.StatusCode == 202:
		// Content-type (text/event-stream) unsupported

	}

	switch {
	case rsp.StatusCode == 429:
		var headers CreateInvocationHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetInvocationHTTPResponse parses an HTTP response from a GetInvocationWithResponse call
func ParseGetInvocationHTTPResponse(rsp *http.Response) (*GetInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Invocation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetInvocationHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseCancelInvocationHTTPResponse parses an HTTP response from a CancelInvocationWithResponse call
func ParseCancelInvocationHTTPResponse(rsp *http.Response) (*CancelInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CancelInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Invocation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseInterruptInvocationHTTPResponse parses an HTTP response from a InterruptInvocationWithResponse call
func ParseInterruptInvocationHTTPResponse(rsp *http.Response) (*InterruptInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &InterruptInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Invocation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseNudgeInvocationHTTPResponse parses an HTTP response from a NudgeInvocationWithResponse call
func ParseNudgeInvocationHTTPResponse(rsp *http.Response) (*NudgeInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &NudgeInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest NudgeAcknowledgement
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseListPendingInputsHTTPResponse parses an HTTP response from a ListPendingInputsWithResponse call
func ParseListPendingInputsHTTPResponse(rsp *http.Response) (*ListPendingInputsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListPendingInputsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PendingInputList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseCancelPendingInputHTTPResponse parses an HTTP response from a CancelPendingInputWithResponse call
func ParseCancelPendingInputHTTPResponse(rsp *http.Response) (*CancelPendingInputHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CancelPendingInputHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PendingInput
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseGetInvocationResultHTTPResponse parses an HTTP response from a GetInvocationResultWithResponse call
func ParseGetInvocationResultHTTPResponse(rsp *http.Response) (*GetInvocationResultHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetInvocationResultHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest InvocationResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetInvocationResultHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseResumeInvocationHTTPResponse parses an HTTP response from a ResumeInvocationWithResponse call
func ParseResumeInvocationHTTPResponse(rsp *http.Response) (*ResumeInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ResumeInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest Invocation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ResumeInvocationHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseStreamInvocationHTTPResponse parses an HTTP response from a StreamInvocationWithResponse call
func ParseStreamInvocationHTTPResponse(rsp *http.Response) (*StreamInvocationHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &StreamInvocationHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers StreamInvocationHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListToolCallsHTTPResponse parses an HTTP response from a ListToolCallsWithResponse call
func ParseListToolCallsHTTPResponse(rsp *http.Response) (*ListToolCallsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListToolCallsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ToolCallList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListToolCallsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseSubmitHostToolResultsHTTPResponse parses an HTTP response from a SubmitHostToolResultsWithResponse call
func ParseSubmitHostToolResultsHTTPResponse(rsp *http.Response) (*SubmitHostToolResultsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitHostToolResultsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest SubmitHostToolResultsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseListMCPToolsHTTPResponse parses an HTTP response from a ListMCPToolsWithResponse call
func ParseListMCPToolsHTTPResponse(rsp *http.Response) (*ListMCPToolsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListMCPToolsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MCPListToolsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest MCPDiscoveryFailed
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListMCPToolsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListModelsHTTPResponse parses an HTTP response from a ListModelsWithResponse call
func ParseListModelsHTTPResponse(rsp *http.Response) (*ListModelsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListModelsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ModelList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case rsp.StatusCode == 304:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers ListModelsHTTPResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	case rsp.StatusCode == 304:
		var headers ListModelsHTTPResponse304Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers304 = &headers
	case rsp.StatusCode == 429:
		var headers ListModelsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetModelHTTPResponse parses an HTTP response from a GetModelWithResponse call
func ParseGetModelHTTPResponse(rsp *http.Response) (*GetModelHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetModelHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ModelDescriptor
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case rsp.StatusCode == 304:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 200:
		var headers GetModelHTTPResponse200Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers200 = &headers
	case rsp.StatusCode == 304:
		var headers GetModelHTTPResponse304Headers
		if values := rsp.Header.Values("ETag"); len(values) > 0 {
			var value string
			if err := runtime.BindStyledParameterWithOptions("simple", "ETag", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: ""}); err != nil {
				return nil, err
			}
			headers.ETag = &value
		}
		response.Headers304 = &headers
	case rsp.StatusCode == 429:
		var headers GetModelHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListProviderKeysHTTPResponse parses an HTTP response from a ListProviderKeysWithResponse call
func ParseListProviderKeysHTTPResponse(rsp *http.Response) (*ListProviderKeysHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListProviderKeysHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ProviderKeyList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseCreateProviderKeyHTTPResponse parses an HTTP response from a CreateProviderKeyWithResponse call
func ParseCreateProviderKeyHTTPResponse(rsp *http.Response) (*CreateProviderKeyHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateProviderKeyHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ProviderKey
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseRevokeProviderKeyHTTPResponse parses an HTTP response from a RevokeProviderKeyWithResponse call
func ParseRevokeProviderKeyHTTPResponse(rsp *http.Response) (*RevokeProviderKeyHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RevokeProviderKeyHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ProviderKey
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseGetProviderKeyHTTPResponse parses an HTTP response from a GetProviderKeyWithResponse call
func ParseGetProviderKeyHTTPResponse(rsp *http.Response) (*GetProviderKeyHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetProviderKeyHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ProviderKey
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseRotateProviderKeyHTTPResponse parses an HTTP response from a RotateProviderKeyWithResponse call
func ParseRotateProviderKeyHTTPResponse(rsp *http.Response) (*RotateProviderKeyHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RotateProviderKeyHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ProviderKey
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseGetProviderKeyUsageHTTPResponse parses an HTTP response from a GetProviderKeyUsageWithResponse call
func ParseGetProviderKeyUsageHTTPResponse(rsp *http.Response) (*GetProviderKeyUsageHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetProviderKeyUsageHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ProviderKeyUsage
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseListSessionsHTTPResponse parses an HTTP response from a ListSessionsWithResponse call
func ParseListSessionsHTTPResponse(rsp *http.Response) (*ListSessionsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListSessionsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListSessionsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseCreateSessionHTTPResponse parses an HTTP response from a CreateSessionWithResponse call
func ParseCreateSessionHTTPResponse(rsp *http.Response) (*CreateSessionHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateSessionHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest Session
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest ErrorResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers CreateSessionHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseDeleteSessionHTTPResponse parses an HTTP response from a DeleteSessionWithResponse call
func ParseDeleteSessionHTTPResponse(rsp *http.Response) (*DeleteSessionHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteSessionHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers DeleteSessionHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetSessionHTTPResponse parses an HTTP response from a GetSessionWithResponse call
func ParseGetSessionHTTPResponse(rsp *http.Response) (*GetSessionHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSessionHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Session
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetSessionHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseUpdateSessionHTTPResponse parses an HTTP response from a UpdateSessionWithResponse call
func ParseUpdateSessionHTTPResponse(rsp *http.Response) (*UpdateSessionHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateSessionHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Session
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers UpdateSessionHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListSessionCompactionsHTTPResponse parses an HTTP response from a ListSessionCompactionsWithResponse call
func ParseListSessionCompactionsHTTPResponse(rsp *http.Response) (*ListSessionCompactionsHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListSessionCompactionsHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionCompactionList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListSessionCompactionsHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseForkSessionHTTPResponse parses an HTTP response from a ForkSessionWithResponse call
func ParseForkSessionHTTPResponse(rsp *http.Response) (*ForkSessionHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ForkSessionHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest Session
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ForkSessionHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseListSessionMessagesHTTPResponse parses an HTTP response from a ListSessionMessagesWithResponse call
func ParseListSessionMessagesHTTPResponse(rsp *http.Response) (*ListSessionMessagesHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListSessionMessagesHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionMessageList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers ListSessionMessagesHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetSessionTranscriptHTTPResponse parses an HTTP response from a GetSessionTranscriptWithResponse call
func ParseGetSessionTranscriptHTTPResponse(rsp *http.Response) (*GetSessionTranscriptHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSessionTranscriptHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest TranscriptSnapshot
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetSessionTranscriptHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseStreamSessionTranscriptHTTPResponse parses an HTTP response from a StreamSessionTranscriptWithResponse call
func ParseStreamSessionTranscriptHTTPResponse(rsp *http.Response) (*StreamSessionTranscriptHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &StreamSessionTranscriptHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest RateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers StreamSessionTranscriptHTTPResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetDailyUsageHTTPResponse parses an HTTP response from a GetDailyUsageWithResponse call
func ParseGetDailyUsageHTTPResponse(rsp *http.Response) (*GetDailyUsageHTTPResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetDailyUsageHTTPResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest DailyUsage
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest InvalidRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthenticated
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Internal
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Unavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// WebhookInitiator sends OpenAPI 3.1 webhook requests to target URLs.
// Modeled on the generated Client, but with no stored Server -- the full
// target URL is provided per-call by the caller (typically discovered
// from a subscription registration).
type WebhookInitiator struct {
	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// WebhookInitiatorOption allows setting custom parameters during construction.
type WebhookInitiatorOption func(*WebhookInitiator) error

// NewWebhookInitiator creates a new WebhookInitiator with reasonable defaults.
func NewWebhookInitiator(opts ...WebhookInitiatorOption) (*WebhookInitiator, error) {
	initiator := WebhookInitiator{}
	for _, o := range opts {
		if err := o(&initiator); err != nil {
			return nil, err
		}
	}
	if initiator.Client == nil {
		initiator.Client = &http.Client{}
	}
	return &initiator, nil
}

// WithWebhookHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithWebhookHTTPClient(doer HttpRequestDoer) WebhookInitiatorOption {
	return func(p *WebhookInitiator) error {
		p.Client = doer
		return nil
	}
}

// WithWebhookRequestEditorFn allows setting up a callback function, which
// will be called right before sending the webhook request. This can be
// used to mutate the request, e.g. to add signature headers.
func WithWebhookRequestEditorFn(fn RequestEditorFn) WebhookInitiatorOption {
	return func(p *WebhookInitiator) error {
		p.RequestEditors = append(p.RequestEditors, fn)
		return nil
	}
}

func (p *WebhookInitiator) applyWebhookEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range p.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// WebhookInitiatorInterface is the interface specification for the webhook initiator.
type WebhookInitiatorInterface interface {
	// ReceiveToolCallbackWithBody fires the toolCallback webhook with any body
	ReceiveToolCallbackWithBody(ctx context.Context, targetURL string, params *ReceiveToolCallbackParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	ReceiveToolCallbackWithApplicationVndNvokenToolCallbackPlusJSONVersion1Body(ctx context.Context, targetURL string, params *ReceiveToolCallbackParams, body ReceiveToolCallbackApplicationVndNvokenToolCallbackPlusJSONVersion1RequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)
}

func (p *WebhookInitiator) ReceiveToolCallbackWithBody(ctx context.Context, targetURL string, params *ReceiveToolCallbackParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveToolCallbackWebhookRequestWithBody(targetURL, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := p.applyWebhookEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return p.Client.Do(req)
}

func (p *WebhookInitiator) ReceiveToolCallbackWithApplicationVndNvokenToolCallbackPlusJSONVersion1Body(ctx context.Context, targetURL string, params *ReceiveToolCallbackParams, body ReceiveToolCallbackApplicationVndNvokenToolCallbackPlusJSONVersion1RequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReceiveToolCallbackWebhookRequestWithApplicationVndNvokenToolCallbackPlusJSONVersion1Body(targetURL, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := p.applyWebhookEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return p.Client.Do(req)
}

// NewReceiveToolCallbackWebhookRequestWithApplicationVndNvokenToolCallbackPlusJSONVersion1Body builds a application/vnd.nvoken.tool-callback+json; version=1 POST request for the toolCallback webhook
func NewReceiveToolCallbackWebhookRequestWithApplicationVndNvokenToolCallbackPlusJSONVersion1Body(targetURL string, params *ReceiveToolCallbackParams, body ReceiveToolCallbackApplicationVndNvokenToolCallbackPlusJSONVersion1RequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReceiveToolCallbackWebhookRequestWithBody(targetURL, params, "application/vnd.nvoken.tool-callback+json; version=1", bodyReader)
}

// NewReceiveToolCallbackWebhookRequestWithBody builds a POST request for the toolCallback webhook with any body
func NewReceiveToolCallbackWebhookRequestWithBody(targetURL string, params *ReceiveToolCallbackParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error
	_ = err

	reqURL, err := url.Parse(targetURL)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, reqURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		var headerParam0 string

		headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Signature", params.XNvokenSignature, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Signature", headerParam0)

		var headerParam1 string

		headerParam1, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Signature-Version", params.XNvokenSignatureVersion, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Signature-Version", headerParam1)

		var headerParam2 string

		headerParam2, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Timestamp", params.XNvokenTimestamp, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "integer", Format: "int64"})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Timestamp", headerParam2)

		var headerParam3 string

		headerParam3, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Delivery-Id", params.XNvokenDeliveryID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Delivery-Id", headerParam3)

		var headerParam4 string

		headerParam4, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Signing-Key-Id", params.XNvokenSigningKeyID, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Signing-Key-Id", headerParam4)

		var headerParam5 string

		headerParam5, err = runtime.StyleParamWithOptions("simple", false, "X-Nvoken-Signing-Key-Version", params.XNvokenSigningKeyVersion, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "integer", Format: "int64"})
		if err != nil {
			return nil, err
		}

		req.Header.Set("X-Nvoken-Signing-Key-Version", headerParam5)

		var headerParam6 string

		headerParam6, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
		if err != nil {
			return nil, err
		}

		req.Header.Set("Idempotency-Key", headerParam6)

	}
	return req, nil
}
