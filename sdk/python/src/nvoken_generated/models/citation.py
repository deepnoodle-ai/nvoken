# coding: utf-8

"""
    nvoken Runtime API

    This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI's bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller's behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken's defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from nvoken_generated.models.char_location_citation import CharLocationCitation
from nvoken_generated.models.url_citation import URLCitation
from nvoken_generated.models.web_search_result_location_citation import WebSearchResultLocationCitation
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

CITATION_ONE_OF_SCHEMAS = ["CharLocationCitation", "URLCitation", "WebSearchResultLocationCitation"]

class Citation(BaseModel):
    """
    Citation
    """
    # data type: CharLocationCitation
    oneof_schema_1_validator: Optional[CharLocationCitation] = None
    # data type: WebSearchResultLocationCitation
    oneof_schema_2_validator: Optional[WebSearchResultLocationCitation] = None
    # data type: URLCitation
    oneof_schema_3_validator: Optional[URLCitation] = None
    actual_instance: Optional[Union[CharLocationCitation, URLCitation, WebSearchResultLocationCitation]] = None
    one_of_schemas: Set[str] = { "CharLocationCitation", "URLCitation", "WebSearchResultLocationCitation" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Citation.model_construct()
        error_messages = []
        match = 0
        # validate data type: CharLocationCitation
        if not isinstance(v, CharLocationCitation):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CharLocationCitation`")
        else:
            match += 1
        # validate data type: WebSearchResultLocationCitation
        if not isinstance(v, WebSearchResultLocationCitation):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WebSearchResultLocationCitation`")
        else:
            match += 1
        # validate data type: URLCitation
        if not isinstance(v, URLCitation):
            error_messages.append(f"Error! Input type `{type(v)}` is not `URLCitation`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Citation with oneOf schemas: CharLocationCitation, URLCitation, WebSearchResultLocationCitation. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Citation with oneOf schemas: CharLocationCitation, URLCitation, WebSearchResultLocationCitation. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into CharLocationCitation
        try:
            instance.actual_instance = CharLocationCitation.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into WebSearchResultLocationCitation
        try:
            instance.actual_instance = WebSearchResultLocationCitation.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into URLCitation
        try:
            instance.actual_instance = URLCitation.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Citation with oneOf schemas: CharLocationCitation, URLCitation, WebSearchResultLocationCitation. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Citation with oneOf schemas: CharLocationCitation, URLCitation, WebSearchResultLocationCitation. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], CharLocationCitation, URLCitation, WebSearchResultLocationCitation]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
