# coding: utf-8

"""
    nvoken API

    nvoken runs agent turns for you. You describe a turn — an Agent Definition and some input — and nvoken queues it, runs it in the background, keeps running it across restarts and failures, and lets you either watch it live or come back for the result later.  Your application stays in charge of what your agents are and when they run. nvoken owns the conversation: it stores the messages, tracks the state of every turn, and handles talking to the model providers.  ## Getting started  `POST /v1/invocations` starts a turn and returns a `202` right away. From there:  - Follow it live with `GET /v1/invocations/{invocation_id}/stream`, or   read `GET /v1/invocations/{invocation_id}/result` whenever you want the   finished answer. Disconnecting never cancels anything. - If your agent uses tools you run yourself, the turn stops with status   `waiting` and lists what it needs. Run them, post the results to   `/tool-results`, and the turn continues where it left off. - Sessions carry conversation history from one turn to the next. They   last until you delete them, or until a retention window you set runs   out.  Also here: tools nvoken calls back to over HTTPS, remote MCP servers, structured output validated against your JSON Schema, reusable Agent Definitions, image and document input, your own model provider keys, and spending limits.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. A configured trusted console may also present a short-lived Ed25519 issuer token; this is an authentication presentation only and does not create a user identity model in nvoken.  - App-scoped Runtime credentials may call every Runtime operation and GET /v1/identity. - App-scoped Viewer credentials may call Runtime reads and GET /v1/identity. - App-scoped Operator credentials may call every Runtime operation, GET /v1/identity, and all credential lifecycle operations. - Org-scoped Viewer and Operator credentials are management and reporting   identities only. They resolve no tenant and cannot perform Runtime   operations; Operators can register and manage Apps and their App   credentials, while Viewers have read-only access.  Tenant, Session, operation, and expiry constraints only narrow these grants.  ## Familiar names  Where a name already means something in other agent APIs, nvoken uses it the same way rather than inventing its own. `metadata` follows OpenAI's limits of 16 keys, 64-character names, and 512-byte values. `output_text` is the assistant's text joined into one string. `reasoning.effort` takes `low`, `medium`, `high`, `xhigh`, and `max`. `stop_reason: end_turn`, status `running`, and the `commentary` and `final_answer` message phases are the same idea you have seen elsewhere. If you have integrated another agent API, these should need no translation.  ## You can always read back what applied  Anything nvoken decides on your behalf is readable on the resource that used it. You never have to work out what happened by combining the request you sent with your own assumptions about nvoken's defaults — just read the resource.  A turn reports the `limits` it is really running under, after defaults and minimums; the `agent_definition` it ran with, exactly as stored; and `provenance`, which records what actually served the request. A Session reports its compaction (summarization) policy with `auto` already resolved to a real number and a real model, and its retention window as accepted. New settings will work the same way: a default you cannot read back is a setting only the server knows about.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, Optional, Union
from typing_extensions import Annotated
from nvoken_generated.models.budget_scope import BudgetScope
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Budget(BaseModel):
    """
    What one spending limit has used up so far in its current window. `spent_estimated_cost_usd` is spend from model calls whose cost is final; `reserved_estimated_cost_usd` is spend committed to turns still running; `available_estimated_cost_usd` is what is left. These are nvoken's own running tally for deciding when to stop work — deleting transcripts does not give quota back, and these numbers are an estimate for enforcement, not a bill.
    """ # noqa: E501
    id: Annotated[str, Field(min_length=1, strict=True)] = Field(description="Opaque identifier with the public `budg_` prefix. Treat the body as opaque.")
    scope: BudgetScope
    tenant_key: Optional[StrictStr]
    user_key: Optional[StrictStr]
    agent_id: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(description="Opaque identifier with the public `agent_` prefix. Treat the body as opaque.")
    provider_key_id: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(description="Opaque identifier with the public `pkey_` prefix. Treat the body as opaque.")
    credential_family_id: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(description="Root API credential ID shared by its rotation chain.")
    window_start: datetime
    window_end: datetime
    max_estimated_cost_usd: Union[Annotated[float, Field(multiple_of=0.0000010, strict=True)], Annotated[int, Field(strict=True)]]
    spent_estimated_cost_usd: Union[Annotated[float, Field(multiple_of=0.0000010, strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]
    reserved_estimated_cost_usd: Union[Annotated[float, Field(multiple_of=0.0000010, strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]
    available_estimated_cost_usd: Union[Annotated[float, Field(multiple_of=0.0000010, strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]
    paused_invocations: Annotated[int, Field(strict=True, ge=0)]
    created_at: datetime
    updated_at: datetime
    __properties: ClassVar[List[str]] = ["id", "scope", "tenant_key", "user_key", "agent_id", "provider_key_id", "credential_family_id", "window_start", "window_end", "max_estimated_cost_usd", "spent_estimated_cost_usd", "reserved_estimated_cost_usd", "available_estimated_cost_usd", "paused_invocations", "created_at", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Budget from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if tenant_key (nullable) is None
        # and model_fields_set contains the field
        if self.tenant_key is None and "tenant_key" in self.model_fields_set:
            _dict['tenant_key'] = None

        # set to None if user_key (nullable) is None
        # and model_fields_set contains the field
        if self.user_key is None and "user_key" in self.model_fields_set:
            _dict['user_key'] = None

        # set to None if agent_id (nullable) is None
        # and model_fields_set contains the field
        if self.agent_id is None and "agent_id" in self.model_fields_set:
            _dict['agent_id'] = None

        # set to None if provider_key_id (nullable) is None
        # and model_fields_set contains the field
        if self.provider_key_id is None and "provider_key_id" in self.model_fields_set:
            _dict['provider_key_id'] = None

        # set to None if credential_family_id (nullable) is None
        # and model_fields_set contains the field
        if self.credential_family_id is None and "credential_family_id" in self.model_fields_set:
            _dict['credential_family_id'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Budget from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "scope": obj.get("scope"),
            "tenant_key": obj.get("tenant_key"),
            "user_key": obj.get("user_key"),
            "agent_id": obj.get("agent_id"),
            "provider_key_id": obj.get("provider_key_id"),
            "credential_family_id": obj.get("credential_family_id"),
            "window_start": obj.get("window_start"),
            "window_end": obj.get("window_end"),
            "max_estimated_cost_usd": obj.get("max_estimated_cost_usd"),
            "spent_estimated_cost_usd": obj.get("spent_estimated_cost_usd"),
            "reserved_estimated_cost_usd": obj.get("reserved_estimated_cost_usd"),
            "available_estimated_cost_usd": obj.get("available_estimated_cost_usd"),
            "paused_invocations": obj.get("paused_invocations"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        return _obj
