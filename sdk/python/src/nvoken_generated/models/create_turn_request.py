# coding: utf-8

"""
    nvoken API

    nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque and use `agent_`, `arev_`, `mspc_`, `conv_`, and `turn_` prefixes. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes after that Turn becomes terminal. A Conversation stream follows current and future Turns and may stay open while idle. Both use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from nvoken_generated.models.limits import Limits
from nvoken_generated.models.mcp_server_headers import MCPServerHeaders
from nvoken_generated.models.provider_key_selection import ProviderKeySelection
from nvoken_generated.models.turn_behavior_selection import TurnBehaviorSelection
from nvoken_generated.models.turn_context_item import TurnContextItem
from nvoken_generated.models.turn_conversation import TurnConversation
from nvoken_generated.models.turn_input import TurnInput
from nvoken_generated.models.turn_memory_selection import TurnMemorySelection
from nvoken_generated.models.turn_trigger import TurnTrigger
from nvoken_generated.models.webhook_target import WebhookTarget
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CreateTurnRequest(BaseModel):
    """
    Starts exactly one durable Turn. Machine callers supply `tenant_key` and `behavior`; browser callers omit both because their verified grant supplies the tenant and one exact AgentRevision. Browser callers also cannot replace the grant's actor or memory authority. Behavior, memory, Conversation, and actor are independent coordinates and all participate in idempotency comparison.
    """ # noqa: E501
    tenant_key: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=255)]] = Field(default=None, description="Tenant in which this Turn executes. Required for machine callers; browser callers omit it because the verified grant supplies it. ")
    user_key: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=255)]] = Field(default=None, description="Optional machine-selected actor. Browser callers omit it because their verified grant supplies the actor. It does not select Agent or Conversation ownership. ")
    idempotency_key: Annotated[str, Field(min_length=1, strict=True, max_length=255)] = Field(description="Trusted-host keys are scoped only to the resolved tenant. An equal retry returns the original Turn; a changed request conflicts. ")
    behavior: Optional[TurnBehaviorSelection] = None
    memory: Optional[TurnMemorySelection] = Field(default=None, description="Omission applies the selected behavior's default. `none` disables memory. User memory requires `user_key`; inline user or tenant memory requires a namespace at admission. ")
    conversation: Optional[TurnConversation] = Field(default=None, description="Omit for a standalone Turn with no public Conversation.")
    limits: Optional[Limits] = Field(default=None, description="Monotonic narrowing only; values may never widen stored behavior limits.")
    input: TurnInput
    metadata: Optional[Dict[str, Annotated[str, Field(strict=True, max_length=512)]]] = Field(default=None, description="Opaque host correlation data. nvoken stores it, returns it verbatim, and never interprets it — it exists so a support engineer holding an Turn id can get back to the board, ticket, or surface the turn came from, which nvoken deliberately knows nothing about.  At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`, `:`, and `-`; values are at most 512 bytes of UTF-8.  A Conversation similarly has no dedicated `title` field. A host may store one in that Conversation's own metadata object, which keeps nvoken from forming opinions about a string only the host renders.  Metadata is correlation data, not an authorization anchor. Delivery receivers authorize from their own trusted product state and the signed nvoken coordinates, never from model-authored input or metadata. ")
    context: Optional[Annotated[List[TurnContextItem], Field(max_length=8)]] = None
    triggered_by: Optional[TurnTrigger] = None
    webhook: Optional[WebhookTarget] = None
    on_budget_exhausted: Optional[StrictStr] = 'stop'
    mcp_server_headers: Optional[Annotated[List[MCPServerHeaders], Field(max_length=8)]] = None
    provider_keys: Optional[Annotated[List[ProviderKeySelection], Field(min_length=1, max_length=1)]] = None
    __properties: ClassVar[List[str]] = ["tenant_key", "user_key", "idempotency_key", "behavior", "memory", "conversation", "limits", "input", "metadata", "context", "triggered_by", "webhook", "on_budget_exhausted", "mcp_server_headers", "provider_keys"]

    @field_validator('on_budget_exhausted')
    def on_budget_exhausted_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['stop', 'hold']):
            raise ValueError("must be one of enum values ('stop', 'hold')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateTurnRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of behavior
        if self.behavior:
            _dict['behavior'] = self.behavior.to_dict()
        # override the default output from pydantic by calling `to_dict()` of memory
        if self.memory:
            _dict['memory'] = self.memory.to_dict()
        # override the default output from pydantic by calling `to_dict()` of conversation
        if self.conversation:
            _dict['conversation'] = self.conversation.to_dict()
        # override the default output from pydantic by calling `to_dict()` of limits
        if self.limits:
            _dict['limits'] = self.limits.to_dict()
        # override the default output from pydantic by calling `to_dict()` of input
        if self.input:
            _dict['input'] = self.input.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in context (list)
        _items = []
        if self.context:
            for _item_context in self.context:
                if _item_context:
                    _items.append(_item_context.to_dict())
            _dict['context'] = _items
        # override the default output from pydantic by calling `to_dict()` of triggered_by
        if self.triggered_by:
            _dict['triggered_by'] = self.triggered_by.to_dict()
        # override the default output from pydantic by calling `to_dict()` of webhook
        if self.webhook:
            _dict['webhook'] = self.webhook.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in mcp_server_headers (list)
        _items = []
        if self.mcp_server_headers:
            for _item_mcp_server_headers in self.mcp_server_headers:
                if _item_mcp_server_headers:
                    _items.append(_item_mcp_server_headers.to_dict())
            _dict['mcp_server_headers'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in provider_keys (list)
        _items = []
        if self.provider_keys:
            for _item_provider_keys in self.provider_keys:
                if _item_provider_keys:
                    _items.append(_item_provider_keys.to_dict())
            _dict['provider_keys'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateTurnRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "tenant_key": obj.get("tenant_key"),
            "user_key": obj.get("user_key"),
            "idempotency_key": obj.get("idempotency_key"),
            "behavior": TurnBehaviorSelection.from_dict(obj["behavior"]) if obj.get("behavior") is not None else None,
            "memory": TurnMemorySelection.from_dict(obj["memory"]) if obj.get("memory") is not None else None,
            "conversation": TurnConversation.from_dict(obj["conversation"]) if obj.get("conversation") is not None else None,
            "limits": Limits.from_dict(obj["limits"]) if obj.get("limits") is not None else None,
            "input": TurnInput.from_dict(obj["input"]) if obj.get("input") is not None else None,
            "metadata": obj.get("metadata"),
            "context": [TurnContextItem.from_dict(_item) for _item in obj["context"]] if obj.get("context") is not None else None,
            "triggered_by": TurnTrigger.from_dict(obj["triggered_by"]) if obj.get("triggered_by") is not None else None,
            "webhook": WebhookTarget.from_dict(obj["webhook"]) if obj.get("webhook") is not None else None,
            "on_budget_exhausted": obj.get("on_budget_exhausted") if obj.get("on_budget_exhausted") is not None else 'stop',
            "mcp_server_headers": [MCPServerHeaders.from_dict(_item) for _item in obj["mcp_server_headers"]] if obj.get("mcp_server_headers") is not None else None,
            "provider_keys": [ProviderKeySelection.from_dict(_item) for _item in obj["provider_keys"]] if obj.get("provider_keys") is not None else None
        })
        return _obj
