# coding: utf-8

"""
    nvoken Runtime API

    This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ModelPricing(BaseModel):
    """
    Standard local price evidence used by nvoken's estimated-cost guardrail. A `priced` object also carries currency, unit, input, output, and update date. `unpriced` and `unknown` carry only status and the opaque pricing version. Clients compare versions for equality only.
    """ # noqa: E501
    status: StrictStr
    currency: Optional[StrictStr] = None
    unit: Optional[StrictStr] = None
    input: Optional[Annotated[str, Field(strict=True)]] = None
    output: Optional[Annotated[str, Field(strict=True)]] = None
    cache_read: Optional[Annotated[str, Field(strict=True)]] = None
    cache_write: Optional[Annotated[str, Field(strict=True)]] = None
    updated_at: Optional[date] = None
    pricing_version: Annotated[str, Field(min_length=1, strict=True)] = Field(description="Opaque nvoken-owned identifier for the local pricing snapshot. Values never encode an upstream dependency name or version. ")
    __properties: ClassVar[List[str]] = ["status", "currency", "unit", "input", "output", "cache_read", "cache_write", "updated_at", "pricing_version"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['priced', 'unpriced', 'unknown']):
            raise ValueError("must be one of enum values ('priced', 'unpriced', 'unknown')")
        return value

    @field_validator('currency')
    def currency_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['USD']):
            raise ValueError("must be one of enum values ('USD')")
        return value

    @field_validator('unit')
    def unit_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['per_million_tokens']):
            raise ValueError("must be one of enum values ('per_million_tokens')")
        return value

    @field_validator('input')
    def input_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(0|[1-9][0-9]*)(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)(\.[0-9]+)?$/")
        return value

    @field_validator('output')
    def output_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(0|[1-9][0-9]*)(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)(\.[0-9]+)?$/")
        return value

    @field_validator('cache_read')
    def cache_read_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(0|[1-9][0-9]*)(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)(\.[0-9]+)?$/")
        return value

    @field_validator('cache_write')
    def cache_write_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(0|[1-9][0-9]*)(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)(\.[0-9]+)?$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ModelPricing from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ModelPricing from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "currency": obj.get("currency"),
            "unit": obj.get("unit"),
            "input": obj.get("input"),
            "output": obj.get("output"),
            "cache_read": obj.get("cache_read"),
            "cache_write": obj.get("cache_write"),
            "updated_at": obj.get("updated_at"),
            "pricing_version": obj.get("pricing_version")
        })
        return _obj
