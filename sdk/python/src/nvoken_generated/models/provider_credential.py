# coding: utf-8

"""
    nvoken Runtime API

    This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback client tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, Optional
from typing_extensions import Annotated
from nvoken_generated.models.model_provider import ModelProvider
from nvoken_generated.models.provider_credential_scope import ProviderCredentialScope
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ProviderCredential(BaseModel):
    """
    Safe metadata only; secret material is never represented.
    """ # noqa: E501
    id: Annotated[str, Field(strict=True)] = Field(description="UUIDv7 with the public `pcrd_` prefix.")
    provider: ModelProvider
    scope: ProviderCredentialScope
    tenant_ref: Optional[StrictStr]
    status: StrictStr = Field(description="Active roots remain rotatable and revocable even when their current version has expired.")
    version: Annotated[int, Field(strict=True, ge=1)]
    version_id: Annotated[str, Field(strict=True)] = Field(description="UUIDv7 with the public `pcvr_` prefix.")
    previous_version_id: Optional[Annotated[str, Field(strict=True)]] = Field(description="UUIDv7 with the public `pcvr_` prefix.")
    version_status: StrictStr = Field(description="An expired current version is unusable; rotate the active credential root before any admission or model call can use it.")
    expires_at: Optional[datetime]
    overlap_expires_at: Optional[datetime]
    created_by: StrictStr
    created_at: datetime
    updated_at: datetime
    revoked_at: Optional[datetime]
    __properties: ClassVar[List[str]] = ["id", "provider", "scope", "tenant_ref", "status", "version", "version_id", "previous_version_id", "version_status", "expires_at", "overlap_expires_at", "created_by", "created_at", "updated_at", "revoked_at"]

    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^pcrd_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$", value):
            raise ValueError(r"must validate the regular expression /^pcrd_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'revoked']):
            raise ValueError("must be one of enum values ('active', 'revoked')")
        return value

    @field_validator('version_id')
    def version_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^pcvr_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$", value):
            raise ValueError(r"must validate the regular expression /^pcvr_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/")
        return value

    @field_validator('previous_version_id')
    def previous_version_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^pcvr_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$", value):
            raise ValueError(r"must validate the regular expression /^pcvr_[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/")
        return value

    @field_validator('version_status')
    def version_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'overlap', 'expired', 'revoked']):
            raise ValueError("must be one of enum values ('active', 'overlap', 'expired', 'revoked')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProviderCredential from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if tenant_ref (nullable) is None
        # and model_fields_set contains the field
        if self.tenant_ref is None and "tenant_ref" in self.model_fields_set:
            _dict['tenant_ref'] = None

        # set to None if previous_version_id (nullable) is None
        # and model_fields_set contains the field
        if self.previous_version_id is None and "previous_version_id" in self.model_fields_set:
            _dict['previous_version_id'] = None

        # set to None if expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.expires_at is None and "expires_at" in self.model_fields_set:
            _dict['expires_at'] = None

        # set to None if overlap_expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.overlap_expires_at is None and "overlap_expires_at" in self.model_fields_set:
            _dict['overlap_expires_at'] = None

        # set to None if revoked_at (nullable) is None
        # and model_fields_set contains the field
        if self.revoked_at is None and "revoked_at" in self.model_fields_set:
            _dict['revoked_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProviderCredential from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "provider": obj.get("provider"),
            "scope": obj.get("scope"),
            "tenant_ref": obj.get("tenant_ref"),
            "status": obj.get("status"),
            "version": obj.get("version"),
            "version_id": obj.get("version_id"),
            "previous_version_id": obj.get("previous_version_id"),
            "version_status": obj.get("version_status"),
            "expires_at": obj.get("expires_at"),
            "overlap_expires_at": obj.get("overlap_expires_at"),
            "created_by": obj.get("created_by"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at"),
            "revoked_at": obj.get("revoked_at")
        })
        return _obj
