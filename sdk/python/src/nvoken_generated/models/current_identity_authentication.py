# coding: utf-8

"""
    nvoken API

    nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque UUIDs and carry no type prefix. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes with `connection.closing` reason `settled` once that Turn's terminal change is delivered. A Conversation stream follows current and future Turns and may stay open while idle. Both routes accept the same `cursor`, `deltas`, and `Last-Event-ID` parameters and use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.  Reconnect with your last saved `cursor` after any close but `settled`. Honor the `retry:` delay the stream opens with, and back off when the server keeps refusing the connection: a flat retry from every client is what turns an outage into a load spike.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from nvoken_generated.models.browser_conversation_access import BrowserConversationAccess
from nvoken_generated.models.browser_memory_access import BrowserMemoryAccess
from nvoken_generated.models.credential_type import CredentialType
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CurrentIdentityAuthentication(BaseModel):
    """
    CurrentIdentityAuthentication
    """ # noqa: E501
    credential_id: Optional[StrictStr] = Field(default=None, description="Machine credentials only.")
    app_id: Optional[StrictStr] = Field(description="Null for installation credentials and Org console presentations. A browser grant is always App-scoped, so this is never null for one. ")
    org_id: Optional[StrictStr] = Field(default=None, description="Short-lived console presentations only.")
    type: Optional[CredentialType] = Field(default=None, description="Machine API keys only.")
    agent_id: Optional[StrictStr] = Field(default=None, description="Browser grants only. The Agent this grant is pinned to.")
    agent_revision_id: Optional[StrictStr] = Field(default=None, description="Browser grants only. Exact immutable behavior pinned by the grant.")
    tenant_key: Optional[StrictStr] = Field(default=None, description="Null only when a machine credential resolves no tenant. A browser grant is always tenant-pinned, so this is never null for one. ")
    user_key: Optional[StrictStr] = Field(default=None, description="Browser subject and Turn actor; null for machine credentials.")
    memory_access: Optional[BrowserMemoryAccess] = Field(default=None, description="Browser grants only. Omission grants no memory authority.")
    conversation_access: Optional[BrowserConversationAccess] = Field(default=None, description="Browser grants only. Omission grants no Conversation authority.")
    method: StrictStr = Field(description="How this caller authenticated. New values may be added; handle a value you do not recognize as an unknown caller rather than refusing the response. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["credential_id", "app_id", "org_id", "type", "agent_id", "agent_revision_id", "tenant_key", "user_key", "memory_access", "conversation_access", "method"]

    @field_validator('method')
    def method_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['api_key', 'issuer_token', 'client_token', 'anonymous_token']):
            raise ValueError("must be one of enum values ('api_key', 'issuer_token', 'client_token', 'anonymous_token')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CurrentIdentityAuthentication from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of memory_access
        if self.memory_access:
            _dict['memory_access'] = self.memory_access.to_dict()
        # override the default output from pydantic by calling `to_dict()` of conversation_access
        if self.conversation_access:
            _dict['conversation_access'] = self.conversation_access.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if app_id (nullable) is None
        # and model_fields_set contains the field
        if self.app_id is None and "app_id" in self.model_fields_set:
            _dict['app_id'] = None

        # set to None if org_id (nullable) is None
        # and model_fields_set contains the field
        if self.org_id is None and "org_id" in self.model_fields_set:
            _dict['org_id'] = None

        # set to None if tenant_key (nullable) is None
        # and model_fields_set contains the field
        if self.tenant_key is None and "tenant_key" in self.model_fields_set:
            _dict['tenant_key'] = None

        # set to None if user_key (nullable) is None
        # and model_fields_set contains the field
        if self.user_key is None and "user_key" in self.model_fields_set:
            _dict['user_key'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CurrentIdentityAuthentication from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "credential_id": obj.get("credential_id"),
            "app_id": obj.get("app_id"),
            "org_id": obj.get("org_id"),
            "type": obj.get("type"),
            "agent_id": obj.get("agent_id"),
            "agent_revision_id": obj.get("agent_revision_id"),
            "tenant_key": obj.get("tenant_key"),
            "user_key": obj.get("user_key"),
            "memory_access": BrowserMemoryAccess.from_dict(obj["memory_access"]) if obj.get("memory_access") is not None else None,
            "conversation_access": BrowserConversationAccess.from_dict(obj["conversation_access"]) if obj.get("conversation_access") is not None else None,
            "method": obj.get("method")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
