# coding: utf-8

"""
    nvoken Runtime API

    This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI's bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller's behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken's defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictBool, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from nvoken_generated.models.model import Model
from nvoken_generated.models.model_control_capabilities import ModelControlCapabilities
from nvoken_generated.models.model_pricing import ModelPricing
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ModelDescriptor(BaseModel):
    """
    Cataloged descriptors include all maintained metadata fields. Uncataloged descriptors include only provider, id, cataloged, and pricing; absent metadata is unknown rather than false or zero. A cataloged descriptor's controls are the same qualified capabilities used by admission; omitted controls on an uncataloged descriptor mean unknown, not unsupported. Deprecated models remain admissible until `retires_at`; after that date, new admission returns `model_retired` with this entry's replacement.
    """ # noqa: E501
    provider: Annotated[str, Field(strict=True)] = Field(description="Extensible canonical provider identifier. Consumers must preserve unknown values so adding a provider does not break decoding. Request positions still reject providers not registered by the installation. ")
    id: Annotated[str, Field(min_length=1, strict=True, max_length=255)]
    cataloged: StrictBool = Field(description="Whether nvoken advertises and maintains metadata for this selection.")
    display_name: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    description: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    context_window_tokens: Optional[Annotated[int, Field(strict=True, ge=1)]] = None
    max_output_tokens: Optional[Annotated[int, Field(strict=True, ge=1)]] = None
    input_modalities: Optional[Annotated[List[Annotated[str, Field(strict=True)]], Field(min_length=1)]] = Field(default=None, description="Input modalities admission accepts for this model selection. Media modalities also publish their exact media types under controls.input.media. ")
    recommended: Optional[StrictBool] = Field(default=None, description="nvoken's suggested general-purpose starting point for this provider.")
    deprecated: Optional[StrictBool] = None
    deprecated_at: Optional[date] = Field(default=None, description="Date nvoken marked this model deprecated.")
    retires_at: Optional[date] = Field(default=None, description="Date nvoken starts refusing new admission for this model.")
    replacement: Optional[Model] = None
    controls: Optional[ModelControlCapabilities] = None
    pricing: ModelPricing
    __properties: ClassVar[List[str]] = ["provider", "id", "cataloged", "display_name", "description", "context_window_tokens", "max_output_tokens", "input_modalities", "recommended", "deprecated", "deprecated_at", "retires_at", "replacement", "controls", "pricing"]

    @field_validator('provider')
    def provider_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-z][a-z0-9_]*$", value):
            raise ValueError(r"must validate the regular expression /^[a-z][a-z0-9_]*$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ModelDescriptor from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of replacement
        if self.replacement:
            _dict['replacement'] = self.replacement.to_dict()
        # override the default output from pydantic by calling `to_dict()` of controls
        if self.controls:
            _dict['controls'] = self.controls.to_dict()
        # override the default output from pydantic by calling `to_dict()` of pricing
        if self.pricing:
            _dict['pricing'] = self.pricing.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ModelDescriptor from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "provider": obj.get("provider"),
            "id": obj.get("id"),
            "cataloged": obj.get("cataloged"),
            "display_name": obj.get("display_name"),
            "description": obj.get("description"),
            "context_window_tokens": obj.get("context_window_tokens"),
            "max_output_tokens": obj.get("max_output_tokens"),
            "input_modalities": obj.get("input_modalities"),
            "recommended": obj.get("recommended"),
            "deprecated": obj.get("deprecated"),
            "deprecated_at": obj.get("deprecated_at"),
            "retires_at": obj.get("retires_at"),
            "replacement": Model.from_dict(obj["replacement"]) if obj.get("replacement") is not None else None,
            "controls": ModelControlCapabilities.from_dict(obj["controls"]) if obj.get("controls") is not None else None,
            "pricing": ModelPricing.from_dict(obj["pricing"]) if obj.get("pricing") is not None else None
        })
        return _obj
