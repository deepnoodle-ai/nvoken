# coding: utf-8

"""
    nvoken API

    nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque UUIDs and carry no type prefix. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes with `connection.closing` reason `settled` once that Turn's terminal change is delivered. A Conversation stream follows current and future Turns and may stay open while idle. Both routes accept the same `cursor`, `deltas`, and `Last-Event-ID` parameters and use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.  Reconnect with your last saved `cursor` after any close but `settled`. Honor the `retry:` delay the stream opens with, and back off when the server keeps refusing the connection: a flat retry from every client is what turns an outage into a load spike.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from nvoken_generated.models.credit_block import CreditBlock
from nvoken_generated.models.model_provenance import ModelProvenance
from nvoken_generated.models.model_usage import ModelUsage
from nvoken_generated.models.structured_output_provenance import StructuredOutputProvenance
from nvoken_generated.models.tool_call_summary import ToolCallSummary
from nvoken_generated.models.turn_failure import TurnFailure
from nvoken_generated.models.turn_status import TurnStatus
from nvoken_generated.models.turn_stop_reason import TurnStopReason
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TurnChange(BaseModel):
    """
    One lifecycle step of one turn, as the stream and the JSON transcript deliver it. Order changes by `revision` and fold to the highest one to get current state.  **A turn is over when a change for it carries `terminal: true`.** That is the terminal signal, and there is no other. Because it is saved it replays on reconnect at any cursor, so a turn that settled while you were away is still settled when you return.  A change has two kinds of field. The log fields are always present and describe the step itself: `turn_id`, `conversation_id`, `content_expires_at`, `revision`, `status`, `terminal`, `current`, `through_message_sequence`, `error`, `structured_output`, and `occurred_at`; a nullable one is `null` when it has no value. The detail fields describe where the turn stands now, so they are present only on the change that is still the turn's current state (`current: true`) and omitted from every replayed earlier change: `stop_reason` for a turn that ended, `credit_block` for one held on credits, `tool_calls` for what its tools are doing, with `arguments` on the ones waiting for you to run them, `usage` and `provenance` for its model calls, `structured_output_provenance`, and `final_answer_message_id`. Read detail from the current change alone and you never need a second request to find out why a turn is not moving.
    """ # noqa: E501
    turn_id: StrictStr = Field(description="RFC 9562 UUIDv7 in canonical lowercase text. Identifiers carry no type prefix; treat the value as opaque.")
    conversation_id: Optional[StrictStr] = Field(description="RFC 9562 UUIDv7 in canonical lowercase text. Identifiers carry no type prefix; treat the value as opaque.")
    content_expires_at: Optional[datetime] = Field(description="Scheduled private-content expiry for a terminal standalone Turn. Null while it is nonterminal and for conversation-bound work. ")
    revision: Annotated[int, Field(strict=True, ge=1)]
    status: TurnStatus
    terminal: StrictBool = Field(description="Whether this change is the turn's end. It is exactly `status IN (completed, incomplete, failed, cancelled)`, computed here so you never restate that set — read this rather than testing the status against a list of your own, and a status added later cannot silently change what your code believes.  It describes *this change*, not the turn's current state. A replayed `running` change reports `false` even after the turn has ended, which is what keeps the ordering rule intact: fold messages before changes and a turn is never settled before its final message exists. ")
    current: StrictBool = Field(description="Whether this change is still the turn's current state as of the read that produced it. At most one change per turn in a frame or snapshot carries `true`, and only that change carries the detail fields. A replayed change is `false` even though it was current when it happened. ")
    stop_reason: Optional[TurnStopReason] = Field(default=None, description="Why the turn stopped. Present on the current change once the turn has stopped, so an `incomplete` change names the limit that ran out. Absent otherwise. ")
    credit_block: Optional[CreditBlock] = Field(default=None, description="Present on the current change while the turn is on budget hold for spending capacity. Omitted for browser client tokens. ")
    tool_calls: Optional[List[ToolCallSummary]] = Field(default=None, description="Every tool call this turn has made, with its current status. Present on the current change, as `[]` when the turn has made none, and omitted from replayed changes. A tool call changing state is itself a change, so a failed or long-running call is visible before its result message arrives, and a client that was disconnected sees it on replay. ")
    through_message_sequence: Optional[Annotated[int, Field(strict=True, ge=1)]]
    error: Optional[TurnFailure]
    usage: Optional[ModelUsage] = Field(default=None, description="Model usage summed across the turn's recorded model calls. Present on the current terminal change of a turn that made one. Omitted for browser client tokens. ")
    provenance: Optional[ModelProvenance] = Field(default=None, description="Which provider and model served the turn's latest recorded call. Present on the current terminal change of a turn that made one. Omitted for browser client tokens. ")
    structured_output: Optional[Dict[str, Any]]
    structured_output_provenance: Optional[StructuredOutputProvenance] = Field(default=None, description="Present on the current terminal change of a turn that produced `structured_output`. ")
    final_answer_message_id: Optional[StrictStr] = Field(default=None, description="The message that is this turn's answer: its last assistant message, present on the current change once the turn settled `completed` with stop reason `end_turn`, and absent otherwise. This is the stream's spelling of `phase: final_answer`, which ordinary reads work out for you and a stream frame cannot. ")
    occurred_at: datetime
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["turn_id", "conversation_id", "content_expires_at", "revision", "status", "terminal", "current", "stop_reason", "credit_block", "tool_calls", "through_message_sequence", "error", "usage", "provenance", "structured_output", "structured_output_provenance", "final_answer_message_id", "occurred_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TurnChange from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of credit_block
        if self.credit_block:
            _dict['credit_block'] = self.credit_block.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in tool_calls (list)
        _items = []
        if self.tool_calls:
            for _item_tool_calls in self.tool_calls:
                if _item_tool_calls:
                    _items.append(_item_tool_calls.to_dict())
            _dict['tool_calls'] = _items
        # override the default output from pydantic by calling `to_dict()` of error
        if self.error:
            _dict['error'] = self.error.to_dict()
        # override the default output from pydantic by calling `to_dict()` of usage
        if self.usage:
            _dict['usage'] = self.usage.to_dict()
        # override the default output from pydantic by calling `to_dict()` of provenance
        if self.provenance:
            _dict['provenance'] = self.provenance.to_dict()
        # override the default output from pydantic by calling `to_dict()` of structured_output_provenance
        if self.structured_output_provenance:
            _dict['structured_output_provenance'] = self.structured_output_provenance.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if conversation_id (nullable) is None
        # and model_fields_set contains the field
        if self.conversation_id is None and "conversation_id" in self.model_fields_set:
            _dict['conversation_id'] = None

        # set to None if content_expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.content_expires_at is None and "content_expires_at" in self.model_fields_set:
            _dict['content_expires_at'] = None

        # set to None if through_message_sequence (nullable) is None
        # and model_fields_set contains the field
        if self.through_message_sequence is None and "through_message_sequence" in self.model_fields_set:
            _dict['through_message_sequence'] = None

        # set to None if error (nullable) is None
        # and model_fields_set contains the field
        if self.error is None and "error" in self.model_fields_set:
            _dict['error'] = None

        # set to None if structured_output (nullable) is None
        # and model_fields_set contains the field
        if self.structured_output is None and "structured_output" in self.model_fields_set:
            _dict['structured_output'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TurnChange from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "turn_id": obj.get("turn_id"),
            "conversation_id": obj.get("conversation_id"),
            "content_expires_at": obj.get("content_expires_at"),
            "revision": obj.get("revision"),
            "status": obj.get("status"),
            "terminal": obj.get("terminal"),
            "current": obj.get("current"),
            "stop_reason": obj.get("stop_reason"),
            "credit_block": CreditBlock.from_dict(obj["credit_block"]) if obj.get("credit_block") is not None else None,
            "tool_calls": [ToolCallSummary.from_dict(_item) for _item in obj["tool_calls"]] if obj.get("tool_calls") is not None else None,
            "through_message_sequence": obj.get("through_message_sequence"),
            "error": TurnFailure.from_dict(obj["error"]) if obj.get("error") is not None else None,
            "usage": ModelUsage.from_dict(obj["usage"]) if obj.get("usage") is not None else None,
            "provenance": ModelProvenance.from_dict(obj["provenance"]) if obj.get("provenance") is not None else None,
            "structured_output": obj.get("structured_output"),
            "structured_output_provenance": StructuredOutputProvenance.from_dict(obj["structured_output_provenance"]) if obj.get("structured_output_provenance") is not None else None,
            "final_answer_message_id": obj.get("final_answer_message_id"),
            "occurred_at": obj.get("occurred_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
