/* tslint:disable */
/* eslint-disable */
/**
 * nvoken API
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential\'s existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque and use `agent_`, `arev_`, `mspc_`, `conv_`, and `turn_` prefixes. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes after that Turn becomes terminal. A Conversation stream follows current and future Turns and may stay open while idle. Both use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * Optional requested limits. Total time bounds the entire turn, active
 * time bounds model and tool execution, and waiting time bounds the
 * cumulative time parked for host or callback tool results. Installation
 * defaults supply all three time limits and the iteration limit.
 * Output-token and estimated-cost limits are unlimited when omitted.
 * Installation maxima may be lower than the schema's numeric range.
 *
 * @export
 * @interface Limits
 */
export interface Limits {
    /**
     *
     * @type {number}
     * @memberof Limits
     */
    totalTimeoutSeconds?: number;
    /**
     *
     * @type {number}
     * @memberof Limits
     */
    activeTimeoutSeconds?: number;
    /**
     * Optional cumulative external-waiting SLA. When omitted, host and
     * callback ToolCalls may remain pending indefinitely; waiting does
     * not consume the Turn's total or active execution budgets.
     *
     * @type {number}
     * @memberof Limits
     */
    waitingTimeoutSeconds?: number;
    /**
     *
     * @type {number}
     * @memberof Limits
     */
    maxOutputTokens?: number;
    /**
     * A cost ceiling for this turn, in USD, estimated from list prices.
     * It does not reserve funds and is not a bill.
     *
     * The model must have a known price. If it does not, the turn fails
     * with `cost_estimate_unavailable` — and where nvoken can
     * tell in advance, it refuses before calling the provider at all, so
     * you are not charged for a turn it cannot police.
     *
     * Hitting the ceiling is not itself a failure. If the turn can stop
     * cleanly it ends `incomplete` with
     * `stop_reason: max_estimated_cost` and keeps everything it
     * produced.
     *
     * @type {number}
     * @memberof Limits
     */
    maxEstimatedCostUsd?: number;
    /**
     *
     * @type {number}
     * @memberof Limits
     */
    maxIterations?: number;
}

/**
 * Check if a given object implements the Limits interface.
 */
export function instanceOfLimits(value: object): value is Limits {
    return true;
}

export function LimitsFromJSON(json: any): Limits {
    return LimitsFromJSONTyped(json, false);
}

export function LimitsFromJSONTyped(json: any, ignoreDiscriminator: boolean): Limits {
    if (json == null) {
        return json;
    }
    return {

        'totalTimeoutSeconds': json['total_timeout_seconds'] == null ? undefined : json['total_timeout_seconds'],
        'activeTimeoutSeconds': json['active_timeout_seconds'] == null ? undefined : json['active_timeout_seconds'],
        'waitingTimeoutSeconds': json['waiting_timeout_seconds'] == null ? undefined : json['waiting_timeout_seconds'],
        'maxOutputTokens': json['max_output_tokens'] == null ? undefined : json['max_output_tokens'],
        'maxEstimatedCostUsd': json['max_estimated_cost_usd'] == null ? undefined : json['max_estimated_cost_usd'],
        'maxIterations': json['max_iterations'] == null ? undefined : json['max_iterations'],
    };
}

export function LimitsToJSON(json: any): Limits {
    return LimitsToJSONTyped(json, false);
}

export function LimitsToJSONTyped(value?: Limits | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {

        'total_timeout_seconds': value['totalTimeoutSeconds'],
        'active_timeout_seconds': value['activeTimeoutSeconds'],
        'waiting_timeout_seconds': value['waitingTimeoutSeconds'],
        'max_output_tokens': value['maxOutputTokens'],
        'max_estimated_cost_usd': value['maxEstimatedCostUsd'],
        'max_iterations': value['maxIterations'],
    };
}
