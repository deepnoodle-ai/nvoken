/* tslint:disable */
/* eslint-disable */
/**
 * nvoken Runtime API
 * This focused contract defines nvoken\'s implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet\'s Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI\'s bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller\'s behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken\'s defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { SessionBudget } from './SessionBudget.js';
import {
    SessionBudgetFromJSON,
    SessionBudgetFromJSONTyped,
    SessionBudgetToJSON,
    SessionBudgetToJSONTyped,
} from './SessionBudget.js';
import type { CompactionPolicy } from './CompactionPolicy.js';
import {
    CompactionPolicyFromJSON,
    CompactionPolicyFromJSONTyped,
    CompactionPolicyToJSON,
    CompactionPolicyToJSONTyped,
} from './CompactionPolicy.js';
import type { RetentionPolicy } from './RetentionPolicy.js';
import {
    RetentionPolicyFromJSON,
    RetentionPolicyFromJSONTyped,
    RetentionPolicyToJSON,
    RetentionPolicyToJSONTyped,
} from './RetentionPolicy.js';

/**
 * Durable Session options. Every member is optional and at least one must
 * be present. A new Session stores the supplied values. When a keyed
 * upsert or admission resolves an existing Session, supplied values are
 * comparison-only: equal values are accepted and different values return
 * `session_options_conflict` with `details.conflicting_paths`.
 *
 * `compaction` requires an Invocation because the policy is validated
 * against that turn's model. It may be installed on any Session that has
 * no compaction policy yet, including a Session created ahead of its first
 * Invocation or a long-running Session opting in later. An `auto` trigger
 * is materialized from the installing Invocation's context window. Once
 * set, the resolved policy is immutable. `POST /v1/sessions` therefore
 * still rejects `compaction`, while admissions may supply it with either
 * `session_id`, `session_key`, or a new Session.
 *
 * @export
 * @interface SessionOptions
 */
export interface SessionOptions {
    /**
     *
     * @type {CompactionPolicy}
     * @memberof SessionOptions
     */
    compaction?: CompactionPolicy;
    /**
     *
     * @type {RetentionPolicy}
     * @memberof SessionOptions
     */
    retention?: RetentionPolicy;
    /**
     *
     * @type {SessionBudget}
     * @memberof SessionOptions
     */
    budget?: SessionBudget;
    /**
     * Opaque host correlation data. nvoken stores it, returns it verbatim,
     * and never interprets it — it exists so a support engineer holding an
     * Invocation id can get back to the board, ticket, or surface the turn
     * came from, which nvoken deliberately knows nothing about.
     *
     * At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`,
     * `:`, and `-`; values are at most 512 bytes of UTF-8.
     *
     * There is no `title` field on a Session by design. A title is one of
     * these entries (`{"title": "Refund policy"}`), which keeps nvoken from
     * forming opinions about a string only the host renders.
     *
     * @type {{ [key: string]: string; }}
     * @memberof SessionOptions
     */
    metadata?: { [key: string]: string; };
}

/**
 * Check if a given object implements the SessionOptions interface.
 */
export function instanceOfSessionOptions(value: object): value is SessionOptions {
    return true;
}

export function SessionOptionsFromJSON(json: any): SessionOptions {
    return SessionOptionsFromJSONTyped(json, false);
}

export function SessionOptionsFromJSONTyped(json: any, ignoreDiscriminator: boolean): SessionOptions {
    if (json == null) {
        return json;
    }
    return {

        'compaction': json['compaction'] == null ? undefined : CompactionPolicyFromJSON(json['compaction']),
        'retention': json['retention'] == null ? undefined : RetentionPolicyFromJSON(json['retention']),
        'budget': json['budget'] == null ? undefined : SessionBudgetFromJSON(json['budget']),
        'metadata': json['metadata'] == null ? undefined : json['metadata'],
    };
}

export function SessionOptionsToJSON(json: any): SessionOptions {
    return SessionOptionsToJSONTyped(json, false);
}

export function SessionOptionsToJSONTyped(value?: SessionOptions | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {

        'compaction': CompactionPolicyToJSON(value['compaction']),
        'retention': RetentionPolicyToJSON(value['retention']),
        'budget': SessionBudgetToJSON(value['budget']),
        'metadata': value['metadata'],
    };
}
