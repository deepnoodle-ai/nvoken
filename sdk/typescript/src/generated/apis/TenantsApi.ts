/* tslint:disable */
/* eslint-disable */
/**
 * nvoken API
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential\'s existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque and use `agent_`, `arev_`, `mspc_`, `conv_`, and `turn_` prefixes. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes after that Turn becomes terminal. A Conversation stream follows current and future Turns and may stay open while idle. Both use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type TenantList,
    TenantListFromJSON,
    TenantListToJSON,
} from '../models/TenantList.js';

export interface DeleteTenantRequest {
    tenantId: string;
}

export interface ListTenantsRequest {
    tenantKey?: string;
    cursor?: string;
    limit?: number;
}

/**
 *
 */
export class TenantsApi extends runtime.BaseAPI {

    /**
     * Creates request options for deleteTenant without sending the request
     */
    async deleteTenantRequestOpts(requestParameters: DeleteTenantRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['tenantId'] == null) {
            throw new runtime.RequiredError(
                'tenantId',
                'Required parameter "tenantId" was null or undefined when calling deleteTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tenants/{tenant_id}`;
        urlPath = urlPath.replace('{tenant_id}', encodeURIComponent(String(requestParameters['tenantId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Removes a tenant along with the credit account and allocations that belong to it alone. It exists to undo a mistake -- a key funded with a typo in it, a tenant created by a misconfigured client -- and is deliberately narrow.  A tenant with any Conversation, Turn, retained model-call fact, durable memory, provider key, or dispatch is refused with `409 tenant_in_use` and the counts that made it load-bearing. This is not a policy choice: every runtime table references `tenants` with ON DELETE RESTRICT, and the retained usage facts must outlive the transcripts they describe, so deleting a tenant that has run work would mean destroying accounting evidence. To remove transcripts, delete the Conversations; the facts and their spend stay.  Deleting a funded tenant destroys its allocation history, and allocations cannot be recreated with their original identity, so the released amount is written to the operator log at deletion.
     * Delete a tenant that never ran anything
     */
    async deleteTenantRaw(requestParameters: DeleteTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteTenantRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a tenant along with the credit account and allocations that belong to it alone. It exists to undo a mistake -- a key funded with a typo in it, a tenant created by a misconfigured client -- and is deliberately narrow.  A tenant with any Conversation, Turn, retained model-call fact, durable memory, provider key, or dispatch is refused with `409 tenant_in_use` and the counts that made it load-bearing. This is not a policy choice: every runtime table references `tenants` with ON DELETE RESTRICT, and the retained usage facts must outlive the transcripts they describe, so deleting a tenant that has run work would mean destroying accounting evidence. To remove transcripts, delete the Conversations; the facts and their spend stay.  Deleting a funded tenant destroys its allocation history, and allocations cannot be recreated with their original identity, so the released amount is written to the operator log at deletion.
     * Delete a tenant that never ran anything
     */
    async deleteTenant(requestParameters: DeleteTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTenantRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for listTenants without sending the request
     */
    async listTenantsRequestOpts(requestParameters: ListTenantsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['tenantKey'] != null) {
            queryParameters['tenant_key'] = requestParameters['tenantKey'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/tenants`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * One row per tenant key nvoken has interned for this App, with its credit position and the last time it ran a turn.  A tenant row is an interning of a key, not tenant state: nvoken stores no configuration, profile, or entitlement for it. What this list reports is therefore what is attached to the key, not what is configured on it.  A key that has only ever been refused does not appear here, because it was never interned. Those keys are in `GET /v1/admissions`, and the difference between the two lists is the set of tenants a host is addressing but has never successfully run.
     * List the App\'s interned tenants
     */
    async listTenantsRaw(requestParameters: ListTenantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantList>> {
        const requestOptions = await this.listTenantsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantListFromJSON(jsonValue));
    }

    /**
     * One row per tenant key nvoken has interned for this App, with its credit position and the last time it ran a turn.  A tenant row is an interning of a key, not tenant state: nvoken stores no configuration, profile, or entitlement for it. What this list reports is therefore what is attached to the key, not what is configured on it.  A key that has only ever been refused does not appear here, because it was never interned. Those keys are in `GET /v1/admissions`, and the difference between the two lists is the set of tenants a host is addressing but has never successfully run.
     * List the App\'s interned tenants
     */
    async listTenants(requestParameters: ListTenantsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantList> {
        const response = await this.listTenantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
