/* tslint:disable */
/* eslint-disable */
/**
 * nvoken API
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential\'s existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque and use `agent_`, `arev_`, `mspc_`, `conv_`, and `turn_` prefixes. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes after that Turn becomes terminal. A Conversation stream follows current and future Turns and may stay open while idle. Both use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ConversationStreamEvent,
    ConversationStreamEventFromJSON,
    ConversationStreamEventToJSON,
} from '../models/ConversationStreamEvent.js';
import {
    type CreateNudgeRequest,
    CreateNudgeRequestFromJSON,
    CreateNudgeRequestToJSON,
} from '../models/CreateNudgeRequest.js';
import {
    type CreateTurnRequest,
    CreateTurnRequestFromJSON,
    CreateTurnRequestToJSON,
} from '../models/CreateTurnRequest.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type Nudge,
    NudgeFromJSON,
    NudgeToJSON,
} from '../models/Nudge.js';
import {
    type NudgeAcknowledgement,
    NudgeAcknowledgementFromJSON,
    NudgeAcknowledgementToJSON,
} from '../models/NudgeAcknowledgement.js';
import {
    type NudgeList,
    NudgeListFromJSON,
    NudgeListToJSON,
} from '../models/NudgeList.js';
import {
    type NudgeStatus,
    NudgeStatusFromJSON,
    NudgeStatusToJSON,
} from '../models/NudgeStatus.js';
import {
    type ResumeTurnRequest,
    ResumeTurnRequestFromJSON,
    ResumeTurnRequestToJSON,
} from '../models/ResumeTurnRequest.js';
import {
    type SubmitHostToolResultsRequest,
    SubmitHostToolResultsRequestFromJSON,
    SubmitHostToolResultsRequestToJSON,
} from '../models/SubmitHostToolResultsRequest.js';
import {
    type SubmitHostToolResultsResponse,
    SubmitHostToolResultsResponseFromJSON,
    SubmitHostToolResultsResponseToJSON,
} from '../models/SubmitHostToolResultsResponse.js';
import {
    type ToolCallList,
    ToolCallListFromJSON,
    ToolCallListToJSON,
} from '../models/ToolCallList.js';
import {
    type Trace,
    TraceFromJSON,
    TraceToJSON,
} from '../models/Trace.js';
import {
    type TraceList,
    TraceListFromJSON,
    TraceListToJSON,
} from '../models/TraceList.js';
import {
    type Turn,
    TurnFromJSON,
    TurnToJSON,
} from '../models/Turn.js';
import {
    type TurnList,
    TurnListFromJSON,
    TurnListToJSON,
} from '../models/TurnList.js';
import {
    type TurnLogList,
    TurnLogListFromJSON,
    TurnLogListToJSON,
} from '../models/TurnLogList.js';
import {
    type TurnResult,
    TurnResultFromJSON,
    TurnResultToJSON,
} from '../models/TurnResult.js';
import {
    type TurnStatus,
    TurnStatusFromJSON,
    TurnStatusToJSON,
} from '../models/TurnStatus.js';
import {
    type TurnTimeline,
    TurnTimelineFromJSON,
    TurnTimelineToJSON,
} from '../models/TurnTimeline.js';

export interface CancelNudgeRequest {
    turnId: string;
    nudgeId: string;
}

export interface CancelTurnRequest {
    turnId: string;
}

export interface CreateNudgeOperationRequest {
    turnId: string;
    createNudgeRequest: CreateNudgeRequest;
}

export interface CreateTurnOperationRequest {
    createTurnRequest: CreateTurnRequest;
    xAnthropicApiKey?: string;
    xOpenaiApiKey?: string;
    xGeminiApiKey?: string;
    xXaiApiKey?: string;
}

export interface DeleteTurnRequest {
    turnId: string;
}

export interface GetTraceRequest {
    traceId: string;
}

export interface GetTurnRequest {
    turnId: string;
}

export interface GetTurnResultRequest {
    turnId: string;
}

export interface GetTurnTimelineRequest {
    turnId: string;
}

export interface InterruptTurnRequest {
    turnId: string;
}

export interface ListNudgesRequest {
    turnId: string;
    status?: NudgeStatus;
    cursor?: string;
    limit?: number;
}

export interface ListToolCallsRequest {
    turnId: string;
    cursor?: string;
    limit?: number;
}

export interface ListTurnLogsRequest {
    turnId: string;
    cursor?: string;
    limit?: number;
    traceId?: string;
}

export interface ListTurnTracesRequest {
    turnId: string;
    cursor?: string;
    limit?: number;
}

export interface ListTurnsRequest {
    tenantKey?: string;
    defaultTenant?: boolean;
    userKey?: string;
    conversationId?: string;
    agentId?: string;
    status?: Array<TurnStatus>;
    parentTurnId?: string;
    ended?: boolean;
    endedSince?: Date;
    cursor?: string;
    limit?: number;
}

export interface ResumeTurnOperationRequest {
    turnId: string;
    resumeTurnRequest: ResumeTurnRequest;
}

export interface StreamTurnRequest {
    turnId: string;
    cursor?: string;
    deltas?: boolean;
    lastEventID?: string;
}

export interface SubmitHostToolResultsOperationRequest {
    turnId: string;
    submitHostToolResultsRequest: SubmitHostToolResultsRequest;
}

/**
 *
 */
export class TurnsApi extends runtime.BaseAPI {

    /**
     * Creates request options for cancelNudge without sending the request
     */
    async cancelNudgeRequestOpts(requestParameters: CancelNudgeRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling cancelNudge().'
            );
        }

        if (requestParameters['nudgeId'] == null) {
            throw new runtime.RequiredError(
                'nudgeId',
                'Required parameter "nudgeId" was null or undefined when calling cancelNudge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/nudges/{nudge_id}/cancel`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));
        urlPath = urlPath.replace('{nudge_id}', encodeURIComponent(String(requestParameters['nudgeId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Withdraws direction you sent with `/nudges`, as long as the turn has not picked it up yet. Cancelling something already cancelled returns it unchanged, so retrying is safe.  Cancelling races the turn, and whichever happens first wins outright: you either withdraw it cleanly or the turn uses it. It is never half-applied. If the turn got there first, you get a conflict and the entry stays `drained`.
     * Withdraw a Nudge the Turn has not taken
     */
    async cancelNudgeRaw(requestParameters: CancelNudgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Nudge>> {
        const requestOptions = await this.cancelNudgeRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NudgeFromJSON(jsonValue));
    }

    /**
     * Withdraws direction you sent with `/nudges`, as long as the turn has not picked it up yet. Cancelling something already cancelled returns it unchanged, so retrying is safe.  Cancelling races the turn, and whichever happens first wins outright: you either withdraw it cleanly or the turn uses it. It is never half-applied. If the turn got there first, you get a conflict and the entry stays `drained`.
     * Withdraw a Nudge the Turn has not taken
     */
    async cancelNudge(requestParameters: CancelNudgeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Nudge> {
        const response = await this.cancelNudgeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for cancelTurn without sending the request
     */
    async cancelTurnRequestOpts(requestParameters: CancelTurnRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling cancelTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/cancel`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Stops a turn and discards what it produced. The turn ends `cancelled` and its work does not carry into the next turn — use interrupt instead if you want to keep it.  Safe to repeat. Cancelling a turn that already finished returns it unchanged rather than failing. A successful response means the cancellation is recorded and will stick. Work already sent to the model provider stops as soon as it can, so you may still be billed for what had run by then.  Send an empty request body.
     * Stop a Turn and discard its work
     */
    async cancelTurnRaw(requestParameters: CancelTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Turn>> {
        const requestOptions = await this.cancelTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnFromJSON(jsonValue));
    }

    /**
     * Stops a turn and discards what it produced. The turn ends `cancelled` and its work does not carry into the next turn — use interrupt instead if you want to keep it.  Safe to repeat. Cancelling a turn that already finished returns it unchanged rather than failing. A successful response means the cancellation is recorded and will stick. Work already sent to the model provider stops as soon as it can, so you may still be billed for what had run by then.  Send an empty request body.
     * Stop a Turn and discard its work
     */
    async cancelTurn(requestParameters: CancelTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Turn> {
        const response = await this.cancelTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createNudge without sending the request
     */
    async createNudgeRequestOpts(requestParameters: CreateNudgeOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling createNudge().'
            );
        }

        if (requestParameters['createNudgeRequest'] == null) {
            throw new runtime.RequiredError(
                'createNudgeRequest',
                'Required parameter "createNudgeRequest" was null or undefined when calling createNudge().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/nudges`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateNudgeRequestToJSON(requestParameters['createNudgeRequest']),
        };
    }

    /**
     * Sends extra direction to a turn that is already running — \"focus on the marine segment\" — without stopping it and without losing the work you are steering. Use this when a long turn is heading the wrong way and you want to correct it in place.  Compare with `conversation.if_active: supersede` on a new Turn, which replaces the running turn and discards what it had produced. Steering a long turn that way throws away exactly the work you were trying to redirect.  **A nudge is not an interrupt, and it is not immediate.** The turn picks it up at its next model-call boundary: before the next model call in a running builtin or MCP tool loop, when a host-tool result resumes the next execution segment, or when a turn that thought it was finished re-enters its loop to answer you. A parked host tool is not woken; its result still has to resume the turn. A model call or tool run already in flight is never aborted to deliver a Nudge. A turn you have interrupted is never given more work — the interrupt wins and the direction you staged expires unused.  Nudges and Turns never turn into each other. Posting to `/v1/turns` against a busy Conversation behaves exactly as its `conversation.if_active` setting says; it never quietly becomes a nudge, and a nudge never quietly becomes a new turn.  If the turn ends without ever picking it up, your Nudge is marked `expired` at that moment and has no effect on any later turn. Check `GET .../nudges` to see whether it was used or missed. Whether to re-send missed direction as the next turn\'s input is your call.  `content` must be text — a string, or an array of text blocks. Images and documents are fine on a turn\'s own input but are refused here, because a turn resuming in place carries text only, and silently dropping your attachment would be worse than telling you now.  Requires the same permission as cancelling the turn.
     * Send extra direction to a running Turn
     */
    async createNudgeRaw(requestParameters: CreateNudgeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NudgeAcknowledgement>> {
        const requestOptions = await this.createNudgeRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NudgeAcknowledgementFromJSON(jsonValue));
    }

    /**
     * Sends extra direction to a turn that is already running — \"focus on the marine segment\" — without stopping it and without losing the work you are steering. Use this when a long turn is heading the wrong way and you want to correct it in place.  Compare with `conversation.if_active: supersede` on a new Turn, which replaces the running turn and discards what it had produced. Steering a long turn that way throws away exactly the work you were trying to redirect.  **A nudge is not an interrupt, and it is not immediate.** The turn picks it up at its next model-call boundary: before the next model call in a running builtin or MCP tool loop, when a host-tool result resumes the next execution segment, or when a turn that thought it was finished re-enters its loop to answer you. A parked host tool is not woken; its result still has to resume the turn. A model call or tool run already in flight is never aborted to deliver a Nudge. A turn you have interrupted is never given more work — the interrupt wins and the direction you staged expires unused.  Nudges and Turns never turn into each other. Posting to `/v1/turns` against a busy Conversation behaves exactly as its `conversation.if_active` setting says; it never quietly becomes a nudge, and a nudge never quietly becomes a new turn.  If the turn ends without ever picking it up, your Nudge is marked `expired` at that moment and has no effect on any later turn. Check `GET .../nudges` to see whether it was used or missed. Whether to re-send missed direction as the next turn\'s input is your call.  `content` must be text — a string, or an array of text blocks. Images and documents are fine on a turn\'s own input but are refused here, because a turn resuming in place carries text only, and silently dropping your attachment would be worse than telling you now.  Requires the same permission as cancelling the turn.
     * Send extra direction to a running Turn
     */
    async createNudge(requestParameters: CreateNudgeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NudgeAcknowledgement> {
        const response = await this.createNudgeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createTurn without sending the request
     */
    async createTurnRequestOpts(requestParameters: CreateTurnOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createTurnRequest'] == null) {
            throw new runtime.RequiredError(
                'createTurnRequest',
                'Required parameter "createTurnRequest" was null or undefined when calling createTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xAnthropicApiKey'] != null) {
            headerParameters['X-Anthropic-Api-Key'] = String(requestParameters['xAnthropicApiKey']);
        }

        if (requestParameters['xOpenaiApiKey'] != null) {
            headerParameters['X-Openai-Api-Key'] = String(requestParameters['xOpenaiApiKey']);
        }

        if (requestParameters['xGeminiApiKey'] != null) {
            headerParameters['X-Gemini-Api-Key'] = String(requestParameters['xGeminiApiKey']);
        }

        if (requestParameters['xXaiApiKey'] != null) {
            headerParameters['X-Xai-Api-Key'] = String(requestParameters['xXaiApiKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateTurnRequestToJSON(requestParameters['createTurnRequest']),
        };
    }

    /**
     * Resolves one stored AgentRevision or inline behavior, optional memory, and optional Conversation in the same admission transaction. A `202` means the Turn is durable and may be followed, recovered, or resumed by ID.  Omit `conversation` for a standalone Turn. Behavior, memory, Conversation, and actor are independent: a Conversation supplies continuity only, and `user_key` attributes this Turn without selecting Agent or memory ownership.  Trusted-host idempotency keys are scoped to the resolved tenant. An equal retry returns the original Turn; a changed request conflicts. If that Turn\'s content has since been erased, the equal admission retry still returns `202` with `deduplicated: true`, `erased_at`, and the retained content-free Turn receipt. It does not re-resolve the requested Agent, MemorySpace, or Conversation. An ordinary `GET /v1/turns/{turn_id}` continues to return `410 turn_erased`. Machine callers supply `behavior`; browser callers rely on the exact AgentRevision pinned by their verified grant.
     * Start one Turn in the background
     */
    async createTurnRaw(requestParameters: CreateTurnOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Turn>> {
        const requestOptions = await this.createTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnFromJSON(jsonValue));
    }

    /**
     * Resolves one stored AgentRevision or inline behavior, optional memory, and optional Conversation in the same admission transaction. A `202` means the Turn is durable and may be followed, recovered, or resumed by ID.  Omit `conversation` for a standalone Turn. Behavior, memory, Conversation, and actor are independent: a Conversation supplies continuity only, and `user_key` attributes this Turn without selecting Agent or memory ownership.  Trusted-host idempotency keys are scoped to the resolved tenant. An equal retry returns the original Turn; a changed request conflicts. If that Turn\'s content has since been erased, the equal admission retry still returns `202` with `deduplicated: true`, `erased_at`, and the retained content-free Turn receipt. It does not re-resolve the requested Agent, MemorySpace, or Conversation. An ordinary `GET /v1/turns/{turn_id}` continues to return `410 turn_erased`. Machine callers supply `behavior`; browser callers rely on the exact AgentRevision pinned by their verified grant.
     * Start one Turn in the background
     */
    async createTurn(requestParameters: CreateTurnOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Turn> {
        const response = await this.createTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteTurn without sending the request
     */
    async deleteTurnRequestOpts(requestParameters: DeleteTurnRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling deleteTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Erases the private content carried by a terminal standalone turn while retaining its minimal usage, lineage, and idempotency facts. A conversation-bound Turn is erased through its Conversation instead.
     * Erase a terminal standalone Turn
     */
    async deleteTurnRaw(requestParameters: DeleteTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Erases the private content carried by a terminal standalone turn while retaining its minimal usage, lineage, and idempotency facts. A conversation-bound Turn is erased through its Conversation instead.
     * Erase a terminal standalone Turn
     */
    async deleteTurn(requestParameters: DeleteTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTurnRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getTrace without sending the request
     */
    async getTraceRequestOpts(requestParameters: GetTraceRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['traceId'] == null) {
            throw new runtime.RequiredError(
                'traceId',
                'Required parameter "traceId" was null or undefined when calling getTrace().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/traces/{trace_id}`;
        urlPath = urlPath.replace('{trace_id}', encodeURIComponent(String(requestParameters['traceId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a content-free projection of up to 200 OpenTelemetry spans. Use the pageable Turn log endpoint with `trace_id` for associated logs. `is_partial` says when the agent root has not arrived or the bounded read omitted spans. nvoken grounds the trace\'s Turn attribution in its durable Turn record before returning it; knowing a W3C trace ID grants no authority.
     * Read one hosted agent trace
     */
    async getTraceRaw(requestParameters: GetTraceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Trace>> {
        const requestOptions = await this.getTraceRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TraceFromJSON(jsonValue));
    }

    /**
     * Returns a content-free projection of up to 200 OpenTelemetry spans. Use the pageable Turn log endpoint with `trace_id` for associated logs. `is_partial` says when the agent root has not arrived or the bounded read omitted spans. nvoken grounds the trace\'s Turn attribution in its durable Turn record before returning it; knowing a W3C trace ID grants no authority.
     * Read one hosted agent trace
     */
    async getTrace(requestParameters: GetTraceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Trace> {
        const response = await this.getTraceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTurn without sending the request
     */
    async getTurnRequestOpts(requestParameters: GetTurnRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling getTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The turn\'s current state, including anything that went wrong after it started.  A credential that can authenticate but lacks permission for this read gets `forbidden`. A turn belonging to another tenant is reported as `not_found` rather than `forbidden`, so you cannot use this endpoint to discover whether an ID exists outside your scope.
     * Read authoritative Turn identity and state
     */
    async getTurnRaw(requestParameters: GetTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Turn>> {
        const requestOptions = await this.getTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnFromJSON(jsonValue));
    }

    /**
     * The turn\'s current state, including anything that went wrong after it started.  A credential that can authenticate but lacks permission for this read gets `forbidden`. A turn belonging to another tenant is reported as `not_found` rather than `forbidden`, so you cannot use this endpoint to discover whether an ID exists outside your scope.
     * Read authoritative Turn identity and state
     */
    async getTurn(requestParameters: GetTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Turn> {
        const response = await this.getTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTurnResult without sending the request
     */
    async getTurnResultRequestOpts(requestParameters: GetTurnResultRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling getTurnResult().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/result`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the turn and the messages it produced, at any status. This is the convenient read for \"what did the agent answer?\" — `output_text` gives you the final-answer text already joined into a single string, without intermediate commentary, so you do not have to walk the message blocks yourself.  The turn and its messages are read from one consistent database snapshot, so you will never see a finished turn whose last message is missing.  Authentication, tenant scoping, and the not-found behavior are the same as reading the Turn on its own.
     * Read a Turn together with its messages
     */
    async getTurnResultRaw(requestParameters: GetTurnResultRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TurnResult>> {
        const requestOptions = await this.getTurnResultRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnResultFromJSON(jsonValue));
    }

    /**
     * Returns the turn and the messages it produced, at any status. This is the convenient read for \"what did the agent answer?\" — `output_text` gives you the final-answer text already joined into a single string, without intermediate commentary, so you do not have to walk the message blocks yourself.  The turn and its messages are read from one consistent database snapshot, so you will never see a finished turn whose last message is missing.  Authentication, tenant scoping, and the not-found behavior are the same as reading the Turn on its own.
     * Read a Turn together with its messages
     */
    async getTurnResult(requestParameters: GetTurnResultRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TurnResult> {
        const response = await this.getTurnResultRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTurnTimeline without sending the request
     */
    async getTurnTimelineRequestOpts(requestParameters: GetTurnTimelineRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling getTurnTimeline().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/timeline`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Assembles lifecycle waits, model calls, tool calls, nudges, and compactions from one database snapshot. It contains timings and usage, never prompts, responses, tool arguments, results, or error text. After Conversation erasure it degrades to the retained facts-only skeleton.
     * Read the durable execution waterfall for one Turn
     */
    async getTurnTimelineRaw(requestParameters: GetTurnTimelineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TurnTimeline>> {
        const requestOptions = await this.getTurnTimelineRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnTimelineFromJSON(jsonValue));
    }

    /**
     * Assembles lifecycle waits, model calls, tool calls, nudges, and compactions from one database snapshot. It contains timings and usage, never prompts, responses, tool arguments, results, or error text. After Conversation erasure it degrades to the retained facts-only skeleton.
     * Read the durable execution waterfall for one Turn
     */
    async getTurnTimeline(requestParameters: GetTurnTimelineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TurnTimeline> {
        const response = await this.getTurnTimelineRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for interruptTurn without sending the request
     */
    async interruptTurnRequestOpts(requestParameters: InterruptTurnRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling interruptTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/interrupt`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Asks a running turn to stop at its next clean stopping point. It ends `completed` with `stop_reason: interrupted`, and everything it produced — the model\'s replies and any tool results — stays in the conversation for the next turn. That is the whole difference from cancelling, which throws the turn\'s work away.  The request is recorded and safe to repeat. What happens next depends on what the turn was doing:  - Between steps (`queued`, `waiting`, or `running` with nothing   actively executing) it stops before this call returns. Any tool   calls you still owed results for are closed out, so submitting one   afterwards returns `409`. - Mid-step, nvoken records the request and returns the turn still   `running`. It stops at the next checkpoint, at worst one model call   later. Watch the stream or re-read the turn to see it end.  Interrupting a turn that has already finished changes nothing and returns it as-is. A turn that was asked for structured output but never produced a valid object ends `failed` with `structured_output_unsatisfied` rather than `completed`. Either way usage is reported in full and billed, because the work was kept.  Send an empty request body.
     * Stop a Turn but keep what it produced
     */
    async interruptTurnRaw(requestParameters: InterruptTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Turn>> {
        const requestOptions = await this.interruptTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnFromJSON(jsonValue));
    }

    /**
     * Asks a running turn to stop at its next clean stopping point. It ends `completed` with `stop_reason: interrupted`, and everything it produced — the model\'s replies and any tool results — stays in the conversation for the next turn. That is the whole difference from cancelling, which throws the turn\'s work away.  The request is recorded and safe to repeat. What happens next depends on what the turn was doing:  - Between steps (`queued`, `waiting`, or `running` with nothing   actively executing) it stops before this call returns. Any tool   calls you still owed results for are closed out, so submitting one   afterwards returns `409`. - Mid-step, nvoken records the request and returns the turn still   `running`. It stops at the next checkpoint, at worst one model call   later. Watch the stream or re-read the turn to see it end.  Interrupting a turn that has already finished changes nothing and returns it as-is. A turn that was asked for structured output but never produced a valid object ends `failed` with `structured_output_unsatisfied` rather than `completed`. Either way usage is reported in full and billed, because the work was kept.  Send an empty request body.
     * Stop a Turn but keep what it produced
     */
    async interruptTurn(requestParameters: InterruptTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Turn> {
        const response = await this.interruptTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listNudges without sending the request
     */
    async listNudgesRequestOpts(requestParameters: ListNudgesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling listNudges().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/nudges`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Lists the direction you have sent to this turn with `/nudges`, in the order the turn will pick it up. Entries stay listed after they are used or missed, so you can answer \"what did the user say, and did the model ever see it?\"  Check `status` on each entry: `drained` means the turn used it, `expired` means the turn ended first, `cancelled` means you withdrew it.
     * List Nudges for a Turn
     */
    async listNudgesRaw(requestParameters: ListNudgesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NudgeList>> {
        const requestOptions = await this.listNudgesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NudgeListFromJSON(jsonValue));
    }

    /**
     * Lists the direction you have sent to this turn with `/nudges`, in the order the turn will pick it up. Entries stay listed after they are used or missed, so you can answer \"what did the user say, and did the model ever see it?\"  Check `status` on each entry: `drained` means the turn used it, `expired` means the turn ended first, `cancelled` means you withdrew it.
     * List Nudges for a Turn
     */
    async listNudges(requestParameters: ListNudgesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NudgeList> {
        const response = await this.listNudgesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listToolCalls without sending the request
     */
    async listToolCallsRequestOpts(requestParameters: ListToolCallsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling listToolCalls().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/tool-calls`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns ToolCalls in execution discovery order. Every execution mode is included. The records contain lifecycle and timing facts only. Tool inputs and results remain in the canonical Conversation transcript.  Callback records include a delivery object. Its terminal outcome, attempt count, and last HTTP status remain available after the bounded delivery transport row is pruned. These records use the same authentication, tenant scope, Conversation constraint, and nondisclosing not_found behavior as the Turn read. Deleting the Conversation deletes these records with the rest of its subtree.
     * Page through durable ToolCall execution records
     */
    async listToolCallsRaw(requestParameters: ListToolCallsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ToolCallList>> {
        const requestOptions = await this.listToolCallsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ToolCallListFromJSON(jsonValue));
    }

    /**
     * Returns ToolCalls in execution discovery order. Every execution mode is included. The records contain lifecycle and timing facts only. Tool inputs and results remain in the canonical Conversation transcript.  Callback records include a delivery object. Its terminal outcome, attempt count, and last HTTP status remain available after the bounded delivery transport row is pruned. These records use the same authentication, tenant scope, Conversation constraint, and nondisclosing not_found behavior as the Turn read. Deleting the Conversation deletes these records with the rest of its subtree.
     * Page through durable ToolCall execution records
     */
    async listToolCalls(requestParameters: ListToolCallsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ToolCallList> {
        const response = await this.listToolCallsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTurnLogs without sending the request
     */
    async listTurnLogsRequestOpts(requestParameters: ListTurnLogsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling listTurnLogs().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['traceId'] != null) {
            queryParameters['trace_id'] = requestParameters['traceId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/logs`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the content-free structured lifecycle logs associated by the Turn ID. Arbitrary attributes and raw error values are omitted. `status` is `disabled` when this installation has no configured observation store.
     * Page through hosted structured logs for one Turn
     */
    async listTurnLogsRaw(requestParameters: ListTurnLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TurnLogList>> {
        const requestOptions = await this.listTurnLogsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnLogListFromJSON(jsonValue));
    }

    /**
     * Returns the content-free structured lifecycle logs associated by the Turn ID. Arbitrary attributes and raw error values are omitted. `status` is `disabled` when this installation has no configured observation store.
     * Page through hosted structured logs for one Turn
     */
    async listTurnLogs(requestParameters: ListTurnLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TurnLogList> {
        const response = await this.listTurnLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTurnTraces without sending the request
     */
    async listTurnTracesRequestOpts(requestParameters: ListTurnTracesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling listTurnTraces().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/traces`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns newest-first, content-free summaries exported from Dive through OpenTelemetry. A child-only trace is returned as `is_partial: true` while its agent root is still open or if the process exits before that root is exported. Traces remain diagnostic and best-effort; the durable Turn timeline is the execution authority. `status` is `disabled` when this installation has no configured observation store.
     * Page through hosted agent traces for one Turn
     */
    async listTurnTracesRaw(requestParameters: ListTurnTracesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TraceList>> {
        const requestOptions = await this.listTurnTracesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TraceListFromJSON(jsonValue));
    }

    /**
     * Returns newest-first, content-free summaries exported from Dive through OpenTelemetry. A child-only trace is returned as `is_partial: true` while its agent root is still open or if the process exits before that root is exported. Traces remain diagnostic and best-effort; the durable Turn timeline is the execution authority. `status` is `disabled` when this installation has no configured observation store.
     * Page through hosted agent traces for one Turn
     */
    async listTurnTraces(requestParameters: ListTurnTracesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TraceList> {
        const response = await this.listTurnTracesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTurns without sending the request
     */
    async listTurnsRequestOpts(requestParameters: ListTurnsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['tenantKey'] != null) {
            queryParameters['tenant_key'] = requestParameters['tenantKey'];
        }

        if (requestParameters['defaultTenant'] != null) {
            queryParameters['default_tenant'] = requestParameters['defaultTenant'];
        }

        if (requestParameters['userKey'] != null) {
            queryParameters['user_key'] = requestParameters['userKey'];
        }

        if (requestParameters['conversationId'] != null) {
            queryParameters['conversation_id'] = requestParameters['conversationId'];
        }

        if (requestParameters['agentId'] != null) {
            queryParameters['agent_id'] = requestParameters['agentId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['parentTurnId'] != null) {
            queryParameters['parent_turn_id'] = requestParameters['parentTurnId'];
        }

        if (requestParameters['ended'] != null) {
            queryParameters['ended'] = requestParameters['ended'];
        }

        if (requestParameters['endedSince'] != null) {
            queryParameters['ended_since'] = (requestParameters['endedSince'] as any).toISOString();
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns newest-first durable Turn state. Exact filters combine with AND. An App credential without a tenant constraint may list all tenant partitions in that App, one named partition with `tenant_key`, or the default partition with `default_tenant=true`. A tenant-constrained credential is always scoped to its partition. The opaque cursor is bound to the normalized filter set and credential tenant scope. Agent filtering uses the persisted Agent ID; an `agent_key` is owner-namespaced lookup input, not a Turn relationship.  ## `ended=true` makes this a reconciliation feed  Set it and the same operation reverses into a feed: every Turn that reached a terminal status, **oldest first by the moment it ended**, each appearing exactly once. Walk it and append by `id`.  This is the backstop for settlement. `turn.ended` webhooks are delivered at least once, which narrows the window but does not close it: a delivery that never lands leaves a turn nobody settles, and that failure is silent — no error, just a ledger row that was never written. Reading the feed to the end is how you find out.  The default listing cannot do that job. Newest-first over current state means a turn that ends while you page moves under you, and filtering by terminal status gives you a set with no position in it. Ending order is the only order you can resume.  Start with `ended_since`, or with no position at all to begin at the oldest retained turn. Then send back `next_cursor`, which in this mode is present on every response including an empty page, so a consumer that catches up keeps its position without special-casing. Keep going while `has_more` is true; when it is false you are caught up and can wait before asking again.  `complete_through` is returned in this mode and is the instant the feed is complete to. Turns that ended after it are held back until their settling transactions are certainly visible, because a turn that appeared behind your cursor would be one you never see again. It trails the present by a bounded interval, so a consumer is always slightly behind and never wrong; it is also the number to alarm on, since a `complete_through` that stops advancing means settlement has stalled rather than that nothing ended.  A cursor carries its mode, so one from the default listing cannot resume the feed and the reverse is also refused. Erased Conversations take their Turns with them, so a turn deleted before you read it never appears; reconcile before you erase.
     * List authoritative Turns, or walk the ones that ended
     */
    async listTurnsRaw(requestParameters: ListTurnsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TurnList>> {
        const requestOptions = await this.listTurnsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnListFromJSON(jsonValue));
    }

    /**
     * Returns newest-first durable Turn state. Exact filters combine with AND. An App credential without a tenant constraint may list all tenant partitions in that App, one named partition with `tenant_key`, or the default partition with `default_tenant=true`. A tenant-constrained credential is always scoped to its partition. The opaque cursor is bound to the normalized filter set and credential tenant scope. Agent filtering uses the persisted Agent ID; an `agent_key` is owner-namespaced lookup input, not a Turn relationship.  ## `ended=true` makes this a reconciliation feed  Set it and the same operation reverses into a feed: every Turn that reached a terminal status, **oldest first by the moment it ended**, each appearing exactly once. Walk it and append by `id`.  This is the backstop for settlement. `turn.ended` webhooks are delivered at least once, which narrows the window but does not close it: a delivery that never lands leaves a turn nobody settles, and that failure is silent — no error, just a ledger row that was never written. Reading the feed to the end is how you find out.  The default listing cannot do that job. Newest-first over current state means a turn that ends while you page moves under you, and filtering by terminal status gives you a set with no position in it. Ending order is the only order you can resume.  Start with `ended_since`, or with no position at all to begin at the oldest retained turn. Then send back `next_cursor`, which in this mode is present on every response including an empty page, so a consumer that catches up keeps its position without special-casing. Keep going while `has_more` is true; when it is false you are caught up and can wait before asking again.  `complete_through` is returned in this mode and is the instant the feed is complete to. Turns that ended after it are held back until their settling transactions are certainly visible, because a turn that appeared behind your cursor would be one you never see again. It trails the present by a bounded interval, so a consumer is always slightly behind and never wrong; it is also the number to alarm on, since a `complete_through` that stops advancing means settlement has stalled rather than that nothing ended.  A cursor carries its mode, so one from the default listing cannot resume the feed and the reverse is also refused. Erased Conversations take their Turns with them, so a turn deleted before you read it never appears; reconcile before you erase.
     * List authoritative Turns, or walk the ones that ended
     */
    async listTurns(requestParameters: ListTurnsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TurnList> {
        const response = await this.listTurnsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for resumeTurn without sending the request
     */
    async resumeTurnRequestOpts(requestParameters: ResumeTurnOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling resumeTurn().'
            );
        }

        if (requestParameters['resumeTurnRequest'] == null) {
            throw new runtime.RequiredError(
                'resumeTurnRequest',
                'Required parameter "resumeTurnRequest" was null or undefined when calling resumeTurn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/resume`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResumeTurnRequestToJSON(requestParameters['resumeTurnRequest']),
        };
    }

    /**
     * Continues a turn on `budget_hold` because one of its own consumption limits ran out. Send `limits` containing only the limit that ran out, raised above both its old value and what the turn has already used, and still within what your installation allows.  If the turn is held because the tenant ran out of credits rather than on a limit of its own, allocate credits to that account instead — this endpoint refuses it, and funding the account continues the turn on its own. Deadlines never put a turn on budget hold, so they never bring you here.
     * Raise a held Turn\'s limit and continue it
     */
    async resumeTurnRaw(requestParameters: ResumeTurnOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Turn>> {
        const requestOptions = await this.resumeTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TurnFromJSON(jsonValue));
    }

    /**
     * Continues a turn on `budget_hold` because one of its own consumption limits ran out. Send `limits` containing only the limit that ran out, raised above both its old value and what the turn has already used, and still within what your installation allows.  If the turn is held because the tenant ran out of credits rather than on a limit of its own, allocate credits to that account instead — this endpoint refuses it, and funding the account continues the turn on its own. Deadlines never put a turn on budget hold, so they never bring you here.
     * Raise a held Turn\'s limit and continue it
     */
    async resumeTurn(requestParameters: ResumeTurnOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Turn> {
        const response = await this.resumeTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for streamTurn without sending the request
     */
    async streamTurnRequestOpts(requestParameters: StreamTurnRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling streamTurn().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['deltas'] != null) {
            queryParameters['deltas'] = requestParameters['deltas'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['lastEventID'] != null) {
            headerParameters['Last-Event-ID'] = String(requestParameters['lastEventID']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/stream`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Carries only this turn\'s durable transcript updates and optional live previews, then closes after its terminal change is delivered. The cursor is scoped to the Turn and never reveals an internal carrier Conversation for a standalone turn.
     * Follow one Turn over Server-Sent Events
     */
    async streamTurnRaw(requestParameters: StreamTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationStreamEvent>> {
        const requestOptions = await this.streamTurnRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationStreamEventFromJSON(jsonValue));
    }

    /**
     * Carries only this turn\'s durable transcript updates and optional live previews, then closes after its terminal change is delivered. The cursor is scoped to the Turn and never reveals an internal carrier Conversation for a standalone turn.
     * Follow one Turn over Server-Sent Events
     */
    async streamTurn(requestParameters: StreamTurnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationStreamEvent> {
        const response = await this.streamTurnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for submitHostToolResults without sending the request
     */
    async submitHostToolResultsRequestOpts(requestParameters: SubmitHostToolResultsOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['turnId'] == null) {
            throw new runtime.RequiredError(
                'turnId',
                'Required parameter "turnId" was null or undefined when calling submitHostToolResults().'
            );
        }

        if (requestParameters['submitHostToolResultsRequest'] == null) {
            throw new runtime.RequiredError(
                'submitHostToolResultsRequest',
                'Required parameter "submitHostToolResultsRequest" was null or undefined when calling submitHostToolResults().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/turns/{turn_id}/tool-results`;
        urlPath = urlPath.replace('{turn_id}', encodeURIComponent(String(requestParameters['turnId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubmitHostToolResultsRequestToJSON(requestParameters['submitHostToolResultsRequest']),
        };
    }

    /**
     * Atomically accepts one bounded batch for a waiting Turn. The first committed result for each ToolCall wins. An equal replay is acknowledged as deduplicated; a changed replay conflicts. Partial batches leave the Turn waiting. Closing the final pending call queues the same Turn and its successor dispatch before returning `202`.  This command accepts only host- or callback-mode calls owned by the path Turn and authenticated tenant scope. It is not a generic Conversation append endpoint. The body is limited to 1 MiB; each result content value is valid JSON limited to 256 KiB and 32 nesting levels.  `content` accepts any JSON value and the stored transcript retains it verbatim. Before a result reaches the model, a string or an array of content blocks passes through unchanged; any other value is serialized to its compact JSON text and sent as a string, so the model sees the same bytes a host that pre-stringifies would send.
     * Submit durable results for pending host and callback ToolCalls
     */
    async submitHostToolResultsRaw(requestParameters: SubmitHostToolResultsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubmitHostToolResultsResponse>> {
        const requestOptions = await this.submitHostToolResultsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubmitHostToolResultsResponseFromJSON(jsonValue));
    }

    /**
     * Atomically accepts one bounded batch for a waiting Turn. The first committed result for each ToolCall wins. An equal replay is acknowledged as deduplicated; a changed replay conflicts. Partial batches leave the Turn waiting. Closing the final pending call queues the same Turn and its successor dispatch before returning `202`.  This command accepts only host- or callback-mode calls owned by the path Turn and authenticated tenant scope. It is not a generic Conversation append endpoint. The body is limited to 1 MiB; each result content value is valid JSON limited to 256 KiB and 32 nesting levels.  `content` accepts any JSON value and the stored transcript retains it verbatim. Before a result reaches the model, a string or an array of content blocks passes through unchanged; any other value is serialized to its compact JSON text and sent as a string, so the model sees the same bytes a host that pre-stringifies would send.
     * Submit durable results for pending host and callback ToolCalls
     */
    async submitHostToolResults(requestParameters: SubmitHostToolResultsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubmitHostToolResultsResponse> {
        const response = await this.submitHostToolResultsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
