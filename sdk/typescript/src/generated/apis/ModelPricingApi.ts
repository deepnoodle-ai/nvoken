/* tslint:disable */
/* eslint-disable */
/**
 * nvoken Runtime API
 * This focused contract defines nvoken\'s implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet\'s Data and retention section.  Inline and callback client tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type ModelPricingCapability,
    ModelPricingCapabilityFromJSON,
    ModelPricingCapabilityToJSON,
} from '../models/ModelPricingCapability.js';
import {
    type ModelProvider,
    ModelProviderFromJSON,
    ModelProviderToJSON,
} from '../models/ModelProvider.js';

export interface GetModelPricingCapabilityRequest {
    provider: ModelProvider;
    model: string;
}

/**
 *
 */
export class ModelPricingApi extends runtime.BaseAPI {

    /**
     * Creates request options for getModelPricingCapability without sending the request
     */
    async getModelPricingCapabilityRequestOpts(requestParameters: GetModelPricingCapabilityRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling getModelPricingCapability().'
            );
        }

        if (requestParameters['model'] == null) {
            throw new runtime.RequiredError(
                'model',
                'Required parameter "model" was null or undefined when calling getModelPricingCapability().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['model'] != null) {
            queryParameters['model'] = requestParameters['model'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/model-pricing-capabilities`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Reports whether this nvoken installation\'s embedded pricing registry has standard USD pricing for the exact provider and model ID. This is a local guardrail capability check only: it does not call the provider, verify provider-account access, or guarantee that a provider response will identify the same served model and include usable token evidence.  `priced` means the exact requested model has a standard USD entry. `unpriced` means the local registry can determine that it has no standard USD entry, so a cost-capped Invocation will fail before a provider call. `unknown` means the active adapter cannot decide before execution; capped work can fail closed after normalized response evidence is available.
     * Inspect local USD pricing capability for an exact model
     */
    async getModelPricingCapabilityRaw(requestParameters: GetModelPricingCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelPricingCapability>> {
        const requestOptions = await this.getModelPricingCapabilityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelPricingCapabilityFromJSON(jsonValue));
    }

    /**
     * Reports whether this nvoken installation\'s embedded pricing registry has standard USD pricing for the exact provider and model ID. This is a local guardrail capability check only: it does not call the provider, verify provider-account access, or guarantee that a provider response will identify the same served model and include usable token evidence.  `priced` means the exact requested model has a standard USD entry. `unpriced` means the local registry can determine that it has no standard USD entry, so a cost-capped Invocation will fail before a provider call. `unknown` means the active adapter cannot decide before execution; capped work can fail closed after normalized response evidence is available.
     * Inspect local USD pricing capability for an exact model
     */
    async getModelPricingCapability(requestParameters: GetModelPricingCapabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelPricingCapability> {
        const response = await this.getModelPricingCapabilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
