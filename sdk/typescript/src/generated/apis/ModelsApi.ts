/* tslint:disable */
/* eslint-disable */
/**
 * nvoken Runtime API
 * This focused contract defines nvoken\'s implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet\'s Data and retention section.  Inline and callback host tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse.js';
import {
    type ModelDescriptor,
    ModelDescriptorFromJSON,
    ModelDescriptorToJSON,
} from '../models/ModelDescriptor.js';
import {
    type ModelList,
    ModelListFromJSON,
    ModelListToJSON,
} from '../models/ModelList.js';
import {
    type ModelProvider,
    ModelProviderFromJSON,
    ModelProviderToJSON,
} from '../models/ModelProvider.js';

export interface GetModelRequest {
    provider: ModelProvider;
    modelId: string;
    ifNoneMatch?: string;
}

export interface ListModelsRequest {
    provider?: ModelProvider;
    includeDeprecated?: boolean;
    ifNoneMatch?: string;
}

/**
 *
 */
export class ModelsApi extends runtime.BaseAPI {

    /**
     * Creates request options for getModel without sending the request
     */
    async getModelRequestOpts(requestParameters: GetModelRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['provider'] == null) {
            throw new runtime.RequiredError(
                'provider',
                'Required parameter "provider" was null or undefined when calling getModel().'
            );
        }

        if (requestParameters['modelId'] == null) {
            throw new runtime.RequiredError(
                'modelId',
                'Required parameter "modelId" was null or undefined when calling getModel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['If-None-Match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/models/{provider}/{model_id}`;
        urlPath = urlPath.replace('{provider}', encodeURIComponent(String(requestParameters['provider'])));
        urlPath = urlPath.replace('{model_id}', encodeURIComponent(String(requestParameters['modelId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a descriptor for any valid installed provider and exact model ID, including IDs outside the curated catalog. `cataloged` distinguishes maintained metadata from tolerant inspection. Pricing is the standard local USD estimate used by nvoken\'s estimated-cost guardrail, not provider billing or proof of account access.
     * Inspect one exact provider/model selection
     */
    async getModelRaw(requestParameters: GetModelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelDescriptor>> {
        const requestOptions = await this.getModelRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelDescriptorFromJSON(jsonValue));
    }

    /**
     * Returns a descriptor for any valid installed provider and exact model ID, including IDs outside the curated catalog. `cataloged` distinguishes maintained metadata from tolerant inspection. Pricing is the standard local USD estimate used by nvoken\'s estimated-cost guardrail, not provider billing or proof of account access.
     * Inspect one exact provider/model selection
     */
    async getModel(requestParameters: GetModelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelDescriptor> {
        const response = await this.getModelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listModels without sending the request
     */
    async listModelsRequestOpts(requestParameters: ListModelsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['includeDeprecated'] != null) {
            queryParameters['include_deprecated'] = requestParameters['includeDeprecated'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['ifNoneMatch'] != null) {
            headerParameters['If-None-Match'] = String(requestParameters['ifNoneMatch']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/models`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the complete bounded set of text-generation models nvoken intentionally advertises. Catalog membership means nvoken maintains metadata for the exact provider/model selection. It does not prove that the caller\'s provider account, region, or selected credential can access the model. Ordering is deterministic but has no semantic meaning.
     * List nvoken\'s curated model catalog
     */
    async listModelsRaw(requestParameters: ListModelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelList>> {
        const requestOptions = await this.listModelsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelListFromJSON(jsonValue));
    }

    /**
     * Returns the complete bounded set of text-generation models nvoken intentionally advertises. Catalog membership means nvoken maintains metadata for the exact provider/model selection. It does not prove that the caller\'s provider account, region, or selected credential can access the model. Ordering is deterministic but has no semantic meaning.
     * List nvoken\'s curated model catalog
     */
    async listModels(requestParameters: ListModelsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelList> {
        const response = await this.listModelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
