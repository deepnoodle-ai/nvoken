/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI's bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller's behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken's defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use super::{configuration, ContentType, Error};
use crate::{apis::ResponseContent, models};
use reqwest;
use serde::{de::Error as _, Deserialize, Serialize};

/// struct for typed errors of method [`cancel_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CancelInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`cancel_pending_input`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CancelPendingInputError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status409(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status409(models::ErrorResponse),
    Status422(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_invocation_result`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetInvocationResultError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`interrupt_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum InterruptInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_invocations`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListInvocationsError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_pending_inputs`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListPendingInputsError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_tool_calls`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListToolCallsError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`nudge_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum NudgeInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status409(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`resume_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ResumeInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status409(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`stream_invocation`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum StreamInvocationError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status429(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`submit_host_tool_results`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum SubmitHostToolResultsError {
    Status400(models::ErrorResponse),
    Status401(models::ErrorResponse),
    Status403(models::ErrorResponse),
    Status404(models::ErrorResponse),
    Status409(models::ErrorResponse),
    Status500(models::ErrorResponse),
    Status503(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// Atomically makes nonterminal work `cancelled`. Repeating the request, or cancelling an Invocation that already completed or failed, returns the unchanged authoritative terminal row. A successful response means cancellation is durable; provider work stops cooperatively and may have already incurred external cost. The request body must be empty.
pub async fn cancel_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
) -> Result<models::Invocation, Error<CancelInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/cancel",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Invocation`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Invocation`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<CancelInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Cancels input that is still `pending`. Cancelling input that is already `cancelled` returns it unchanged, so a retry is safe.  Cancel and drain serialize on the row, so whichever commits first wins outright: a cancelled input is never seen by the model, and input the executor already drained is reported `409 pending_input_settled` with the current resource in `details.pending_input` rather than being withdrawn from a transcript it is already part of. The request body must be empty.  Requires the same authority as cancelling the Invocation.
pub async fn cancel_pending_input(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    pending_input_id: &str,
) -> Result<models::PendingInput, Error<CancelPendingInputError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_path_pending_input_id = pending_input_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/pending-inputs/{pending_input_id}/cancel",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id),
        pending_input_id = crate::apis::urlencode(p_path_pending_input_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::PendingInput`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::PendingInput`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<CancelPendingInputError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Resolves or creates the installation-wide Agent identity anchor, resolves or creates a Session, resolves the inline or referenced definition, appends exactly one caller-input message, and creates one queued Invocation in a single transaction. The response is sent only after that transaction commits. The request handler never executes the model.  `session_id` and `session_key` are mutually exclusive. A Session ID must belong to the named Agent, or be a Session created without one — the first admitted Invocation binds the Agent permanently. An installation-wide credential may omit `tenant_key` and use the Session's stored partition. A tenant-constrained credential cannot cross its partition; an explicit mismatch is rejected with `403 forbidden` before resource lookup.  `idempotency_key` is scoped to the effective tenant partition, and `agent_key`. A same-key replay is checked before the Session's one-nonterminal-Invocation rule. It returns the original records without appending input, even when the Invocation is terminal. Material equality covers the Session selector kind and value, the resolved definition, and input. A `definition_id` and its byte-identical inline definition compare equal. Requested limits are compared before default resolution, so an explicit default differs from omission. JSON object member order is ignored, array order is significant, and strings are not rewritten. A changed material field returns `idempotency_conflict`.  `if_active` defaults to `reject`, preserving `session_invocation_active`. Explicit `supersede` requires both create and cancel authority. While holding the Session lock, it durably cancels any nonterminal Invocation and admits this replacement in the same transaction. With no active work it admits normally. Equal replay returns the original work before supersession and never cancels newer active work.  `interrupt` requires the same authority and keeps the same lock ordering, but stops the active Invocation gracefully instead of cancelling it: work already at a seam settles and the replacement is admitted in the same transaction, while work under a live lease has the interrupt recorded and this request waits for it to settle. If it has not settled within the wait, the response is `session_invocation_active` with `details.interrupt_requested = true` and the request may be reissued.  A cataloged model continues to admit while deprecated. On and after its `retires_at` date, a new admission is refused with `422 model_retired`; `details` carries the refused `model`, `retires_at`, the exact `replacement` provider/id pair, and the request `path`. Equal idempotent replay still returns work admitted before retirement.  A text-only request body is limited to 1 MiB. A body carrying image or document blocks is limited to 24 MiB, within which decoded media is bounded separately: at most 8 media blocks, 16 MiB decoded in total, 5 MiB per image, and 16 MiB per document. Requests over any of these limits are rejected before admission. URL media is fetched after an idempotent replay check and before any row is admitted. The guarded fetch accepts public HTTPS only, enforces the item limit while reading, and sniffs the returned bytes. nvoken stores those bytes and does not fetch the URL again.  The supported streaming pattern is admit-then-stream: admit with a plain JSON POST, then follow the Invocation over `GET /v1/invocations/{invocation_id}/stream`. It survives a dropped connection without re-admitting and does not depend on front-end streaming behavior. `Accept: text/event-stream` on this request is a deployment-dependent convenience, not a first-class mode: the committed admission is the first `invocation.accepted` frame and the connection tails that Invocation through `invocation.result`, but delivery requires the deployment front end to stream a non-`200` POST response unbuffered. Some managed front ends (including Cloud Run's) buffer it until the Invocation settles, which makes a host-tool turn's `waiting` state invisible on this connection.
pub async fn create_invocation(
    configuration: &configuration::Configuration,
    create_invocation_request: models::CreateInvocationRequest,
    x_anthropic_api_key: Option<&str>,
    x_openai_api_key: Option<&str>,
    x_gemini_api_key: Option<&str>,
    x_xai_api_key: Option<&str>,
) -> Result<models::Invocation, Error<CreateInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_body_create_invocation_request = create_invocation_request;
    let p_header_x_anthropic_api_key = x_anthropic_api_key;
    let p_header_x_openai_api_key = x_openai_api_key;
    let p_header_x_gemini_api_key = x_gemini_api_key;
    let p_header_x_xai_api_key = x_xai_api_key;

    let uri_str = format!("{}/v1/invocations", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_x_anthropic_api_key {
        req_builder = req_builder.header("X-Anthropic-Api-Key", param_value.to_string());
    }
    if let Some(param_value) = p_header_x_openai_api_key {
        req_builder = req_builder.header("X-Openai-Api-Key", param_value.to_string());
    }
    if let Some(param_value) = p_header_x_gemini_api_key {
        req_builder = req_builder.header("X-Gemini-Api-Key", param_value.to_string());
    }
    if let Some(param_value) = p_header_x_xai_api_key {
        req_builder = req_builder.header("X-Xai-Api-Key", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_create_invocation_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Invocation`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Invocation`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<CreateInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns the durable current state. Post-admission failures appear here. A credential authenticated for Runtime but denied this read operation receives `forbidden`. A resource outside the caller's tenant constraint is reported as `not_found` when disclosure would reveal its existence.
pub async fn get_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
) -> Result<models::Invocation, Error<GetInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Invocation`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Invocation`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns one InvocationResult at any status: the authoritative Invocation, this Invocation's canonical messages composed at read time, and the output_text convenience projection. The Invocation and its messages are read in one repeatable-read snapshot, so the payload never shows a terminal status with a missing message tail. Authentication, tenant scoping, and the nondisclosing not_found rule match the plain Invocation read exactly.
pub async fn get_invocation_result(
    configuration: &configuration::Configuration,
    invocation_id: &str,
) -> Result<models::InvocationResult, Error<GetInvocationResultError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/result",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::InvocationResult`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::InvocationResult`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetInvocationResultError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Asks the turn to stop at its next execution seam and settle `completed` with `stop_reason = interrupted`, so its assistant and tool messages stay in the next turn's generation context. That is the whole difference from cancellation, which drops the turn's work from every later turn.  The request is durable and idempotent. Work already at a seam — `queued`, `waiting`, or `running` with no live owner — settles before this call returns, closing any pending tool calls with synthetic results; a subsequently submitted tool result then gets the ordinary terminal `409`. Work under a live lease records the request and returns the Invocation unchanged, still `running`: its executor stops at the next checkpoint boundary, at worst one provider call away. Follow the Invocation stream or re-read it to observe settlement.  Interrupting terminal work is a no-op that returns the unchanged terminal row. A turn whose definition carries `structured_output` and that never published a validated object fails `structured_output_unsatisfied` instead of completing — the obligation was not met. Usage and settlement evidence are complete: the work was kept, so it is charged. The request body must be empty.
pub async fn interrupt_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
) -> Result<models::Invocation, Error<InterruptInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/interrupt",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Invocation`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Invocation`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<InterruptInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns newest-first durable Invocation state. Exact filters combine with AND. An installation-wide caller may list all tenant partitions, one named partition with `tenant_key`, or the default partition with `default_tenant=true`. A tenant-constrained credential is always scoped to its partition. The opaque cursor is bound to the normalized filter set and credential tenant scope. `agent_id` and `agent_key` are mutually exclusive; both normalize to the resolved Agent ID for cursor binding, so an equivalent cursor may resume under either spelling.
pub async fn list_invocations(
    configuration: &configuration::Configuration,
    tenant_key: Option<&str>,
    default_tenant: Option<bool>,
    user_key: Option<&str>,
    session_id: Option<&str>,
    agent_id: Option<&str>,
    agent_key: Option<&str>,
    status: Option<Vec<models::InvocationStatus>>,
    cursor: Option<&str>,
    limit: Option<u32>,
) -> Result<models::InvocationList, Error<ListInvocationsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_query_tenant_key = tenant_key;
    let p_query_default_tenant = default_tenant;
    let p_query_user_key = user_key;
    let p_query_session_id = session_id;
    let p_query_agent_id = agent_id;
    let p_query_agent_key = agent_key;
    let p_query_status = status;
    let p_query_cursor = cursor;
    let p_query_limit = limit;

    let uri_str = format!("{}/v1/invocations", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_tenant_key {
        req_builder = req_builder.query(&[("tenant_key", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_default_tenant {
        req_builder = req_builder.query(&[("default_tenant", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_user_key {
        req_builder = req_builder.query(&[("user_key", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_session_id {
        req_builder = req_builder.query(&[("session_id", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_agent_id {
        req_builder = req_builder.query(&[("agent_id", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_agent_key {
        req_builder = req_builder.query(&[("agent_key", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_status {
        req_builder = match "multi" {
            "multi" => req_builder.query(
                &param_value
                    .into_iter()
                    .map(|p| ("status".to_owned(), p.to_string()))
                    .collect::<Vec<(std::string::String, std::string::String)>>(),
            ),
            _ => req_builder.query(&[(
                "status",
                &param_value
                    .into_iter()
                    .map(|p| p.to_string())
                    .collect::<Vec<String>>()
                    .join(",")
                    .to_string(),
            )]),
        };
    }
    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::InvocationList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::InvocationList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ListInvocationsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns the staged queue in `(created_at, id)` ascending order — the order the turn will consume it — with settled rows retained so a caller can answer \"what did the user say, and did the model see it\". This, not an event vocabulary, is the reconciliation source for a surface that shows queued direction: a drained input also appears on the transcript stream as an ordinary `user` message.
pub async fn list_pending_inputs(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    status: Option<models::PendingInputStatus>,
    cursor: Option<&str>,
    limit: Option<u32>,
) -> Result<models::PendingInputList, Error<ListPendingInputsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_query_status = status;
    let p_query_cursor = cursor;
    let p_query_limit = limit;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/pending-inputs",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_status {
        req_builder = req_builder.query(&[("status", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::PendingInputList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::PendingInputList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ListPendingInputsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Returns ToolCalls in execution discovery order. Every execution mode is included. The records contain lifecycle and timing facts only. Tool inputs and results remain in the canonical Session transcript.  Callback records include a delivery object. Its terminal outcome, attempt count, and last HTTP status remain available after the bounded delivery transport row is pruned. These records use the same authentication, tenant scope, Session constraint, and nondisclosing not_found behavior as the Invocation read. Deleting the Session deletes these records with the rest of its subtree.
pub async fn list_tool_calls(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    cursor: Option<&str>,
    limit: Option<u32>,
) -> Result<models::ToolCallList, Error<ListToolCallsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_query_cursor = cursor;
    let p_query_limit = limit;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/tool-calls",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::ToolCallList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::ToolCallList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ListToolCallsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Stages caller guidance against a turn that is already running — \"focus on the marine segment\" — without ending it and without discarding the work being steered. This is the difference from `if_active: supersede`, which rewinds: a superseded turn's assistant and tool work product is dropped from the next generation, so steering a long turn by supersession throws away exactly what is being steered. Unlike Claude's `system.message`, a nudge is durable staged input consumed at an execution seam, not an event injected immediately into a managed session.  A nudge is not an interrupt. The earliest injection point is the next execution seam: the start of the next execution segment, which is a host-tool turn's park boundary, or the stop seam where a turn that considers itself finished re-enters its loop to answer the input in place. An in-flight provider call or tool run is never aborted for it, and an Invocation that has been interrupted is never given more to do — the interrupt wins the seam and the staged input expires.  A nudge is never implicitly minted and never implicitly converted. `POST /v1/invocations` against a busy Session keeps its `if_active` semantics; admission is never silently turned into a nudge, and a nudge is never silently turned into an Invocation. nvoken holds no agent configuration — the definition travels inline on every admission — so there is nothing it could legitimately run converted input with.  Input that the turn never takes is settled `expired` when the Invocation settles, in the same transaction and under the same Session lock this request takes. It never influences a later Invocation. Re-sending missed direction as the next Invocation's input is the host's call to make, and `GET .../pending-inputs` is where the miss is visible.  `content` is text: a string, or an array of text blocks. Image and document blocks are accepted on an Invocation's own input but not here, because the stop seam carries a continuation as text and staging content one seam would silently drop is worse than refusing it.  Requires the same authority as cancelling the Invocation.
pub async fn nudge_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    nudge_invocation_request: models::NudgeInvocationRequest,
) -> Result<models::NudgeAcknowledgement, Error<NudgeInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_body_nudge_invocation_request = nudge_invocation_request;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/nudge",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_nudge_invocation_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::NudgeAcknowledgement`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::NudgeAcknowledgement`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<NudgeInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Requeues an Invocation paused on a turn-level consumption ceiling. `limits` must contain only the exhausted field, raised above both its prior value and the amount already consumed, and the new value must remain within installation policy. A Session-budget pause resumes by raising or removing the Session budget instead. Deadlines never pause.
pub async fn resume_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    resume_invocation_request: models::ResumeInvocationRequest,
) -> Result<models::Invocation, Error<ResumeInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_body_resume_invocation_request = resume_invocation_request;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/resume",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_resume_invocation_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Invocation`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Invocation`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ResumeInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Replays durable updates after `cursor`, optionally forwards ephemeral output previews for this Invocation, and ends only when this Invocation settles or the connection rotates. Durable frames carry an SSE `id`; previews and control frames do not. After `stream.resync`, discard provisional output and wait for durable state.  The explicit `cursor` query parameter takes precedence over `Last-Event-ID`. `stream.end` reason `rotate` means reconnect with the last durable ID. `deltas=false` skips preview fan-out without changing replay, polling, cursor, or terminal semantics. Disconnecting never cancels the Invocation.
pub async fn stream_invocation(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    cursor: Option<&str>,
    deltas: Option<bool>,
    last_event_id: Option<&str>,
) -> Result<models::InvocationStreamEvent, Error<StreamInvocationError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_query_cursor = cursor;
    let p_query_deltas = deltas;
    let p_header_last_event_id = last_event_id;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/stream",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_cursor {
        req_builder = req_builder.query(&[("cursor", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_query_deltas {
        req_builder = req_builder.query(&[("deltas", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_last_event_id {
        req_builder = req_builder.header("Last-Event-ID", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::InvocationStreamEvent`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::InvocationStreamEvent`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<StreamInvocationError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// Atomically accepts one bounded batch for a waiting Invocation. The first committed result for each ToolCall wins. An equal replay is acknowledged as deduplicated; a changed replay conflicts. Partial batches leave the Invocation waiting. Closing the final pending call queues the same Invocation and its successor execution dispatch before returning `202`.  This command accepts only host-mode calls owned by the path Invocation and authenticated tenant scope. It is not a generic Session append endpoint. The body is limited to 1 MiB; each result content value is valid JSON limited to 256 KiB and 32 nesting levels.  `content` accepts any JSON value and the stored transcript retains it verbatim. Before a result reaches the model, a string or an array of content blocks passes through unchanged; any other value is serialized to its compact JSON text and sent as a string, so the model sees the same bytes a host that pre-stringifies would send.
pub async fn submit_host_tool_results(
    configuration: &configuration::Configuration,
    invocation_id: &str,
    submit_host_tool_results_request: models::SubmitHostToolResultsRequest,
) -> Result<models::SubmitHostToolResultsResponse, Error<SubmitHostToolResultsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_invocation_id = invocation_id;
    let p_body_submit_host_tool_results_request = submit_host_tool_results_request;

    let uri_str = format!(
        "{}/v1/invocations/{invocation_id}/tool-results",
        configuration.base_path,
        invocation_id = crate::apis::urlencode(p_path_invocation_id)
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_submit_host_tool_results_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::SubmitHostToolResultsResponse`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::SubmitHostToolResultsResponse`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<SubmitHostToolResultsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}
