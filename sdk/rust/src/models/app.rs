/*
 * nvoken API
 *
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque UUIDs and carry no type prefix. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes with `connection.closing` reason `settled` once that Turn's terminal change is delivered. A Conversation stream follows current and future Turns and may stay open while idle. Both routes accept the same `cursor`, `deltas`, and `Last-Event-ID` parameters and use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.  Reconnect with your last saved `cursor` after any close but `settled`. Honor the `retry:` delay the stream opens with, and back off when the server keeps refusing the connection: a flat retry from every client is what turns an outage into a load spike.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct App {
    /// The generated nvoken app identifier.
    #[serde(rename = "id")]
    pub id: String,
    /// Durable owning Org. Nullable only during the staged console migration and tightened after every existing App is claimed.
    #[serde(rename = "org_id", deserialize_with = "Option::deserialize")]
    pub org_id: Option<String>,
    /// The unique host-chosen name for this app.
    #[serde(rename = "name")]
    pub name: String,
    /// Transitional console-owner reference retained only to stamp existing Apps into Orgs during rollout. It is not an authorization boundary and is removed by the gated cleanup migration.
    #[serde(rename = "external_ref", deserialize_with = "Option::deserialize")]
    pub external_ref: Option<String>,
    /// Human-facing label; `name` stays the unique handle.
    #[serde(rename = "display_name", deserialize_with = "Option::deserialize")]
    pub display_name: Option<String>,
    /// Resolved deadline for each callback HTTP request whose tool does not name one of its own. Defaults to 10. Webhook delivery is unaffected.  This ceiling is 60 while a single tool may declare up to 300 in `callback.timeout_seconds`. The asymmetry is the point: this value governs every callback the App makes, so raising it to cover one slow tool would make a hung delivery of a fast one invisible for just as long. Slow is named per tool.
    #[serde(rename = "callback_timeout_seconds")]
    pub callback_timeout_seconds: u64,
    /// App-wide admission ceilings shared by machine, client-token, and anonymous-token callers. Null means unlimited machine admission; browser access requires finite values.
    #[serde(
        rename = "default_rate_limits",
        deserialize_with = "Option::deserialize"
    )]
    pub default_rate_limits: Option<Box<models::AppDefaultRateLimits>>,
    /// Optional per-tenant and per-user concurrency ceilings for machine credentials. Null leaves machine traffic subject only to the App-wide default_rate_limits.
    #[serde(
        rename = "machine_concurrency_limits",
        deserialize_with = "Option::deserialize"
    )]
    pub machine_concurrency_limits: Option<Box<models::MachineConcurrencyLimits>>,
    /// Complete browser-direct configuration. Null means browser access is disabled and client JWTs receive no browser CORS permission.
    #[serde(rename = "browser_access", deserialize_with = "Option::deserialize")]
    pub browser_access: Option<Box<models::BrowserAccess>>,
    /// Managed public-browser mode. Null means nvoken will not mint anonymous tokens for this App.
    #[serde(rename = "anonymous_access", deserialize_with = "Option::deserialize")]
    pub anonymous_access: Option<Box<models::AnonymousAccess>>,
    #[serde(rename = "credit_policy")]
    pub credit_policy: models::CreditPolicy,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    /// When the App was archived, or null while it is live. An archived App refuses admission and grant-minting with `409 app_archived` while every read, settlement, erasure, configuration, and revocation path stays open. Its credentials keep authenticating.
    #[serde(rename = "archived_at", deserialize_with = "Option::deserialize")]
    pub archived_at: Option<chrono::DateTime<chrono::FixedOffset>>,
}

impl App {
    pub fn new(
        id: String,
        org_id: Option<String>,
        name: String,
        external_ref: Option<String>,
        display_name: Option<String>,
        callback_timeout_seconds: u64,
        default_rate_limits: Option<models::AppDefaultRateLimits>,
        machine_concurrency_limits: Option<models::MachineConcurrencyLimits>,
        browser_access: Option<models::BrowserAccess>,
        anonymous_access: Option<models::AnonymousAccess>,
        credit_policy: models::CreditPolicy,
        created_at: chrono::DateTime<chrono::FixedOffset>,
        archived_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    ) -> App {
        App {
            id,
            org_id,
            name,
            external_ref,
            display_name,
            callback_timeout_seconds,
            default_rate_limits: if let Some(x) = default_rate_limits {
                Some(Box::new(x))
            } else {
                None
            },
            machine_concurrency_limits: if let Some(x) = machine_concurrency_limits {
                Some(Box::new(x))
            } else {
                None
            },
            browser_access: if let Some(x) = browser_access {
                Some(Box::new(x))
            } else {
                None
            },
            anonymous_access: if let Some(x) = anonymous_access {
                Some(Box::new(x))
            } else {
                None
            },
            credit_policy,
            created_at,
            archived_at,
        }
    }
}
