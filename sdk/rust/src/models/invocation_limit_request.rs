/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// InvocationLimitRequest : Optional requested limits. Total time bounds the entire turn, active time bounds model and tool execution, and waiting time bounds the cumulative time parked for host or callback tool results. Installation defaults supply all three time limits and the iteration limit. Output-token and estimated-cost limits are unlimited when omitted. Installation maxima may be lower than the schema's numeric range.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct InvocationLimitRequest {
    #[serde(
        rename = "total_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub total_timeout_seconds: Option<u32>,
    #[serde(
        rename = "active_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub active_timeout_seconds: Option<u32>,
    #[serde(
        rename = "waiting_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub waiting_timeout_seconds: Option<u32>,
    #[serde(rename = "max_output_tokens", skip_serializing_if = "Option::is_none")]
    pub max_output_tokens: Option<u32>,
    /// Dive list-price guardrail, not preauthorization or a billing ledger. Requires known USD pricing for the selected model and otherwise fails closed with `budget_exceeded` and `details.kind = estimated_cost_unavailable`. When pricing absence is knowable before execution, nvoken rejects before a provider call.
    #[serde(
        rename = "max_estimated_cost_usd",
        skip_serializing_if = "Option::is_none"
    )]
    pub max_estimated_cost_usd: Option<f64>,
    #[serde(rename = "max_iterations", skip_serializing_if = "Option::is_none")]
    pub max_iterations: Option<u32>,
}

impl InvocationLimitRequest {
    /// Optional requested limits. Total time bounds the entire turn, active time bounds model and tool execution, and waiting time bounds the cumulative time parked for host or callback tool results. Installation defaults supply all three time limits and the iteration limit. Output-token and estimated-cost limits are unlimited when omitted. Installation maxima may be lower than the schema's numeric range.
    pub fn new() -> InvocationLimitRequest {
        InvocationLimitRequest {
            total_timeout_seconds: None,
            active_timeout_seconds: None,
            waiting_timeout_seconds: None,
            max_output_tokens: None,
            max_estimated_cost_usd: None,
            max_iterations: None,
        }
    }
}
