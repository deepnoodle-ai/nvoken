/*
 * nvoken API
 *
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque UUIDs and carry no type prefix. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes with `connection.closing` reason `settled` once that Turn's terminal change is delivered. A Conversation stream follows current and future Turns and may stay open while idle. Both routes accept the same `cursor`, `deltas`, and `Last-Event-ID` parameters and use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.  Reconnect with your last saved `cursor` after any close but `settled`. Honor the `retry:` delay the stream opens with, and back off when the server keeps refusing the connection: a flat retry from every client is what turns an outage into a load spike.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CreateTurnRequest : Starts exactly one durable Turn. Machine callers supply `tenant_key` and `behavior`; browser callers omit both because their verified grant supplies the tenant and one exact AgentRevision. Browser callers also cannot replace the grant's actor or memory authority. Behavior, memory, Conversation, and actor are independent coordinates and all participate in idempotency comparison.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateTurnRequest {
    /// Tenant in which this Turn executes. Required for machine callers; browser callers omit it because the verified grant supplies it.
    #[serde(rename = "tenant_key", skip_serializing_if = "Option::is_none")]
    pub tenant_key: Option<String>,
    /// Optional machine-selected actor. Browser callers omit it because their verified grant supplies the actor. It does not select Agent or Conversation ownership.
    #[serde(rename = "user_key", skip_serializing_if = "Option::is_none")]
    pub user_key: Option<String>,
    /// Trusted-host keys are scoped only to the resolved tenant. An equal retry returns the original Turn; a changed request conflicts.
    #[serde(rename = "idempotency_key")]
    pub idempotency_key: String,
    #[serde(rename = "behavior", skip_serializing_if = "Option::is_none")]
    pub behavior: Option<Box<models::TurnBehaviorSelection>>,
    /// Omission applies the selected behavior's default. `none` disables memory. User memory requires `user_key`; inline user or tenant memory requires a namespace at admission.
    #[serde(rename = "memory", skip_serializing_if = "Option::is_none")]
    pub memory: Option<Box<models::TurnMemorySelection>>,
    /// Omit for a standalone Turn with no public Conversation.
    #[serde(rename = "conversation", skip_serializing_if = "Option::is_none")]
    pub conversation: Option<Box<models::TurnConversation>>,
    /// Monotonic narrowing only; values may never widen stored behavior limits.
    #[serde(rename = "limits", skip_serializing_if = "Option::is_none")]
    pub limits: Option<Box<models::Limits>>,
    #[serde(rename = "input")]
    pub input: Box<models::TurnInput>,
    /// Opaque host correlation data. nvoken stores it, returns it verbatim, and never interprets it — it exists so a support engineer holding an Turn id can get back to the board, ticket, or surface the turn came from, which nvoken deliberately knows nothing about.  At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`, `:`, and `-`; values are at most 512 bytes of UTF-8.  A Conversation similarly has no dedicated `title` field. A host may store one in that Conversation's own metadata object, which keeps nvoken from forming opinions about a string only the host renders.  Metadata is correlation data, not an authorization anchor. Delivery receivers authorize from their own trusted product state and the signed nvoken coordinates, never from model-authored input or metadata.
    #[serde(rename = "metadata", skip_serializing_if = "Option::is_none")]
    pub metadata: Option<std::collections::HashMap<String, String>>,
    #[serde(rename = "context", skip_serializing_if = "Option::is_none")]
    pub context: Option<Vec<models::TurnContextItem>>,
    #[serde(rename = "triggered_by", skip_serializing_if = "Option::is_none")]
    pub triggered_by: Option<Box<models::TurnTrigger>>,
    #[serde(rename = "webhook", skip_serializing_if = "Option::is_none")]
    pub webhook: Option<Box<models::WebhookTarget>>,
    #[serde(
        rename = "on_budget_exhausted",
        skip_serializing_if = "Option::is_none"
    )]
    pub on_budget_exhausted: Option<OnBudgetExhausted>,
    #[serde(rename = "mcp_server_headers", skip_serializing_if = "Option::is_none")]
    pub mcp_server_headers: Option<Vec<models::McpServerHeaders>>,
    #[serde(rename = "provider_keys", skip_serializing_if = "Option::is_none")]
    pub provider_keys: Option<Vec<models::ProviderKeySelection>>,
}

impl CreateTurnRequest {
    /// Starts exactly one durable Turn. Machine callers supply `tenant_key` and `behavior`; browser callers omit both because their verified grant supplies the tenant and one exact AgentRevision. Browser callers also cannot replace the grant's actor or memory authority. Behavior, memory, Conversation, and actor are independent coordinates and all participate in idempotency comparison.
    pub fn new(idempotency_key: String, input: models::TurnInput) -> CreateTurnRequest {
        CreateTurnRequest {
            tenant_key: None,
            user_key: None,
            idempotency_key,
            behavior: None,
            memory: None,
            conversation: None,
            limits: None,
            input: Box::new(input),
            metadata: None,
            context: None,
            triggered_by: None,
            webhook: None,
            on_budget_exhausted: None,
            mcp_server_headers: None,
            provider_keys: None,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum OnBudgetExhausted {
    #[serde(rename = "stop")]
    Stop,
    #[serde(rename = "hold")]
    Hold,
}

impl Default for OnBudgetExhausted {
    fn default() -> OnBudgetExhausted {
        Self::Stop
    }
}
