/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback client tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct GenerationDeltaEvent {
    #[serde(rename = "event_type")]
    pub event_type: EventType,
    /// UUIDv7 with the public `sesn_` prefix.
    #[serde(rename = "session_id")]
    pub session_id: String,
    /// UUIDv7 with the public `invk_` prefix.
    #[serde(rename = "invocation_id")]
    pub invocation_id: String,
    /// Fenced execution attempt that emitted this live preview.
    #[serde(rename = "lease_attempt")]
    pub lease_attempt: u64,
    /// Attempt-local live ordering hint; never a replay cursor.
    #[serde(rename = "delta_sequence")]
    pub delta_sequence: u64,
    #[serde(rename = "delta")]
    pub delta: Box<models::GenerationDeltaEventDelta>,
    #[serde(rename = "emitted_at")]
    pub emitted_at: chrono::DateTime<chrono::FixedOffset>,
}

impl GenerationDeltaEvent {
    pub fn new(
        event_type: EventType,
        session_id: String,
        invocation_id: String,
        lease_attempt: u64,
        delta_sequence: u64,
        delta: models::GenerationDeltaEventDelta,
        emitted_at: chrono::DateTime<chrono::FixedOffset>,
    ) -> GenerationDeltaEvent {
        GenerationDeltaEvent {
            event_type,
            session_id,
            invocation_id,
            lease_attempt,
            delta_sequence,
            delta: Box::new(delta),
            emitted_at,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum EventType {
    #[serde(rename = "generation.delta")]
    EventGenerationDelta,
}

impl Default for EventType {
    fn default() -> EventType {
        Self::EventGenerationDelta
    }
}
