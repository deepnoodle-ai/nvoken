/*
 * nvoken API
 *
 * nvoken runs durable Agent Turns in the background. Each Turn selects one immutable AgentRevision or inline behavior, optional retained Conversation, and optional MemorySpace. Those coordinates remain independent: a Conversation supplies continuity only, and an optional user is the Turn actor.  ## Getting started  `POST /v1/turns` durably admits work and returns `202`. Follow it with `GET /v1/turns/{turn_id}/stream`, or read `/result` later. A standalone Turn omits `conversation`; a conversational Turn explicitly selects new, existing, or continue-or-create continuity. Disconnecting never cancels work.  Reusable behavior lives in owner-namespaced Agents. Creating an Agent creates revision 1 atomically; publishing appends an immutable AgentRevision and advances current without updating one resource per tenant or user. Exact HTTP key lookup always states App, tenant, or user ownership and never applies owner precedence.  ## Authorization  Machine credentials use `nvk_…` bearer API keys. Installation credentials manage Apps but resolve no tenant runtime data; App credentials operate only inside their App. `X-Nvoken-Tenant-Key` and `X-Nvoken-User-Key` are optional per-request assertions: they narrow reads and attribute writes, but never redefine Agent or Conversation ownership. A user assertion requires its tenant assertion. Callers may only narrow the credential's existing scope.  Browser callers use narrow short-lived grants. A verified browser grant pins one exact AgentRevision, so browser Turn admission omits behavior. Browser projections may omit machine-only detail while retaining the same resource schema. Anonymous work is memoryless.  ## Keys and IDs  Caller-owned keys are names in explicit owner namespaces. Service-owned IDs are opaque UUIDs and carry no type prefix. Paths take IDs; key resolution uses exact request or query coordinates.  ## Streams  A Turn stream closes with `connection.closing` reason `settled` once that Turn's terminal change is delivered. A Conversation stream follows current and future Turns and may stay open while idle. Both routes accept the same `cursor`, `deltas`, and `Last-Event-ID` parameters and use the same frame vocabulary. A Conversation-bound Turn cursor may resume on its Conversation stream; a standalone Turn cursor cannot.  Reconnect with your last saved `cursor` after any close but `settled`. Honor the `retry:` delay the stream opens with, and back off when the server keeps refusing the connection: a flat retry from every client is what turns an outage into a load spike.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Turn : One durable Turn. Trusted reads include behavior details and all coordinates. A browser projection may omit machine-only fields while retaining this same response schema. An exact admission replay after content erasure returns a content-free projection with `deduplicated` and `erased_at`; content-bearing optional fields remain absent or null.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Turn {
    /// RFC 9562 UUIDv7 in canonical lowercase text. Identifiers carry no type prefix; treat the value as opaque.
    #[serde(rename = "id")]
    pub id: String,
    #[serde(rename = "tenant_key")]
    pub tenant_key: String,
    /// Optional actor attributed to this Turn.
    #[serde(
        rename = "user_key",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub user_key: Option<Option<String>>,
    #[serde(rename = "behavior_source", skip_serializing_if = "Option::is_none")]
    pub behavior_source: Option<Box<models::TurnBehaviorSource>>,
    /// Present on point reads and results; null or omitted from list projections.
    #[serde(
        rename = "effective_behavior",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub effective_behavior: Option<Option<Box<models::BehaviorInput>>>,
    #[serde(
        rename = "effective_behavior_digest",
        skip_serializing_if = "Option::is_none"
    )]
    pub effective_behavior_digest: Option<String>,
    /// Null for a standalone Turn.
    #[serde(rename = "conversation_id", deserialize_with = "Option::deserialize")]
    pub conversation_id: Option<String>,
    /// RFC 9562 UUIDv7 in canonical lowercase text. Identifiers carry no type prefix; treat the value as opaque.
    #[serde(rename = "memory_space_id", deserialize_with = "Option::deserialize")]
    pub memory_space_id: Option<String>,
    /// The resolved memory selection; null means no MemorySpace.
    #[serde(
        rename = "memory",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub memory: Option<Option<Box<models::TurnMemorySelection>>>,
    /// Scheduled private-content expiry for a terminal standalone Turn. Null while it is nonterminal and for conversation-bound work.
    #[serde(
        rename = "content_expires_at",
        deserialize_with = "Option::deserialize"
    )]
    pub content_expires_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(
        rename = "triggered_by",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub triggered_by: Option<Option<Box<models::TurnTrigger>>>,
    #[serde(rename = "child_turn_counts", skip_serializing_if = "Option::is_none")]
    pub child_turn_counts: Option<Box<models::TurnChildCounts>>,
    #[serde(
        rename = "context",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub context: Option<Option<Vec<models::TurnContextItem>>>,
    /// Present only on admission responses.
    #[serde(rename = "deduplicated", skip_serializing_if = "Option::is_none")]
    pub deduplicated: Option<bool>,
    #[serde(rename = "status")]
    pub status: models::TurnStatus,
    #[serde(rename = "stop_reason", deserialize_with = "Option::deserialize")]
    pub stop_reason: Option<models::TurnStopReason>,
    #[serde(
        rename = "credit_block",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub credit_block: Option<Option<Box<models::CreditBlock>>>,
    #[serde(rename = "attempt")]
    pub attempt: u32,
    #[serde(rename = "error", deserialize_with = "Option::deserialize")]
    pub error: Option<Box<models::TurnFailure>>,
    #[serde(
        rename = "usage",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub usage: Option<Option<Box<models::ModelUsage>>>,
    #[serde(
        rename = "provenance",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub provenance: Option<Option<Box<models::ModelProvenance>>>,
    #[serde(rename = "structured_output", deserialize_with = "Option::deserialize")]
    pub structured_output: Option<std::collections::HashMap<String, serde_json::Value>>,
    #[serde(
        rename = "structured_output_provenance",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub structured_output_provenance: Option<Option<Box<models::StructuredOutputProvenance>>>,
    /// Opaque host correlation data. nvoken stores it, returns it verbatim, and never interprets it — it exists so a support engineer holding an Turn id can get back to the board, ticket, or surface the turn came from, which nvoken deliberately knows nothing about.  At most 16 entries. Keys are 1–64 bytes of letters, digits, `_`, `.`, `:`, and `-`; values are at most 512 bytes of UTF-8.  A Conversation similarly has no dedicated `title` field. A host may store one in that Conversation's own metadata object, which keeps nvoken from forming opinions about a string only the host renders.  Metadata is correlation data, not an authorization anchor. Delivery receivers authorize from their own trusted product state and the signed nvoken coordinates, never from model-authored input or metadata.
    #[serde(
        rename = "metadata",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub metadata: Option<Option<std::collections::HashMap<String, String>>>,
    #[serde(rename = "limits", skip_serializing_if = "Option::is_none")]
    pub limits: Option<Box<models::ResolvedLimits>>,
    #[serde(rename = "active_execution_ms")]
    pub active_execution_ms: u32,
    #[serde(rename = "deadline_at", deserialize_with = "Option::deserialize")]
    pub deadline_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updated_at")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "ended_at", deserialize_with = "Option::deserialize")]
    pub ended_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// Present on an exact admission replay whose Turn content has been erased.
    #[serde(
        rename = "erased_at",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub erased_at: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    #[serde(rename = "tool_calls")]
    pub tool_calls: Vec<models::ToolCallSummary>,
}

impl Turn {
    /// One durable Turn. Trusted reads include behavior details and all coordinates. A browser projection may omit machine-only fields while retaining this same response schema. An exact admission replay after content erasure returns a content-free projection with `deduplicated` and `erased_at`; content-bearing optional fields remain absent or null.
    pub fn new(
        id: String,
        tenant_key: String,
        conversation_id: Option<String>,
        memory_space_id: Option<String>,
        content_expires_at: Option<chrono::DateTime<chrono::FixedOffset>>,
        status: models::TurnStatus,
        stop_reason: Option<models::TurnStopReason>,
        attempt: u32,
        error: Option<models::TurnFailure>,
        structured_output: Option<std::collections::HashMap<String, serde_json::Value>>,
        active_execution_ms: u32,
        deadline_at: Option<chrono::DateTime<chrono::FixedOffset>>,
        created_at: chrono::DateTime<chrono::FixedOffset>,
        updated_at: chrono::DateTime<chrono::FixedOffset>,
        ended_at: Option<chrono::DateTime<chrono::FixedOffset>>,
        tool_calls: Vec<models::ToolCallSummary>,
    ) -> Turn {
        Turn {
            id,
            tenant_key,
            user_key: None,
            behavior_source: None,
            effective_behavior: None,
            effective_behavior_digest: None,
            conversation_id,
            memory_space_id,
            memory: None,
            content_expires_at,
            triggered_by: None,
            child_turn_counts: None,
            context: None,
            deduplicated: None,
            status,
            stop_reason,
            credit_block: None,
            attempt,
            error: if let Some(x) = error {
                Some(Box::new(x))
            } else {
                None
            },
            usage: None,
            provenance: None,
            structured_output,
            structured_output_provenance: None,
            metadata: None,
            limits: None,
            active_execution_ms,
            deadline_at,
            created_at,
            updated_at,
            ended_at,
            erased_at: None,
            tool_calls,
        }
    }
}
