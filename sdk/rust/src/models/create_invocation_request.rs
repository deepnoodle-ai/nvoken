/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI's bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller's behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken's defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateInvocationRequest {
    /// Stable caller-controlled Agent key, unique within the installation. The resulting Agent anchor stores identity only and is shared across tenant partitions.
    #[serde(rename = "agent_key")]
    pub agent_key: String,
    /// Optional tenant partition. For Session-key resolution or a new Session, precedence is credential constraint, this explicit value, then the default partition. For Session-ID resolution, an installation-wide caller may omit it and use the stored partition.
    #[serde(rename = "tenant_key", skip_serializing_if = "Option::is_none")]
    pub tenant_key: Option<String>,
    /// Optional host-owned end-user label recorded on this Invocation and its input message. The Session retains the label from the request that opened it, while later turns may identify different end users. Filtering only; not an isolation boundary.
    #[serde(rename = "user_key", skip_serializing_if = "Option::is_none")]
    pub user_key: Option<String>,
    /// Existing Session to continue. Mutually exclusive with session_key.
    #[serde(rename = "session_id", skip_serializing_if = "Option::is_none")]
    pub session_id: Option<String>,
    /// Caller key resolved within (effective tenant partition, Agent, session_key). Mutually exclusive with session_id.
    #[serde(rename = "session_key", skip_serializing_if = "Option::is_none")]
    pub session_key: Option<String>,
    /// Durable options for the resolved Session. On an existing Session, every supplied value is compared with the stored value: equal is accepted and different returns `session_options_conflict`. Compaction is the one late-installable option: when no policy is stored yet, this admission installs its model-validated policy. Session options are not part of the reusable execution snapshot or its idempotency comparison.
    #[serde(rename = "session_options", skip_serializing_if = "Option::is_none")]
    pub session_options: Option<Box<models::SessionOptions>>,
    /// Opaque host correlation data recorded on this Invocation. It is part of the admitted input, so it is immutable and material to idempotency: a replay carrying different metadata conflicts rather than updating it. A genuine retry describes the same originating request and so carries the same values by construction.  Session metadata is separate and mutable — see `session_options.metadata` and `PATCH /v1/sessions/{session_id}`.
    #[serde(rename = "metadata", skip_serializing_if = "Option::is_none")]
    pub metadata: Option<std::collections::HashMap<String, String>>,
    /// Stable admission identity scoped to (effective tenant partition, agent_key). Reuse an unchanged request after any 5xx, timeout, disconnect, or missing acknowledgement. Deduplication is guaranteed while the original Invocation is retained.
    #[serde(rename = "idempotency_key")]
    pub idempotency_key: String,
    /// Policy when the resolved Session already has a nonterminal Invocation. `reject` returns `session_invocation_active`. `supersede` atomically applies the ordinary durable cancellation transition to that Invocation before admitting this replacement, dropping its work from later turns. `interrupt` stops it gracefully and admits only after it has settled `completed`, so the replacement builds on what the stopped turn produced: it is \"stop and redo\" where supersede is \"discard and redo\". Omission and explicit `reject` compare equal on replay.
    #[serde(rename = "if_active", skip_serializing_if = "Option::is_none")]
    pub if_active: Option<IfActive>,
    /// `settle` ends a coherent consumption-budget stop as `incomplete`. `pause` parks it as resumable `paused`. Applies to iteration, output-token, per-turn estimated-cost, and Session estimated-cost ceilings; deadlines always settle.
    #[serde(
        rename = "on_budget_exhausted",
        skip_serializing_if = "Option::is_none"
    )]
    pub on_budget_exhausted: Option<OnBudgetExhausted>,
    #[serde(rename = "input")]
    pub input: Box<models::InvocationInput>,
    #[serde(rename = "webhook", skip_serializing_if = "Option::is_none")]
    pub webhook: Option<Box<models::WebhookTarget>>,
    /// Immutable content-addressed definition previously admitted inline by this app. Unknown and foreign IDs return `definition_not_found`. Mutually exclusive with every inline definition field. A reference and the byte-identical inline definition are equal on idempotent replay.
    #[serde(rename = "definition_id", skip_serializing_if = "Option::is_none")]
    pub definition_id: Option<String>,
    /// Optional model instructions. Omission adds no hidden default.
    #[serde(rename = "instructions", skip_serializing_if = "Option::is_none")]
    pub instructions: Option<String>,
    #[serde(rename = "model", skip_serializing_if = "Option::is_none")]
    pub model: Option<Box<models::ModelInput>>,
    #[serde(rename = "sampling", skip_serializing_if = "Option::is_none")]
    pub sampling: Option<Box<models::Sampling>>,
    #[serde(rename = "reasoning", skip_serializing_if = "Option::is_none")]
    pub reasoning: Option<Box<models::Reasoning>>,
    #[serde(rename = "tool_choice", skip_serializing_if = "Option::is_none")]
    pub tool_choice: Option<Box<models::ToolChoice>>,
    #[serde(rename = "limits", skip_serializing_if = "Option::is_none")]
    pub limits: Option<Box<models::Limits>>,
    #[serde(rename = "structured_output", skip_serializing_if = "Option::is_none")]
    pub structured_output: Option<Box<models::StructuredOutput>>,
    #[serde(rename = "tools", skip_serializing_if = "Option::is_none")]
    pub tools: Option<Vec<models::ToolDeclaration>>,
    #[serde(rename = "mcp_servers", skip_serializing_if = "Option::is_none")]
    pub mcp_servers: Option<Vec<models::McpServer>>,
    #[serde(rename = "provider_tools", skip_serializing_if = "Option::is_none")]
    pub provider_tools: Option<Vec<models::ProviderTool>>,
    /// Explicit nonsecret source selection for the model provider. Omission resolves the default chain at admission: the app's stored key for the provider when one exists, else a self-hosted installation's environment key (config_byok), else platform funding when the installation enables it. The chosen source is bound durably at admission; execution never falls through to a different payer afterward. The selection is stored outside the reusable execution snapshot; only caller_ephemeral accepts secret material, and a caller may equivalently supply it with the X-Anthropic-Api-Key, X-Openai-Api-Key, X-Gemini-Api-Key, or X-Xai-Api-Key request header naming the model provider. Equal idempotent replay never replaces the original encrypted key. The field is deliberately plural even though this contract currently permits one entry: future routed invocations may select keys for more than one model provider.
    #[serde(rename = "provider_keys", skip_serializing_if = "Option::is_none")]
    pub provider_keys: Option<Vec<models::ProviderKeySelection>>,
}

impl CreateInvocationRequest {
    pub fn new(
        agent_key: String,
        idempotency_key: String,
        input: models::InvocationInput,
    ) -> CreateInvocationRequest {
        CreateInvocationRequest {
            agent_key,
            tenant_key: None,
            user_key: None,
            session_id: None,
            session_key: None,
            session_options: None,
            metadata: None,
            idempotency_key,
            if_active: None,
            on_budget_exhausted: None,
            input: Box::new(input),
            webhook: None,
            definition_id: None,
            instructions: None,
            model: None,
            sampling: None,
            reasoning: None,
            tool_choice: None,
            limits: None,
            structured_output: None,
            tools: None,
            mcp_servers: None,
            provider_tools: None,
            provider_keys: None,
        }
    }
}
/// Policy when the resolved Session already has a nonterminal Invocation. `reject` returns `session_invocation_active`. `supersede` atomically applies the ordinary durable cancellation transition to that Invocation before admitting this replacement, dropping its work from later turns. `interrupt` stops it gracefully and admits only after it has settled `completed`, so the replacement builds on what the stopped turn produced: it is \"stop and redo\" where supersede is \"discard and redo\". Omission and explicit `reject` compare equal on replay.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum IfActive {
    #[serde(rename = "reject")]
    Reject,
    #[serde(rename = "supersede")]
    Supersede,
    #[serde(rename = "interrupt")]
    Interrupt,
}

impl Default for IfActive {
    fn default() -> IfActive {
        Self::Reject
    }
}
/// `settle` ends a coherent consumption-budget stop as `incomplete`. `pause` parks it as resumable `paused`. Applies to iteration, output-token, per-turn estimated-cost, and Session estimated-cost ceilings; deadlines always settle.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum OnBudgetExhausted {
    #[serde(rename = "settle")]
    Settle,
    #[serde(rename = "pause")]
    Pause,
}

impl Default for OnBudgetExhausted {
    fn default() -> OnBudgetExhausted {
        Self::Settle
    }
}
