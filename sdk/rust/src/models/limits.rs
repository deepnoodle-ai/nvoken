/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion or retention-control operation. Session context compaction creates a private provider projection without mutating canonical Session messages. Authoritative records exposed by this contract, plus private compaction state, are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, reusable definitions, URL media input, and reusable model provider key lifecycle are included. General administrative APIs remain outside this version.  ## Protected vocabulary  Several names deliberately converge with established agent APIs and must not be casually renamed: `metadata` uses OpenAI's bounds of 16 keys, 64-character names, and 512-byte values; `output_text` is the composed text convenience; `reasoning.effort` uses `low`, `medium`, `high`, `xhigh`, and `max`; `stop_reason: end_turn`, status `running`, and message phases `commentary` and `final_answer` are likewise intentional.  ## Echo resolved config  Every setting the runtime resolves on the caller's behalf is readable back, resolved, on the resource that used it. A host never has to reconstruct what applied from the request it sent plus its own model of nvoken's defaults — the resource says.  Concretely: an Invocation echoes `limits` after installation defaults and feature floors, alongside the `definition` it was admitted with exactly as snapshotted, and `provenance` records what actually ran. A Session echoes its compaction policy with `auto` already materialized to the integer it resolved to and the model it bound, and its retention window as accepted. New settings are expected to follow: a default the caller cannot read back is a setting only the server knows about.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Limits : Optional requested limits. Total time bounds the entire turn, active time bounds model and tool execution, and waiting time bounds the cumulative time parked for host or callback tool results. Installation defaults supply all three time limits and the iteration limit. Output-token and estimated-cost limits are unlimited when omitted. Installation maxima may be lower than the schema's numeric range.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Limits {
    #[serde(
        rename = "total_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub total_timeout_seconds: Option<u32>,
    #[serde(
        rename = "active_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub active_timeout_seconds: Option<u32>,
    /// Optional cumulative external-waiting SLA. When omitted, host and callback ToolCalls may remain pending indefinitely; waiting does not consume the Invocation's total or active execution budgets.
    #[serde(
        rename = "waiting_timeout_seconds",
        skip_serializing_if = "Option::is_none"
    )]
    pub waiting_timeout_seconds: Option<u32>,
    #[serde(rename = "max_output_tokens", skip_serializing_if = "Option::is_none")]
    pub max_output_tokens: Option<u32>,
    /// nvoken's standard list-price guardrail, not preauthorization or a billing ledger. Requires known USD pricing for the selected model and otherwise fails closed with `budget_exceeded` and `details.kind = estimated_cost_unavailable`. When pricing absence is knowable before execution, nvoken rejects before a provider call. Reaching the ceiling itself is not a failure when it is noticed at an execution seam: the turn settles `incomplete` with `stop_reason: max_estimated_cost` and keeps its work.
    #[serde(
        rename = "max_estimated_cost_usd",
        skip_serializing_if = "Option::is_none"
    )]
    pub max_estimated_cost_usd: Option<f64>,
    #[serde(rename = "max_iterations", skip_serializing_if = "Option::is_none")]
    pub max_iterations: Option<u32>,
}

impl Limits {
    /// Optional requested limits. Total time bounds the entire turn, active time bounds model and tool execution, and waiting time bounds the cumulative time parked for host or callback tool results. Installation defaults supply all three time limits and the iteration limit. Output-token and estimated-cost limits are unlimited when omitted. Installation maxima may be lower than the schema's numeric range.
    pub fn new() -> Limits {
        Limits {
            total_timeout_seconds: None,
            active_timeout_seconds: None,
            waiting_timeout_seconds: None,
            max_output_tokens: None,
            max_estimated_cost_usd: None,
            max_iterations: None,
        }
    }
}
