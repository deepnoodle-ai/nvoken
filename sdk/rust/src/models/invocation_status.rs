/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback host tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// InvocationStatus : `completed`, `failed`, and `cancelled` are terminal and immutable. Deadline or budget exhaustion settles as `failed`. `waiting` means the Invocation has durable pending host ToolCalls and owns no execution lease. Final result acceptance moves it back to `queued`. Checkpoint recovery may also move `running` back to `queued`; lifecycle revision orders every transition.
/// `completed`, `failed`, and `cancelled` are terminal and immutable. Deadline or budget exhaustion settles as `failed`. `waiting` means the Invocation has durable pending host ToolCalls and owns no execution lease. Final result acceptance moves it back to `queued`. Checkpoint recovery may also move `running` back to `queued`; lifecycle revision orders every transition.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum InvocationStatus {
    #[serde(rename = "queued")]
    Queued,
    #[serde(rename = "running")]
    Running,
    #[serde(rename = "waiting")]
    Waiting,
    #[serde(rename = "completed")]
    Completed,
    #[serde(rename = "failed")]
    Failed,
    #[serde(rename = "cancelled")]
    Cancelled,
}

impl std::fmt::Display for InvocationStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Queued => write!(f, "queued"),
            Self::Running => write!(f, "running"),
            Self::Waiting => write!(f, "waiting"),
            Self::Completed => write!(f, "completed"),
            Self::Failed => write!(f, "failed"),
            Self::Cancelled => write!(f, "cancelled"),
        }
    }
}

impl Default for InvocationStatus {
    fn default() -> InvocationStatus {
        Self::Queued
    }
}
