/*
 * nvoken Runtime API
 *
 * This focused contract defines nvoken's implemented background Runtime surface: durable Invocation admission, authoritative Invocation and Session reads, cursor-based transcript recovery, and resumable Session output streaming.  The Runtime API has no deletion, compaction, or retention-control operation. Authoritative records exposed by this contract are retained by default; the complete inventory and any future ordered-deletion contract are governed by the design packet's Data and retention section.  Inline and callback client tools, structured output, and reusable model provider credential lifecycle are included. Spec references and general administrative APIs remain outside this version.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct InvocationResult {
    #[serde(rename = "invocation")]
    pub invocation: Box<models::Invocation>,
    /// Every canonical SessionMessage owned by this Invocation, all roles, ascending sequence, composed at read time from the canonical transcript. Nothing is stored twice.
    #[serde(rename = "messages")]
    pub messages: Vec<models::SessionMessage>,
    /// The text content blocks of this Invocation's assistant-role messages concatenated in transcript order without separators. Non-null only when the Invocation is completed and at least one assistant text block exists. Failed and cancelled Invocations keep their messages readable as evidence while output_text stays null.
    #[serde(rename = "output_text", deserialize_with = "Option::deserialize")]
    pub output_text: Option<String>,
}

impl InvocationResult {
    pub fn new(
        invocation: models::Invocation,
        messages: Vec<models::SessionMessage>,
        output_text: Option<String>,
    ) -> InvocationResult {
        InvocationResult {
            invocation: Box::new(invocation),
            messages,
            output_text,
        }
    }
}
